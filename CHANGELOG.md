# Changelog

> **EN:** All notable changes to glintfx. Format based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/); this project follows [Semantic Versioning](https://semver.org/).
> **PT:** Todas as mudanças notáveis do glintfx. Formato baseado em [Keep a Changelog](https://keepachangelog.com/en/1.1.0/); o projeto segue [Versionamento Semântico](https://semver.org/).
>
> **EN:** Mirrored on [Codeberg](https://codeberg.org/petrinhu/glintfx) · [GitHub](https://github.com/petrinhu/glintfx) -- every `vX.Y.Z` tag exists identically on both remotes.
> **PT:** Espelhado no [Codeberg](https://codeberg.org/petrinhu/glintfx) · [GitHub](https://github.com/petrinhu/glintfx) -- cada tag `vX.Y.Z` existe idêntica nos dois remotos.

---

## [Unreleased]

---

## [0.11.0] - 2026-07-16 · [GitHub](https://github.com/petrinhu/glintfx/releases/tag/v0.11.0)

**EN:** **Minor** release -- the **backdrop-ripple** decorator (`L1.22-WAVE`/`L1.22-CAPTURE`). glintfx can now **capture the host's FBO 0 into an internal texture** and refract it screen-space via a data-driven `decorator: ripple(...)` in RCSS -- a shimmer over glintfx's own UI, driven entirely by custom RCSS properties (no imperative effect API, per the project principle). Scope is honest: this refracts glintfx's **own** content (the composed UI layer), **not** the host's battle scene -- an embed host that wants to distort its own render target does that host-side (see the reference GLSL in `docs/embed-integration.md`). The capture is **structurally cost-zero when no ripple is active** (arm/ensure pattern: an unconditional zero-GL arm in `begin_frame_compose`, plus a lazy on-demand capture reached only from inside the `ripple` shader path during `Context::Render()`). Additive and drop-in from v0.10.0: no existing public signature changes; a consumer that never uses `decorator: ripple` pays nothing. See [ADR-0012](docs/adr/0012-backdrop-capture.md).
**PT:** Release **minor** -- o decorator **backdrop-ripple** (`L1.22-WAVE`/`L1.22-CAPTURE`). O glintfx agora **captura o FBO 0 do host numa textura interna** e o refrata em screen-space via um `decorator: ripple(...)` data-driven no RCSS -- um shimmer sobre a própria UI do glintfx, controlado inteiramente por propriedades RCSS customizadas (sem API imperativa de efeito, conforme o princípio do projeto). O escopo é honesto: isto refrata o conteúdo **próprio** do glintfx (a camada de UI composta), **não** a cena de batalha do host -- um host embed que queira distorcer o próprio render target faz isso host-side (ver o GLSL de referência em `docs/embed-integration.md`). A captura é **estruturalmente custo-zero quando nenhum ripple está ativo** (padrão arm/ensure: um arm incondicional de zero-GL em `begin_frame_compose`, mais uma captura lazy on-demand alcançada só de dentro do caminho do shader `ripple` durante o `Context::Render()`). Aditiva e drop-in a partir da v0.10.0: nenhuma assinatura pública existente muda; um consumidor que nunca usa `decorator: ripple` não paga nada. Ver [ADR-0012](docs/adr/0012-backdrop-capture.md).

### Added / Adicionado

- **EN:** `decorator: ripple(...)` RCSS decorator -- captures FBO 0 and applies a radial refraction ring
  over the element's box. Custom properties `ripple-origin-x`/`-y` (epicentre, 0..1 of the element),
  `ripple-phase` (0..1 lifecycle), `ripple-strength` (displacement amount), `ripple-width` (ring
  thickness) -- all `<number>`, animatable via `@keyframes`. The ring composites alpha-shaped
  (`alpha = ring`), so only the wavefront is drawn. Registered via `RenderShader` override (the same
  mechanism as `image-tint`); no private RmlUi backend member is touched.
- **EN:** FBO-0 backdrop capture into `backdrop_capture_tex_` (`RenderGl3`), read back with a
  Y-flip for the RCSS top-left coordinate convention. `GlStateGuard` extended to save/restore
  `GL_READ_FRAMEBUFFER_BINDING` + `GL_READ_BUFFER` so the host's read-framebuffer is never clobbered.
- **PT:** decorator RCSS `decorator: ripple(...)` -- captura o FBO 0 e aplica um anel de refração
  radial sobre a caixa do elemento. Propriedades customizadas `ripple-origin-x`/`-y` (epicentro,
  0..1 do elemento), `ripple-phase` (ciclo 0..1), `ripple-strength` (deslocamento), `ripple-width`
  (espessura do anel) -- todas `<number>`, animáveis via `@keyframes`. O anel compõe alpha-shaped
  (`alpha = ring`), então só a frente de onda é desenhada. Registrado via override de `RenderShader`
  (mesmo mecanismo do `image-tint`); nenhum membro privado do backend RmlUi é tocado.
- **PT:** captura de backdrop do FBO 0 em `backdrop_capture_tex_` (`RenderGl3`), lida de volta com
  flip em Y pra convenção top-left do RCSS. `GlStateGuard` estendido pra salvar/restaurar
  `GL_READ_FRAMEBUFFER_BINDING` + `GL_READ_BUFFER`, pra que o read-framebuffer do host nunca seja
  sobrescrito.

### Notes / Notas

- **EN:** Two independent adversarial gates ship with this feature: `ripple_sanity` (pixel-correct
  refraction on both axes; cost-zero-when-inactive proven via a `glCopyTexSubImage2D` entry-point
  spy, not a pixel check) and `ripple_stress_sanity` (differential proof that four elements drawn
  *after* the ripple in the same `Context::Render()` stay bit-exact with vs without the mid-frame
  capture, plus a host-GL-state sentinel). Full suite 52/52 under ASan+UBSan+LSan.
- **EN:** Known limitation: `decorator: ripple` is inert in standalone `App` mode (no host owns the
  scene, so there is no backdrop to capture) -- it only does anything in embed/`UiLayer` mode.
- **PT:** Dois gates adversariais independentes acompanham a feature: `ripple_sanity` (refração
  correta em pixel nos dois eixos; custo-zero-quando-inativo provado por um espião no entry point
  `glCopyTexSubImage2D`, não por check de pixel) e `ripple_stress_sanity` (prova diferencial de que
  quatro elementos desenhados *depois* do ripple no mesmo `Context::Render()` ficam bit-exatos com
  vs sem a captura mid-frame, mais um sentinela de estado GL do host). Suíte completa 52/52 sob
  ASan+UBSan+LSan.
- **PT:** Limitação conhecida: `decorator: ripple` é inerte em modo `App` standalone (nenhum host é
  dono da cena, então não há backdrop pra capturar) -- só faz efeito em modo embed/`UiLayer`.

---

## [0.10.0] - 2026-07-15 · [GitHub](https://github.com/petrinhu/glintfx/releases/tag/v0.10.0)

**EN:** **Minor** release -- the "soft font flip" (`L1.20-FONTFLIP`). glintfx's own clean-room font engine (SOV-SFNT/SOV-RAST/SOV-HINT, `L1.19-FONTENG`, introduced opt-in in v0.9.1) is now the **default** rasteriser glintfx installs into RmlUi (`GLINTFX_OWN_FONT_ENGINE=ON` at build time, `FontEngine::Own` at runtime) -- a public, additive `FontEngine` enum on `AppConfig`/`UiLayerConfig` selects the engine per-instance. **This is a soft flip, not a drop:** FreeType stays linked and fully supported either way (`RMLUI_FONT_ENGINE` is still `freetype`); a consumer that hits a regression rolls back with a **one-line config change and no rebuild** (`font_engine = FontEngine::FreeType`). No existing public signature is removed; this release is additive (drop-in from v0.9.2 for consumers that don't touch `font_engine`, since `Own` was already exercised in production-adjacent testing throughout v0.9.1/v0.9.2 -- see [ADR-0011](docs/adr/0011-soft-font-flip.md)).
**PT:** Release **minor** -- o "flip suave de fonte" (`L1.20-FONTFLIP`). O motor de fonte próprio clean-room do glintfx (SOV-SFNT/SOV-RAST/SOV-HINT, `L1.19-FONTENG`, introduzido opt-in na v0.9.1) agora é o rasterizador **default** que o glintfx instala no RmlUi (`GLINTFX_OWN_FONT_ENGINE=ON` em tempo de build, `FontEngine::Own` em runtime) -- um enum público e aditivo `FontEngine` em `AppConfig`/`UiLayerConfig` seleciona o motor por-instância. **É um flip suave, não um drop:** o FreeType continua linkado e plenamente suportado de qualquer forma (`RMLUI_FONT_ENGINE` continua `freetype`); um consumidor que encontrar uma regressão reverte com uma **mudança de config de uma linha e sem rebuild** (`font_engine = FontEngine::FreeType`). Nenhuma assinatura pública existente é removida; esta release é aditiva (drop-in a partir da v0.9.2 pra consumidores que não tocam `font_engine`, já que o `Own` já vinha sendo exercitado em teste próximo-de-produção ao longo da v0.9.1/v0.9.2 -- ver [ADR-0011](docs/adr/0011-soft-font-flip.md)).

### Changed / Alterado

- **EN:** Default font engine flips from FreeType to glintfx's own (`GLINTFX_OWN_FONT_ENGINE` CMake
  option default `ON`; `AppConfig::font_engine`/`UiLayerConfig::font_engine` default
  `FontEngine::Own`). If the library was instead built with `GLINTFX_OWN_FONT_ENGINE=OFF` (a
  consumer opt-out at BUILD time), requesting `Own` at runtime falls back to FreeType with a
  warning logged -- never a crash or a hard failure. Nitidez (crispness) at small body sizes was a
  precondition for the flip: pen-snap on the X axis and a vertical grid-fitter (`vsnap`) on the Y
  axis close the gap against FreeType's own auto-hinter at 9-15px, the size range that matters to
  the first embed consumer (GusWorld's HUD, 9-13dp). Stem-darkening was tried and rejected (made
  the flip axis worse, not better) -- see `TODO.md` L1.20-FONTFLIP history for the full
  A/B record.
  **PT:** O motor de fonte default vira o próprio do glintfx (opção CMake
  `GLINTFX_OWN_FONT_ENGINE` com default `ON`; `AppConfig::font_engine`/`UiLayerConfig::font_engine`
  com default `FontEngine::Own`). Se a lib foi buildada com `GLINTFX_OWN_FONT_ENGINE=OFF` (um
  opt-out do consumidor em tempo de BUILD), pedir `Own` em runtime cai no FreeType com um aviso
  logado -- nunca um crash ou falha dura. Nitidez em corpo pequeno era pré-condição pro flip:
  pen-snap no eixo X e um grid-fitter vertical (`vsnap`) no eixo Y fecham o gap contra o
  auto-hinter do próprio FreeType em 9-15px, a faixa de tamanho que importa pro 1º consumidor embed
  (HUD do GusWorld, 9-13dp). Stem-darkening foi tentado e reprovado (piorou o eixo do flip, não
  melhorou) -- ver o histórico L1.20-FONTFLIP no `TODO.md` pro registro A/B completo.

### Added / Adicionado

- **EN:** Public `glintfx::FontEngine` enum (`glintfx/include/glintfx/font_engine.hpp`) -- a plain,
  engine-agnostic selector (`Own`/`FreeType`), no `Rml::` or FreeType type leaking into the public
  header, consistent with every other header under `glintfx/include/glintfx/`. Wired as a new
  field on `AppConfig` and `UiLayerConfig`, resolved once at `Bootstrap::init()` (construction
  time only -- RmlUi resolves its registered `FontEngineInterface` once and does not support
  flipping it after `Rml::Initialise()`; reconfigure-and-reconstruct is the supported way to
  change engines at runtime).
  **PT:** Enum público `glintfx::FontEngine` (`glintfx/include/glintfx/font_engine.hpp`) -- um
  seletor simples, agnóstico de engine (`Own`/`FreeType`), sem nenhum tipo `Rml::` ou FreeType
  vazando pro header público, consistente com todo outro header sob `glintfx/include/glintfx/`.
  Ligado como campo novo em `AppConfig` e `UiLayerConfig`, resolvido uma única vez em
  `Bootstrap::init()` (só em tempo de construção -- o RmlUi resolve sua `FontEngineInterface`
  registrada uma vez e não suporta trocá-la depois do `Rml::Initialise()`; reconfigurar-e-reconstruir
  é a forma suportada de trocar de motor em runtime).
- **EN:** Layer 0's font-engine C core (`glx_sfnt`/`glx_raster`/`glx_hint`) vendored under
  `glintfx/vendor/core/` (`src/`+`include/core/`, byte-identical copy of the canonical Camada-0
  sources, README documenting provenance and re-sync steps) -- makes the own engine buildable from
  a plain `glintfx/` checkout, no sibling Layer-0 tree required, portable to any consumer that
  fetches glintfx standalone (`FetchContent`/`find_package`).
  **PT:** O núcleo C do motor de fonte da Camada 0 (`glx_sfnt`/`glx_raster`/`glx_hint`) vendorizado
  em `glintfx/vendor/core/` (`src/`+`include/core/`, cópia byte-idêntica das fontes canônicas da
  Camada 0, README documentando procedência e passos de re-sincronização) -- torna o motor próprio
  buildável a partir de um checkout puro de `glintfx/`, sem exigir uma árvore-irmã da Camada 0,
  portável pra qualquer consumidor que busca o glintfx standalone (`FetchContent`/`find_package`).
- **EN:** Per-glyph font fallback -- a face registered with `fallback_face=true` is now actually
  consulted when the primary face lacks a codepoint (e.g. Greek `Ω`/`α` or a `²` superscript in a
  Latin-only fixture such as PixelOperatorMono, GusWorld's own font), including a negative-cache
  for CJK misses and case-insensitive family-name resolution (a production bug the fallback work
  surfaced: `FindBestFace()` compared family names case-sensitively across two code paths that
  didn't normalize the same way).
  **PT:** Fallback de fonte por-glifo -- uma face registrada com `fallback_face=true` agora é
  de fato consultada quando a face primária não tem um codepoint (ex.: `Ω`/`α` gregos ou um `²`
  sobrescrito numa fixture Latin-only como a PixelOperatorMono, a própria fonte do GusWorld),
  incluindo negative-cache pra misses CJK e resolução de nome-de-família case-insensitive (um bug
  de produção que o trabalho de fallback revelou: `FindBestFace()` comparava nomes de família
  case-sensitive entre dois caminhos de código que não normalizavam do mesmo jeito).

### Fixed / Corrigido

- **EN:** Kerning lookup in the own engine's `kern` format-0 subtable was O(n) linear scan
  (18694 pairs in Open Sans, walked on every glyph pair, every frame) -- now O(log n) binary
  search over the spec-required `(left<<16|right)` ordering, with memory safety unchanged (bounds
  are proven before the search, independent of ordering; a hostile unsorted table degrades to "no
  match", never OOB/UB). Validated by an exhaustive equivalence test against all 18694 real pairs
  from Open Sans, cross-checked against an independent linear-scan reference witness.
  **PT:** O lookup de kerning na subtable `kern` format-0 do motor próprio era varredura linear
  O(n) (18694 pares na Open Sans, percorridos a cada par de glifo, a cada frame) -- agora é busca
  binária O(log n) sobre a ordenação `(left<<16|right)` exigida pela spec, com segurança de memória
  inalterada (os bounds são provados antes da busca, independente da ordenação; uma tabela hostil
  desordenada degrada pra "sem match", nunca OOB/UB). Validado por um teste de equivalência
  exaustivo contra os 18694 pares reais da Open Sans, cross-checado contra uma testemunha
  independente de varredura linear.
- **EN:** `FontEngineOwn::RegisterFace()` used to push a new `LoadedFace` (a full copy of the font
  blob, ~214KB for Open Sans) unconditionally on every call, with no dedup -- since
  `Rml::LoadFontFace()` is invoked once per `Context::LoadDocument()` for every `@font-face` block,
  reloading the same document repeatedly leaked one blob copy per reload. Fixed with a dedup check
  against the already-registered face set.
  **PT:** `FontEngineOwn::RegisterFace()` empilhava incondicionalmente um novo `LoadedFace` (cópia
  completa do blob da fonte, ~214KB pra Open Sans) a cada chamada, sem dedup -- como
  `Rml::LoadFontFace()` é chamado uma vez por `Context::LoadDocument()` pra cada bloco
  `@font-face`, recarregar o mesmo documento repetidamente vazava uma cópia de blob por reload.
  Corrigido com uma checagem de dedup contra o conjunto de faces já registradas.

---

## [0.9.2] - 2026-07-14 · [GitHub](https://github.com/petrinhu/glintfx/releases/tag/v0.9.2)

**EN:** **Patch** release -- consumer-driven fix, no public API surface change. The headline fix unblocks the Windows build of the generated GL loader (`L1.14-GLLOADER-WIN`), reported by GusWorld while preparing a Windows target. Also landing: a CI restructuring (gate order, self-hosted heavy sanitizer job, pre-push local gate) and a documentation reinforcement carried over from `[Unreleased]`. **No existing signature changed; this release is a drop-in upgrade from v0.9.1.**
**PT:** Release **patch** -- fix consumer-driven, sem mudança de superfície pública de API. O fix de destaque destrava o build Windows do loader GL gerado (`L1.14-GLLOADER-WIN`), reportado pelo GusWorld ao preparar um alvo Windows. Também chegando: uma reestruturação de CI (ordem de gate, job pesado de sanitizer self-hosted, gate local pre-push) e um reforço de documentação carregado do `[Unreleased]`. **Nenhuma assinatura existente mudou; esta release é um upgrade drop-in a partir da v0.9.1.**

### Fixed / Corrigido

- **EN:** `glintfx/src/gl_loader.c` (and its generator, `tools/gen_glloader.py` -- the `.c`/`.h`
  pair is GENERATED, never hand-edited) only resolved GL entry points via `dlopen`/`dlsym`
  (`<dlfcn.h>`), a POSIX-only API that does not exist on Windows -- any consumer attempting a
  Windows build (GusWorld, preparing a Windows target) hit a hard compile break at the very first
  `#include <dlfcn.h>`. Fixed with an `#ifdef _WIN32` branch in the generator's source preamble
  and load-function template: the Win32 codepath mirrors the well-known GLAD
  `GLAD_PLATFORM_WIN32` resolution strategy -- `wglGetProcAddress()` first (covers GL >=1.2 core
  entry points and every extension, with a guard against its 4 documented failure sentinel values
  `NULL`/`0x1`/`0x2`/`0x3`/`-1`, which the naive nullptr-only check would miss), falling back to a
  direct `GetProcAddress()` against `opengl32.dll` (covers GL 1.0/1.1, which
  `wglGetProcAddress` is documented to never resolve). `opengl32.dll` is loaded via
  `LoadLibraryA()`, never linked (`-lopengl32`) -- mirrors the POSIX branch's
  dlopen-not-link posture, so consumers' link lines stay unchanged. The `#else` POSIX branch is
  byte-identical to the pre-fix source (diff is additions only); `gl_loader.h` is untouched.
  Validated by a MinGW-w64 cross-compile (produces a valid PE/COFF x86-64 object) plus a
  non-regression build + `ctest -R gl_loader` on Linux.
  **PT:** `glintfx/src/gl_loader.c` (e seu gerador, `tools/gen_glloader.py` -- o par `.c`/`.h` é
  GERADO, nunca editado à mão) só resolvia entry points GL via `dlopen`/`dlsym` (`<dlfcn.h>`), uma
  API POSIX-only que não existe no Windows -- qualquer consumidor tentando um build Windows
  (GusWorld, preparando um alvo Windows) batia numa quebra de compilação logo no primeiro
  `#include <dlfcn.h>`. Corrigido com um ramo `#ifdef _WIN32` no preâmbulo de source e no template
  da função de load do gerador: o caminho Win32 espelha a estratégia de resolução
  `GLAD_PLATFORM_WIN32` do GLAD, bem conhecida -- `wglGetProcAddress()` primeiro (cobre entry
  points core GL >=1.2 e toda extensão, com uma guarda contra os 4 valores-sentinela de falha
  documentados `NULL`/`0x1`/`0x2`/`0x3`/`-1`, que a checagem ingênua só-de-nullptr deixaria
  passar), caindo para `GetProcAddress()` direto em `opengl32.dll` (cobre GL 1.0/1.1, que o
  `wglGetProcAddress` é documentado a nunca resolver). `opengl32.dll` é carregada via
  `LoadLibraryA()`, nunca linkada (`-lopengl32`) -- espelha a postura dlopen-sem-linkar do ramo
  POSIX, então as linhas de link dos consumidores não mudam. O ramo `#else` POSIX é
  byte-idêntico ao source pré-fix (diff é só adições); `gl_loader.h` está intocado. Validado por
  um cross-compile MinGW-w64 (produz um objeto PE/COFF x86-64 válido) mais um build de
  não-regressão + `ctest -R gl_loader` no Linux.

### Infrastructure / Infraestrutura

- **EN:** CI gate order canonicalized as `github` > `claudio` (self-hosted local runner,
  `.forgejo/workflows/heavy.yml`, `fedora:42` container, runs ASan+UBSan sanitize + the own-font-engine
  regression job) > `codeberg` (paritypruned to a single build leg, voluntary, never blocks a
  release); a single green leg is sufficient. Added a local pre-push gate
  (`tools/preci.sh` + `.githooks/pre-push`, `TST-L1-PRECI`) that reproduces the fast checks before
  a push leaves the machine. Fixed a `CXXFLAGS`/`SYS_futex` build break on the `fedora:42`
  container's `clang` for the font-engine job.
  **PT:** ordem de gate de CI canonizada como `github` > `claudio` (runner local self-hosted,
  `.forgejo/workflows/heavy.yml`, container `fedora:42`, roda ASan+UBSan sanitize + o job de
  regressão do motor de fonte próprio) > `codeberg` (podado a uma única perna de build, voluntário,
  nunca bloqueia release); uma perna verde já basta. Adicionado um gate local pre-push
  (`tools/preci.sh` + `.githooks/pre-push`, `TST-L1-PRECI`) que reproduz as checagens rápidas antes
  de um push sair da máquina. Corrigida uma quebra de build de `CXXFLAGS`/`SYS_futex` no `clang` do
  container `fedora:42` pro job do motor de fonte.

### Documentation / Documentação

- **EN:** `docs/embed-integration.md` §18 reinforced with a real consumer-found integration case
  (GusWorld, 2026-07-10): a new "If your host declares RmlUi before glintfx" subsection documents
  CMake `FetchContent`'s **first-population-wins** rule -- when a host declares its own
  `FetchContent_Declare(RmlUi ...)` before glintfx's (a common pattern for pinning a `GIT_TAG` /
  setting `RMLUI_FONT_ENGINE`), glintfx's `PATCH_COMMAND` for the teardown-UB patch never reaches the
  winning tree, so the 2 `vptr:*` UBs from section 18's intro persist even on glintfx v0.9.1+. The fix
  (apply the same patch to the host's own `FetchContent_Declare(RmlUi ...)`, pointing at glintfx's
  tracked `.patch` file with the same idempotent `--reverse --check || apply` idiom) is validated:
  `vptr:*` findings 9 -> 0 under UBSan, full 650-test suite green. Also added: a short note that
  one-time LSan leak reports at the first GL context (`libEGL_nvidia`/GLVND `LoadVendors`,
  `eglGetProcAddress`, Mesa/llvmpipe JIT regions) are third-party driver/JIT noise, not glintfx's or
  RmlUi's to fix -- diagnose with `fast_unwind_on_malloc=0:print_module_map=1`, suppress host-side.
  Doc-only change, no code/patch/CMake touched.
- **PT:** a §18 de `docs/embed-integration.md` foi reforçada com um caso de integração real
  encontrado por um consumidor (GusWorld, 2026-07-10): uma nova subseção "Se o seu host declara o
  RmlUi antes do glintfx" documenta a regra **first-population-wins** do `FetchContent` do CMake --
  quando um host declara a própria `FetchContent_Declare(RmlUi ...)` antes da da glintfx (padrão
  comum pra pinar um `GIT_TAG` / setar `RMLUI_FONT_ENGINE`), o `PATCH_COMMAND` da glintfx pro patch de
  UB de teardown nunca alcança a árvore vencedora, então os 2 UBs `vptr:*` da introdução da seção 18
  persistem mesmo na glintfx v0.9.1+. A correção (aplicar o mesmo patch na própria
  `FetchContent_Declare(RmlUi ...)` do host, apontando pro arquivo `.patch` rastreado da glintfx com o
  mesmo idiom idempotente `--reverse --check || apply`) está validada: achados `vptr:*` de 9 para 0
  sob UBSan, suíte completa de 650 testes verde. Também adicionada: uma nota curta de que relatórios
  de leak únicos do LSan no primeiro contexto GL (`libEGL_nvidia`/`LoadVendors` do GLVND,
  `eglGetProcAddress`, regiões JIT do Mesa/llvmpipe) são ruído de driver/JIT de terceiro, não da
  glintfx nem do RmlUi corrigir -- diagnosticar com `fast_unwind_on_malloc=0:print_module_map=1`,
  suprimir do lado do host. Mudança só-de-doc, nenhum código/patch/CMake tocado.

---

## [0.9.1] - 2026-07-10 · [GitHub](https://github.com/petrinhu/glintfx/releases/tag/v0.9.1)

**EN:** **Patch** release -- robustness/infra, no public API surface change. The headline fix is a RmlUi teardown-order undefined-behavior patch (consumer-driven by GusWorld, corrects a real UB in every from-source build of glintfx). Also landing: glintfx's own font engine (`L1.19-FONTENG`) with an opt-in kerning pass (`GLINTFX_OWN_FONT_ENGINE` stays `OFF` by default -- zero behaviour change for existing consumers), a new CI job gating that opt-in build, and a CI-only fix for a `libstdc++14`+`clang` compile break. **No existing signature changed; this release is a drop-in upgrade from v0.9.0.**
**PT:** Release **patch** -- robustez/infra, sem mudança de superfície pública de API. O fix de destaque é um patch de undefined-behavior de ordem-de-teardown do RmlUi (consumer-driven pelo GusWorld, corrige um UB real em todo build from-source do glintfx). Também chegando: o motor de fonte próprio do glintfx (`L1.19-FONTENG`) com um passe de kerning opt-in (`GLINTFX_OWN_FONT_ENGINE` continua `OFF` por padrão -- zero mudança de comportamento pra consumidor existente), um job de CI novo protegendo esse build opt-in, e um fix só-de-CI pra uma quebra de compilação `libstdc++14`+`clang`. **Nenhuma assinatura existente mudou; esta release é um upgrade drop-in a partir da v0.9.0.**

### Fixed / Corrigido

- **EN:** `glintfx/patches/rmlui-2cd28864-teardown-ub.patch` -- the 2 RmlUi teardown-order UBs
  documented in the entry right below (`vptr:ElementDocument`, `vptr:*WidgetScroll.cpp*` +
  `vptr:*RemoveEventListener*`) are now fixed AT THE ROOT in our own build via an explicit,
  tracked source patch applied to the fetched RmlUi source through `FetchContent_Declare`'s
  `PATCH_COMMAND` (`glintfx/CMakeLists.txt`), instead of being suppressed at the symptom. This
  is not a silent fork of the pinned commit: the patch is a committed file, reviewed, and
  meant to be temporary (reported/submitted upstream, dropped once mikke89/RmlUi carries an
  equivalent fix and the pin is bumped past it) -- see `glintfx/patches/README.md` for the
  full writeup of both fixes. `PATCH_COMMAND` is idempotent by design (`git apply --reverse
  --check` probes whether the patch is already applied before attempting a forward `git
  apply`), verified safe across repeated reconfigures. The 3 corresponding suppression
  entries in `glintfx/tests/ubsan_suppressions.txt` are REMOVED (the file now documents why it
  is empty rather than suppressing anything) -- validated under `GLINTFX_SANITIZE=ON`
  (ASan+UBSan): zero `vptr:*` findings for these 3 patterns across the full suite (both
  `GLINTFX_BACKEND_GLFW=ON`/`OFF`), where previously the suppressions were required for a
  clean run. **Gotcha for existing local build directories:** CMake `FetchContent` only
  populates (and therefore only patches) a dependency ONCE per build directory -- a stale
  `build*/_deps/rmlui-src` fetched before this patch existed will NOT be retroactively
  patched by a routine reconfigure; delete the build directory (or at least its
  `_deps/rmlui-*` subdirectories) to force a fresh, patched populate.
  **PT:** `glintfx/patches/rmlui-2cd28864-teardown-ub.patch` -- os 2 UBs de ordem-de-teardown
  do RmlUi documentados na entrada logo abaixo (`vptr:ElementDocument`,
  `vptr:*WidgetScroll.cpp*` + `vptr:*RemoveEventListener*`) agora são corrigidos NA RAIZ no
  nosso próprio build via um patch de fonte explícito e rastreado, aplicado ao source fetchado
  do RmlUi através do `PATCH_COMMAND` do `FetchContent_Declare` (`glintfx/CMakeLists.txt`), em
  vez de suprimidos no sintoma. Isto não é um fork silencioso do commit pinado: o patch é um
  arquivo commitado, revisado, e pensado para ser temporário (reportado/submetido ao upstream,
  removido assim que o mikke89/RmlUi carregar uma correção equivalente e o pin avançar para
  além dela) -- ver `glintfx/patches/README.md` para o relato completo das duas correções. O
  `PATCH_COMMAND` é idempotente por design (`git apply --reverse --check` sonda se o patch já
  está aplicado antes de tentar um `git apply` pra frente), validado seguro em reconfigurações
  repetidas. As 3 entradas de suppression correspondentes em
  `glintfx/tests/ubsan_suppressions.txt` são REMOVIDAS (o arquivo agora documenta por que está
  vazio em vez de suprimir qualquer coisa) -- validado sob `GLINTFX_SANITIZE=ON` (ASan+UBSan):
  zero achados `vptr:*` para estes 3 padrões em toda a suíte (`GLINTFX_BACKEND_GLFW=ON` e
  `OFF`), onde antes as suppressions eram necessárias para uma execução limpa. **Gotcha para
  diretórios de build locais já existentes:** o `FetchContent` do CMake só popula (e portanto
  só patcheia) uma dependência UMA VEZ por diretório de build -- um `build*/_deps/rmlui-src`
  obsoleto, fetchado antes deste patch existir, NÃO é retroativamente patcheado por uma
  reconfiguração de rotina; apague o diretório de build (ou ao menos as subpastas
  `_deps/rmlui-*` dele) para forçar um populate novo e patcheado.

- **EN:** **CI-only fix:** an `SYS_futex`-undeclared compile break on RmlUi's from-scratch build under GitHub Actions' `ubuntu-latest` runner (mirrored on Codeberg's `ghcr.io/catthehacker/ubuntu:act-latest` container) -- a known `libstdc++14`+`clang` interaction where `<bits/atomic_wait.h>` references the `SYS_futex` macro without including `<sys/syscall.h>` itself (GCC's own headers pull that include in transitively elsewhere; clang's driver does not). Surfaced first in the new `build-and-test-fonteng` job below (a `_deps/RmlUi` cache miss forced a from-scratch recompile), but was a latent "cache time-bomb" in every job that compiles RmlUi (`build-and-test`, `coverage` too) -- any of them would have broken on the next cache-key rotation/expiry, not a problem isolated to the new job. Fixed additively, `CXXFLAGS="-include sys/syscall.h"` via job `env` in all three jobs that run `cmake --build`, both `.github/workflows/ci.yml` and `.forgejo/workflows/ci.yml`, kept in sync (`lint-and-scan` untouched -- it only configures, never compiles RmlUi). Forces the declaration to exist without changing the futex codepath itself (unlike the alternative `-D_GLIBCXX_HAVE_LINUX_FUTEX=0`). CI-only; no library source touched.
  **PT:** **Fix só-de-CI:** uma quebra de compilação por `SYS_futex` não-declarado no build from-scratch do RmlUi sob o runner `ubuntu-latest` do GitHub Actions (espelhado no container `ghcr.io/catthehacker/ubuntu:act-latest` do Codeberg) -- uma interação conhecida `libstdc++14`+`clang` em que `<bits/atomic_wait.h>` referencia a macro `SYS_futex` sem incluir `<sys/syscall.h>` ele mesmo (os headers do GCC puxam esse include transitivamente em outro lugar; o driver do clang não). Surgiu primeiro no novo job `build-and-test-fonteng` abaixo (um cache miss de `_deps/RmlUi` forçou uma recompilação from-scratch), mas era uma "bomba-relógio de cache" latente em todo job que compila o RmlUi (`build-and-test`, `coverage` também) -- qualquer um deles quebraria na próxima rotação/expiração de chave de cache, não um problema isolado do job novo. Corrigido de forma aditiva, `CXXFLAGS="-include sys/syscall.h"` via `env` de job nos três jobs que rodam `cmake --build`, nos dois workflows `.github/workflows/ci.yml` e `.forgejo/workflows/ci.yml`, em sincronia (`lint-and-scan` intocado -- só configura, nunca compila o RmlUi). Força a declaração a existir sem mudar o codepath do futex em si (ao contrário da alternativa `-D_GLIBCXX_HAVE_LINUX_FUTEX=0`). Só CI; nenhum source da biblioteca tocado.

### Added / Adicionado

- **EN:** Doc-only: `docs/embed-integration.md` section 18, "Running the host under sanitizers (ASan/UBSan/LSan)", answering a real triage request from the GusWorld consumer, which hit the same RmlUi teardown-order UBs (`vptr:ElementDocument`, `vptr:*WidgetScroll.cpp*` + `vptr:*RemoveEventListener*`) when running its host under `-fsanitize=address,undefined`. **Written twice within this same `[Unreleased]` window:** first (superseded) to summarize the 3 UBSan suppressions that were then recommended; **revised** once the root-cause patch above landed and the suppressions were removed -- now explains that hosts building glintfx from source (`FetchContent`/`add_subdirectory`) no longer need them, keeps the 3 `vptr:...` lines only as a fallback for hosts pinning their own RmlUi copy or an older glintfx release, and links `glintfx/patches/README.md`. Also adds an explicit RmlUi credit/acknowledgment (`README.md`, `NOTICE`, wiki `Layer-1-glintfx`/`FAQ`) noting the intent to report this patch upstream to mikke89/RmlUi. No code or suppression-file change.
  **PT:** Só doc: seção 18 de `docs/embed-integration.md`, "Rodando o host sob sanitizers (ASan/UBSan/LSan)", respondendo a um pedido real de triagem do consumidor GusWorld, que topou nos mesmos UBs de ordem-de-teardown do RmlUi (`vptr:ElementDocument`, `vptr:*WidgetScroll.cpp*` + `vptr:*RemoveEventListener*`) ao rodar o próprio host sob `-fsanitize=address,undefined`. **Escrita duas vezes dentro desta mesma janela `[Unreleased]`:** primeiro (superada) resumindo as 3 supressões UBSan então recomendadas; **revisada** assim que o patch de causa-raiz acima entrou e as supressões foram removidas -- agora explica que hosts que buildam o glintfx do fonte (`FetchContent`/`add_subdirectory`) não precisam mais delas, mantém as 3 linhas `vptr:...` só como fallback pra hosts que pinam a própria cópia do RmlUi ou uma release mais antiga do glintfx, e linka `glintfx/patches/README.md`. Também adiciona crédito/agradecimento explícito ao RmlUi (`README.md`, `NOTICE`, wiki `Layer-1-glintfx`/`FAQ`) sinalizando a intenção de reportar este patch ao upstream mikke89/RmlUi. Nenhuma mudança de código ou de arquivo de suppression.

- **EN:** `L1.19-FONTENG` (FT-F3) -- glintfx's own font engine, the JUNCTION piece between the two layers of this repository: a new `Rml::FontEngineInterface` implementation (`glintfx/src/font_engine_own.{hpp,cpp}`) backed entirely by Layer 0's clean-room `SOV-SFNT` (TrueType parsing, `src/sfnt.c`) and `SOV-RAST` (anti-aliased outline rasterization, `src/raster.c`), `extern "C"`-linked directly into the `glintfx` static library per ADR-0009's "hosted shell + C meat" pattern. New CMake option `GLINTFX_OWN_FONT_ENGINE` (default `OFF`) is a **runtime** A/B gate -- `RMLUI_FONT_ENGINE=freetype` stays the CMake-time default for the fetched RmlUi build either way; `Rml::SetFontEngineInterface()` overrides RmlUi's active engine at runtime in `Bootstrap::init()`, so switching sides needs no RmlUi rebuild. `OFF` (default) is a fully inert no-op -- zero behaviour change for every existing consumer (e.g. GusWorld). Requires building from inside the `loucura_c_asm` monorepo (Layer 0's `src/sfnt.c`/`src/raster.c`/`include/core/*.h` must be siblings of `glintfx/`) -- a hard `FATAL_ERROR` guard explains this clearly if a standalone `glintfx/`-only checkout tries to enable it. **v1 scope (documented, not a request for review):** a face instance eagerly bakes its ENTIRE glyph atlas once, on first `GetFontFaceHandle()` resolution, over a FIXED codepoint set (printable ASCII + the pt-br-accented Latin-1 Supplement -- á/à/â/ã/ç/é/ê/í/ó/ô/õ/ú/ü/ñ and uppercase); no lazy/incremental atlas growth, no font effects (`PrepareFontEffects` is a no-op), no kerning (`SOV-SFNT` itself does not parse `kern`/`GPOS`) **-- kerning support was added later within this same release, see the entry below**, no `.ttc` collection support, no `name`-table parsing. New engine-agnostic visual test `fonteng_own_sanity` (registered unconditionally, both `GLINTFX_OWN_FONT_ENGINE` sides) proves legible glyph coverage (not blank, not a filled block) across 3 lines incl. the accented set. **A real correctness bug was found and fixed before landing:** `Rml::CallbackTexture` (our atlas's GPU resource) must be released BEFORE `Rml::Shutdown()` destroys each `Context`'s `RenderManager` -- `Rml::Shutdown()` itself calls `contexts.clear()` BEFORE it calls `font_interface->Shutdown()` (confirmed reading the pinned RmlUi source), so relying on the `FontEngineInterface::Shutdown()` hook alone crashed on every test teardown ("Leaking CallbackTexture detected... will likely result in memory corruption"). Fixed by having `Bootstrap::shutdown()` call `FontEngineOwn::Shutdown()` EXPLICITLY, ahead of its own `Rml::Shutdown()` call. Suite grows to **39 tests with `GLINTFX_BACKEND_GLFW=ON`** (both `GLINTFX_OWN_FONT_ENGINE=ON` and `=OFF` verified green, 39/39 each); embed-only (`=OFF`) build unaffected/untouched by this change.
  **PT:** `L1.19-FONTENG` (FT-F3) -- motor de fonte próprio do glintfx, a peça de JUNÇÃO entre as duas camadas deste repositório: uma nova implementação de `Rml::FontEngineInterface` (`glintfx/src/font_engine_own.{hpp,cpp}`) apoiada inteiramente no `SOV-SFNT` clean-room da Camada 0 (parsing TrueType, `src/sfnt.c`) e `SOV-RAST` (rasterização anti-aliased de outline, `src/raster.c`), linkados via `extern "C"` diretamente na biblioteca estática `glintfx` conforme o padrão "casca-hosted + carne-C" da ADR-0009. Nova option de CMake `GLINTFX_OWN_FONT_ENGINE` (padrão `OFF`) é um gate A/B de **runtime** -- `RMLUI_FONT_ENGINE=freetype` continua o padrão em tempo de CMake pro build fetchado do RmlUi de qualquer forma; `Rml::SetFontEngineInterface()` sobrepõe o motor ativo do RmlUi em runtime no `Bootstrap::init()`, então trocar de lado não exige rebuild do RmlUi. `OFF` (padrão) é um no-op totalmente inerte -- zero mudança de comportamento pra todo consumidor existente (ex.: GusWorld). Exige buildar de dentro do monorepo `loucura_c_asm` (`src/sfnt.c`/`src/raster.c`/`include/core/*.h` da Camada 0 precisam ser irmãos de `glintfx/`) -- um guard `FATAL_ERROR` duro explica isso claramente se um checkout standalone só-`glintfx/` tentar habilitar. **Escopo v1 (documentado, não um pedido de revisão):** uma instância de face empacota o atlas de glyph INTEIRO avidamente, uma vez, na 1ª resolução de `GetFontFaceHandle()`, sobre um conjunto FIXO de codepoint (ASCII imprimível + o Latin-1 Supplement acentuado pt-br -- á/à/â/ã/ç/é/ê/í/ó/ô/õ/ú/ü/ñ e maiúsculas); sem crescimento de atlas preguiçoso/incremental, sem efeitos de fonte (`PrepareFontEffects` é no-op), sem kerning (o próprio `SOV-SFNT` não parseia `kern`/`GPOS`) **-- suporte a kerning foi adicionado depois dentro desta mesma release, ver a entrada abaixo**, sem suporte a coleção `.ttc`, sem parsing de tabela `name`. Novo teste visual agnóstico-de-motor `fonteng_own_sanity` (registrado incondicionalmente, nos dois lados de `GLINTFX_OWN_FONT_ENGINE`) prova cobertura de glyph legível (nem vazio, nem bloco preenchido) em 3 linhas incl. o conjunto acentuado. **Um bug real de correção foi achado e corrigido antes de subir:** `Rml::CallbackTexture` (o recurso GPU do nosso atlas) precisa ser liberado ANTES do `Rml::Shutdown()` destruir o `RenderManager` de cada `Context` -- o próprio `Rml::Shutdown()` chama `contexts.clear()` ANTES de chamar `font_interface->Shutdown()` (confirmado lendo o source pinado do RmlUi), então confiar só no hook `FontEngineInterface::Shutdown()` crashava em todo teardown de teste ("Leaking CallbackTexture detected... will likely result in memory corruption"). Corrigido fazendo o `Bootstrap::shutdown()` chamar `FontEngineOwn::Shutdown()` EXPLICITAMENTE, antes da própria chamada a `Rml::Shutdown()`. Suíte cresce para **39 testes com `GLINTFX_BACKEND_GLFW=ON`** (`GLINTFX_OWN_FONT_ENGINE=ON` e `=OFF` verificados verdes, 39/39 cada); build embed-only (`=OFF`) não afetado/intocado por esta mudança.

- **EN:** **Kerning in glintfx's own font engine** (`L1.19-FONTENG`/`L1.20-FONTFLIP`, opt-in -- `GLINTFX_OWN_FONT_ENGINE` stays `OFF` by default, zero behaviour change for every existing consumer including GusWorld): `FontEngineOwn::IterateGlyphs()` (the single code path shared by `GetStringWidth()` and `GenerateString()`, guaranteeing layout width == mesh width) now folds `glx_sfnt_kern(face, prev_gid, cur_gid)` -- Layer 0's new clean-room `kern`-table reader (classic format, header `version==0`, format-0 horizontal subtable only; `GPOS` and Microsoft's "extended" `version==0x0001` variant are out of scope, cleanly detected and skipped) -- into the pen position between every consecutive baked-glyph pair, scaled by the same `pixel_size/units_per_em` factor already used for advance/outline. `GlyphInfo` gained a `gid` field (the `kern` table is indexed by glyph id, not codepoint), filled in `BakeFaceInstance()`. Horizontal LTR pairs only; no kerning before the first glyph of a string or around a non-baked glyph; no mid-loop rounding -- only the final total is rounded (`std::lround` in both `GetStringWidth()`/`GenerateString()`), preserving the layout==mesh guarantee even with kerning applied. New numeric-oracle test `fonteng_own_kern_sanity` (measures via `UiLayer::get_element_box`, not visual): `"AV"` (`kern(A,V) = -82` font units in Open Sans, confirmed against the real `.ttf`) measures at least 2px tighter than `width(A)+width(V)`; `"HH"` (no kerning entry) stays within +-1px of the control (a tolerance that documents a rounding artifact -- "sum floats then round once" vs. "round each part" -- unrelated to kerning itself). Registered outside the `GLINTFX_BACKEND_GLFW` gate (runs in both backend configs), inside `GLINTFX_OWN_FONT_ENGINE`. With the flag `ON`: suite grows to 41 tests (`GLFW=ON`) / 24 (embed-only). With the default `OFF`: unaffected, still **39/22, no regression**.
  **PT:** **Kerning no motor de fonte próprio do glintfx** (`L1.19-FONTENG`/`L1.20-FONTFLIP`, opt-in -- `GLINTFX_OWN_FONT_ENGINE` continua `OFF` por padrão, zero mudança de comportamento pra todo consumidor existente incl. o GusWorld): `FontEngineOwn::IterateGlyphs()` (o único caminho de código compartilhado por `GetStringWidth()` e `GenerateString()`, que garante largura-de-layout == largura-do-mesh) agora dobra `glx_sfnt_kern(face, prev_gid, cur_gid)` -- o novo leitor clean-room da tabela `kern` da Camada 0 (formato clássico, header `version==0`, só subtable horizontal formato 0; `GPOS` e a variante "estendida" da Microsoft (`version==0x0001`) ficam fora de escopo, detectadas e puladas de forma limpa) -- na posição do pen entre todo par consecutivo de glyphs bakeados, escalado pelo mesmo fator `pixel_size/units_per_em` já usado pra advance/outline. `GlyphInfo` ganhou o campo `gid` (a tabela `kern` é indexada por gid, não codepoint), preenchido em `BakeFaceInstance()`. Só pares horizontais LTR; sem kern antes do 1º glyph da string ou ao redor de um glyph não-bakeado; sem arredondamento no meio do laço -- só o total final é arredondado (`std::lround` em `GetStringWidth()`/`GenerateString()`), preservando a garantia layout==mesh mesmo com kerning aplicado. Novo teste de oráculo NUMÉRICO `fonteng_own_kern_sanity` (mede via `UiLayer::get_element_box`, não visual): `"AV"` (`kern(A,V) = -82` FUnits na Open Sans, confirmado contra o `.ttf` real) mede pelo menos 2px mais apertado que `width(A)+width(V)`; `"HH"` (sem entrada de kerning) fica dentro de +-1px do controle (tolerância que documenta um artefato de arredondamento -- "soma floats depois arredonda uma vez" vs. "arredonda cada parte" -- independente do kerning em si). Registrado fora do gate `GLINTFX_BACKEND_GLFW` (roda nas duas configs de backend), dentro de `GLINTFX_OWN_FONT_ENGINE`. Com a flag `ON`: suíte cresce para 41 testes (`GLFW=ON`) / 24 (embed-only). Com o padrão `OFF`: não afetado, continua **39/22, sem regressão**.

- **EN:** **New CI job `build-and-test-fonteng`** (both `.github/workflows/ci.yml` and `.forgejo/workflows/ci.yml`, mirrored step-for-step): builds and tests glintfx with `-DGLINTFX_OWN_FONT_ENGINE=ON -DGLINTFX_BUILD_TESTS=ON` across both `GLINTFX_BACKEND_GLFW` values (`ON`/`OFF`, the embed-only mode GusWorld actually consumes), under Xvfb/Mesa llvmpipe, on every push/PR. Closes the regression gap for the opt-in font engine (`L1.19-FONTENG`, `SOV-SFNT`/`SOV-RAST`), which was previously exercised only locally/ad hoc -- `fonteng_own_sanity` (engine-agnostic oracle, `GLFW=ON`) and `fonteng_own_embed_sanity` (`UiLayer` path, both backends) now run in CI on every push/PR. Own build directory and RmlUi `FetchContent` cache key (suffixed `fonteng`), same pinned RmlUi commit as the default job, compiled with clang like every other job. Validated locally with `forgejo-runner exec -l` and `forgejo-runner exec -j build-and-test-fonteng -n` (dry-run) before landing, per the project's rule for CI workflow changes. Infra-only -- no change to the default (`GLINTFX_OWN_FONT_ENGINE=OFF`) build or its test count.
  **PT:** **Novo job de CI `build-and-test-fonteng`** (`.github/workflows/ci.yml` e `.forgejo/workflows/ci.yml`, espelhados passo-a-passo): builda e testa o glintfx com `-DGLINTFX_OWN_FONT_ENGINE=ON -DGLINTFX_BUILD_TESTS=ON` nos dois valores de `GLINTFX_BACKEND_GLFW` (`ON`/`OFF`, o modo embed-only que o GusWorld de fato consome), sob Xvfb/Mesa llvmpipe, em todo push/PR. Fecha a lacuna de regressão do motor de fonte opt-in (`L1.19-FONTENG`, `SOV-SFNT`/`SOV-RAST`), antes só exercitado localmente/ad hoc -- `fonteng_own_sanity` (oráculo agnóstico de motor, `GLFW=ON`) e `fonteng_own_embed_sanity` (caminho `UiLayer`, ambos os backends) agora rodam no CI em todo push/PR. Build dir e chave de cache do `FetchContent` do RmlUi próprios (sufixo `fonteng`), mesmo commit pinado do RmlUi que o job default, compilado com clang igual aos demais jobs. Validado localmente com `forgejo-runner exec -l` e `forgejo-runner exec -j build-and-test-fonteng -n` (dry-run) antes de subir, conforme a regra do projeto pra mudança de workflow de CI. Só infra -- nenhuma mudança no build default (`GLINTFX_OWN_FONT_ENGINE=OFF`) nem na sua contagem de testes.

---

## [0.9.0] - 2026-07-10 · [GitHub](https://github.com/petrinhu/glintfx/releases/tag/v0.9.0)

**EN:** **Minor** release -- new public API: DOM read/write by id plus data-model read-back (`set_text`/`add_class`/`remove_class`/`set_property`, `get_number`/`get_string`/`get_bool`, `L1.16-DOMRW`) and 5 form/DOM event callbacks (`set_change`/`set_submit`/`set_focus`/`set_blur`/`set_hover_callback`, `L1.15-FORMEV`), plus self-hosted code coverage in CI (llvm-cov, badge, 75% floor gate, `L1.18-COVCI`, infra-only, no API surface change). No existing signature changed; drop-in from a consumer's point of view.
**PT:** Release **minor** -- API pública nova: leitura/escrita de DOM por id mais read-back do data-model (`set_text`/`add_class`/`remove_class`/`set_property`, `get_number`/`get_string`/`get_bool`, `L1.16-DOMRW`) e 5 callbacks de evento form/DOM (`set_change`/`set_submit`/`set_focus`/`set_blur`/`set_hover_callback`, `L1.15-FORMEV`), mais cobertura de código self-hosted no CI (llvm-cov, badge, gate de piso 75%, `L1.18-COVCI`, só infra, sem mudança de superfície de API). Nenhuma assinatura existente mudou; drop-in do ponto de vista de um consumidor.

### Added / Adicionado

- **EN:** `L1.18-COVCI` -- self-hosted code coverage (líder's 2026-07-04 decision: llvm-cov, **not** Codecov or any third-party uploader; zero data leaves the repo/CI). New CMake option `GLINTFX_COVERAGE` (Clang source-based coverage, `-fprofile-instr-generate -fcoverage-mapping`; a non-Clang compiler is a hard `message(FATAL_ERROR)`, no gcov/lcov fallback) applies instrumentation **per-target** via the new `glintfx_instrument_coverage()` CMake function -- called on the `glintfx` library target and, from a single loop over `tests/`'s `BUILDSYSTEM_TARGETS`, on every test/demo-helper target -- deliberately NOT the directory-wide `add_compile_options()` idiom `GLINTFX_SANITIZE` uses, so RmlUi (`_deps`, ~200 TUs) and `third_party/` stay uninstrumented. `OFF` by default: zero-effect on the normal build. New local-first script `tools/coverage_report.sh` (the "gêmeo local" the CI wraps): configure+build (`GLFW=ON`, `GLINTFX_BUILD_DEMOS=OFF` -- an uninstrumented demo cannot link an instrumented library) -> `ctest` under Xvfb with `LLVM_PROFILE_FILE` -> `llvm-profdata merge` -> `llvm-cov show` (HTML report) + `llvm-cov export -summary-only` (%) -> `--floor N` CI gate mode / `--update-badge` writes the `shields.io` endpoint JSON (`glintfx/coverage-badge.json`) locally only -- the CI `coverage` job (new, both `.github/workflows/ci.yml` and `.forgejo/workflows/ci.yml`) validates against `--floor 75` (83.8% measured 2026-07-10, rounded down to the nearest multiple of 5 minus 5pp headroom -- see `TESTES.md` `TST-L1-COV`) and uploads the HTML report as an artifact, but never commits/pushes the badge JSON back. README badge added (`img.shields.io/endpoint` reading the committed JSON). Validated with `forgejo-runner exec` scoped to the `coverage` job before landing (project rule for CI workflow changes).
  **PT:** `L1.18-COVCI` -- cobertura de código self-hosted (decisão do líder de 2026-07-04: llvm-cov, **não** Codecov nem nenhum uploader de terceiro; zero dado sai do repo/CI). Nova option CMake `GLINTFX_COVERAGE` (cobertura source-based do Clang, `-fprofile-instr-generate -fcoverage-mapping`; compilador não-Clang é `message(FATAL_ERROR)` duro, sem fallback gcov/lcov) aplica instrumentação **por-alvo** via a nova função CMake `glintfx_instrument_coverage()` -- chamada no alvo da biblioteca `glintfx` e, a partir de um único loop sobre os `BUILDSYSTEM_TARGETS` de `tests/`, em todo alvo de teste/helper de demo -- deliberadamente NÃO o idioma `add_compile_options()` de escopo de diretório que o `GLINTFX_SANITIZE` usa, para que o RmlUi (`_deps`, ~200 TUs) e `third_party/` fiquem não-instrumentados. `OFF` por padrão: zero efeito no build normal. Novo script local-first `tools/coverage_report.sh` (o "gêmeo local" que o CI envolve): configure+build (`GLFW=ON`, `GLINTFX_BUILD_DEMOS=OFF` -- um demo não-instrumentado não linka uma biblioteca instrumentada) -> `ctest` sob Xvfb com `LLVM_PROFILE_FILE` -> `llvm-profdata merge` -> `llvm-cov show` (relatório HTML) + `llvm-cov export -summary-only` (%) -> modo gate `--floor N` pro CI / `--update-badge` escreve o JSON endpoint do `shields.io` (`glintfx/coverage-badge.json`) só localmente -- o job `coverage` do CI (novo, nos dois remotos `.github/workflows/ci.yml` e `.forgejo/workflows/ci.yml`) valida contra `--floor 75` (83.8% medido em 2026-07-10, arredondado pra baixo ao múltiplo de 5 mais próximo menos 5pp de folga -- ver `TESTES.md` `TST-L1-COV`) e sobe o relatório HTML como artifact, mas nunca commita/pusha o JSON do badge de volta. Badge no README adicionado (`img.shields.io/endpoint` lendo o JSON commitado). Validado com `forgejo-runner exec` escopado ao job `coverage` antes de subir (regra do projeto pra mudança de workflow de CI).

- **EN:** `L1.15-FORMEV` -- 5 observable-event callbacks on both `UiLayer` and `App` (10 methods), closing `AUD-PUB-6(c)`: `set_change_callback(fn(id, value))`, `set_submit_callback(fn(id))`, `set_focus_callback(fn(id))`, `set_blur_callback(fn(id))`, `set_hover_callback(fn(id, entered))` -- forms were inert to the host before this (the only observable element activity was click/scroll). glintfx does NOT play audio itself; these are the hooks a host uses to trigger its own sound/UX reaction (the líder's explicit anchor use case, including hover/focus sound hooks). `change`/`submit` are bubble-phase document-root listeners, a mechanical extension of `set_scroll_callback`'s pattern; `value` on `change` is RmlUi's own `"value"` event parameter, forwarded verbatim (already normalised per control kind -- text/checkbox/radio/select/slider); `submit` reports the `<form>` element's own id (the real dispatch target of RmlUi's `Submit` event). `focus`/`blur` required a **CAPTURE-phase** listener (`in_capture_phase=true`) instead of the bubble-phase idiom every other listener in this family uses -- confirmed against the pinned RmlUi 6.3 source that `Focus`/`Blur` do not bubble, but RmlUi's capture phase always descends root->target regardless of the `bubbles` flag. `hover` is implemented with a SINGLE internal listener on `Mouseover` only (no separate `Mouseout` listener), holding a `current_hover_id_` dedup state that survives across raw events and resets to empty on every fresh `load()` -- this is what prevents re-firing while the cursor crosses id-less children inside the same labeled container. **Two empirical findings not anticipated by the design doc, verified by reading the pinned RmlUi source and proven in the new test (see the doc-comments on `set_focus_callback`/`set_blur_callback` in `bootstrap.hpp` for the full writeup):** (1) `Element::Focus()` on an ALREADY-focused element is a true no-op -- `Context::OnFocusChange`/`SendEvents` dedup by ancestor-chain SET DIFFERENCE, so refocusing the exact current focus target dispatches neither `Blur` nor `Focus`; (2) a SINGLE focus transition can fire `focus_callback`/`blur_callback` MORE THAN ONCE -- RmlUi dispatches one independent event PER newly-entered/exited ancestor in the chain difference (not a single event that bubbles), so a host should treat any non-empty id as a valid signal rather than assume exactly one firing per `set_focus()`/`clear_focus()` call. **A real correctness bug was found and fixed before merge:** `Mouseover` suffers from the SAME chain-set-difference fan-out as Focus/Blur (`Context::UpdateHoverChain` uses the identical `SendEvents` helper), which would have made the hover dedup non-deterministic (order-dependent across fan-out dispatches) if resolved naively from `event.GetTargetElement()`; fixed by resolving the dedup id from `Rml::Context::GetHoverElement()` instead (confirmed assigned BEFORE any Mouseover/Mouseout dispatch begins, so every fan-out dispatch within one logical mouse move converges on the identical, deterministic id). **Mandatory AUD-TEC-3 reentrancy discipline** (functor copied to a local before invoking, the v0.4.1 UAF precedent) applies to all five new listeners, same as every existing callback in this family. New TDD test `form_events_sanity` (registered in both `GLINTFX_BACKEND_GLFW` configs; drives `Engine` directly plus `Rml::Context::ProcessMouseMove`/`ProcessMouseButtonDown`/`ProcessMouseButtonUp` for input simulation, same precedent as `focus_sanity`/`domrw_sanity`) -- covers checkbox change/uncheck, form submit, the focus no-op + multi-fire + clean-blur findings above, the hover dedup "dente" (two different targets resolving to the same ancestor id must not re-fire), and reentrancy for both `submit_callback` (plain) and `hover_callback` (extra dedup state). Suite grows to **38 tests with `GLINTFX_BACKEND_GLFW=ON`, 22 with `=OFF`** (embed-only), both green.
  **PT:** `L1.15-FORMEV` -- 5 callbacks de evento observável em `UiLayer` e `App` (10 métodos), fechando `AUD-PUB-6(c)`: `set_change_callback(fn(id, value))`, `set_submit_callback(fn(id))`, `set_focus_callback(fn(id))`, `set_blur_callback(fn(id))`, `set_hover_callback(fn(id, entered))` -- formulários eram inertes pro host antes disto (a única atividade observável de elemento era click/scroll). A glintfx NÃO toca áudio; estes são os ganchos que um host usa pra disparar sua própria reação sonora/UX (o caso âncora explícito do líder, incluindo ganchos de som de hover/focus). `change`/`submit` são listeners de fase bubble na raiz do documento, extensão mecânica do padrão de `set_scroll_callback`; o `value` do `change` é o próprio parâmetro de evento `"value"` do RmlUi, encaminhado verbatim (já normalizado por tipo de controle -- texto/checkbox/radio/select/slider); `submit` reporta o próprio id do elemento `<form>` (o alvo real de despacho do evento `Submit` do RmlUi). `focus`/`blur` exigiram um listener de fase-**CAPTURA** (`in_capture_phase=true`) em vez do idioma de fase bubble que todo outro listener desta família usa -- confirmado contra o source pinado do RmlUi 6.3 que `Focus`/`Blur` não borbulham, mas a fase de captura do RmlUi sempre desce raiz->alvo independente da flag `bubbles`. `hover` é implementado com um ÚNICO listener interno em `Mouseover` apenas (sem listener separado de `Mouseout`), guardando um estado de dedup `current_hover_id_` que sobrevive entre eventos crus e reseta pra vazio a cada `load()` novo -- é isso que impede o redisparo enquanto o cursor cruza filhos sem id dentro do mesmo container com id. **Dois achados empíricos não previstos pelo doc de design, verificados lendo o source pinado do RmlUi e provados no teste novo (ver os doc-comments de `set_focus_callback`/`set_blur_callback` em `bootstrap.hpp` pro relato completo):** (1) `Element::Focus()` num elemento JÁ focado é um no-op verdadeiro -- `Context::OnFocusChange`/`SendEvents` dedupam por DIFERENÇA DE CONJUNTO da cadeia de ancestrais, então refocar o alvo de foco atual exato não despacha nem `Blur` nem `Focus`; (2) uma ÚNICA transição de foco pode disparar `focus_callback`/`blur_callback` MAIS DE UMA VEZ -- o RmlUi despacha um evento independente POR ancestral recém-entrado/saído na diferença de cadeia (não um único evento que borbulha), então um host deve tratar qualquer id não-vazio como sinal válido em vez de assumir exatamente um disparo por chamada `set_focus()`/`clear_focus()`. **Um bug real de correção foi achado e corrigido antes do merge:** o `Mouseover` sofre do MESMO fan-out por diferença-de-conjunto-de-cadeia que Focus/Blur (`Context::UpdateHoverChain` usa o mesmo helper `SendEvents`), o que tornaria o dedup do hover não-determinístico (dependente de ordem entre despachos de fan-out) se resolvido ingenuamente a partir de `event.GetTargetElement()`; corrigido resolvendo o id de dedup a partir de `Rml::Context::GetHoverElement()` em vez disso (confirmadamente atribuído ANTES de qualquer despacho de Mouseover/Mouseout começar, então todo despacho de fan-out dentro de um único movimento lógico de mouse converge pro mesmo id determinístico). **Disciplina obrigatória de reentrância AUD-TEC-3** (functor copiado pra uma local antes de invocar, o precedente UAF da v0.4.1) se aplica aos cinco listeners novos, igual a todo callback já existente desta família. Novo teste TDD `form_events_sanity` (registrado nas duas configs de `GLINTFX_BACKEND_GLFW`; dirige o `Engine` diretamente mais `Rml::Context::ProcessMouseMove`/`ProcessMouseButtonDown`/`ProcessMouseButtonUp` pra simulação de input, mesmo precedente de `focus_sanity`/`domrw_sanity`) -- cobre marcar/desmarcar checkbox, submit de form, os achados de no-op de foco + disparo múltiplo + blur limpo acima, o "dente" de dedup do hover (dois alvos diferentes resolvendo pro mesmo id ancestral não podem redisparar), e reentrância tanto pro `submit_callback` (simples) quanto pro `hover_callback` (estado extra de dedup). Suíte cresce para **38 testes com `GLINTFX_BACKEND_GLFW=ON`, 22 com `=OFF`** (embed-only), ambos verdes.

- **EN:** `L1.16-DOMRW` -- DOM read/write by id on both `UiLayer` and `App`, closing `AUD-PUB-6(d)`+`(e)`: `bool set_text(id, text)`, `bool add_class(id, cls)`, `bool remove_class(id, cls)`, `bool set_property(id, prop, value)` (thin wrappers over `Rml::Element::SetInnerRML`/`SetClass`/`SetProperty`, verified against the pinned RmlUi 6.3 source), plus `bool get_number(key, double&)`, `bool get_string(key, std::string&)`, `bool get_bool(key, bool&)` completing the data-model API that was write-only before this (`create_data_model`/`bind_*`/`set_*` only). **Security hardening on `set_text` (mandatory, not optional):** `text` is always treated as literal text, never markup -- `&`, `<`, `>`, `"` are escaped via `Rml::StringUtilities::EncodeRml` (the same encoder RmlUi's own `ElementText::GetRML()` uses internally) before the string reaches `SetInnerRML`, so untrusted text (chat, a player-chosen display name) can never be used to inject RML/tags into the document. `set_property` propagates RmlUi's own parse-failure `false` unchanged (e.g. `set_property(id, "color", "not-a-color")`); `SetClass`/`SetInnerRML` return `void` at the RmlUi level, so `add_class`/`remove_class`/`set_text` returning `true` means only "id (and class name) valid, element found". Data-model `get_*` reads glintfx's own bound cell directly -- `Rml::DataModelConstructor::Bind(name, ptr)` wraps that SAME pointer, so any RmlUi-driven bidirectional write (e.g. a user typing into a data-bound `<input>`) is visible through `get_*` with no extra RmlUi call, and a bound key is readable both before and after `load()` (only `bind_*` itself must precede `load()`). Guards (`AUD-TEC-5` fail-high, same shape as `get_element_box`/`set_focus`): all four DOM methods return `false` when no document is loaded, `id` is null/empty (`""`) or not found; `add_class`/`remove_class` additionally reject a null/empty `cls`; `set_property` additionally rejects a null/empty `prop`. `get_*` return `false` (`out` untouched) when the key was never bound -- "unknown key" and "wrong order" (reading before the matching `bind_*`) collapse to the same guard. New TDD test `domrw_sanity` (registered in both `GLINTFX_BACKEND_GLFW` configs; drives `Engine` directly, same precedent as `focus_sanity`/`document_reload_leak`) -- includes an RML-injection "bite" test proving a malicious `& < > "` payload round-trips through `GetInnerRML()` as the exactly-escaped string and creates zero child elements, not a real tag. Suite grows to **37 tests with `GLINTFX_BACKEND_GLFW=ON`, 21 with `=OFF`** (embed-only). **Cosmetic doc correction (same item, ex-INBOX finding from the `L1.17-FOCUS` review):** `docs/embed-integration.md` section 5 and `bootstrap.hpp`'s `clear_focus` doc-comment previously claimed RmlUi "never resets focus to null" -- true only for `Element::Blur()`'s own path. `Rml::Context::OnElementDetach` (confirmed against the pinned source) sets `focus = nullptr` directly whenever the focused element is detached from the tree (e.g. by `set_text` replacing children via `SetInnerRML`). glintfx's `clear_focus()` was already correct/idempotent regardless (`if (!focused) return true;`) -- no behaviour change, prose-only correction.
  **PT:** `L1.16-DOMRW` -- leitura/escrita de DOM por id em `UiLayer` e `App`, fechando `AUD-PUB-6(d)`+`(e)`: `bool set_text(id, text)`, `bool add_class(id, cls)`, `bool remove_class(id, cls)`, `bool set_property(id, prop, value)` (wrappers finos sobre `Rml::Element::SetInnerRML`/`SetClass`/`SetProperty`, verificados contra o source pinado do RmlUi 6.3), mais `bool get_number(key, double&)`, `bool get_string(key, std::string&)`, `bool get_bool(key, bool&)` completando a API de data-model que era write-only antes disto (só `create_data_model`/`bind_*`/`set_*`). **Hardening de segurança em `set_text` (obrigatório, não opcional):** `text` é sempre tratado como texto literal, nunca markup -- `&`, `<`, `>`, `"` são escapados via `Rml::StringUtilities::EncodeRml` (o mesmo encoder que o próprio `ElementText::GetRML()` do RmlUi usa internamente) antes da string chegar em `SetInnerRML`, então texto não confiável (chat, nome de exibição escolhido pelo jogador) nunca pode ser usado pra injetar RML/tags no documento. `set_property` propaga o `false` de falha de parse do próprio RmlUi sem mudança (ex.: `set_property(id, "color", "not-a-color")`); `SetClass`/`SetInnerRML` retornam `void` em nível RmlUi, então `add_class`/`remove_class`/`set_text` retornando `true` significa só "id (e nome de classe) válido, elemento encontrado". `get_*` do data-model lê a própria célula ligada da glintfx diretamente -- `Rml::DataModelConstructor::Bind(name, ptr)` envolve esse MESMO ponteiro, então qualquer escrita bidirecional dirigida pelo RmlUi (ex.: um usuário digitando num `<input>` ligado ao data-model) fica visível via `get_*` sem chamada extra ao RmlUi, e uma chave ligada é legível tanto antes quanto depois de `load()` (só o próprio `bind_*` deve preceder `load()`). Guards (fail-high `AUD-TEC-5`, mesmo formato de `get_element_box`/`set_focus`): os quatro métodos de DOM retornam `false` quando nenhum documento estiver carregado, `id` for nulo/vazio (`""`) ou não encontrado; `add_class`/`remove_class` adicionalmente rejeitam um `cls` nulo/vazio; `set_property` adicionalmente rejeita um `prop` nulo/vazio. `get_*` retornam `false` (`out` intocado) quando a chave nunca foi ligada -- "chave desconhecida" e "ordem errada" (ler antes do `bind_*` correspondente) colapsam pro mesmo guard. Novo teste TDD `domrw_sanity` (registrado nas duas configs de `GLINTFX_BACKEND_GLFW`; dirige o `Engine` diretamente, mesmo precedente de `focus_sanity`/`document_reload_leak`) -- inclui um teste "mordida" de injeção de RML provando que um payload malicioso `& < > "` faz round-trip por `GetInnerRML()` como a string exatamente escapada e cria zero elementos filhos, não uma tag real. Suíte cresce para **37 testes com `GLINTFX_BACKEND_GLFW=ON`, 21 com `=OFF`** (embed-only). **Correção cosmética de doc (mesmo item, achado ex-INBOX do review do `L1.17-FOCUS`):** a seção 5 de `docs/embed-integration.md` e o doc-comment de `clear_focus` em `bootstrap.hpp` antes afirmavam que o RmlUi "nunca reseta o foco pra nulo" -- verdade só para o caminho do próprio `Element::Blur()`. `Rml::Context::OnElementDetach` (confirmado contra o source pinado) define `focus = nullptr` diretamente sempre que o elemento focado é desanexado da árvore (ex.: pelo `set_text` substituindo filhos via `SetInnerRML`). O `clear_focus()` da glintfx já era correto/idempotente de qualquer forma (`if (!focused) return true;`) -- nenhuma mudança de comportamento, correção só de prosa.

---

## [0.8.0] - 2026-07-09 · [GitHub](https://github.com/petrinhu/glintfx/releases/tag/v0.8.0)

**EN:** **Minor** release -- new public API (`set_focus`/`clear_focus`, `L1.17-FOCUS`) plus an internal GL loader clean-room swap (`L1.14-GLLOADER`). No existing signature changed; drop-in from a consumer's point of view.
**PT:** Release **minor** -- API pública nova (`set_focus`/`clear_focus`, `L1.17-FOCUS`) mais uma troca clean-room interna do loader GL (`L1.14-GLLOADER`). Nenhuma assinatura existente mudou; drop-in do ponto de vista de um consumidor.

### Added / Adicionado

- **EN:** `L1.17-FOCUS` -- `bool set_focus(const char* id)` and `bool clear_focus()` on both `UiLayer` and `App`, closing the `GAP-4` roadmap item (docs/embed-integration.md section 5): programmatic focus control for hosts whose MODEL owns selection (e.g. a game menu driven by data-binding rather than RmlUi's own Tab/arrow navigation). `set_focus` is a thin wrapper over `Rml::Element::Focus(false)`; `clear_focus` wraps `Rml::Context::GetFocusElement()` + `Rml::Element::Blur()`. **Correction vs. HTML-tabindex intuition, confirmed by reading the pinned RmlUi 6.3 source (not assumed):** `Focus()` gates ONLY on the element's RCSS `focus` property (keyword `auto`|`none`), whose RmlUi-registered default is `auto` -- by default EVERY element (including a plain `<div>`) accepts `set_focus()`, UNLESS explicitly authored `focus: none;`. The separate `tab-index` property (Tab-key traversal order, default `none`) has NO effect on this method -- there is no RmlUi/glintfx equivalent of HTML's implicit "only tabindex-bearing elements are focusable" rule. Also confirmed against the pinned source: `Rml::Context::focus` is initialised to the context's internal root element at construction and is never reset to null, so `clear_focus()` cannot produce "nothing focused" -- its achievable semantics is "move focus away from wherever it currently is" (idempotent `true` when nothing meaningful was focused already). Hardening: both guard on no-document-loaded; `set_focus` additionally rejects a null/empty (`""`, AUD-TEC-5 convention) or unknown `id`, and returns `false` when the resolved element itself is authored `focus: none;`. New TDD test `focus_sanity` (registered in both `GLINTFX_BACKEND_GLFW` configs; drives `Engine` directly, same precedent as `document_reload_leak`, since the discriminating oracle -- `Rml::Context::GetFocusElement()` -- needs `RmlUi::Core` types not exposed through `UiLayer`'s public header). Suite grows to **36 tests with `GLINTFX_BACKEND_GLFW=ON`, 20 with `=OFF`** (embed-only).
  **PT:** `L1.17-FOCUS` -- `bool set_focus(const char* id)` e `bool clear_focus()` em `UiLayer` e `App`, fechando o item de roadmap `GAP-4` (docs/embed-integration.md seção 5): controle de foco programático para hosts cujo MODELO é dono da seleção (ex.: um menu de jogo dirigido por data-binding em vez da navegação Tab/setas própria do RmlUi). `set_focus` é um wrapper fino sobre `Rml::Element::Focus(false)`; `clear_focus` encapsula `Rml::Context::GetFocusElement()` + `Rml::Element::Blur()`. **Correção vs. intuição de tabindex do HTML, confirmada lendo o source pinado do RmlUi 6.3 (não assumida):** `Focus()` se protege SÓ pela propriedade RCSS `focus` do elemento (keyword `auto`|`none`), cujo default registrado no RmlUi é `auto` -- por padrão TODO elemento (incluindo uma `<div>` lisa) aceita `set_focus()`, A MENOS QUE autorado explicitamente com `focus: none;`. A propriedade separada `tab-index` (ordem de travessia por tecla Tab, default `none`) NÃO tem efeito neste método -- não há equivalente RmlUi/glintfx da regra implícita do HTML de "só elementos com tabindex são focáveis". Também confirmado contra o source pinado: `Rml::Context::focus` é inicializado com o elemento raiz interno do contexto na construção e nunca é resetado para nulo, então `clear_focus()` não consegue produzir "nada focado" -- sua semântica alcançável é "mover o foco pra longe de onde está agora" (`true` idempotente quando nada significativo já estava focado). Hardening: os dois protegem contra nenhum-documento-carregado; `set_focus` adicionalmente rejeita um `id` nulo/vazio (`""`, convenção AUD-TEC-5) ou desconhecido, e retorna `false` quando o próprio elemento resolvido está autorado com `focus: none;`. Novo teste TDD `focus_sanity` (registrado nas duas configs de `GLINTFX_BACKEND_GLFW`; dirige o `Engine` diretamente, mesmo precedente do `document_reload_leak`, já que o oráculo discriminante -- `Rml::Context::GetFocusElement()` -- precisa de tipos `RmlUi::Core` não expostos pelo header público do `UiLayer`). Suíte cresce para **36 testes com `GLINTFX_BACKEND_GLFW=ON`, 20 com `=OFF`** (embed-only).

### Changed / Alterado

- **EN:** `L1.14-GLLOADER` -- replaced the vendored third-party `gl3w` OpenGL loader with glintfx's own **clean-room** GL 3.3 core-profile function-pointer loader, `glintfx/src/gl_loader.h`/`.c` (344 commands), generated by the new `tools/gen_glloader.py` from the public Khronos `gl.xml` API registry (Apache-2.0, vendored at `glintfx/third_party/khronos/gl.xml`) plus the already-vendored MIT `glcorearb.h`/`khrplatform.h` headers (moved to `glintfx/third_party/khronos/GL|KHR/`). The new `int glx_gl_load(void)` entry point resolves every symbol via `glXGetProcAddress`(`ARB`) -> `eglGetProcAddress` -> `dlsym`, in that order, and returns `0` on success (same polarity as the `gl3wInit()` call sites it replaces in `window_glfw.cpp` and `ui_layer.cpp`). Clean-room discipline: `third_party/gl3w/src/gl3w.c` and `.../include/GL/gl3w.h` were **not read** while writing the generator or the generated loader (see `tools/gen_glloader.py`'s header comment for the full method and sources actually consulted); `third_party/gl3w/` is now deleted from the tree. New TDD test `gl_loader_sanity` (registered in both `GLINTFX_BACKEND_GLFW` configs) proves `glx_gl_load() == 0`, a representative sample of core pointers (`glGetString`, `glCreateShader`, `glBindFramebuffer`) resolves non-null, and `glGetString(GL_VERSION)` is callable against a real context. Suite grows to **35 tests with `GLINTFX_BACKEND_GLFW=ON`, 19 with `=OFF`** (embed-only), both green. `NOTICE` updated (the `gl3w` Unlicense entry is gone; the Khronos MIT headers move to their new path; a new Apache-2.0 `gl.xml` entry discloses the generator's input data). 1st milestone of the long-term internalization track (`L1-INTERNALIZE`, `AUD-TRANS-1`).
  **PT:** `L1.14-GLLOADER` -- substituído o loader OpenGL de terceiro `gl3w` (vendorizado) pelo loader próprio **clean-room** de ponteiros de função GL 3.3 core profile do glintfx, `glintfx/src/gl_loader.h`/`.c` (344 comandos), gerado pelo novo `tools/gen_glloader.py` a partir do registro público de API `gl.xml` da Khronos (Apache-2.0, vendorizado em `glintfx/third_party/khronos/gl.xml`) mais os headers MIT `glcorearb.h`/`khrplatform.h` já vendorizados (movidos para `glintfx/third_party/khronos/GL|KHR/`). O novo ponto de entrada `int glx_gl_load(void)` resolve cada símbolo via `glXGetProcAddress`(`ARB`) -> `eglGetProcAddress` -> `dlsym`, nessa ordem, e retorna `0` em sucesso (mesma polaridade dos call-sites de `gl3wInit()` que substitui em `window_glfw.cpp` e `ui_layer.cpp`). Disciplina clean-room: `third_party/gl3w/src/gl3w.c` e `.../include/GL/gl3w.h` NÃO foram lidos durante a escrita do gerador nem do loader gerado (ver o comentário de cabeçalho de `tools/gen_glloader.py` para o método completo e as fontes de fato consultadas); `third_party/gl3w/` agora está deletado da árvore. Novo teste TDD `gl_loader_sanity` (registrado nas duas configs de `GLINTFX_BACKEND_GLFW`) prova `glx_gl_load() == 0`, uma amostra representativa de ponteiros core (`glGetString`, `glCreateShader`, `glBindFramebuffer`) resolvida não-nula, e `glGetString(GL_VERSION)` chamável contra um contexto real. Suíte cresce para **35 testes com `GLINTFX_BACKEND_GLFW=ON`, 19 com `=OFF`** (embed-only), ambos verdes. `NOTICE` atualizado (a entrada Unlicense do `gl3w` sai; os headers MIT da Khronos mudam de caminho; entra uma nova entrada Apache-2.0 do `gl.xml` divulgando o dado de entrada do gerador). 1º marco da trilha de internalização de longo prazo (`L1-INTERNALIZE`, `AUD-TRANS-1`).

---

## [0.7.0] - 2026-07-09 · [GitHub](https://github.com/petrinhu/glintfx/releases/tag/v0.7.0)

**EN:** **Minor** release -- decorator/props new, following up the `image-color` tint documented in v0.6.0's `docs/effects.md`. **No existing signature changed; this release is a drop-in upgrade from v0.6.0.**
**PT:** Release **minor** -- decorator/props novos, desdobramento do tingimento `image-color` documentado em `docs/effects.md` na v0.6.0. **Nenhuma assinatura existente mudou; esta release é um upgrade drop-in a partir da v0.6.0.**

### Added / Adicionado

- **EN:** New decorator `decorator: image-tint( url )` plus three standalone, globally-registered, independently-animatable RCSS properties -- `image-tint-color` (color, accepts `var()`), `image-tint-mode` (`none` | `multiply` | `luminance-multiply` | `screen`, default `none`), `image-tint-threshold` (number, luminance cut, default `0.55`). RmlUi already ships native `image-color` (documented in `docs/effects.md` since v0.6.0), a uniform multiply that tints every texel of a texture equally -- `image-tint(luminance-multiply)` is the step beyond it: a **luminance-key** tint that recolors only light+neutral texels while leaving already-saturated (e.g. gold trim) and dark (e.g. stone) texels alone, weighted by `smoothstep(threshold, 1, luminance) × (1 − saturation)`. Consumer-driven by GusWorld: one neutral base texture (white runes + navy stone + gold trim), recolored into N domain colors at runtime via RCSS, without shipping N textures. `image-tint-mode: none` (the default) is a true no-op -- identical cost/behavior to a plain `image()` decorator, zero shader compiled. Implemented as a genuinely new fragment shader (`"glintfx-tint"`) injected into `Gl3RenderInterface` via tagged-wrapper `CompileShader`/`RenderShader`/`ReleaseShader` overrides -- reached exclusively through RmlUi's public API surface, **zero patch to the vendored RmlUi core**. Full design rationale, including why the decorator needed its own name (RmlUi's decorator model does not let one decorator read/compose over another's render output) and the colour-space handling (un-premultiply before the luminance/saturation math, re-premultiply the output; deliberately no gamma/sRGB correction, consistent with the rest of the renderer): [ADR-0010](docs/adr/0010-image-tint-luminance-key.md). Hardening: an unrecognized `image-tint-mode` keyword falls back to `none` (defense-in-depth, RmlUi's own keyword parser already gates it at parse time); `image-tint-threshold` is clamped to `[0, 0.999]` after an `isfinite` guard -- the upper bound is strictly below `1.0` because the shader's `smoothstep(threshold, 1.0, L)` is GLSL undefined behaviour the instant `edge0 >= edge1`; `image-tint-color` needs no extra clamp; a missing/failed texture load is a no-op, same as the native `image()` decorator. **Honest fidelity note:** "preserves saturated texels" is approximate, not an exact no-op -- the `(1 − saturation)` weighting is linear, so a highly-but-not-fully-saturated texel (e.g. `sat≈0.69`) still receives a non-trivial fraction of the tint at the default threshold (`0.55`). Near-total preservation of a specific saturated asset requires tuning `image-tint-threshold` above that asset's own luminance, trading off against how strongly the neutral zone gets tinted -- see the ADR-0010 addendum and the new "How-to: luminance-key tint" section in `docs/effects.md` for the tuning guidance. (`GLINTFX-TINT-1/2/3`)
  **PT:** Decorator novo `decorator: image-tint( url )` mais três propriedades RCSS standalone, registradas globalmente, animáveis independentemente -- `image-tint-color` (cor, aceita `var()`), `image-tint-mode` (`none` | `multiply` | `luminance-multiply` | `screen`, default `none`), `image-tint-threshold` (número, corte de luminância, default `0.55`). O RmlUi já embarca o `image-color` nativo (documentado em `docs/effects.md` desde a v0.6.0), um multiply uniforme que tinge todo texel de uma textura igualmente -- `image-tint(luminance-multiply)` é o passo além dele: um tingimento **luminance-key** que recolore só texels claros+neutros, preservando texels já saturados (ex.: trim ouro) e escuros (ex.: pedra), ponderado por `smoothstep(threshold, 1, luminância) × (1 − saturação)`. Consumer-driven pelo GusWorld: uma textura base neutra (runas brancas + pedra navy + trim ouro), recolorida em N cores de domínio em runtime via RCSS, sem shippar N texturas. `image-tint-mode: none` (o default) é um no-op de verdade -- custo/comportamento idêntico a um decorator `image()` simples, zero shader compilado. Implementado como um fragment shader genuinamente novo (`"glintfx-tint"`) injetado no `Gl3RenderInterface` via overrides de wrapper-marcado de `CompileShader`/`RenderShader`/`ReleaseShader` -- alcançado exclusivamente pela superfície pública de API do RmlUi, **zero patch no core vendorizado do RmlUi**. Racional de design completa, incluindo por que o decorator precisou do próprio nome (o modelo de decorator do RmlUi não deixa um decorator ler/compor sobre a saída de render de outro) e o tratamento de espaço de cor (despremultiplicar antes da matemática de luminância/saturação, repremultiplicar a saída; deliberadamente sem correção gama/sRGB, consistente com o resto do renderer): [ADR-0010](docs/adr/0010-image-tint-luminance-key.md). Hardening: uma keyword `image-tint-mode` não reconhecida cai pra `none` (defesa-em-profundidade, o próprio parser keyword do RmlUi já protege no momento do parse); `image-tint-threshold` é clampado a `[0, 0.999]` após uma guarda `isfinite` -- o limite superior é estritamente abaixo de `1.0` porque o `smoothstep(threshold, 1.0, L)` do shader é comportamento indefinido do GLSL no instante em que `edge0 >= edge1`; `image-tint-color` não precisa de clamp extra; falha/ausência de textura é no-op, igual ao decorator `image()` nativo. **Nota honesta de fidelidade:** "preserva texels saturados" é aproximado, não um no-op exato -- a ponderação `(1 − saturação)` é linear, então um texel altamente-mas-não-totalmente-saturado (ex.: `sat≈0.69`) ainda recebe uma fração não-trivial do tingimento no threshold default (`0.55`). Preservação quase-total de um asset saturado específico exige tunar `image-tint-threshold` acima da luminância própria daquele asset, em troca de o quanto a zona neutra é tingida -- ver o adendo do ADR-0010 e a nova seção "How-to: luminance-key tint" em `docs/effects.md` pra orientação de tuning. (`GLINTFX-TINT-1/2/3`)

### Testing / Testes

- **EN:** `image_tint_sanity` -- selectivity load-proof across 3 zones (strongly-tinted light+neutral, lightly-tinted light+saturated, untouched dark), the uniform (non-selective) `multiply` mode as contrast, `mode=None`'s true no-op parity with the native `image()` decorator, fail-high hardening (unknown keyword / out-of-range threshold / missing texture / empty `src` -- no crash), CSS opacity compositing, fresh-per-frame property reads (`image-tint-color` driven live via a data-model binding, NOT baked at `GenerateElementData` time), and `ResetProgram()`'s state-restore discipline (a `polygon()`-gradient / tint / `polygon()`-gradient triple in the SAME frame, asserting the LAST gradient renders identically to the FIRST). Runs in both `GLINTFX_BACKEND_GLFW` configs (the decorator + shader injection are backend-agnostic). Suite grows to **34 tests with `GLINTFX_BACKEND_GLFW=ON`, 18 with `=OFF`** (embed-only).
  **PT:** `image_tint_sanity` -- prova de carga de seletividade em 3 zonas (claro+neutro fortemente tingido, claro+saturado levemente tingido, escuro intocado), o modo `multiply` uniforme (não-seletivo) como contraste, a paridade de no-op verdadeiro do `mode=None` com o decorator `image()` nativo, hardening fail-high (keyword desconhecida / threshold fora de faixa / textura ausente / `src` vazio -- sem crash), composição de opacity do CSS, leitura fresca-por-frame de propriedade (`image-tint-color` dirigido ao vivo via um binding de data-model, NÃO embutido no momento de `GenerateElementData`), e a disciplina de restauração de estado do `ResetProgram()` (uma tripla polygon()-gradiente / tint / polygon()-gradiente no MESMO frame, afirmando que o ÚLTIMO gradiente renderiza idêntico ao PRIMEIRO). Roda nas duas configs de `GLINTFX_BACKEND_GLFW` (o decorator + a injeção de shader são backend-agnósticos). Suíte cresce para **34 testes com `GLINTFX_BACKEND_GLFW=ON`, 18 com `=OFF`** (embed-only).

---

## [0.6.0] - 2026-07-08 · [GitHub](https://github.com/petrinhu/glintfx/releases/tag/v0.6.0)

**EN:** **Minor** release -- addition of public API surface, following up `GLINTFX-SCROLL-1` (v0.4.0). **No existing signature changed; this release is a drop-in upgrade from v0.5.0.**
**PT:** Release **minor** -- adição de superfície de API pública, desdobramento do `GLINTFX-SCROLL-1` (v0.4.0). **Nenhuma assinatura existente mudou; esta release é um upgrade drop-in a partir da v0.5.0.**

### Added / Adicionado

- **EN:** `set_scroll_callback(std::function<void(const char* id)>)` on both `App` and `UiLayer` -- fires whenever an element's OWN scroll offset changes, via RmlUi's native `Rml::EventId::Scroll`. Reports the id of the nearest ancestor-or-self of the scrolled element (`""` if none); **id-only, no numeric payload** -- the `Rml::EventId::Scroll` event underneath carries none, so call `get_element_scroll_top(id)` from inside the callback if the offset is needed. glintfx does not play audio -- this is the hook a host uses to trigger its own scroll sound. One callback per **source**, not distinguished by glintfx: mouse wheel, native scrollbar interaction (thumb/track/arrows), and the programmatic `scroll_element_into_view()`/`set_element_scroll_top()`. Deduplicated against the CURRENT offset by RmlUi itself (setting the same scroll position twice in a row does not re-fire). Null/empty callback is a safe no-op; a reentrancy guard (functor copied before invocation) protects against the callback replacing itself mid-call. Two documented gotchas: **(a)** wheel smoothscroll fires **once per animated step**, not just once when it settles -- a host wiring a single "whoosh" sound per gesture must debounce/coalesce on its own side; **(b)** `scroll_element_into_view()`/`set_element_scroll_top()` dispatch the underlying `Scroll` event SYNCHRONOUSLY on the same call stack, re-entering this callback -- calling either of them again from inside the callback with a value that does **not converge** to the element's current offset (e.g. a badly-written dynamic clamp/snap) recurses without bound -> stack overflow. Safe as long as the written value converges (RmlUi's own dedup stops the cycle, typically in 1-2 iterations for a constant target). `set_click_callback`/`set_click_info_callback` are untouched and keep firing independently. Verified by `scroll_sanity` (fires from wheel and from `set_element_scroll_top`, dedup on a same-value set, safe no-op after clearing, and a dedicated case proving the convergent-recursion pattern settles rather than overflowing). (`GLINTFX-SCROLL-1` follow-up)
  **PT:** `set_scroll_callback(std::function<void(const char* id)>)` em `App` e `UiLayer` -- dispara sempre que o offset de rolagem PRÓPRIO de um elemento muda, via o evento nativo `Rml::EventId::Scroll` do RmlUi. Reporta o id do ancestral-ou-o-próprio mais próximo do elemento rolado (`""` se nenhum); **só-id, sem payload numérico** -- o evento `Rml::EventId::Scroll` subjacente não carrega nenhum, então chame `get_element_scroll_top(id)` de dentro do callback se o offset for necessário. O glintfx não toca áudio -- este é o gancho que um host usa para disparar seu próprio som de rolagem. Uma chamada por **fonte**, que o glintfx não distingue: wheel do mouse, interação com a scrollbar nativa (thumb/trilho/setas), e as chamadas programáticas `scroll_element_into_view()`/`set_element_scroll_top()`. Deduplicado contra o offset CORRENTE pelo próprio RmlUi (definir a mesma posição de rolagem duas vezes seguidas não redispara). Callback nulo/vazio é no-op seguro; uma guarda de reentrância (functor copiado antes de invocar) protege contra o callback se substituir no meio da própria chamada. Duas pegadinhas documentadas: **(a)** o smoothscroll do wheel dispara **uma vez por passo animado**, não só uma vez ao assentar -- um host que quer um único som de "whoosh" por gesto precisa debounce/coalescer do próprio lado; **(b)** `scroll_element_into_view()`/`set_element_scroll_top()` despacham o evento `Scroll` subjacente SINCRONAMENTE na mesma pilha de chamada, reentrando neste callback -- chamar qualquer um deles novamente de dentro do callback com um valor que NÃO converge para o offset atual do elemento (ex.: um clamp/snap dinâmico mal escrito) recursa sem limite -> stack overflow. Seguro desde que o valor escrito convirja (o dedup do próprio RmlUi para o ciclo, tipicamente em 1-2 iterações para um alvo constante). `set_click_callback`/`set_click_info_callback` seguem intactos e continuam disparando independentemente. Verificado por `scroll_sanity` (dispara a partir de wheel e do `set_element_scroll_top`, dedup num set de mesmo valor, no-op seguro após limpar, e um caso dedicado provando que o padrão de recursão convergente assenta em vez de estourar a pilha). (desdobramento do `GLINTFX-SCROLL-1`)
- **EN:** Sane scrollbar defaults in the built-in UA stylesheet -- without any authored RCSS, RmlUi's synthesized `scrollbarvertical`/`scrollbarhorizontal`/`slidertrack`/`sliderbar`/`sliderarrowdec`/`sliderarrowinc` elements are zero-sized, invisible, and unclickable (`Source/Core/WidgetScroll.cpp` zeroes track/arrow size when no height/width resolves). The UA stylesheet now ships a minimum visible+usable bar (thickness, translucent thumb with a minimum drag size, faint track, sized+coloured arrows), merged as a low-specificity base by `Bootstrap::load()` -- same mechanism as the existing `display: block` defaults. **Specificity caveat:** the new rules are two-tag compound selectors (e.g. `scrollbarvertical sliderbar`, ~20000 specificity), higher than a single-tag author override in isolation (e.g. `sliderbar { ... }`, ~10000) -- to override, match the same compound-tag shape or add a class/id ancestor (see the how-to in `docs/effects.md`).
  **PT:** Defaults sãos de scrollbar na UA-stylesheet embutida -- sem RCSS autorado nenhum, os elementos `scrollbarvertical`/`scrollbarhorizontal`/`slidertrack`/`sliderbar`/`sliderarrowdec`/`sliderarrowinc` sintetizados pelo RmlUi ficam com tamanho zero, invisíveis e inclicáveis (`Source/Core/WidgetScroll.cpp` zera o tamanho de trilho/setas quando nenhuma altura/largura resolve). A UA-stylesheet agora embarca uma barra mínima visível+usável (espessura, thumb translúcido com tamanho mínimo de arraste, trilho discreto, setas dimensionadas+coloridas), mesclada como base de baixa especificidade pelo `Bootstrap::load()` -- mesmo mecanismo dos defaults `display: block` já existentes. **Ressalva de especificidade:** as regras novas são seletores compostos de duas tags (ex.: `scrollbarvertical sliderbar`, ~20000 de especificidade), maior que uma sobreposição de autor de uma tag só isolada (ex.: `sliderbar { ... }`, ~10000) -- para sobrepor, iguale o mesmo formato composto ou adicione um ancestral classe/id (ver o how-to em `docs/effects.md`).
- **EN:** Docs: `docs/effects.md` gained a "How-to: style scrollbars" section -- vertical/horizontal bar thickness, thumb color/hover, arrow color/hover, fixed-size vs. proportional-auto (the free default) thumb sizing, and the specificity caveat above worked out in full RCSS examples.
  **PT:** Docs: `docs/effects.md` ganhou uma seção "How-to: estilizar barras de rolagem (scrollbars)" -- espessura de barra vertical/horizontal, cor/hover do thumb, cor/hover das setas, tamanho de thumb fixo vs. proporcional-automático (o default de graça), e a ressalva de especificidade acima resolvida em exemplos RCSS completos.

### Testing / Testes

- **EN:** `data_model_embed_sanity` closes the embed-path (`UiLayer`) coverage gap for the data-model API found by audit (`AUD-TEC-7`): the pre-existing four data-model tests all ran only through `App`, inside the `GLINTFX_BACKEND_GLFW=ON` block, leaving `set_bool` and the `UiLayer` path entirely unexercised. The new test registers OUTSIDE that block (runs in both configs), covers `create_data_model`/`bind_number`/`bind_string`/`bind_bool`/`bind_list`/`load`/`set_number`/`set_string`/`set_bool`/`set_list` end-to-end through `UiLayer`, and documents+asserts the pre-existing silent-no-op behaviour of a second `create_data_model()` call (even under a different name). Suite grows to **33 tests with `GLINTFX_BACKEND_GLFW=ON`, 17 with `=OFF`** (embed-only).
  **PT:** `data_model_embed_sanity` fecha o gap de cobertura do caminho embed (`UiLayer`) para a API de data-model achado por auditoria (`AUD-TEC-7`): os quatro testes de data-model pré-existentes rodavam só via `App`, dentro do bloco `GLINTFX_BACKEND_GLFW=ON`, deixando `set_bool` e o caminho `UiLayer` totalmente sem exercício. O teste novo se registra FORA desse bloco (roda nos 2 configs), cobre `create_data_model`/`bind_number`/`bind_string`/`bind_bool`/`bind_list`/`load`/`set_number`/`set_string`/`set_bool`/`set_list` ponta-a-ponta via `UiLayer`, e documenta+afirma o comportamento pré-existente de no-op silencioso de uma segunda chamada a `create_data_model()` (mesmo sob nome diferente). Suíte cresce para **33 testes com `GLINTFX_BACKEND_GLFW=ON`, 17 com `=OFF`** (embed-only).

---

## [0.5.0] - 2026-07-07 · [GitHub](https://github.com/petrinhu/glintfx/releases/tag/v0.5.0)

**EN:** **Minor** release -- addition of public API surface, driven by the `AUDIT_FIND.md` public-distribution audit (`AUD-PUB-*`) plus real integration feedback from GusWorld. **No existing signature changed; this release is a drop-in upgrade from v0.4.1.**
**PT:** Release **minor** -- adição de superfície de API pública, motivada pela auditoria de distribuição pública `AUDIT_FIND.md` (`AUD-PUB-*`) mais feedback real de integração do GusWorld. **Nenhuma assinatura existente mudou; esta release é um upgrade drop-in a partir da v0.4.1.**

### Added / Adicionado

- **EN:** `enum class Key` extended with 5 text-editing/document-navigation keys RmlUi already supports natively: `Delete`, `Home`, `End`, `PageUp`, `PageDown` (mapped to `KI_DELETE`/`KI_HOME`/`KI_END`/`KI_PRIOR`/`KI_NEXT` in `to_rml_key()`, confirmed against the pinned RmlUi source). Purely additive -- existing enumerators and their values are untouched. Letters/digits (e.g. Ctrl+C/V/Z shortcuts) remain out of scope, deferred as `AUD-PUB-6` in the `TODO.md` INBOX. (`AUD-PUB-3`)
  **PT:** `enum class Key` estendido com 5 teclas de edição de texto/navegação de documento que o RmlUi já suporta nativamente: `Delete`, `Home`, `End`, `PageUp`, `PageDown` (mapeadas para `KI_DELETE`/`KI_HOME`/`KI_END`/`KI_PRIOR`/`KI_NEXT` em `to_rml_key()`, confirmado contra o source pinado do RmlUi). Puramente aditivo -- os enumeradores existentes e seus valores ficam intocados. Letras/dígitos (ex.: atalhos Ctrl+C/V/Z) seguem fora de escopo, adiados como `AUD-PUB-6` na INBOX do `TODO.md`. (`AUD-PUB-3`)
- **EN:** `set_click_info_callback(std::function<void(const ClickInfo&)>)` on both `App` and `UiLayer` -- a second, richer click channel alongside `set_click_callback` (id-only), which **remains completely unchanged and keeps firing independently**; both can be registered at the same time. New public header `glintfx/include/glintfx/click_info.hpp`: `struct ClickInfo { const char* id; int button; float x; float y; bool double_click; }`. `button` is **functional for all three mouse buttons**: left (`0`) fires from RmlUi's native `Click`/`Dblclick` events; right (`1`) and middle (`2`) are synthesized from a paired `Mousedown`+`Mouseup` on the same element id (RmlUi's own `Context` never dispatches `Click`/`Dblclick` for non-primary buttons) -- a down-on-A/up-on-B drag does not fire, mirroring the native left-click rule. `double_click` is **left-only**: RmlUi has no `Dblclick` equivalent for non-primary buttons, and inventing detection via a custom timer was deliberately rejected; a right/middle `ClickInfo` always reports `double_click=false`. `x`/`y` follow the same window-space physical-pixel, top-left-origin convention as `get_element_box()`. Verified by `click_callback_sanity` (single click, the exact 4-event double-click ordering, right/middle-click parity, drag-off non-firing, and no-regression of the id-only channel). (`AUD-PUB-4`)
  **PT:** `set_click_info_callback(std::function<void(const ClickInfo&)>)` em `App` e `UiLayer` -- um segundo canal de clique, mais rico, ao lado de `set_click_callback` (só-id), que **permanece completamente inalterado e continua disparando independentemente**; ambos podem ser registrados ao mesmo tempo. Novo header público `glintfx/include/glintfx/click_info.hpp`: `struct ClickInfo { const char* id; int button; float x; float y; bool double_click; }`. O `button` é **funcional para os três botões do mouse**: esquerdo (`0`) dispara dos eventos nativos `Click`/`Dblclick` do RmlUi; direito (`1`) e meio (`2`) são sintetizados de um par `Mousedown`+`Mouseup` no mesmo id de elemento (o próprio `Context` do RmlUi nunca dispara `Click`/`Dblclick` para botões não-primários) -- um arrasto down-em-A/up-em-B não dispara, espelhando a regra nativa do clique esquerdo. O `double_click` é **esquerdo-apenas**: o RmlUi não tem equivalente de `Dblclick` para botões não-primários, e inventar detecção via timer customizado foi deliberadamente rejeitado; um `ClickInfo` de direito/meio sempre reporta `double_click=false`. `x`/`y` seguem a mesma convenção de pixels físicos espaço-janela, origem superior-esquerda, do `get_element_box()`. Verificado por `click_callback_sanity` (clique simples, a ordem exata dos 4 eventos de duplo-clique, paridade direito/meio-clique, não-disparo de drag-off, e não-regressão do canal só-id). (`AUD-PUB-4`)

### Fixed / Corrigido

- **EN:** `App` no longer freezes UI layout on window resize -- `App::render()` read the window's `(w, h)` and called `Engine::render_standalone(w, h)` but never `Engine::set_viewport(w, h)`, the only path that reaches `Rml::Context::SetDimensions()`. The GLFW window resized at the GL/framebuffer level, but the RmlUi layout stayed frozen at the `AppConfig` construction dimensions forever. `App::snapshot()` had the same bug independently (no shared cache with `render()`), found by adversarial review of the first fix -- a host resizing and calling only `snapshot()` (no `render()` in between, e.g. capturing a thumbnail) still captured a PPM with stale layout. Both call sites now share one helper (`App::Impl::sync_viewport`), a single source of truth that diffs against the last-rendered size and calls `set_viewport` only when it changed -- zero new public API. `UiLayer` never had this bug (embed hosts already call `set_viewport()` explicitly). Verified by `app_resize_smoke` (pre-fix: box frozen at construction dims after a `glfwSetWindowSize` resize; post-fix: box tracks the new size, through both `render()` and `snapshot()`-only paths). (`AUD-PUB-1`)
  **PT:** o `App` não congela mais o layout da UI ao redimensionar a janela -- `App::render()` lia o `(w, h)` da janela e chamava `Engine::render_standalone(w, h)` mas nunca `Engine::set_viewport(w, h)`, o único caminho que alcança `Rml::Context::SetDimensions()`. A janela GLFW redimensionava no nível GL/framebuffer, mas o layout do RmlUi ficava congelado nas dimensões de construção do `AppConfig` para sempre. O `App::snapshot()` tinha o mesmo bug de forma independente (sem cache compartilhado com `render()`), achado pelo review adversarial do primeiro fix -- um host que redimensiona e chama só `snapshot()` (sem `render()` no meio, ex.: capturar uma miniatura) ainda capturava um PPM com layout desatualizado. Os dois pontos de chamada agora compartilham um único helper (`App::Impl::sync_viewport`), fonte única de verdade que compara com o último tamanho renderizado e só chama `set_viewport` quando mudou -- zero API pública nova. O `UiLayer` nunca teve esse bug (hosts embed já chamam `set_viewport()` explicitamente). Verificado por `app_resize_smoke` (pré-fix: caixa congelada nas dims de construção após um resize via `glfwSetWindowSize`; pós-fix: caixa acompanha o novo tamanho, tanto pelo caminho `render()` quanto pelo caminho só-`snapshot()`). (`AUD-PUB-1`)

### Testing / Testes

- **EN:** CI now builds with **clang** (infrastructure-only change, no API impact). Suite remains **32 tests** with `GLINTFX_BACKEND_GLFW=ON`, **16** with `=OFF` (embed-only); `app_resize_smoke` and the extended `click_callback_sanity`/`ui_layer_events` cases are the net-new coverage for this release.
  **PT:** o CI agora builda com **clang** (mudança só de infraestrutura, sem impacto de API). A suíte permanece em **32 testes** com `GLINTFX_BACKEND_GLFW=ON`, **16** com `=OFF` (embed-only); `app_resize_smoke` e os casos estendidos de `click_callback_sanity`/`ui_layer_events` são a cobertura líquida-nova desta release.

---

## [0.4.1] - 2026-07-07 · [GitHub](https://github.com/petrinhu/glintfx/releases/tag/v0.4.1)

**EN:** Patch release. All findings below came out of a public-distribution audit (`AUDIT_FIND.md`) followed by adversarial review that reproduced each defect live before the fix landed. **No public API changed** -- signatures, semantics, and return types are identical to v0.4.0; this release is a drop-in upgrade.
**PT:** Release de patch. Todos os achados abaixo vieram de uma auditoria de distribuição pública (`AUDIT_FIND.md`) seguida de review adversarial que reproduziu cada defeito ao vivo antes do fix entrar. **Nenhuma API pública mudou** -- assinaturas, semântica e tipos de retorno são idênticos à v0.4.0; esta release é um upgrade drop-in.

### Fixed / Corrigido

- **EN:** `App::snapshot(nullptr)` [Important, UB] -- a null `ppm_path` reached `fopen(NULL, "wb")` unguarded, undefined behaviour that crashes on glibc. Same class as the `load(nullptr)` fix from v0.3.0, missed in that release's sweep because `snapshot()` is `App`-only. Now returns `false` like every other null-path entry point. (`AUD-TEC-1`)
  **PT:** `App::snapshot(nullptr)` [Important, UB] -- um `ppm_path` nulo chegava sem guard em `fopen(NULL, "wb")`, comportamento indefinido que crasha na glibc. Mesma classe do fix de `load(nullptr)` da v0.3.0, escapou daquela varredura porque `snapshot()` é exclusivo do `App`. Agora retorna `false`, como qualquer outro ponto de entrada com caminho nulo. (`AUD-TEC-1`)
- **EN:** Non-finite (`NaN`/`inf`) mouse coordinates and wheel deltas [Important, UB] -- `UiLayer::process_event` cast `ev.x`/`ev.y` to `int` on `MouseMove` and passed them raw to `Rml::Context::ProcessMouseWheel` on `MouseWheel`, without the finiteness guard already applied elsewhere in the API (e.g. `set_dp_ratio`, `set_element_scroll_top`). A float-to-int cast of `NaN`/`inf` is UB, and a non-finite wheel delta could poison scroll state. Both event cases now reject non-finite `x`/`y` as a safe no-op. (`AUD-TEC-2`)
  **PT:** Coordenadas de mouse e deltas de wheel não-finitos (`NaN`/`inf`) [Important, UB] -- `UiLayer::process_event` fazia cast de `ev.x`/`ev.y` para `int` em `MouseMove` e passava crus para `Rml::Context::ProcessMouseWheel` em `MouseWheel`, sem o guard de finitude já aplicado em outros pontos da API (ex.: `set_dp_ratio`, `set_element_scroll_top`). Um cast float-para-int de `NaN`/`inf` é UB, e um delta de wheel não-finito podia envenenar o estado de rolagem. Os dois casos de evento agora rejeitam `x`/`y` não-finitos como no-op seguro. (`AUD-TEC-2`)
- **EN:** Use-after-free by reentrancy in `set_click_callback` [Critical, heap-use-after-free] -- the internal click listener invoked the registered `std::function` through a pointer into its owning storage; if the handler itself called `set_click_callback(...)` (a plausible pattern: "clicked Play -> swap in the next screen's handler"), the in-flight functor was destroyed by its own invocation. Fixed by copying the functor to a local variable before invoking it, so a reentrant reassignment can no longer free memory the call is still executing. Reproduced live under ASan before the fix, clean after. (`AUD-TEC-3`)
  **PT:** Use-after-free por reentrância em `set_click_callback` [Critical, heap-use-after-free] -- o listener interno de clique invocava o `std::function` registrado através de um ponteiro para dentro do seu próprio storage; se o handler chamasse `set_click_callback(...)` de dentro de si mesmo (padrão plausível: "clicou em Play -> troca o handler pra tela seguinte"), o functor em execução era destruído pela própria invocação. Corrigido copiando o functor para uma variável local antes de invocar, então uma reatribuição reentrante não pode mais liberar memória que a chamada ainda está executando. Reproduzido ao vivo sob ASan antes do fix, limpo depois. (`AUD-TEC-3`)
- **EN:** Signed integer overflow in viewport dimensions [Important, UB] -- `UiLayer::set_viewport` validated `w`/`h` against `>0` but had no ceiling, so `target_h - y - h` (the GL viewport offset math) could overflow with values near `INT_MAX`. Added a sane ceiling (`kMaxViewportDim = 32768`): `w`/`h`/`target_h` outside `(0, 32768]` and `x`/`y` outside `[-32768, 32768]` are now rejected as a no-op, consistent with the existing `w/h<=0` guard. (`AUD-TEC-4`)
  **PT:** Overflow de inteiro com sinal nas dimensões do viewport [Important, UB] -- `UiLayer::set_viewport` validava `w`/`h` contra `>0` mas não tinha teto, então a matemática de offset do viewport GL (`target_h - y - h`) podia transbordar com valores perto de `INT_MAX`. Adicionado um teto são (`kMaxViewportDim = 32768`): `w`/`h`/`target_h` fora de `(0, 32768]` e `x`/`y` fora de `[-32768, 32768]` agora são rejeitados como no-op, consistente com o guard `w/h<=0` já existente. (`AUD-TEC-4`)
- **EN:** Empty string (`id=""`) accepted where it should fail [Important] -- all six by-id methods (`get_element_box`, `scroll_element_into_view`, `get/set_element_scroll_top`, `get_element_scroll_height`, `get_element_client_height`) guarded against a null `id` but not an empty one, so `""` fell through to RmlUi's lookup instead of being rejected up front. Guard widened to `!id || !*id` at all six call sites. (`AUD-TEC-5`)
  **PT:** String vazia (`id=""`) aceita onde deveria falhar [Important] -- os seis métodos por-id (`get_element_box`, `scroll_element_into_view`, `get/set_element_scroll_top`, `get_element_scroll_height`, `get_element_client_height`) protegiam contra `id` nulo mas não contra vazio, então `""` caía na busca do RmlUi em vez de ser rejeitado de saída. Guard ampliado para `!id || !*id` nos seis pontos de chamada. (`AUD-TEC-5`)
- **EN:** Phantom tag `v0.3.2` [docs only] -- eight remaining doc-comment references to "GLINTFX-SCROLL-1 (v0.3.2)" corrected to v0.4.0, the release the feature actually shipped in; `v0.3.2` never existed as a tag. (`AUD-TEC-8`)
  **PT:** Tag fantasma `v0.3.2` [somente docs] -- as oito referências restantes em doc-comments a "GLINTFX-SCROLL-1 (v0.3.2)" corrigidas para v0.4.0, a release em que a feature de fato saiu; `v0.3.2` nunca existiu como tag. (`AUD-TEC-8`)

### Added / Adicionado

- **EN:** Compile-time guard against including `<glintfx/app.hpp>` directly in an embed-only build (`GLINTFX_BACKEND_GLFW=OFF`) -- previously failed at link time with a cryptic missing-symbol error (`app.cpp` was never compiled into that configuration); now a clear `#error "glintfx::App requires GLINTFX_BACKEND_GLFW=ON (embed-only build: use glintfx::UiLayer)"` at the point of inclusion. (`AUD-TEC-9`)
  **PT:** Guard em tempo de compilação contra incluir `<glintfx/app.hpp>` diretamente num build embed-only (`GLINTFX_BACKEND_GLFW=OFF`) -- antes falhava em tempo de link com um erro críptico de símbolo ausente (`app.cpp` nunca era compilado nessa configuração); agora um `#error "glintfx::App requires GLINTFX_BACKEND_GLFW=ON (embed-only build: use glintfx::UiLayer)"` claro, no ponto da inclusão. (`AUD-TEC-9`)
- **EN:** New public docs: `docs/packaging.md` (the full `cmake --install` -> `find_package(glintfx)` flow) and `docs/troubleshooting.md` (the likeliest setup errors: missing GLFW, running under llvmpipe, `find_package` not finding the install, translucent-window compositor quirks). A compatibility table (RmlUi version, OpenGL profile, compiler, CMake minimum, platform) was added to the README. (`AUD-PUB-8`)
  **PT:** Novas docs públicas: `docs/packaging.md` (o fluxo completo `cmake --install` -> `find_package(glintfx)`) e `docs/troubleshooting.md` (os erros de setup mais prováveis: GLFW ausente, rodando sob llvmpipe, `find_package` não achando a instalação, peculiaridades de compositor com janela translúcida). Uma tabela de compatibilidade (versão do RmlUi, perfil OpenGL, compilador, mínimo de CMake, plataforma) foi adicionada ao README. (`AUD-PUB-8`)
- **EN:** Explicit SemVer policy in `CONTRIBUTING.md`: while in the 0.x line, **minor** = any addition or change to the public API/ABI, **patch** = bug fixes, docs, or internal-only changes -- closing a gap where past releases had bumped inconsistently (a signature change once shipped as a patch). Applies going forward; past tags are not renamed. (`AUD-PUB-9`)
  **PT:** Política explícita de SemVer no `CONTRIBUTING.md`: enquanto na linha 0.x, **minor** = qualquer adição ou mudança na API/ABI pública, **patch** = correção de bug, docs, ou mudança interna -- fechando uma lacuna em que releases passadas fizeram bump de forma inconsistente (uma mudança de assinatura já saiu como patch). Vale daqui pra frente; tags passadas não são renomeadas. (`AUD-PUB-9`)
- **EN:** GLFW attribution added to `NOTICE` (zlib/libpng License, linked by default via `GLINTFX_BACKEND_GLFW=ON`) -- it was the one linked dependency missing from the file. (`AUD-PUB-10`)
  **PT:** Atribuição do GLFW adicionada ao `NOTICE` (licença zlib/libpng, linkado por default via `GLINTFX_BACKEND_GLFW=ON`) -- era a única dependência linkada que faltava no arquivo. (`AUD-PUB-10`)

### Changed / Alterado

- **EN:** Public-facing docs (`README.md`, `docs/embed-integration.md`, `docs/effects.md`) neutralized of first-consumer-specific jargon (GusWorld's "brass screw-head" polygon example, its cockpit/verb-menu naming in code samples) in favor of generic examples (a hexagonal badge, a generic overlay panel) -- GusWorld remains credited as the first real consumer in status/history sections (CHANGELOG, ADRs), which is where that rationale belongs. Content-only; no behaviour change. (`AUD-PUB-7`)
  **PT:** Docs públicas (`README.md`, `docs/embed-integration.md`, `docs/effects.md`) neutralizadas de jargão específico do 1º consumidor (o exemplo "cabeça de parafuso de latão" do polígono do GusWorld, sua nomenclatura de cockpit/menu-de-verbos nos exemplos de código) em favor de exemplos genéricos (um badge hexagonal, um painel de overlay genérico) -- o GusWorld segue creditado como 1º consumidor real nas seções de status/histórico (CHANGELOG, ADRs), onde esse rationale pertence. Só conteúdo; sem mudança de comportamento. (`AUD-PUB-7`)

---

## [0.4.0] - 2026-07-07 · [GitHub](https://github.com/petrinhu/glintfx/releases/tag/v0.4.0)

### Added / Adicionado

- **EN:** Scrolling in embed mode (`GLINTFX-SCROLL-1`) -- consumer-driven by GusWorld: an `overflow-y: auto` list in a `UiLayer` document had no way to scroll, either by mouse wheel or programmatically. Two additions, both non-breaking:
  - **Wheel forwarding:** new `UiEvent::Type::MouseWheel`, reusing the existing `x`/`y` fields as scroll delta (not pointer position -- same dual-purpose convention `width`/`height` already use for `Resize`) plus the existing `modifiers` field. `UiLayer::process_event` forwards it to `Rml::Context::ProcessMouseWheel`. **Which element scrolls is HOVER, not focus** (confirmed by reading the pinned RmlUi source and independently reproduced behaviourally by the adversarial review: a two-container probe scene proved the hovered container scrolls and the merely-focused one does not move at all) -- a `MouseWheel` event needs a preceding `MouseMove` over the target area if the host does not already track hover via its own per-frame move events; a hover with no scrollable ancestor is a safe no-op, never scrolls an unrelated ancestor "by accident". Documented in full (with exact RmlUi source line references) in the `UiEvent::Type::MouseWheel` doc-comment. **`glintfx::App` does not forward this (or any) input event today** -- `App` has no `process_event` at all, a pre-existing gap unrelated to this feature; wheel forwarding is `UiLayer`-only, which is exactly where GusWorld's embed-mode pain is.
  - **Programmatic scroll** (5 methods, identical signatures across `UiLayer` and `App`, all `bool`-returning with an `id`-lookup + out-param shape matching `get_element_box`'s established convention): `scroll_element_into_view(id, align_with_top=true)` (wraps `Rml::Element::ScrollIntoView(bool)`); `get_element_scroll_top`/`set_element_scroll_top(id, float&)`; and the scroll-metrics trio's other two members, `get_element_scroll_height`/`get_element_client_height` -- added alongside `scroll_top` (not requested verbatim by the consumer, but the natural completion of the same trio RmlUi itself groups together in `Element.h`; needed by any host building a custom scrollbar thumb/percentage, e.g. `scroll_fraction = scroll_top / (scroll_height - client_height)`). All five are null-id/unknown-id/no-document-safe (`false`, no crash); `set_element_scroll_top` rejects non-finite (`NaN`/`±inf`) values.
  - **Fill hardening quality bar:** the adversarial review's mutation testing caught a real test-coverage gap on `align_with_top` -- the original fixture's oracle item happened to saturate to the same clamped `scroll_top` under both `true` and `false`, so a mutation flipping the parameter went undetected. Closed before tagging with a second oracle (item inside the non-saturating range) whose two alignments produce mathematically distinct, independently-verified `scroll_top` values.
  - Verified by `scroll_sanity` (embed, both build configs) and `app_scroll_smoke` (GLFW-only, `App` parity for the programmatic half). Suite size: **31 tests** with `GLINTFX_BACKEND_GLFW=ON`, **16** with `=OFF` (embed-only). ASan/UBSan/LSan-clean; two new UBSan suppression entries (`vptr:*WidgetScroll.cpp*` / `vptr:*RemoveEventListener*`) document a latent, pre-existing upstream RmlUi teardown-order UB in its own scrollbar widget destructor -- first exercised by this feature (no earlier test materialised a live scrollbar), same root-cause category and rigor as the already-accepted `vptr:ElementDocument` entry; grep-verified glintfx never subclasses `Element`/`WidgetScroll` nor calls `RemoveEventListener` itself, so neither suppression can mask a glintfx bug.
  **PT:** Rolagem em embed mode (`GLINTFX-SCROLL-1`) -- consumer-driven pelo GusWorld: uma lista `overflow-y: auto` num documento `UiLayer` não tinha como rolar, nem por roda do mouse nem programaticamente. Duas adições, ambas não-breaking:
  - **Encaminhamento de wheel:** novo `UiEvent::Type::MouseWheel`, reusando os campos `x`/`y` existentes como delta de rolagem (não posição do ponteiro -- mesma convenção de duplo-propósito que `width`/`height` já usam para `Resize`) mais o campo `modifiers` já existente. `UiLayer::process_event` repassa para `Rml::Context::ProcessMouseWheel`. **Qual elemento rola é o HOVER, não o foco** (confirmado lendo o source pinado do RmlUi e reproduzido comportamentalmente de forma independente pelo review adversarial: uma cena-sonda de dois containers provou que o container em hover rola e o só-com-foco não se move nada) -- um evento `MouseWheel` precisa de um `MouseMove` precedente sobre a área-alvo se o host não já rastrear hover via seus próprios eventos de move por-frame; um hover sem ancestral rolável é um no-op seguro, nunca rola um ancestral não-relacionado "por acidente". Documentado por completo (com referências de linha exatas do source do RmlUi) no doc-comment de `UiEvent::Type::MouseWheel`. **O `glintfx::App` não encaminha este (nem nenhum) evento de input hoje** -- `App` não tem `process_event` nenhum, uma lacuna pré-existente não relacionada a esta feature; o encaminhamento de wheel é exclusivo do `UiLayer`, exatamente onde está a dor de embed mode do GusWorld.
  - **Scroll programático** (5 métodos, assinaturas idênticas em `UiLayer` e `App`, todos retornando `bool` com o formato busca-por-id + out-param que já é a convenção estabelecida pelo `get_element_box`): `scroll_element_into_view(id, align_with_top=true)` (embrulha `Rml::Element::ScrollIntoView(bool)`); `get_element_scroll_top`/`set_element_scroll_top(id, float&)`; e os outros dois membros do trio de métricas de rolagem, `get_element_scroll_height`/`get_element_client_height` -- adicionados junto do `scroll_top` (não pedidos literalmente pelo consumidor, mas a completude natural do mesmo trio que o próprio RmlUi agrupa em `Element.h`; necessário para qualquer host que construa um thumb/percentual de scrollbar customizado, ex.: `fracao_rolada = scroll_top / (scroll_height - client_height)`). Os cinco são seguros com id nulo/desconhecido/sem-documento (`false`, sem crash); `set_element_scroll_top` rejeita valores não-finitos (`NaN`/`±inf`).
  - **Barra de qualidade do hardening:** o mutation testing do review adversarial pegou um gap real de cobertura de teste no `align_with_top` -- o item-oráculo do fixture original saturava no mesmo `scroll_top` clampado sob `true` e `false`, então uma mutação invertendo o parâmetro passava despercebida. Fechado antes da tag com um segundo oráculo (item dentro do range não-saturado) cujos dois alinhamentos produzem valores de `scroll_top` matematicamente distintos e verificados de forma independente.
  - Verificado por `scroll_sanity` (embed, nos 2 configs de build) e `app_scroll_smoke` (GLFW-only, paridade `App` da metade programática). Tamanho da suíte: **31 testes** com `GLINTFX_BACKEND_GLFW=ON`, **16** com `=OFF` (embed-only). ASan/UBSan/LSan-limpo; duas novas entradas de supressão UBSan (`vptr:*WidgetScroll.cpp*` / `vptr:*RemoveEventListener*`) documentam uma UB latente e pré-existente do upstream RmlUi na ordem-de-teardown do próprio destrutor do widget de scrollbar -- exercitada pela primeira vez por esta feature (nenhum teste anterior materializava uma scrollbar viva), mesma categoria de causa raiz e rigor da entrada `vptr:ElementDocument` já aceita; grep confirma que a glintfx nunca subclassa `Element`/`WidgetScroll` nem chama `RemoveEventListener` ela mesma, então nenhuma das duas supressões pode mascarar um bug da glintfx.

---

## [0.3.1] - 2026-07-04 · [GitHub](https://github.com/petrinhu/glintfx/releases/tag/v0.3.1)

### Added / Adicionado

- **EN:** `decorator: polygon(<sides>, <fill>[, <rotation>])`'s 2nd argument now also accepts `radial-gradient(...)` / `linear-gradient(...)` in place of a solid `<color>` -- consumer-driven by GusWorld's brass hexagon corner nodes needing centre-clear -> edge-dark "screw head" volume instead of a flat fill. **Technique chosen: reuse RmlUi's own real gradient fragment shader** (`Rml::RenderManager::CompileShader("radial-gradient"/"linear-gradient", ...)`, the exact public API and Dictionary key contract `Rml::DecoratorRadialGradient`/`Rml::DecoratorLinearGradient` themselves use), not hand-rolled vertex-colour rings: because the shader colours each fragment from an interpolated tex_coord (an affine function of vertex position), the polygon gets a perfectly circular/linear blend under its triangle-fan mesh, not a faceted approximation, at less code than a correct multi-ring implementation would need. No shader code was written -- the fragment shader lives in RmlUi's already-vendored GL3 backend and is reached exclusively through public headers; `render_gl3.cpp` and RmlUi core are untouched. **Syntax:** the shorthand's 2nd positional item is now parsed with the built-in passthrough `"string"` RCSS parser (not `"color"`), deferring the colour-vs-gradient decision to a new `ParseFill()` in `decorator_polygon.cpp`, itself built entirely on RmlUi's own public parsers looked up by name (`StyleSheetSpecification::GetParser("color"/"color_stop_list"/"angle"/"number_percent")`) -- zero hand-rolled colour/number lexing. Radial accepts an optional `circle [at <x%> <y%>]` centre-position prefix (only `circle`, no `ellipse` -- the polygon's fill shape is always its own inscribed circle); linear requires the angle as a mandatory first argument (no CSS `to <side>` keyword form). In both, the **last** stop lands exactly on the polygon's own inscribed-circle radius (not CSS's farthest-corner default), so the outer ring always lines up with the shape's own edge. Solid-color fill (`polygon(6, #C9A24B)`) is fully unchanged, and the existing `sides` input hardening (`[3, 1024]`, float-cast-overflow-safe) is untouched and still runs first. **Fill hardening:** a malformed fill (invalid colour, invalid/unsupported stop unit, missing linear angle, empty gradient) makes the whole decorator ignored, fail-high, exactly like an out-of-range `sides` -- never a partial or garbage render. Verified by new test `polygon_gradient_sanity` (runs in both build configs, UBSan-clean): radial ramp direction, an explicit stop-percentage landing exactly on its own colour, the linear angle-to-direction mapping, the solid fill byte-identical at two sample points, and all four fill/`sides`-hardening rejection cases. Documented in `docs/effects.md`.
  **PT:** o 2º argumento de `decorator: polygon(<lados>, <preenchimento>[, <rotação>])` agora também aceita `radial-gradient(...)` / `linear-gradient(...)` no lugar de uma `<cor>` sólida -- consumer-driven pelos nós hexagonais de latão dos cantos do GusWorld, que precisavam do volume "cabeça de parafuso" centro-claro -> borda-escura em vez de um preenchimento chapado. **Técnica escolhida: reusar o próprio shader de fragmento de gradiente real do RmlUi** (`Rml::RenderManager::CompileShader("radial-gradient"/"linear-gradient", ...)`, a mesma API pública e o mesmo contrato de chaves do Dictionary que `Rml::DecoratorRadialGradient`/`Rml::DecoratorLinearGradient` usam), não anéis de vertex-color feitos à mão: como o shader colore cada fragmento a partir de um tex_coord interpolado (uma função afim da posição do vértice), o polígono recebe um blend perfeitamente circular/linear sob sua malha triangle-fan, não uma aproximação facetada, com menos código do que uma implementação correta multi-anel exigiria. Nenhum código de shader foi escrito -- o shader de fragmento mora no backend GL3 já vendorizado do RmlUi e é alcançado exclusivamente por headers públicos; `render_gl3.cpp` e o core do RmlUi ficam intocados. **Sintaxe:** o 2º item posicional do shorthand agora é parseado com o parser RCSS passthrough embutido `"string"` (não `"color"`), adiando a decisão cor-vs-gradiente para um novo `ParseFill()` em `decorator_polygon.cpp`, ele mesmo construído inteiramente sobre os parsers públicos do próprio RmlUi buscados por nome (`StyleSheetSpecification::GetParser("color"/"color_stop_list"/"angle"/"number_percent")`) -- zero lexing de cor/número feito à mão. O radial aceita um prefixo opcional de posição-de-centro `circle [at <x%> <y%>]` (só `circle`, sem `ellipse` -- a forma de preenchimento do polígono é sempre o próprio círculo inscrito); o linear exige o ângulo como 1º argumento obrigatório (sem a forma de palavra-chave `to <side>` do CSS). Em ambos, o **último** stop cai exatamente no raio do círculo inscrito do próprio polígono (não no default farthest-corner do CSS), então o anel externo sempre alinha com a borda da própria forma. O preenchimento de cor sólida (`polygon(6, #C9A24B)`) fica totalmente inalterado, e o hardening de entrada já existente de `sides` (`[3, 1024]`, seguro contra float-cast-overflow) fica intocado e continua rodando primeiro. **Hardening de preenchimento:** um preenchimento malformado (cor inválida, unidade de stop inválida/não-suportada, ângulo do linear ausente, gradiente vazio) faz o decorator inteiro ser ignorado, fail-high, exatamente como um `lados` fora do range -- nunca um render parcial ou lixo. Verificado pelo novo teste `polygon_gradient_sanity` (roda nos 2 configs de build, UBSan-limpo): direção da rampa radial, um stop de porcentagem explícito caindo exatamente na própria cor, o mapeamento ângulo-para-direção do linear, o preenchimento sólido byte-idêntico em dois pontos de amostra, e os quatro casos de rejeição de hardening de preenchimento/`sides`. Documentado em `docs/effects.md`.

### Fixed / Corrigido

- **EN:** `load(rml_path)` [Important] -- each call used to overwrite the internal document pointer (`Bootstrap::Impl::doc`) with the newly loaded document without ever closing the previous one, leaking it and leaving it still `Show()`n underneath the new one (a visible "ghost" document, especially noticeable with transparency/glow). Reported by GusWorld: reproduces on every `load()` call on the same `UiLayer`/`App` while navigating between screens of a multi-screen menu (reload-on-change pattern) -- each navigation leaked one more document. Fixed by closing the previous document (`ElementDocument::Close()`, destruction deferred to the next `Context::Update()`, already called once per frame by `Engine::update()`) **only after** the new `LoadDocument()` succeeds -- if the new load fails, the host keeps whatever was already on screen instead of being left with zero documents. Verified by new test `document_reload_leak` (A→B→A navigation and 5x repeated reload on the same `Engine`, both build configs): `Rml::Context::GetNumDocuments()` stays at 1 instead of growing; confirmed the test has teeth by temporarily removing the guard (document count grew to 2, test failed) before restoring it. Clean under `GLINTFX_SANITIZE=ON`.
  **PT:** `load(rml_path)` [Important] -- cada chamada sobrescrevia o ponteiro interno do documento (`Bootstrap::Impl::doc`) com o documento recém-carregado sem nunca fechar o anterior, vazando-o e deixando-o ainda `Show()`ado por baixo do novo (um documento "fantasma" visível, especialmente perceptível com transparência/glow). Reportado pelo GusWorld: reproduz em toda chamada de `load()` na mesma `UiLayer`/`App` ao navegar entre telas de um menu com várias telas (padrão reload-on-change) -- cada navegação vazava mais um documento. Corrigido fechando o documento anterior (`ElementDocument::Close()`, destruição adiada até o próximo `Context::Update()`, já chamado uma vez por frame por `Engine::update()`) **só depois** do novo `LoadDocument()` ter sucesso -- se o novo load falhar, o host mantém o que já estava na tela em vez de ficar com zero documentos. Verificado pelo novo teste `document_reload_leak` (navegação A→B→A e 5x reload repetido no mesmo `Engine`, nos 2 configs de build): `Rml::Context::GetNumDocuments()` fica em 1 em vez de crescer; confirmado que o teste tem dente removendo o guard temporariamente (a contagem de documentos cresceu pra 2, o teste falhou) antes de restaurá-lo. Limpo sob `GLINTFX_SANITIZE=ON`.
- **EN:** `DataBinder::bind_number/bind_string/bind_bool/bind_list` [Important, heap-use-after-free] -- found while auditing for siblings of the `load()` bug above (same "reassign an owning handle without releasing/checking the previous one" class, one level worse: memory corruption, not just a leak). Calling `bind_*` twice with the **same key** before `load()` used to overwrite the map slot with a new `std::unique_ptr`, which destroys (frees) the previous cell **before** the second `Rml::DataModelConstructor::Bind()` call runs; that second `Bind()` then fails silently (RmlUi's `DataModel::BindVariable` uses `map::emplace`, which never overwrites an existing key), so RmlUi's own variable map is left pointing at the now-freed first cell -- any subsequent read of that variable (e.g. a `{{key}}` view re-evaluating) is a heap-use-after-free. Reachable only via a caller bug (binding the same key twice), not the ordinary happy path, hence Important rather than Critical -- but a real memory-safety issue, not merely a leak. Fixed with the same check-before-mutate guard `create_data_model` already used: a duplicate key is now rejected (`return false`) **before** touching the map, matching every other entry-point hardening in this API. Verified by new test `data_model_rebind_uaf`: confirmed the fix has teeth by temporarily removing the guard and rebuilding under `GLINTFX_SANITIZE=ON` -- ASan caught the exact heap-use-after-free live (`Rml::Variant::Set` <- `DataViewText::Update` <- `DataModel::Update` <- `Context::LoadDocument`) before the guard was restored and ASan/UBSan confirmed clean again. Non-duplicate keys are unaffected.
  **PT:** `DataBinder::bind_number/bind_string/bind_bool/bind_list` [Important, heap-use-after-free] -- achado ao auditar por gêmeos do bug do `load()` acima (mesma classe "reatribuir um handle possuído sem liberar/checar o anterior", um nível pior: corrupção de memória, não só vazamento). Chamar `bind_*` duas vezes com a **mesma chave** antes do `load()` sobrescrevia o slot do mapa com um novo `std::unique_ptr`, que destrói (libera) a célula anterior **antes** do segundo `Rml::DataModelConstructor::Bind()` rodar; esse segundo `Bind()` então falha silenciosamente (o `DataModel::BindVariable` do RmlUi usa `map::emplace`, que nunca sobrescreve uma chave existente), então o próprio mapa de variáveis do RmlUi fica apontando pra primeira célula já liberada -- qualquer leitura subsequente dessa variável (ex.: uma view `{{chave}}` reavaliando) é um heap-use-after-free. Alcançável só via erro do chamador (ligar a mesma chave duas vezes), não o caminho feliz comum, por isso Important em vez de Critical -- mas um problema real de segurança de memória, não apenas um vazamento. Corrigido com o mesmo guard check-before-mutate que `create_data_model` já usava: uma chave duplicada agora é rejeitada (`return false`) **antes** de tocar o mapa, igualando o resto do hardening de entrada desta API. Verificado pelo novo teste `data_model_rebind_uaf`: confirmado que o fix tem dente removendo o guard temporariamente e reconstruindo sob `GLINTFX_SANITIZE=ON` -- o ASan capturou o heap-use-after-free exato ao vivo (`Rml::Variant::Set` <- `DataViewText::Update` <- `DataModel::Update` <- `Context::LoadDocument`) antes do guard ser restaurado e o ASan/UBSan confirmarem limpo de novo. Chaves não-duplicadas não são afetadas.

### Testing / Testes

- **EN:** 3 new tests this release: `polygon_gradient_sanity` (runs in both build configs) -- radial/linear ramp direction, explicit stop-percentage landing, solid-fill regression, and fill/`sides` hardening rejection; `document_reload_leak` (both build configs) -- A→B→A navigation and 5x repeated reload keep `Rml::Context::GetNumDocuments()` at 1; `data_model_rebind_uaf` (both build configs) -- duplicate-key `bind_*` calls are rejected without corrupting the first binding. All UBSan/ASan-clean under `GLINTFX_SANITIZE=ON`; both fixes were confirmed to have teeth by temporarily reverting the guard and observing the sanitizer/assertion actually catch the regression before restoring it. Suite size: **29 tests** with `GLINTFX_BACKEND_GLFW=ON`, **15** with `=OFF` (embed-only).
  **PT:** 3 testes novos nesta release: `polygon_gradient_sanity` (roda nos 2 configs de build) -- direção da rampa radial/linear, pouso de stop-percentual explícito, regressão do preenchimento sólido, e rejeição de hardening de preenchimento/`sides`; `document_reload_leak` (nos 2 configs de build) -- navegação A→B→A e 5x reload repetido mantêm `Rml::Context::GetNumDocuments()` em 1; `data_model_rebind_uaf` (nos 2 configs de build) -- chamadas `bind_*` com chave duplicada são rejeitadas sem corromper a primeira ligação. Todos UBSan/ASan-limpos sob `GLINTFX_SANITIZE=ON`; ambos os fixes foram confirmados com dente revertendo o guard temporariamente e observando o sanitizer/asserção realmente pegar a regressão antes de restaurá-lo. Tamanho da suíte: **29 testes** com `GLINTFX_BACKEND_GLFW=ON`, **15** com `=OFF` (embed-only).

---

## [0.3.0] - 2026-07-04 · [GitHub](https://github.com/petrinhu/glintfx/releases/tag/v0.3.0)

### Added / Adicionado

- **EN:** `decorator: polygon(<sides>, <color>[, <rotation>])` -- a new custom RCSS decorator that fills an element's box with a solid-color regular N-gon (triangle-fan inscribed in a circle of radius = half the shorter box dimension; `rotation` is optional, in degrees, default orientation points the first vertex straight up, negative/any finite angle allowed). Consumer-driven by GusWorld's hexagonal slider node. No shader, no change to `render_gl3.cpp` or RmlUi core -- pure extension via the public `Rml::Decorator`/`Rml::DecoratorInstancer` interface, modelled directly on `Rml::DecoratorStraightGradient` (new internal files `glintfx/src/decorator_polygon.{hpp,cpp}`, registered by `Bootstrap::init()`). Glow and clip-path use cases need zero new code: `filter: drop-shadow(...)` already follows the polygon's alpha shape, and `mask-image: polygon(...)` already works because any decorator can be a mask source. **Input hardening:** `sides` is validated defensively against the range **`[3, 1024]`** on the raw parsed float *before* any int conversion (RCSS may come from a less-trusted theme/mod/dynamic source). An empty `polygon()`, a below-floor value (`polygon(2)`, negatives), an above-ceiling value (`polygon(100000)` -- a DoS-by-typo guard: ~a billion triangles otherwise), or a non-finite/astronomical value (`polygon(1e30)`, avoiding float-cast-overflow UB) all cause the decorator to be **ignored** (fail-high) with a warning naming the received value and valid range -- never a silent white hexagon. Documented in `docs/effects.md`.
  **PT:** `decorator: polygon(<lados>, <cor>[, <rotação>])` -- um novo decorator RCSS customizado que preenche a caixa de um elemento com um N-ágono regular de cor sólida (triangle-fan inscrito num círculo de raio = metade da dimensão menor da caixa; `rotação` é opcional, em graus, orientação padrão aponta o primeiro vértice reto pra cima, ângulo negativo/qualquer finito permitido). Consumer-driven pelo nó slider hexagonal do GusWorld. Sem shader, sem mudança em `render_gl3.cpp` ou no core do RmlUi -- pura extensão via a interface pública `Rml::Decorator`/`Rml::DecoratorInstancer`, moldada diretamente em `Rml::DecoratorStraightGradient` (novos arquivos internos `glintfx/src/decorator_polygon.{hpp,cpp}`, registrados por `Bootstrap::init()`). Casos de uso de glow e clip-path não precisam de código novo: `filter: drop-shadow(...)` já segue a forma do alpha do polígono, e `mask-image: polygon(...)` já funciona porque qualquer decorator pode ser fonte de máscara. **Hardening de entrada:** `lados` é validado defensivamente contra o range **`[3, 1024]`** no float bruto parseado *antes* de qualquer conversão pra int (RCSS pode vir de tema/mod/fonte dinâmica menos confiável). Um `polygon()` vazio, um valor abaixo do piso (`polygon(2)`, negativos), um valor acima do teto (`polygon(100000)` -- guard de DoS-por-typo: senão ~um bilhão de triângulos), ou um valor não-finito/astronômico (`polygon(1e30)`, evitando UB de float-cast-overflow) fazem o decorator ser **ignorado** (fail-high) com um aviso nomeando o valor recebido e o range válido -- nunca um hexágono branco silencioso. Documentado em `docs/effects.md`.

### Fixed / Corrigido

- **EN:** Input hardening across the public API surface (security-audit findings) -- treat RCSS/parameters as coming from a less-trusted source, not just from a well-behaved caller:
  - **`load(nullptr)` [Critical]** -- previously constructed a `std::string(nullptr)` internally, which is undefined behaviour and crashed the host process via `std::terminate()`. Now returns `false` like an ordinary load failure. The `if (!rml_path) return false;` guard already existed in the sibling entry points (`DataBinder`, `get_element_box`, `set_base_url`) and was simply missing from `load()` itself.
  - **`set_dp_ratio(ratio)` [Important]** -- now rejects `!std::isfinite(ratio) || ratio <= 0` and keeps the previous value instead of applying it. A `NaN` or `0` ratio previously propagated silently into `Rml::Context::SetDensityIndependentPixelRatio`, poisoning every subsequent `dp`-unit layout computation.
  - **`set_viewport(w, h, ...)` [Important]**, both the 2-arg and 5-arg (`UiLayer` letterbox) overloads -- now rejects `w <= 0 || h <= 0` as a no-op, mirroring the guard already present on the `Resize` event path in `process_event()`.
  All three are fail-safe (reject-and-keep-previous-state), never fail-open. Verified by `input_hardening_sanity` (embed, both build configs) and `app_input_hardening_smoke` (GLFW-only), both UBSan-clean under `GLINTFX_SANITIZE=ON`.
  **PT:** Hardening de entrada em toda a superfície pública da API (achados de auditoria de segurança) -- tratando RCSS/parâmetros como vindos de uma fonte menos confiável, não apenas de um chamador bem-comportado:
  - **`load(nullptr)` [Critical]** -- antes construía um `std::string(nullptr)` internamente, comportamento indefinido que derrubava o processo host via `std::terminate()`. Agora retorna `false`, como uma falha de load comum. O guard `if (!rml_path) return false;` já existia nos pontos de entrada irmãos (`DataBinder`, `get_element_box`, `set_base_url`) e faltava só no próprio `load()`.
  - **`set_dp_ratio(ratio)` [Important]** -- agora rejeita `!std::isfinite(ratio) || ratio <= 0` e mantém o valor anterior em vez de aplicá-lo. Um ratio `NaN` ou `0` antes se propagava silenciosamente para `Rml::Context::SetDensityIndependentPixelRatio`, envenenando todo cálculo de layout subsequente com unidade `dp`.
  - **`set_viewport(w, h, ...)` [Important]**, tanto a sobrecarga de 2 args quanto a de 5 args (letterbox do `UiLayer`) -- agora rejeita `w <= 0 || h <= 0` como no-op, replicando o guard já presente no caminho do evento `Resize` em `process_event()`.
  As três são fail-safe (rejeita-e-mantém-estado-anterior), nunca fail-open. Verificado por `input_hardening_sanity` (embed, nos 2 configs de build) e `app_input_hardening_smoke` (GLFW-only), ambos UBSan-limpos sob `GLINTFX_SANITIZE=ON`.
- **EN:** `glintfx::version()` now correctly reports **"0.3.0"**. It had been silently stuck reporting `"0.2.4"` since the v0.2.5 release -- the `project(glintfx ... VERSION ...)` bump in `CMakeLists.txt` was forgotten in that release window, and since `version()` reads the CMake-generated `GLINTFX_VERSION` (fixed to read from CMake, rather than a hardcoded literal, back in v0.2.4 -- see below), the stale build number rode along unnoticed through v0.2.5. Single source of truth (`project(VERSION)`) confirmed via repo-wide grep before this release.
  **PT:** `glintfx::version()` agora reporta corretamente **"0.3.0"**. Estava silenciosamente travado em `"0.2.4"` desde a release v0.2.5 -- o bump de `project(glintfx ... VERSION ...)` no `CMakeLists.txt` foi esquecido naquela janela de release, e como `version()` lê o `GLINTFX_VERSION` gerado pelo CMake (corrigido para ler do CMake, em vez de literal hardcoded, lá na v0.2.4 -- ver abaixo), o número de build defasado seguiu despercebido durante toda a v0.2.5. Fonte única (`project(VERSION)`) confirmada via grep no repo inteiro antes desta release.

### Testing / Testes

- **EN:** 3 new tests this release: `polygon_sanity` (runs in both build configs) -- geometry proofs (alpha-shape hexagon vs octagon, negative-rotation applied) plus the full input-validation matrix (empty/negative/below-floor rejected; floor=3 and ceiling=1024 accepted; above-ceiling and `1e30` rejected with no UB); `input_hardening_sanity` (embed, both build configs) -- `load(nullptr)` returns `false` without aborting, an invalid `dp_ratio` (`NaN`, `0`, negative) leaves the previous baseline untouched, and `set_viewport(0, h)` / `set_viewport(w, 0)` are silent no-ops; `app_input_hardening_smoke` (GLFW-only) -- `App` parity for the same three guards. All UBSan-clean under `GLINTFX_SANITIZE=ON`. Suite size: **26 tests** with `GLINTFX_BACKEND_GLFW=ON`, **13** with `=OFF` (embed-only).
  **PT:** 3 testes novos nesta release: `polygon_sanity` (roda nos 2 configs de build) -- provas de geometria (alpha-shape hexágono vs octógono, rotação negativa aplicada) mais a matriz completa de validação de entrada (vazio/negativo/abaixo-do-piso rejeitados; piso=3 e teto=1024 aceitos; acima-do-teto e `1e30` rejeitados sem UB); `input_hardening_sanity` (embed, nos 2 configs de build) -- `load(nullptr)` retorna `false` sem abortar, um `dp_ratio` inválido (`NaN`, `0`, negativo) mantém a baseline anterior intocada, e `set_viewport(0, h)` / `set_viewport(w, 0)` são no-ops silenciosos; `app_input_hardening_smoke` (GLFW-only) -- paridade do `App` para os mesmos três guards. Todos UBSan-limpos sob `GLINTFX_SANITIZE=ON`. Tamanho da suíte: **26 testes** com `GLINTFX_BACKEND_GLFW=ON`, **13** com `=OFF` (embed-only).

[0.3.0]: https://codeberg.org/petrinhu/glintfx/releases/tag/v0.3.0

---

## [0.2.5] - 2026-07-02 · [GitHub](https://github.com/petrinhu/glintfx/releases/tag/v0.2.5)

### Added / Adicionado

- **EN:** `set_click_callback(std::function<void(const char* element_id)>)` on both `UiLayer` and `App` -- fires on "click" (bubble phase, listener attached to the document root by `load()`), reporting the id of the clicked element, or the nearest ancestor id if the target itself has none, or `""` if no element in the ancestor chain has an id. No ordering constraint versus `load()` -- can be set before or after. Verified by `click_callback_sanity` and `app_click_callback_smoke`. Resolves `L1.11-CLICKCB`.
  **PT:** `set_click_callback(std::function<void(const char* element_id)>)` em `UiLayer` e `App` -- dispara em "click" (fase bubble, listener anexado à raiz do documento por `load()`), reportando o id do elemento clicado, ou o id do ancestral mais próximo se o próprio alvo não tiver, ou `""` se nenhum elemento na cadeia de ancestrais tiver id. Sem restrição de ordem vs. `load()` -- pode ser setado antes ou depois. Verificado por `click_callback_sanity` e `app_click_callback_smoke`. Resolve `L1.11-CLICKCB`.
- **EN:** `get_element_box(const char* id) -> ElementBox { bool found; float x, y, w, h; }` on both `UiLayer` and `App` -- returns the border-box geometry (`Rml::BoxArea::Border`) of an element by id, in window-space physical pixels, or `found=false` if the id does not exist or no document is loaded yet. New public header `glintfx/element_box.hpp`. Verified by `element_box_sanity` and `app_element_box_smoke`. Resolves `L1.12-ELEMBOX`.
  **PT:** `get_element_box(const char* id) -> ElementBox { bool found; float x, y, w, h; }` em `UiLayer` e `App` -- retorna a geometria border-box (`Rml::BoxArea::Border`) de um elemento por id, em pixels físicos espaço-janela, ou `found=false` se o id não existir ou nenhum documento estiver carregado ainda. Novo header público `glintfx/element_box.hpp`. Verificado por `element_box_sanity` e `app_element_box_smoke`. Resolve `L1.12-ELEMBOX`.
- **EN:** `UiLayer::set_viewport(int x, int y, int w, int h, int target_h)` -- places the composited UI within a sub-region of the host's window (letterbox), resolving the hardcoded-`(0,0)`-origin gap tracked as `GAP-2` since ADR-0008. `(x, y)` are top-down window-space (the same convention as `UiEvent`'s mouse coordinates and `get_element_box()`'s return value); `target_h` is the host's full render-target height, used only to convert internally to OpenGL's bottom-up viewport offset. The legacy `set_viewport(w, h)` (2-arg) is unchanged and equivalent to `(0, 0, w, h, h)`. `UiLayer`-only by design -- `App` always owns its whole window, so a sub-viewport offset is meaningless there (see ADR-0008 "F3 implementation update"). Verified by `viewport_origin_sanity`. Resolves `L1.13-VPORIGIN` and the origin half of `GAP-2` (the custom-host-FBO half of `GAP-2` remains open, see `docs/embed-integration.md` section 8).
  **PT:** `UiLayer::set_viewport(int x, int y, int w, int h, int target_h)` -- posiciona a UI composta numa sub-região da janela do host (letterbox), resolvendo a lacuna de origem hardcoded em `(0,0)` rastreada como `GAP-2` desde o ADR-0008. `(x, y)` são espaço-janela top-down (a mesma convenção das coordenadas de mouse do `UiEvent` e do retorno de `get_element_box()`); `target_h` é a altura total do render-target do host, usada só pra converter internamente para o offset de viewport bottom-up do OpenGL. O `set_viewport(w, h)` legado (2 args) é inalterado e equivalente a `(0, 0, w, h, h)`. Só em `UiLayer` por design -- o `App` sempre possui a janela inteira, então um offset de sub-viewport não faz sentido lá (ver "F3 implementation update" no ADR-0008). Verificado por `viewport_origin_sanity`. Resolve `L1.13-VPORIGIN` e a metade "origem" do `GAP-2` (a metade "FBO custom do host" do `GAP-2` segue em aberto, ver `docs/embed-integration.md` seção 8).
- **EN:** Nightly memory gate (`TST-L1-MEM`): CMake option `GLINTFX_SANITIZE` (ASan+UBSan+LSan) with suppression files for third-party noise, mirrored on GitHub Actions and Codeberg Forgejo Actions (cron + manual dispatch). Landed in this release window ahead of the API additions above -- not itself part of F1/F2/F3, noted here for release-window completeness. See `TESTES.md`.
  **PT:** Gate noturno de memória (`TST-L1-MEM`): opção CMake `GLINTFX_SANITIZE` (ASan+UBSan+LSan) com arquivos de supressão para ruído de terceiros, espelhado em GitHub Actions e Codeberg Forgejo Actions (cron + dispatch manual). Entrou nesta janela de release antes das adições de API acima -- não faz parte em si de F1/F2/F3, anotado aqui por completude da janela. Ver `TESTES.md`.

### Docs / Documentação

- **EN:** ADR-0008 -- new dated section "F3 implementation update -- configurable viewport origin (v0.2.5)", added after "F1 implementation constraints" without reopening the `Accepted` decision. `docs/embed-integration.md` -- new section 10 ("Click callback, element geometry, and the window-space coordinate contract"), consolidating the three v0.2.5 additions and their shared coordinate contract; sections 0, 1, 2, and 8 updated in place to point at it.
  **PT:** ADR-0008 -- nova seção datada "F3 implementation update -- configurable viewport origin (v0.2.5)", acrescentada após "F1 implementation constraints" sem reabrir a decisão `Accepted`. `docs/embed-integration.md` -- nova seção 10 ("Callback de clique, geometria de elemento e o contrato de coordenadas espaço-janela"), consolidando as três adições da v0.2.5 e o contrato de coordenadas compartilhado; seções 0, 1, 2 e 8 atualizadas no lugar para apontar para ela.

### Testing / Testes

- **EN:** 5 new tests this release (`click_callback_sanity`, `app_click_callback_smoke`, `element_box_sanity`, `app_element_box_smoke`, `viewport_origin_sanity`; 3 of the 5 run in both build configs). Suite size: **23 tests** with `GLINTFX_BACKEND_GLFW=ON`, **11** with `=OFF` (embed-only).
  **PT:** 5 testes novos nesta release (`click_callback_sanity`, `app_click_callback_smoke`, `element_box_sanity`, `app_element_box_smoke`, `viewport_origin_sanity`; 3 dos 5 rodam nos 2 configs de build). Tamanho da suíte: **23 testes** com `GLINTFX_BACKEND_GLFW=ON`, **11** com `=OFF` (embed-only).

[0.2.5]: https://codeberg.org/petrinhu/glintfx/releases/tag/v0.2.5

---

## [0.2.4] - 2026-07-01

### Added / Adicionado

- **EN:** Embedded User-Agent stylesheet: `display: block` defaults for structural elements (`div, p, h1..h6, ul, ol, li, section, article, header, footer, nav, main`), merged into every document loaded via `load()` on both `App` and `UiLayer` (shared code path in `Bootstrap::load()`). Applied as a low-specificity base -- any author rule of equal or higher specificity overrides it. Verified by `ua_stylesheet_sanity`. Documented in `docs/embed-integration.md` section 9.
  **PT:** Stylesheet User-Agent embutida: defaults de `display: block` para elementos estruturais (`div, p, h1..h6, ul, ol, li, section, article, header, footer, nav, main`), mesclada em todo documento carregado via `load()` tanto em `App` quanto em `UiLayer` (caminho de código compartilhado em `Bootstrap::load()`). Aplicada como base de baixa especificidade -- qualquer regra do autor com especificidade igual ou maior a sobrepõe. Verificado por `ua_stylesheet_sanity`. Documentado em `docs/embed-integration.md` seção 9.

### Fixed / Corrigido

- **EN:** `version()` returned a hardcoded `"0.1.0"`, stale since v0.1.1; now reads `GLINTFX_VERSION` generated by CMake from `project(glintfx VERSION ...)` -- single source of truth.
  **PT:** `version()` retornava `"0.1.0"` hardcoded, defasado desde a v0.1.1; agora lê `GLINTFX_VERSION` gerado pelo CMake a partir de `project(glintfx VERSION ...)` -- fonte única.
- **EN:** Test-count comment in the CI workflows (`.forgejo/`, `.github/`) was stale ("10 tests" / "5 embed tests", from when the suite had 10/5); corrected to the current values (18/8). Comment-only, no change in CI behaviour.
  **PT:** Comentário de contagem de testes nos workflows de CI (`.forgejo/`, `.github/`) estava defasado ("10 testes" / "5 testes embed", de quando a suíte tinha 10/5); corrigido para os valores atuais (18/8). Comment-only, sem mudança de comportamento de CI.

---

## [0.2.3] - 2026-06-30

### Added / Adicionado

- **EN:** Data-model binding via `create_data_model(name)` + `bind_number / bind_string / bind_bool / bind_list(key)` + `set_*(key, value)` on both `UiLayer` and `App`. Mandatory call order: `create_data_model` → `bind_*` → `load()` → `set_*` (calling `bind_*` after `load()` returns `false` -- RmlUi compiles views at load time). `bind_list` supports string-list variables iterable in RML via `data-for="item : list"` and `{{item}}` interpolation -- enables scrolling log panels, menus, and inventory lists. Memory is owned by the engine (`DataBinder` uses one `std::unique_ptr` per key in `std::map` for stable addresses); the consumer passes values by copy only. Verified by `data_model_smoke`, `data_model_scalar`, and `data_model_list` tests.
  **PT:** Ligacao de data-model via `create_data_model(name)` + `bind_number / bind_string / bind_bool / bind_list(key)` + `set_*(key, value)` em `UiLayer` e `App`. Ordem obrigatoria: `create_data_model` → `bind_*` → `load()` → `set_*` (chamar `bind_*` apos `load()` retorna `false` -- o RmlUi compila views no momento do load). `bind_list` suporta variaveis de lista de strings iteraveis no RML via `data-for="item : list"` e interpolacao `{{item}}` -- permite paineis de log rolante, menus e listas de inventario. A memoria e de posse do engine (`DataBinder` usa um `std::unique_ptr` por chave em `std::map` para enderecos estaveis); o consumidor passa valores apenas por copia. Verificado pelos testes `data_model_smoke`, `data_model_scalar` e `data_model_list`.

### Changed / Alterado

- **EN:** `App::load()` and `UiLayer::load()` now return `bool` (true on success, false on failure) -- previously `void`. Source-compatible: callers that discard the return value continue to compile without change. This is a signature and ABI change; callers that store the return type or use the methods in a `decltype` context must be updated.
  **PT:** `App::load()` e `UiLayer::load()` agora retornam `bool` (true em sucesso, false em falha) -- anteriormente `void`. Compativel com source: callers que descartam o retorno continuam compilando sem alteracao. Esta e uma mudanca de assinatura e ABI; callers que armazenam o tipo de retorno ou usam os metodos em contexto `decltype` precisam ser atualizados.

### Fixed / Corrigido

- **EN:** Texture loading now supports **PNG and JPG** (previously only TGA decoded reliably). Decoding is handled by **stb_image** (`glintfx/third_party/stb/stb_image.h`) via an override of `Rml::RenderInterface::LoadTexture`. After decoding, the alpha channel is **premultiplied in-place** (`out.rgb = in.rgb * in.a / 255`) before GPU upload -- required for correct compositing with the GL3 premultiplied-alpha blend (`GL_ONE, GL_ONE_MINUS_SRC_ALPHA`) used by the renderer. Without premultiplication a PNG with straight alpha produces a bright halo around transparent regions. On decode failure the backend falls back to the upstream RmlUi TGA loader so existing TGA assets do not regress. Verified by `texture_png_alpha` test. Documented in `docs/embed-integration.md` section 7.
  **PT:** O carregamento de texturas agora suporta **PNG e JPG** (anteriormente apenas o TGA decodificava de forma confiavel). A decodificacao e feita pelo **stb_image** (`glintfx/third_party/stb/stb_image.h`) via override de `Rml::RenderInterface::LoadTexture`. Apos decodificar, o canal alpha e **premultiplicado in-place** (`out.rgb = in.rgb * in.a / 255`) antes do upload para a GPU -- necessario para composicao correta com o blend premultiplied-alpha GL3 (`GL_ONE, GL_ONE_MINUS_SRC_ALPHA`) usado pelo renderer. Sem premultiplicacao, um PNG com alpha straight produz um halo claro ao redor das regioes transparentes. Em caso de falha de decode, o backend cai no loader TGA do upstream RmlUi para que assets TGA existentes nao regridam. Verificado pelo teste `texture_png_alpha`. Documentado em `docs/embed-integration.md` secao 7.

---

## [0.2.2] - 2026-06-30

### Added / Adicionado

- **EN:** `UiLayerConfig::dp_ratio` (field, default `1.0f`) and `UiLayer::set_dp_ratio(float)` (runtime setter): expose `Rml::Context::SetDensityIndependentPixelRatio` to host consumers. When set, the RCSS `dp` unit scales to `dp_ratio` physical pixels (the `px` unit is unaffected). Recommended pattern: author RCSS at a fixed logical size using `dp` units, call `set_viewport(real_w, real_h)` and `set_dp_ratio(real_w / logical_w)`. `App` parity: `AppConfig::dp_ratio` and `App::set_dp_ratio(float)`. Verified by `dp_ratio_sanity` (100dp×100dp white box; scale factor 4.00x at ratio=2.0 vs 1.0). Resolves GAP-1 from `docs/embed-integration.md`.
  **PT:** `UiLayerConfig::dp_ratio` (campo, padrão `1.0f`) e `UiLayer::set_dp_ratio(float)` (setter runtime): expõe `Rml::Context::SetDensityIndependentPixelRatio` para consumidores host. Quando definido, a unidade RCSS `dp` escala para `dp_ratio` pixels físicos (a unidade `px` não é afetada). Padrão recomendado: autore RCSS num tamanho lógico fixo usando unidades `dp`, chame `set_viewport(real_w, real_h)` e `set_dp_ratio(real_w / logical_w)`. Paridade `App`: `AppConfig::dp_ratio` e `App::set_dp_ratio(float)`. Verificado por `dp_ratio_sanity` (box branco 100dp×100dp; fator de escala 4.00x em ratio=2.0 vs 1.0). Resolve o GAP-1 de `docs/embed-integration.md`.
- **EN:** `UiLayer::set_asset_base_url(const char*)` and `App::set_asset_base_url(const char*)`: install a custom `Rml::FileInterface` (`src/base_url_file_interface.hpp`) that prepends a configurable base URL to relative asset paths. Enables documents to be loaded by relative name when their files live in a subdirectory or absolute root that differs from the process CWD. Absolute paths and paths already resolved by RmlUi are not affected. Call before `load()`. Pass `nullptr` or `""` to clear. Verified by `base_url_sanity` (renders `embed_scene.rml` exclusively from `base_url_assets/`; mean=58.5, dark=50%, bright=35%). Resolves GAP-3 from `docs/embed-integration.md`.
  **PT:** `UiLayer::set_asset_base_url(const char*)` e `App::set_asset_base_url(const char*)`: instalam um `Rml::FileInterface` customizado (`src/base_url_file_interface.hpp`) que prefixa um base URL configuravel a caminhos relativos de assets. Permite que documentos sejam carregados por nome relativo quando seus arquivos estao num subdiretorio ou raiz absoluta diferente do CWD do processo. Caminhos absolutos e paths ja resolvidos pelo RmlUi nao sao afetados. Chame antes de `load()`. Passe `nullptr` ou `""` para limpar. Verificado por `base_url_sanity` (renderiza `embed_scene.rml` exclusivamente de `base_url_assets/`; mean=58.5, dark=50%, bright=35%). Resolve o GAP-3 de `docs/embed-integration.md`.

---

## [0.2.1] - 2026-06-30

### Added / Adicionado

- **EN:** `GLINTFX_BACKEND_GLFW` CMake option (default `ON`): when set to `OFF`, the library is built in embed-only mode -- `glintfx::App`, `window_glfw.cpp`, and `RmlUi_Platform_GLFW.cpp` are excluded and `glfw` is not linked. Enables SDL3 / X11 host consumers (e.g. GusWorld) to link `glintfx::UiLayer` without dragging GLFW as a transitive dependency. `glintfxConfig.cmake` propagates the setting so `find_package(glintfx)` consumers are not forced to have GLFW installed in embed-only builds. The generated `<glintfx/config.hpp>` exposes `GLINTFX_BACKEND_GLFW` (0/1) for compile-time guards.
  **PT:** Opção CMake `GLINTFX_BACKEND_GLFW` (padrão `ON`): quando `OFF`, a biblioteca é compilada em modo embed-only -- `glintfx::App`, `window_glfw.cpp` e `RmlUi_Platform_GLFW.cpp` são excluídos e `glfw` não é linkado. Permite que consumidores host SDL3/X11 (ex.: GusWorld) linkem `glintfx::UiLayer` sem arrastar GLFW como dep transitiva. `glintfxConfig.cmake` propaga a opção para que consumidores `find_package(glintfx)` não sejam forçados a ter GLFW instalado em builds embed-only. O `<glintfx/config.hpp>` gerado expõe `GLINTFX_BACKEND_GLFW` (0/1) para guards em tempo de compilação.
- **EN:** CI -- embed-only build gate (`GLINTFX_BACKEND_GLFW=OFF`) added to the CI matrix: validates that the GLFW-free build path stays compilable and `UiLayer` tests pass on every push/PR.
  **PT:** CI -- gate de build embed-only (`GLINTFX_BACKEND_GLFW=OFF`) adicionado à matriz de CI: valida que o caminho de build sem GLFW permanece compilável e os testes do `UiLayer` passam em todo push/PR.

### Fixed / Corrigido

- **EN:** Embed-only build no longer forces GLFW as a transitive dependency of `glintfx::UiLayer` consumers. Prior to v0.2.1, `find_package(glfw3 REQUIRED)` was always called unconditionally, making any consumer of the library implicitly require GLFW even when only `UiLayer` was used.
  **PT:** Build embed-only não força mais GLFW como dep transitiva de consumidores de `glintfx::UiLayer`. Antes da v0.2.1, `find_package(glfw3 REQUIRED)` era sempre chamado incondicionalmente, fazendo qualquer consumidor da lib exigir implicitamente GLFW mesmo quando só `UiLayer` era usado.

---

## [0.2.0] - 2026-06-30

### Added / Adicionado

- **EN:** `glintfx::UiLayer` -- embed / guest mode: attaches the UI+effects engine to a host-owned GL context (no window created); compose-only render (no clear, no swap); GL state automatically saved and restored on return; neutral `UiEvent` input + gamepad navigation ([ADR-0008](docs/adr/0008-embed-guest-mode.md)). First target consumer: GusWorld.
  **PT:** `glintfx::UiLayer` -- embed / guest mode: anexa o motor de UI+efeitos ao contexto GL de um host (sem criar janela); render compose-only (sem clear, sem swap); estado GL salvo e restaurado automaticamente ao retornar; eventos neutros `UiEvent` + navegação por gamepad ([ADR-0008](docs/adr/0008-embed-guest-mode.md)). 1º consumidor-alvo: GusWorld.
- **EN:** `App::ok()`: distinguishes initialization failure from a window closed later; `running()` no longer conflates the two (L1.1-ERRSTRAT).
  **PT:** `App::ok()`: distingue falha de inicialização de janela fechada depois; `running()` não mistura mais os dois (L1.1-ERRSTRAT).
- **EN:** `find_package(glintfx)` support: `glintfxConfig.cmake` is installed alongside the library and headers; RmlUi is co-installed under the same prefix. Enables consumption from an installed tree without FetchContent (L1.8-FINDPKG).
  **PT:** Suporte a `find_package(glintfx)`: `glintfxConfig.cmake` instalado junto da lib e dos headers; RmlUi co-instalado sob o mesmo prefixo. Habilita consumo via árvore instalada sem FetchContent (L1.8-FINDPKG).
- **EN:** Versioned CI -- GitHub Actions (`.github/workflows/ci.yml`) and Codeberg Forgejo Actions (`.forgejo/workflows/ci.yml`) -- runs the full 5-test Xvfb suite on every push/PR (L1.2-CI).
  **PT:** CI versionado -- GitHub Actions (`.github/workflows/ci.yml`) e Codeberg Forgejo Actions (`.forgejo/workflows/ci.yml`) -- executa a suíte completa de 5 testes Xvfb em todo push/PR (L1.2-CI).

### Fixed / Corrigido

- **EN:** `App::snapshot()` PPM orientation: image was vertically flipped (OpenGL reads bottom-up); rows are now reversed before writing (L1.3-SNAPFLIP).
  **PT:** Orientação do PPM em `App::snapshot()`: imagem estava invertida verticalmente (OpenGL lê de baixo pra cima); linhas agora são revertidas antes de escrever (L1.3-SNAPFLIP).
- **EN:** Null / state guards in `window_glfw` and `App`: early returns on uninitialized state prevent silent UB when methods are called out of order or after a failed init (L1.4-GUARDS).
  **PT:** Guards de null/estado em `window_glfw` e `App`: retornos antecipados em estado não-inicializado evitam UB silencioso quando métodos são chamados fora de ordem ou após init falho (L1.4-GUARDS).
- **EN:** `body` fills the viewport in `showcase.rcss`: added `width: 100%; height: 100%;` so the dark background covers the full window (L1.5-DEMOBG).
  **PT:** `body` preenche o viewport em `showcase.rcss`: adicionado `width: 100%; height: 100%;` para o fundo escuro cobrir toda a janela (L1.5-DEMOBG).
- **EN:** `TESTES.md` -- corrected `xvfb-run` vs real GPU in visual validation and pixel-golden sections: headless/CI uses llvmpipe; real GPU validation runs without `xvfb-run` on the desktop DISPLAY (L1.6-TESTDOC).
  **PT:** `TESTES.md` -- corrigido `xvfb-run` vs GPU real nas seções de validação visual e pixel-golden: headless/CI usa llvmpipe; validação em GPU real roda sem `xvfb-run` no DISPLAY do desktop (L1.6-TESTDOC).

### Docs / Documentação

- **EN:** ADR-0008 -- embed / guest mode: documents the `UiLayer` architecture for attaching to a host-owned GL context (game / engine) without owning the window. First consumer: GusWorld / GusEngine (SDL3). See [`docs/adr/0008-embed-guest-mode.md`](docs/adr/0008-embed-guest-mode.md).
  **PT:** ADR-0008 -- embed / guest mode: documenta a arquitetura do `UiLayer` para anexar a um contexto GL de host (jogo / engine) sem ser dono da janela. Primeiro consumidor: GusWorld / GusEngine (SDL3). Ver [`docs/adr/0008-embed-guest-mode.md`](docs/adr/0008-embed-guest-mode.md).
- **EN:** v2 design spec: approved spec for the game UI component library (Atomic Design, tokens-first -- menus, dialogs, windows, fonts, effects). See [`docs/superpowers/specs/2026-06-30-glintfx-v2-design.md`](docs/superpowers/specs/2026-06-30-glintfx-v2-design.md).
  **PT:** Spec de design da v2: spec aprovada da component library de UI de jogo (Atomic Design, tokens-first -- menus, diálogos, janelas, fontes, efeitos). Ver [`docs/superpowers/specs/2026-06-30-glintfx-v2-design.md`](docs/superpowers/specs/2026-06-30-glintfx-v2-design.md).

---

## [0.1.0] - 2026-06-29 · [GitHub](https://github.com/petrinhu/glintfx/releases/tag/v0.1.0)

First release of **glintfx**: a drop-in C++ library for Linux x86-64 that fuses RmlUi 6.3 (HTML/CSS UI) with a GL3 effects renderer. / Primeiro lançamento do **glintfx**: biblioteca C++ drop-in para Linux x86-64 que funde RmlUi 6.3 (UI HTML/CSS) com um renderer de efeitos GL3.

### Added / Adicionado

- **EN:** Public RAII facade `glintfx::App` (move-only, pImpl) with `AppConfig` and `version()`; the only public header is `<glintfx/glintfx.hpp>` and it exposes no third-party types.
  **PT:** Fachada RAII pública `glintfx::App` (move-only, pImpl) com `AppConfig` e `version()`; o único header público é `<glintfx/glintfx.hpp>` e não expõe tipos de terceiros.
- **EN:** Four internal modules: M0 Bootstrap (RmlUi lifecycle), M1 Platform (GLFW window + FreeType font), M2 Render (wraps `RenderInterface_GL3`), M3 Facade.
  **PT:** Quatro módulos internos: M0 Bootstrap (ciclo de vida do RmlUi), M1 Platform (janela GLFW + fonte FreeType), M2 Render (embrulha o `RenderInterface_GL3`), M3 Facade.
- **EN:** Data-driven effects via RCSS: glow / box-shadow, drop-shadow, gradient, backdrop-blur, blur filter, and mask.
  **PT:** Efeitos data-driven via RCSS: glow / box-shadow, drop-shadow, degradê, backdrop-blur, filtro blur e mask.
- **EN:** Drop-in CMake consumption: single target `glintfx::glintfx` via FetchContent; RmlUi 6.3 fetched automatically, gl3w vendored (offline).
  **PT:** Consumo drop-in via CMake: alvo único `glintfx::glintfx` via FetchContent; RmlUi 6.3 baixado automaticamente, gl3w vendorizado (offline).
- **EN:** `glintfx_showcase` demo exercising all five effects, plus a `glintfx_capture` frame-capture tool (`App::snapshot`).
  **PT:** Demo `glintfx_showcase` exercitando os cinco efeitos, mais a ferramenta de captura `glintfx_capture` (`App::snapshot`).
- **EN:** Test suite under Xvfb: `window_smoke`, `render_smoke`, `bootstrap_smoke`, `app_smoke`, and `render_sanity`; opt-in pixel-exact `golden_test` (`-DGLINTFX_GOLDEN_TEST=ON`).
  **PT:** Suíte de testes sob Xvfb: `window_smoke`, `render_smoke`, `bootstrap_smoke`, `app_smoke` e `render_sanity`; `golden_test` pixel-exato opt-in (`-DGLINTFX_GOLDEN_TEST=ON`).
- **EN:** `consumer-example/` proving end-to-end drop-in consumption with no GL/GLFW/RmlUi references.
  **PT:** `consumer-example/` provando o consumo drop-in ponta a ponta sem referências a GL/GLFW/RmlUi.
- **EN:** Documentation: README, AGENTS.md, CONTRIBUTING.md, SECURITY.md, getting-started tutorial, and effects guide; `NOTICE` with third-party attributions.
  **PT:** Documentação: README, AGENTS.md, CONTRIBUTING.md, SECURITY.md, tutorial getting-started e guia de efeitos; `NOTICE` com atribuições de terceiros.

### Fixed / Corrigido

- **EN:** Opaque backbuffer fix for premultiplied alpha, preventing the compositor (Wayland/X) from showing a translucent window.
  **PT:** Fix de backbuffer opaco para alpha premultiplicado, evitando que o compositor (Wayland/X) mostre a janela translúcida.
- **EN:** MSAA disabled on the render layer (`RMLUI_NUM_MSAA_SAMPLES=0`) to avoid a black postprocess texture under Mesa/llvmpipe.
  **PT:** MSAA desabilitado no render layer (`RMLUI_NUM_MSAA_SAMPLES=0`) para evitar textura de pós-processo preta sob Mesa/llvmpipe.

### Changed / Mudado

- **EN:** Project relicensed from AGPL-3.0-or-later to **MPL-2.0** ([ADR-0007](docs/adr/0007-license-mpl-2.0.md)) to enable external adoption.
  **PT:** Projeto relicenciado de AGPL-3.0-or-later para **MPL-2.0** ([ADR-0007](docs/adr/0007-license-mpl-2.0.md)) para viabilizar adoção externa.

### Known limitations / Limitações conhecidas

- **EN:** Linux x86-64 only; one `App` per process; the `mask` effect requires a real GPU (crashes under Mesa/llvmpipe); GLFW only (SDL/X11 not implemented); no versioned CI yet; consumption via FetchContent / `add_subdirectory` only (full `find_package` is post-v1).
  **PT:** Apenas Linux x86-64; um `App` por processo; o efeito `mask` exige GPU real (crasha sob Mesa/llvmpipe); apenas GLFW (SDL/X11 não implementados); sem CI versionado ainda; consumo só via FetchContent / `add_subdirectory` (`find_package` completo é pós-v1).

> **Note / Nota:** Layer 0 (`loucura_c_asm`, the pure C/ASM runtime) is not part of this release; it is an independent experimental track with its own future tag. / A Camada 0 (`loucura_c_asm`, o runtime C/ASM puro) não faz parte deste lançamento; é uma trilha experimental independente com tag futura própria.

[0.1.0]: https://codeberg.org/petrinhu/glintfx/releases/tag/v0.1.0
