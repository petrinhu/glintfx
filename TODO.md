# TODO — loucura_c_asm

Tabela de pendências e planejamento. **As linhas estão na ordem de execução recomendada** (topological + WSJF, minimiza retrabalho). A coluna **Onda** marca passos paralelizáveis (mesma onda = sem dependência mútua, podem rodar juntos). Porte: early com eixo técnico elevado (cenário B bigtech), cerimônia leve.

> Convenção de frescor: implementação entregue → `🔍 Pendente verificação` (nunca `✅` direto). `✅ Concluído` só após a onda `TST-*`/`AUD-*` correspondente. Citar o ID do item na mensagem do commit. Trabalho novo no meio do caminho → anexar na INBOX (fim do arquivo), não reordenar na hora.

## Ondas de prioridade — roadmap ativo (2026-07-09)

Priorização de EXECUÇÃO cruzando as duas trilhas (Camada 0 + glintfx), emitida pelo Cósimo/CoS após os marcos desta rodada: `core-v0.1.0` taggeada, glintfx `v0.7.0` lançada e **`L1-ADR9` ✅ fechado** (Opção A-faseada — destravou `L1.14-GLLOADER` e a trilha de internalização). Método: topological sort + WSJF + ondas de prioridade. Mesma onda = paralelizável (superfícies de arquivo disjuntas; serializar agents que colidirem no mesmo arquivo). A coluna `Onda` das tabelas abaixo carrega o marcador `P*` correspondente.

- **P1 — atacar AGORA (3 frentes paralelas):** `SOV-ALLOC` ‖ `L1.14-GLLOADER` ‖ `L1.17-FOCUS`. Racional: `SOV-ALLOC` é o hard-blocker de maior lead time da internalização (FreeType/RmlUi) E tem valor próprio na Camada 0 — regra do long pole: o blocker de job grande começa primeiro; `L1.14` acabou de ser destravado pelo ADR-0009, quase não toca heap (NÃO depende de `SOV-ALLOC`) e prova o processo clean-room ponta a ponta na menor peça; `L1.17` é o quick win de maior WSJF (dificuldade Baixa, promessa pública do README). As 3 frentes não compartilham arquivo (alocador C0 / gerador+CMake / `ui_layer`+`app`).
- **P2 — na sequência (conforme frente de P1 liberar):** `SOV-FCONV` ‖ `L1.16-DOMRW` ‖ `L1.15-FORMEV` (só a fase de DESIGN 🎨 — a superfície de callback exige design aprovado ANTES de codar). `SOV-FCONV` independe de `SOV-ALLOC` (arquivos disjuntos na Camada 0); ficou em P2 por limite de WIP (3 frentes), não por dependência.
- **P3 — fecho da generalização:** implementação de `L1.15-FORMEV` (pós-design aprovado) ‖ `L1.18-COVCI` (cobertura mede mais valor DEPOIS que as features LW10 pousarem; toca os 2 workflows de CI — validar com `forgejo-runner exec` e NÃO paralelizar com outro item que toque CI).
- **P4 — pós-tudo:** `DOC-WIKI` (doc pós-tag, item fixo de fim-de-tabela; ganha conteúdo com o roadmap pousado; execução via `technical-writer`).

> **✅ ESTADO 2026-07-16 (pós-frescor) — glintfx `v0.11.1`, Camada 0 `core-v0.3.0`, estado redondo.** As ondas P1–P4 seguem 100% `✅`. **7 releases desde o reorder de 2026-07-11**, todas consumer-driven ou capability de catálogo, CI dual verde: **`v0.9.2`** (blocker Windows do GusWorld — codepath `#ifdef _WIN32` no loader GL gerado, `wglGetProcAddress`+`opengl32.dll`, cross-compile MinGW validado em container efêmero host-limpo); **`v0.10.0`** (flip suave `L1.20-FONTFLIP` — motor de fonte próprio clean-room vira DEFAULT, FreeType selecionável em runtime SEM drop via `FontEngine font_engine`, ADR-0011; precedido por pen-snap/vsnap/lazy-bake/fallback-por-glyph/kern-O(log n) e benchmark de perf); **`v0.11.0`** (backdrop-ripple `L1.22-WAVE`/`L1.22-CAPTURE` — captura de FBO0 + `decorator: ripple(...)` que refrata a UI própria do glintfx, custo-zero quando inativo via arm/ensure, ADR-0012; capability de catálogo, sem demanda de consumidor); **`v0.11.1`** (hardening de DoS de asset — teto de 256 MiB em `BaseUrlFileInterface::Open()` cobrindo imagem+fonte, da onda de auditoria formal **`LW-AUD`**: 4 pareceres `AUD-L1-*` CONFORME + 0 crítico, 2 IMPORTANTE achados-e-fechados por review adversarial que REPRODUZIU o DoS; + `-Wall -Wextra` no alvo glintfx + novo `RELEASE_CHECKLIST.md` + índice do dossiê `docs/auditoria/README.md`; drop-in). **Sprite-eco (`L1.21-ECHO`)** entregue como receituário RCSS puro (zero feature de código). **Consultas do GusWorld fechadas:** screen-wave = host-side (entregue como GLSL de referência); ancoragem de HUD do action-clock = **host-drawn** (parecer medido, GusWorld confirmou, zero demanda pro glintfx). Estado redondo, sem trabalho ativo com gatilho. Demais frentes, TODAS pull/keep-back sem gatilho: LW-TST (pull; **LW-AUD ✅ FECHADA 2026-07-16 na v0.11.1**), `L1.7-BACKENDS`/`L2-COMPONENTS` (keep-back), internalização endgame (RenderInterface próprio → RmlUi core), sementes da INBOX (text-input GusWorld no gatilho SAVE-NOMEAR, `%g` no printf, drop do FreeType, `SOV-MATH`/`SOV-SYS2`). **Watch de drop:** quando o #968 merjar → bumpar o pin do RmlUi + remover o `PATCH_COMMAND`/patch + avisar o GusWorld. **Pull vivo:** piloto do UI Kit 10-card quando o GusWorld começar o porte (RML); adoção da v0.10.0/v0.11.0 pós-M8 deles.

**Fora das ondas (explícito, continua pull/keep-back — NÃO priorizado):** `TST-L1-DECODE`/`GLLEAK`/`CONTRACT`/`PRECI` + `AUD-L1-*` (LW-TST/LW-AUD, pull por achado real), `L1.7-BACKENDS` (LW7 keep-back), `L2-COMPONENTS` (v2 pull incremental, branch parada), FreeType/RmlUi (`AUD-TRANS-2/4` — gated por `SOV-ALLOC` + piloto `L1.14`) e todas as sementes da INBOX (`AUD-PUB-2`, unload/hide/show, `load_font`, cursor/clipboard, saturação do `image-tint`, `SOV-CLOCK`/`SOV-MATH`/`SOV-SYS2`/`SOV-CXXRT`).

### Scoring WSJF (escala 1–10 · CoD = Valor + Criticidade + Redução de Risco/OE · WSJF = CoD ÷ Job Size)

| ID | Valor | Criticidade | Red. Risco/OE | CoD | Job Size | WSJF | Rank | Onda |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :--- |
| L1.17-FOCUS | 6 | 4 | 3 | 13 | 2 | 6.5 | 1 | P1 |
| L1.14-GLLOADER | 7 | 6 | 8 | 21 | 5 | 4.2 | 2 | P1 |
| SOV-FCONV | 6 | 4 | 6 | 16 | 5 | 3.2 | 3 | P2 |
| SOV-ALLOC | 8 | 7 | 9 | 24 | 8 | 3.0 | 4 | **P1** (ajuste topológico) |
| L1.18-COVCI | 5 | 3 | 5 | 13 | 5 | 2.6 | 5 | P3 |
| L1.16-DOMRW | 6 | 3 | 3 | 12 | 5 | 2.4 | 6 | P2 |
| L1.15-FORMEV | 6 | 3 | 4 | 13 | 6 | 2.2 | 7 | P2 (design) → P3 (impl) |
| DOC-WIKI | 4 | 2 | 1 | 7 | 5 | 1.4 | 8 | P4 |

> Ajuste topológico sobre o WSJF cru (registrado): `SOV-ALLOC` sobe pra P1 apesar do rank 4 — WSJF penaliza job grande, mas ele é o hard-blocker de maior lead time da trilha de internalização (long pole primeiro); `SOV-FCONV` desce pra P2 SÓ por limite de WIP, não por dependência. O restante segue o rank.

## Camada 1 — C++23 (RmlUi + GL3) — ver ADR-0006

Trilha da biblioteca C++23 (compat C++17→23) que une RmlUi (UI) + renderer GL3 (efeitos). Independente da Camada 0 (W0–W14 acima). O detalhamento de **features** depende do brainstorm bigtech (`L1-BRAINSTORM`). Ondas `LW*` são internas a esta trilha.

> **Estado:** v1 ENTREGUE e mergeada (glintfx `v0.1.0` → `v0.4.0`, 10 tags) — ondas LW1–LW4 ✅. **LW5 e LW6 FECHADAS, trio CI-now da LW-TST FECHADO** (2026-07-02): CI verde nos 2 remotos com job novo `lint-and-scan` (estático+secrets+deps, 18/8 testes de build+test) + `find_package` verificado ponta-a-ponta + `L1.1-ERRSTRAT` ratificado (`bool ok()` definitivo) + `version()` exposta no embed-only + `UiEvent::Type::None`. **LW9 ✅ FECHADA** (2026-07-02, `v0.2.5`, review final `internal-auditor` APROVA MERGE): 3 pedidos reais do GusWorld -- `set_click_callback` (`L1.11-CLICKCB`), `get_element_box` (`L1.12-ELEMBOX`), `set_viewport(x,y,w,h,target_h)` (`L1.13-VPORIGIN`, ativa o `GAP-2`/origem) -- suíte agora **23 testes GLFW=ON / 11 embed GLFW=OFF** (5 novos: 3 embed + 2 App-only). **v0.3.0 ✅ LANÇADA** (2026-07-04, CI verde nos 2 remotos): decorator RCSS `polygon(<sides>,<color>[,<rotation>])` (1ª shape primitive, consumer-driven pelo nó hexagonal do GusWorld; fail-high `sides∈[3,1024]`) + **hardening de superfície de entrada da API** (achados de auditoria: Critical `load(nullptr)` que abortava o processo host + `set_dp_ratio`/`set_viewport` rejeitam inválido + `version()` corrigido — estava travado em "0.2.4" desde v0.2.5); suíte **26 GLFW=ON / 13 embed**, ASan/UBSan limpo. **v0.3.1 ✅ LANÇADA** (2026-07-04): `polygon()` ganha preenchimento em gradiente radial/linear (reusa o shader real do RmlUi, isotrópico, consumer-driven pelo nó "cabeça de parafuso" do GusWorld) + 2 fixes de segurança de memória achados via auditoria dominó (leak/fantasma no `load()` que não fechava o documento anterior; heap-use-after-free no `bind_*` com chave duplicada, capturado ao vivo pelo ASan) — suíte **29 GLFW=ON / 15 embed**. **v0.4.0 ✅ LANÇADA** (2026-07-07, `GLINTFX-SCROLL-1`): rolagem em embed mode — wheel forwarding (`UiEvent::Type::MouseWheel`, rola o ancestral rolável do elemento em HOVER não foco, confirmado comportalmente por review adversarial) + 5 métodos de scroll programático (`scroll_element_into_view` + trio de métricas `scroll_top`/`scroll_height`/`client_height`) em `UiLayer`+`App`; consumer-driven pela lista de menu não-rolável do GusWorld; achado do review (gap de mutation-testing no `align_with_top`) fechado antes da tag — suíte **31 GLFW=ON / 16 embed**, ASan/UBSan/LSan limpo. **v0.4.1 ✅ LANÇADA** (2026-07-07, CI GitHub verde): patch de robustez/hardening da auditoria de distribuição pública — 16 achados via workflow de 3 agentes Sonnet + review adversarial + ASan (robustez `AUD-TEC-1..5` incl. Critical UAF de reentrância provado sob ASan; docs/licença; version-drift), **nenhuma API mudou (drop-in)**; ctest 31/31 + 16/16 embed. **v0.5.0 ✅ LANÇADA** (2026-07-07): generalização de API pós-auditoria — `enum Key` completo (+Delete/Home/End/PageUp/PageDown), `set_click_info_callback`/`ClickInfo` (`button` funcional pros 3 botões; `double_click` esquerdo-apenas), `App` refaz layout em resize (render+snapshot); CI agora builda com clang. Minor/drop-in, nenhuma API mudou. Suíte **32 GLFW=ON / 16 embed**, review adversarial sem achados. **v0.6.0 ✅ LANÇADA** (2026-07-08): `set_scroll_callback(id)` (host toca som de rolagem; dispara no `EventId::Scroll` nativo — wheel/scrollbar/programático; 2 pegadinhas documentadas: multi-fire no smoothscroll, recursão não-convergente) + **barra de rolagem customizável 100% via RCSS** (defaults sãos na UA-stylesheet; doc em `docs/effects.md`) + cobertura embed do data-model (`AUD-TEC-7`). Minor/drop-in. Suíte **33 GLFW=ON / 17 embed**, review adversarial (2 Minor de doc fechados). `AUD-MEM` (local+global) resolvido. `AUD-C0-2/4` ✅ RESOLVIDOS (2026-07-09): poda de `syscall0/2/4/5` código-morto (`b164cf2`) + adendo YAGNI ao ADR-0002 sobre helpers `IS_ERR`/`ERR_CODE` nunca implementados (`c4237cd`); review adversarial `tech-lead` APROVOU (build limpo, 15 PASS/0 FAIL, reshuffle `syscall1/3/6` intacto incl. `r10←rcx`, Status/Decisão do ADR byte-a-byte). Decisão restante na INBOX: ADR-0009. **v0.7.0 ✅ LANÇADA** (2026-07-09): decorator `image-tint()` com **luminance-key** (recolore só claros/neutros, preserva saturados/escuros) — consumer-driven GusWorld (recolorir 1 base de moldura em N domínios, 1 base → 6 cores); props `image-tint-color`/`-mode`/`-threshold` (interpoláveis, `var()`); shader custom `glintfx-tint` injetado no `Gl3RenderInterface` **SEM patch do RmlUi core** (ADR-0010); sem gamma (consistência); hardening (threshold clamp `[0,0.999]` evita UB do smoothstep). Fidelidade de saturados aproximada (`(1-sat)` linear), tunável por threshold — semente de follow-up (`(1-sat)^k`/sat-threshold) no INBOX. Minor/drop-in. Suíte **34 GLFW=ON / 18 embed**, 2 reviews adversariais (estado GL + premultiply verificados contra o RmlUi real). **v0.8.0 ✅ LANÇADA** (2026-07-09): loader GL 3.3 core-profile próprio **clean-room** (`L1.14-GLLOADER`, `glintfx/src/gl_loader.{h,c}`, 344 comandos, gerado por `tools/gen_glloader.py` do registro público Khronos `gl.xml`; `gl3w` vendorizado eliminado; 1º marco da trilha de internalização) + `set_focus(id)`/`clear_focus()` programáticos em `App`/`UiLayer` (`L1.17-FOCUS`, fecha `GAP-4`; `Focus()` gated só por RCSS `focus:auto|none`, confirmado no source pinado do RmlUi 6.3). Minor (adição de API pública)/drop-in, nenhuma assinatura existente mudou. Suíte **36 GLFW=ON / 20 embed**, 2 reviews adversariais (`backend-engineer` no loader, `tech-lead` no focus, ambos APROVARAM). **v0.9.0 ✅ LANÇADA** (2026-07-10): DOM read/write por id + read-back do data-model (`L1.16-DOMRW`: `set_text`/`add_class`/`remove_class`/`set_property(id,...)` + `get_number/string/bool(key,out)`, `set_text` escapa RML via `EncodeRml` anti-injeção, fecha `AUD-PUB-6(d)+(e)`) + 5 callbacks de evento observável form/DOM (`L1.15-FORMEV`: `set_change/submit/focus/blur/hover_callback` em `App`/`UiLayer`, fecha `AUD-PUB-6(c)`; achados empíricos de fan-out de foco/hover documentados e corrigidos antes do merge) + cobertura de código self-hosted no CI (`L1.18-COVCI`: `GLINTFX_COVERAGE` via llvm-cov por-alvo, `tools/coverage_report.sh` gêmeo local, gate `--floor 75` nos 2 remotos, badge README sem uploader de terceiro). Minor (só adição de API pública)/drop-in, nenhuma assinatura existente mudou. Suíte **38 GLFW=ON / 22 embed**, 3 reviews adversariais (`tech-lead` em DOMRW e FORMEV, `qa-engineer` em COVCI, todas APROVARAM). Pendências pós-v1: **LW-TST/LW-AUD restante** = `TST-L1-DECODE/GLLEAK/CONTRACT/PRECI` + `AUD-L1-*` (5 itens), pull-based, sem gatilho de urgência hoje (`TST-L1-MEM` fechado 2026-07-02 com nightly ASan/UBSan/LSan; 2 achados do `AUD-L1-PARSE` já corrigidos adiantados); **LW7** = backends adiados; **LW8** = internalização clean-room (endgame).
>
> **Ordem recomendada (Kanban, finish-before-start):** LW5/LW6/trio-CI/`TST-L1-MEM` fechados; LW9 ✅ fechada (v0.2.5, `internal-auditor` aprovou merge — ver l.53 e tabela). Próxima frente é **pull-based**: aguardar o próximo achado do GusWorld que justifique `TST-L1-DECODE`/`GLLEAK`/`CONTRACT`, ou puxar `AUD-L1-MEM` (agora desbloqueado -- consome o run sanitizado do `TST-L1-MEM`). **LW7/LW8 = keep-back.** A **v2** roda em modo **pull incremental** (backlog, não agendada — ver seção v2). **Reroteamento do líder (2026-07-09, via Cósimo/CoS): 8 features abertas como roadmap ATIVO** (exceção deliberada ao modo pull, decisão registrada): `SOV-ALLOC`/`SOV-FCONV` (W15, Camada 0) + `L1-ADR9`→`L1.14-GLLOADER` (LW8, gate + 1º marco da internalização) + `L1.15-FORMEV`/`L1.16-DOMRW`/`L1.17-FOCUS`/`L1.18-COVCI` (LW10, generalização de API + infra). Ordem sugerida: **L1-ADR9 primeiro** (decisão barata que destrava a trilha inteira) → LW10 ‖ W15 em paralelo → L1.14 pós-ADR. Anti-OE: 8 de teto 9 — sementes NÃO promovidas (seguem pull): `AUD-PUB-2` (`App::process_event`), unload/hide/show, múltiplos documentos, `load_font` runtime, cursor/clipboard callbacks, saturação dura do `image-tint`, `SOV-CLOCK` (relógio sem consumidor real hoje). **→ Atualização 2026-07-09 (pós `L1-ADR9` ✅): a priorização de EXECUÇÃO vigente é a seção "Ondas de prioridade — roadmap ativo" no TOPO deste arquivo (P1–P4 + scoring WSJF); a coluna `Onda` das tabelas carrega os marcadores `P*`.**

## Framework 2D — batteries-included game framework — ver ADR-0015

> Reposicionamento da glintfx de "camada de UI embutível" para framework de jogos 2D batteries-included (janela, loop, input, áudio, imagem, texto, tempo) mais o diferencial UI HTML/CSS declarativa com efeitos GL data-driven. Arquitetura atomizada por subsistema (módulos `GLINTFX_MODULE_*`, um por capacidade), decisão do líder 2026-07-19. Doc de visão: `docs/superpowers/plans/2026-07-19-framework-2d-vision.md`. Sequência confirmada: **A0** (esqueleto modular de build, sem API nova) → **A1** (módulo input no `App` standalone) → demais fases rankeadas por "mais fácil + melhor ROI primeiro" (ADR-0015 (g)).

## v2 — Component Library (Atomic Design) — SPEC APROVADO — modo PULL INCREMENTAL (não agendada)

> **Decisão do líder (2026-06-29):** o glintfx será **distribuído e reusado em vários projetos** ("todas as features") → vira **produto distribuível**, o que justifica uma **component library / design system** (Atomic Design) sobre o engine da v1. Sequência decidida: **fechar a v1 primeiro**; a v2 é a próxima fase.
>
> **Escopo/breakdown a definir pela bigtech (não autorar inline):** inventário de componentes, tokens, variantes de efeito, faseamento e modelo de distribuição da v2 serão definidos em **brainstorm próprio** por **Capitolino/CPO** (produto) + **ux-ui-designer** (design system) + frontend/qa, com spec e plano próprios — **só quando a v2 iniciar**. Avaliação inicial em `AtomicEval` (ux-ui-designer): tokens-first, faseado por demanda, anti-OE.
>
> **Reroteamento do líder (2026-07-01, via `/bigtech`):** modo **pull incremental** — a F2 NÃO é disparada agora como projeto de 7 tarefas. As 4 releases mais úteis da F1 (v0.2.1–v0.2.4) nasceram de pull real do GusWorld sobre o *engine*; nenhuma exigiu component library, e o cockpit já funciona ponta-a-ponta sem ela. F2 vira **backlog puxado por demanda**: só inicia com um achado real (2º consumidor, ou pedido concreto do GusWorld) que a exija. Branch `feat/v2-f2-components` **parada, não abandonada** — plano pronto pra retomar quando o pull aparecer. Detalhe: evento do vault `Journal/eventos/2026-07-01-glintfx-reroteamento-lean.md` e `.bigtech-porte`.

## Legenda

- **Onda**: leva de execução. Mesma onda = paralelizável. `‖` itens independentes.
- **OWD** (one-way-door): decisão de reverter caro → no topo, exige o líder.
- **Status**: ✅ Concluído · 🔄 Em andamento · 🟡 Parcial · ⏳ Pendente · 💡 Decisão tomada · 🎨 Pendente design · 🔍 Pendente verificação.
- **Estado Auditado**: `—` não auditado · `✓` aprovado · `⚠` com ressalvas.

## Caminho crítico

`A1/A3 → B3 → B4 → B5 → B6 → C1 → D1 → D2 → D3 → E2` (E1 pendurado em A4 + D1). Tudo a jusante depende de fechar o **W0 (as 5 decisões de arquitetura)**.

## Tabela de pendências (consolidada)

> Consolidação mecânica (2026-07-23): as antigas 4 tabelas separadas (Camada 0, Camada 1/glintfx, Framework 2D, v2) foram unidas numa tabela única abaixo, na ordem de execução de cada trilha (Camada 0 → glintfx → Framework 2D → v2) -- sem reordenar por WSJF. Motivo: o parser de sincronização (`~/.claude/githooks/todo_lib.py`) só enxerga a 1a tabela do arquivo (para no 2o cabeçalho `| ID |`); com 4 tabelas, 67 dos 99 itens (68%) ficavam invisíveis ao `todo_sync.py`/`todo_health.py`. A coluna **Grupo** carrega a subcategoria de cada item (ex.: `C++23/API`, `v2/Integração`); onde a linha de origem não tinha valor, foi preenchida com o nome da trilha de origem.

| ID | Onda | Grupo | Descrição Técnica | Prioridade | Pré-requisito | Dificuldade | Status | Estado Auditado |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| A1 | W0 | Arquitetura | ADR — camada de syscall: wrappers genéricos `syscall0..6`. Decidido → [ADR-0001](docs/adr/0001-syscall-layer-style.md). **OWD** | Alta | — | Baixa | 💡 Decisão tomada | — |
| A2 | W0 | Arquitetura | ADR — contrato de erro: retorno `-errno` cru. Decidido → [ADR-0002](docs/adr/0002-error-contract.md). **OWD** | Alta | — | Baixa | 💡 Decisão tomada | — |
| A3 | W0 | Arquitetura | ADR — ABI interna: System V AMD64 estrita; `_start` alinha stack 16B. Decidido → [ADR-0003](docs/adr/0003-internal-abi.md). **OWD** | Alta | — | Baixa | 💡 Decisão tomada | — |
| A4 | W0 | Arquitetura | ADR — alocador: bump sobre `mmap`, assinatura `malloc/free/realloc` final. Decidido → [ADR-0004](docs/adr/0004-allocator-strategy.md). **OWD** | Alta | — | Média | 💡 Decisão tomada | — |
| A5 | W0 | Arquitetura | ADR — layout ELF: estático no-PIE, entry `_start`. Decidido → [ADR-0005](docs/adr/0005-elf-layout.md). **OWD** | Alta | — | Baixa | 💡 Decisão tomada | — |
| A6 | W1 | Build | Build system (`Makefile`): regras `clang -ffreestanding -nostdlib` + `nasm -f elf64` + `ld -nostdlib -static -no-pie -e _start`; alvos build/test/clean/run | Alta | A5 | Média | ✅ Concluído | ✓ |
| B1 | W1 | Bootstrap | `types.h` próprio (sem stdint/stddef): `size_t`, `ssize_t`, `uintptr_t`, `NULL` (`bool`/`true`/`false` = keywords C23, deliberadamente NÃO redefinidos) | Alta | — | Baixa | ✅ Concluído | ✓ |
| B2 | W1 | Bootstrap | Constantes de syscall (números) e flags (open/mmap/etc.) para x86-64 | Alta | — | Baixa | ✅ Concluído | ✓ |
| STD-SPDX | W1 | Convenções | Header SPDX `SPDX-License-Identifier: MPL-2.0` + copyright no topo de TODO arquivo de código (C `//`, NASM `;`, Makefile `#`), aplicado desde a criação (DoD herdado). Definir snippet padrão no `CLAUDE.md` | Média | — | Baixa | ✅ Concluído | ✓ |
| B3 | W2 | Bootstrap | Camada de syscall: wrappers `syscall0..6` em ASM/inline (rax + rdi/rsi/rdx/r10/r8/r9) | Alta | A1, A3, B2 | Média | ✅ Concluído | ✓ |
| B4 | W3 | Bootstrap | `_start` em ASM: alinhar stack, ler argc/argv/envp do stack inicial, chamar `main`, capturar retorno. **Fundação** | Alta | B3, A3, A5 | Alta | ✅ Concluído | ✓ |
| B5 | W4 | Bootstrap | `sys_exit` + binário mínimo `exit(42)`. **GATE: valida pipeline build→link→run** | Alta | B4, A6 | Baixa | ✅ Concluído | ✓ |
| B6 | W5 | Bootstrap | `sys_write` + "hello world" 100% puro (valida saída por syscall) | Alta | B5 | Baixa | ✅ Concluído | ✓ |
| B7 | W6 | Bootstrap | `sys_read` (entrada) | Média | B6 | Baixa | ✅ Concluído | ✓ |
| C1 | W6 | Testes | Harness de teste próprio (sem libc): `assert` próprio + runner que reporta via `write` e exit code (habilita TDD do resto) | Alta | B6, B1 | Média | ✅ Concluído | ✓ |
| D1 | W7 | Libc-Núcleo | Primitivas de memória: `memcpy/memset/memmove/memcmp` (atenção: `-fno-builtin` p/ clang não reintroduzir). Follow-up de teste (ex-INBOX, review D1): add `memmove(dst==src)`/`memmove(n==0)` ao `test_mem.c` no próximo toque | Alta | C1 | Média | ✅ Concluído | ✓ |
| D2 | W8 | Libc-Núcleo | Operações de string: `strlen/strcmp/strncmp/strcpy/strncpy/strcat/strchr` | Alta | D1 | Média | ✅ Concluído | ✓ |
| D3 | W9 | Libc-Núcleo | Conversões `int↔string`: `itoa/utoa` (base 10 e 16), `atoi/atou` | Alta | D2 | Média | ✅ Concluído | ✓ |
| E1 | W10 | Libc-Avançada | Alocador próprio `malloc/free/realloc` via `mmap`/`brk` (bump → free-list). **OWD** | Alta | A4, D1 | Alta | ✅ Concluído | ✓ |
| E2 | W10 | Libc-Avançada | `mini-printf` sobre `sys_write`: `%d %u %x %s %c %%`. **Ao iniciar (ex-INBOX):** fechar o gap de teste da fronteira de saturação de `atoi`/`atou` do D3 -- add `atoi("2147483640")`→2147483640 e `atou("4294967290")` ao `test_conv.c` (código correto, falta o teste que trava contra regressão `>`→`>=`) | Alta | D3 | Média | ✅ Concluído | ✓ |
| TST-INT | W11 | Testes | Testes de integração/E2E: builda e roda cada binário/demo, valida exit code e stdout. **Entregue W11** (`03e90a0`, review adversarial embedded com dente): `make test-negative` (sonda `negative_probe` versionada prova `TEST_ASSERT`→`FAIL:file:line`); golden do flush >256B do `mini_printf` confirmado em `printf_e2e` (321B, ≥2 flushes). Ver `TESTES.md` | Alta | E1, E2 | Média | ✅ Concluído | ✓ |
| TST-STATIC | W11 | Testes | Análise estática (clang static analyzer / `scan-build` / cppcheck freestanding) + presença do header SPDX (STD-SPDX) em todo arquivo. **Sub-tarefas (ex-INBOX):** (a) grep-diff dos pares `#define`/`%define` de `syscall_nums.h`↔`.inc` -- já são 3 constantes (`exit`/`write`/`read`), drift real possível; (b) endurecer `check_spdx.sh` p/ não cair "alto" (`set -eu` + `find`) se uma pasta escaneada for deletada. **Entregue W11** (`a4c3b91`): `make check-static` = cppcheck + `clang --analyze` (0 findings) + `check_spdx` endurecido (falha em vacuidade, testado) + `check_syscall_nums_sync` (gate de drift, testado RED). 1 fix real (`mmap_failed`→`const void*`). Ver `TESTES.md` | Média | D3, E2, STD-SPDX | Baixa | ✅ Concluído | ✓ |
| TST-MEM | W11 | Testes | Validação de memória do alocador: leak, double-free, alinhamento (testes dedicados + valgrind no estático se viável). **Entregue W11** (`bdadc91`): `make test-mem` (valgrind roda no freestanding; limitação documentada -- 0 hooks de libc no bump-`mmap`, cobertura real = asserts de alinhamento/não-sobreposição em `test_alloc`; sonda de corrupção confirmou que valgrind pega "Invalid write"); `malloc((size_t)-1)==NULL` + realloc shrink já cobertos. Ver `TESTES.md` | Alta | E1 | Média | ✅ Concluído | ✓ |
| F1 | W12 | Infra | CI freestanding (Woodpecker LOCAL preferido, ou Forgejo Actions): build + roda toda a suíte de testes. **Entregue** (`5d66c8e`): decisão do líder = Forgejo Actions (Codeberg) + espelho GitHub Actions, workflow dedicado `core-ci.yml` em ambos (`.github/workflows/`, `.forgejo/workflows/`), path-filtered (`src/**`, `include/**`, `tests/**`, `tools/**`, `Makefile`), separado dos workflows do glintfx. Gate duro `make build → test → check-static → test-negative → test-mem`. Validado com `forgejo-runner exec` (job completo verde no container `catthehacker/ubuntu:act-latest`, não só rodado local em shell). **Verificado no runner real** (`eb3c854`, 2026-07-09): GitHub `core-ci` **verde**; Codeberg `core-ci` (run #19) em `waiting` (runner `codeberg-medium`/pool -- padrão conhecido REINCIDENTE da v0.3.1, infra do Codeberg e não do workflow; o `forgejo-runner exec` local no MESMO container `catthehacker` já provara o job verde). | Alta | TST-INT, TST-STATIC, TST-MEM | Média | ✅ Concluído | ✓ |
| AUD-ABI | W12 | Auditoria | Auditoria de conformidade ABI/ASM: convenção de chamada, preservação de registradores callee-saved, alinhamento de stack 16B. **Entregue W12** (`bd2d537`, `embedded-firmware-engineer`): veredito **CONFORME** — 0 CRÍTICO/IMPORTANTE, 1 COSMÉTICO (comentário `syscall.asm:33`). Verificado contra `objdump`/`readelf` do binário REAL (reshuffle `r10←r8` write-before-clobber, callee-saved intactos, `rsp%16==8` na entrada de `main`, ELF EXEC não-PIE sem `PT_INTERP`/libc). Relatório `docs/auditoria/AUD-ABI.md`. Ver `AUDITORIAS.md` | Alta | B3, B4, D1, TST-INT | Média | ✅ Concluído | ✓ |
| AUD-SEC | W12 | Auditoria | Auditoria de segurança: validação de args de syscall, bounds em mem/string, overflow inteiro em conv/alloc, ausência de UB. **Entregue W12** (`402e544`, `security-engineer`): veredito **CONFORME** — 0 CRÍTICO/IMPORTANTE, 3 COSMÉTICOS (trade-offs deliberados: short-write ADR-0002, `str`/`conv` sem NULL-guard = contrato libc "warts included", varargs format-string). 51 checagens adversariais sob ASan+UBSan (harness descartável fora do repo); overflow guard do `malloc` re-verificado (`SIZE_MAX-31` aceito, `-30` rejeitado); `memmove`/`strncpy`/`itoa(INT_MIN)`/saturação de `atoi` todos limpos. Relatório `docs/auditoria/AUD-SEC.md`. Ver `AUDITORIAS.md` | Alta | D3, E1, TST-INT | Média | ✅ Concluído | ✓ |
| REL-TAG | W13 | Release | Tag de versão **`core-v0.1.0`** (DISTINTA do glintfx `v0.1.0`): primeiro runtime funcional + libc própria mínima. **✅ TAGGEADA 2026-07-09** (anotada, obj `09865be` → commit `a21d6a1`, idêntica nos 2 remotos; notas bilíngues descrevendo bootstrap→libc-núcleo→printf→alocador). Todos A6→E2 promovidos a `✅` (TST-* W11 + AUD-ABI/SEC W12 CONFORME + core-ci verde). | Média | F1, AUD-ABI, AUD-SEC | Baixa | ✅ Concluído | ✓ |
| SOV-ALLOC | W15 ‖ · **P1** | Libc-Avançada | **Feature aberta 2026-07-09 (roadmap ativo, Cósimo/CoS):** evoluir o alocador E1 pra **free-list/segregated-fit com `free` real** sobre `mmap` (+ `munmap` de arena vazia se couber — exigiria wrapper novo), mantendo a assinatura POSIX fixada (ADR-0004 one-way-door — só a estratégia INTERNA muda, evolução já prevista no próprio ADR). Valor próprio independente da internalização (workload persistente deixa de ser leak-by-design); pré-requisito de AUD-TRANS-2/4. TDD + `make test-mem` estendido (coalescência, reuso de bloco, alinhamento 16B preservado). Consome semente AUD-TRANS-0 ([`AUDIT_FIND.md` §5.2](AUDIT_FIND.md)). **Implementado 2026-07-09** (`embedded-firmware-engineer`): free-list ÚNICA first-fit (trade-off vs. segregada documentado no cabeçalho de `src/alloc.c`) + boundary tags (header 16B + footer 8B) para coalescência bidirecional + split; `arena_desc_t` embutido no início de cada `mmap` (limites `[base,end)`, sem cruzar arena na coalescência); `MIN_BLOCK_TOTAL`=48B garante espaço pro `free_link_t` (prev/next) sobreposto ao payload de blocos livres; alinhamento 16B preservado em blocos REUSADOS (`_Static_assert`s de invariante). **Stretch `munmap` de arena vazia ENTREGUE**: `syscall2` readicionada (ADR-0001 previa isso), `sys_munmap`/`include/sys_munmap.h` novos, `SYS_munmap`=11 só no `.h` (mesma regra do `SYS_mmap`, sem NASM chamador direto) — confirmado via `strace` (munmap real disparado 2x na suíte nova). Double-free: assert barato de flag + `sys_exit(111)` quando o header ainda está mapeado; documentado em `include/alloc.h` que pode virar `SIGSEGV` se a arena-dona já tinha sido `munmap`ada (achado ao vivo via probe adversarial + gdb, não assumido). TDD RED→GREEN: `tests/test_alloc_free.c` novo (reuso, coalescência com vizinho vivo intocado, split sem desperdício, churn 8 blocos padrões únicos, alinhamento pós-reuso) + `tests/test_alloc.c` teve 1 subteste morto (tocar memória pós-free) ATUALIZADO conscientemente (comentário bilíngue, causa-raiz documentada — reproduziu corrupção real de free-list antes do fix). `make build && make test && make check-static && make test-mem` verdes; zero libc confirmado via `strace` (só mmap/munmap/write/execve). **Review adversarial `security-engineer` pegou 1 CRÍTICO** (integer-overflow em `arena_mmap_size_for`: `ARENA_DESC_SIZE + wanted_total` estourava `size_t` p/ tamanhos que o guard do `malloc` deixava passar → `free_list_remove(NULL)` → SIGSEGV; reproduzido ao vivo) — **CORRIGIDO** (`216ce7d`: guard do `malloc` estendido + defense-in-depth no 2º find-fit + teste de regressão `malloc(SIZE_MAX-39)==NULL`); resto **APROVADO** (double-free honesto, alinhamento em 20k churn, zero-libc, munmap seguro). Re-verificado por sonda independente (NULL-OK). | Média | REL-TAG, A4 | Alta | ✅ Concluído | ✓ |
| SOV-FCONV | W15 ‖ · **P2** | Libc-Núcleo | **Implementado 2026-07-09** (`embedded-firmware-engineer`): `ftoa(double, char*, unsigned precision)` (ponto-fixo, precisão fixa documentada, default 6, arredondamento meio-pra-longe-de-zero, ±0.0/inf/-inf/nan tratados, ZERO libm — `trunc`/classificação via bit-pattern próprio, aritmética SSE2 nativa) + `atof(const char*)` (sinal, int, fração, expoente `e/E`, `inf`/`infinity`/`nan` case-insensitive, overflow→±INF, underflow→0/denormal best-effort, 2 caps defensivos anti-DoS documentados) + `%f` no `mini-printf` via `ftoa` na precisão default. `CONV_FTOA_BUF_MIN`/`CONV_FTOA_BUF_FOR(precision)` novos (conta documentada: 309 dígitos de `DBL_MAX` + defensivo). `%g` **NÃO implementado** (exigiria notação científica que o `ftoa` não oferece — ponto-fixo só; ver INBOX). **Achado documentado, não um bug:** round-trip `atof(ftoa(x))` para `x` na década mais alta abaixo de `DBL_MAX` (~1e303+, incl. `1e308`/`DBL_MAX` literal) pode estourar pra `+Inf` de forma determinística — consequência inerente do algoritmo de precisão fixa (não-bignum), documentada em `include/conv.h` ("KNOWN EDGE BEHAVIOUR") e testada explicitamente (não escondida). DoD cumprido: round-trip vetor (escala humana + até 1e300 seguro), fronteiras ±0.0 (sinal preservado nos 2 sentidos), INF/-INF/NaN (2 sentidos), denormal mínimo, `DBL_MAX`/overflow de expoente, meio-caminho (`0.5`,`2.5`,`0.125`), `1e308`/`1e-308`, malformado (`""`,`"."`,`"e5"`,`"1e"`,`"--1"`) — todos em `tests/test_conv.c` (+`tests/test_printf.c` p/ `%f`). `make test` completo verde (nenhuma regressão em `mini_printf`/`printf_e2e`), `make check-static` verde (1 achado real de cppcheck em `atof` corrigido — dead-store de `i` sem leitura subsequente, 3 pontos), `nm -u` limpo em TODOS os binários (zero símbolo indefinido, prova de zero libc/soft-float). Conversão **float↔string** (`ftoa`/`atof` sobre double, precisão fixa documentada) + `%f` (e `%g` só se sair de graça) no `mini-printf` (E2). Valor imediato pro harness/printf; pré-requisito do parser RCSS da internalização (AUD-TRANS-4). Consome semente AUD-TRANS-0 ([`AUDIT_FIND.md` §5.2](AUDIT_FIND.md)). **Review adversarial `qa-engineer` APROVOU** (2 mutações de arredondamento/sinal mortas; `nm -u` limpo em 12 binários = zero libm/soft-float; `atof` fuzzado além do brief sem crash/UB; guard validar-antes-do-cast nos 2 sites; achado `DBL_MAX`→+Inf honesto e testado). | Média | REL-TAG, D3, E2 | Média | ✅ Concluído | ✓ |
| AUD-ABI-Δ | W16 · Onda 4 | Auditoria | **Re-auditoria delta 2026-07-19** (`docs/auditoria/AUD-C0-PLAN.md` §2.1) de conformidade ABI/ASM da Camada 0 — tudo que pousou depois do `AUD-ABI` original (2026-07-09) e nunca tinha sido re-auditado: `syscall2` (readicionada no SOV-ALLOC), `sys_munmap.c`, a fronteira mista `libcore.a`↔C++ hosted (ADR-0009: alinhamento de stack, red zone, estanqueidade do prefixo `glx_`, gate `nm -u`). **Entregue** (`embedded-firmware-engineer`, mesmo auditor do relatório original — continuidade de método): veredito **CONFORME-COM-RESSALVAS** — 0 CRÍTICO, 1 IMPORTANTE (achado novo: linkar `libcore.a` `-fno-pic` num consumidor C++ hosted derruba o binário-consumidor inteiro pra `ET_EXEC` em silêncio, perdendo ASLR — `readelf -h` confirmado; não é corrupção de memória, `libcore_consumer` 16/16 checks passam), 1 COSMÉTICO (`syscall2`, mesma classe do achado #1 original). Rebuild do zero (`TMPDIR=/var/tmp`) + `objdump`/`readelf`/`strace -f -c` sobre binários reais; todo gate do `AUD-C0-GATE` verde exceto `sanitize-hint` (fora do escopo de escrita deste capítulo, entregue pelo `AUD-SEC-Δ`). **IMPORTANTE resolvido** por adendo de custo-aceito na ADR-0009 (`927738a`) — decisão de aceitar o custo em vez de construir uma variante `-fpic` sem consumidor real (semente `AUD-C0-PICVAR` na INBOX). Relatório: `docs/auditoria/AUD-ABI.md` §"Delta re-audit 2026-07-19". | Alta | SOV-ALLOC, AUD-ABI | Média | ✅ Concluído (tag `core-v0.4.0`) | ✓ |
| AUD-SEC-Δ | W16 · Onda 4 | Auditoria | **Re-auditoria delta 2026-07-19** (`docs/auditoria/AUD-C0-PLAN.md` §2.2) de segurança/memory-safety da Camada 0 — tudo que pousou depois do `AUD-SEC` original (2026-07-09): reescrita completa do alocador free-list, `ftoa`/`atof`/`%f`, `kern`/bsearch do SFNT (`FT-F4-KERN-BSEARCH`, commit de perf nunca revisado por si só), e `src/hint.c` (1ª auditoria completa, nenhum alvo sanitizer existia antes). **Entregue** (`security-engineer`): veredito **CONFORME-COM-RESSALVAS** — 0 CRÍTICO, 1 IMPORTANTE (`atof`: a acumulação de dígito fracionário podia estourar pra valor errado/`+Inf` pra literais decimais legítimos dentro do próprio cap anti-DoS documentado, ex. `atof("0."+"9"×309)` → `inf`; sem crash/UB, mas contradizia a alegação de segurança do próprio doc-comment), 0 COSMÉTICO novo (os 3 do relatório original seguem válidos, inalterados). Toda alegação de fix re-verificada por **prova negativa executada** (guarda desligada numa cópia descartável → sanitizer/assert dispara; restaurada → limpo) — incl. o alocador free-list (double-free nos 2 casos documentados, guarda de overflow em 2 camadas de defesa-em-profundidade) e o `kern` bsearch (6 classes de dado hostil sintético). **Entregável de código:** `tests/sanitize_hint.c` + gate `sanitize-hint` novo no `Makefile` (fecha o último gap de sanitizer hosted-companion do trio SFNT/raster/hint). **IMPORTANTE resolvido**: fix `27efe3f` (acumulador fracionário separado, série geométrica convergente, nunca estoura) → **review adversarial pegou uma REGRESSÃO** (zeros à esquerda esgotavam o orçamento de dígitos significativos antes do dígito real, `atof("0.00...01")` zerava) → re-fix `c789a7a` (conta só a partir do 1º dígito não-zero) → **2º review adversarial APROVADO** (26 casos cruzados contra glibc, 0 divergência; `make test` 13/13, `check-static`/`test-mem` limpos). Relatório: `docs/auditoria/AUD-SEC.md` §"Delta re-audit 2026-07-19". | Alta | SOV-ALLOC, SOV-FCONV, AUD-SEC | Alta | ✅ Concluído (tag `core-v0.4.0`) | ✓ |
| DOC-WIKI | W14 · **P4** | Docs | Wiki do repo + doc `.md` extensa para INICIANTE (bilíngue en→pt), via `technical-writer`. Pré-req: tag de versão. **Entregue 2026-07-10** (`technical-writer`): `docs/GUIA_INICIANTE.md` (guia bilíngue extenso para iniciante total, cobre as 2 camadas, passo a passo de build/teste derivado de `CLAUDE.md`/`README.md`, glossário completo, mapa do repo) + `docs/wiki/` (fonte da Wiki Codeberg/Forgejo: `Home`/`Getting-Started`/`Layer-1-glintfx`/`Layer-0-Core`/`FAQ` + `README.md` com instruções de publicação (Wiki é repo git separado; publicação é ação de remoto do orquestrador/líder, NÃO feita nesta entrega). Comandos de build spot-checados pelo orquestrador (batem com `CLAUDE.md`/`Makefile` reais, nenhum inventado; 28 links relativos aos docs técnicos, sem duplicação). **Wiki PUBLICADA no Codeberg 2026-07-10** (5 páginas via API `/wiki/new`, `has_wiki:true`, https://codeberg.org/petrinhu/glintfx/wiki) — autorizada pelo líder. **Wiki PUBLICADA no GitHub 2026-07-10** (líder criou a 1ª página via UI; as 5 páginas pushadas via `git clone`/push do `glintfx.wiki.git`, branch `master`). **Ambas as wikis (Codeberg + GitHub) no ar.** Fonte-de-verdade em `docs/wiki/`; ressync futuro = repetir `cp`+commit+push contra os 2 `.wiki.git` (ver `docs/wiki/README.md`). Revisão didática final = leitura do líder. **→ RESSYNC 2026-07-16 (v0.9.0→v0.11.0):** `technical-writer` atualizou `GUIA_INICIANTE.md` + `docs/wiki/*` + README (badge/roadmap/backdrop-ripple) + corrigiu 3 divergências doc↔código factuais em `embed-integration.md §17` (`OWN_FONT_ENGINE default OFF→ON`, vendored-core/sem-monorepo, `sem kerning`→kerning opt-in) + orquestrador pôs badges na Home da wiki + atualizou banner do CLAUDE.md. Comandos spot-checados, re-verificados. **Republicado nas 2 wikis** (Codeberg `main` `0d076af` + GitHub `master` `68d8157`). | Média | REL-TAG | Média | ✅ Concluído | ✓ |
| L1-CLONE | LW1 | C++23/Setup | Clonar RmlUi + gl3w em `examples/` (gitignored) para estudo/RE | Alta | — | Baixa | ✅ Concluído | — |
| L1-BRAINSTORM | LW1 | C++23/Produto | Brainstorm bigtech: escopo/efeitos/nome. Gerou spec + plano + ADR-0006/0007; nome **glintfx**, licença MPL-2.0 | Alta | — | Média | ✅ Concluído | ✓ |
| L1-BUILD | LW2 | C++23/Build | Esqueleto CMake + FetchContent (RmlUi 6.3 + gl3w vendorizado) **pronto**; install/export + alias + consumer drop-in via FetchContent entregues (Task 8) | Alta | L1-CLONE | Média | ✅ Concluído | ✓ |
| L1-BACKEND | LW3 | C++23/Plataforma | Janela/contexto **GLFW** + wrap `RenderInterface_GL3` + bootstrap RmlUi/FreeType; fix backbuffer opaco (alpha×compositor). SDL/X11 adiados | Alta | L1-BUILD | Alta | ✅ Concluído | ✓ |
| L1-DEMO | LW4 | C++23/Demo | Showcase com os **4 efeitos** (glow, degradê, backdrop-blur, mask) — validado em GPU real (Intel+NVIDIA/Wayland) pelo líder 2026-06-29 | Alta | L1-BACKEND, L1-BRAINSTORM | Média | ✅ Concluído | ✓ |
| L1-API | LW4 | C++23/API | Fachada `glintfx::App` RAII (pImpl, headers limpos), compat C++17→23 | Alta | L1-BRAINSTORM | Alta | ✅ Concluído | ✓ |
| L1-DOCS | LW4 | C++23/Docs | Docs glintfx v0.1 mergeados: README, AGENTS.md, CHANGELOG, CONTRIBUTING, SECURITY, getting-started, effects | Alta | L1-API, L1-DEMO | Média | ✅ Concluído | ✓ |
| L1.6-TESTDOC | LW5 | C++23/Docs | `TESTES.md`: corrigir `xvfb-run` na seção "GPU real" (Xvfb usa llvmpipe, não GPU real) — já entregue em `0a93b2b`, status estava desatualizado | Média | — | Baixa | ✅ Concluído | — |
| L1.3-SNAPFLIP | LW5 | C++23/Bugfix | `snapshot()` sai de cabeça pra baixo (origem do `glReadPixels`); flip vertical das linhas — já entregue em `a61af92`, status estava desatualizado | Média | L1-API | Baixa | ✅ Concluído | — |
| L1.9-VERSEMBED | LW5 | C++23/API | `version()` movida de `app.cpp` (só GLFW=ON) p/ `glintfx/version.hpp`+`version.cpp` sempre-compilados; `app.hpp`/`glintfx.hpp` incluem transitivamente. Smoke test em `ui_layer_attach` roda nos 2 configs | Baixa | L1-API, L2-EMBED | Baixa | ✅ Concluído | — |
| L1.10-APIDOC | LW5 | C++23/API | (a) UB em objeto movido-de documentado em `App`/`UiLayer`; (b) `UiEvent::Type::None` adicionado como default (era `MouseMove`), tratado explicitamente em `ui_layer.cpp::process_event`. Nenhum teste dependia do default antigo | Baixa | L1-API, L2-EMBED | Baixa | ✅ Concluído | — |
| L1.4-GUARDS | LW5 | C++23/Hardening | Guards null: `window_glfw` (`make_current`/`swap`/`poll` sem checar `win_`; `glfwTerminate` sem flag `glfw_inited_`) e `App` (`poll_events`/`render` sem `impl_->ok`) — já entregue em `a61af92`, status estava desatualizado | Baixa | L1-BACKEND, L1-API | Baixa | ✅ Concluído | — |
| L1.5-DEMOBG | LW5 | C++23/Demo | `body { width: 100% }`: fundo do demo não cobre a janela inteira — já entregue em `a61af92`, status estava desatualizado | Baixa | L1-DEMO | Baixa | ✅ Concluído | — |
| L1.2-CI | LW6 | C++23/Infra | CI versionado: **GitHub Actions** (`.github/workflows/ci.yml`, gate principal) + **Codeberg Forgejo Actions** (`.forgejo/workflows/ci.yml`, soberania) — gate automático da suíte glintfx sob Xvfb/llvmpipe, matriz de 2 configs `GLINTFX_BACKEND_GLFW` (ON = build completo, 18 testes; OFF = embed-only, 8 testes) | Alta | L1-DEMO | Média | ✅ Concluído | — |
| L1.1-ERRSTRAT | LW6 | C++23/API | `App` estratégia de erro de construção: `bool ok()` **ratificada como definitiva** pelo líder (2026-07-02) -- já implementada e testada, nenhuma exception opcional adicional | Alta | L1-API | Média | ✅ Concluído | — |
| L1.8-FINDPKG | LW6 | C++23/Build | `find_package(glintfx)` habilitado via `glintfxConfig.cmake.in` + target `glintfx::glintfx IMPORTED` (sem `install(EXPORT)`, contornando o export-set do CMake 4.x); RmlUi co-instalado. Ambos os caminhos verdes (FetchContent + find_package) | Média | L1-BUILD | Média | ✅ Concluído | — |
| L1.11-CLICKCB | LW9 | C++23/API | `set_click_callback` (UiLayer+App) -- id do elemento clicado, fase bubble c/ fallback ancestral | Alta | L2-EMBED | Baixa | ✅ Concluído | ✓ |
| L1.12-ELEMBOX | LW9 | C++23/API | `get_element_box` (UiLayer+App) -- geometria border-box por id, struct `ElementBox` | Alta | L2-EMBED | Baixa | ✅ Concluído | ✓ |
| L1.13-VPORIGIN | LW9 | C++23/API | `set_viewport(x,y,w,h,target_h)` (UiLayer-only) -- origem de composição configurável (GAP-2 ativado), ADR-0008 atualizado. **API entregue+testada; ainda NÃO exercitada em produção** -- GusWorld pôs no backlog dele por WSJF (não-problema hoje: janela do líder é grande, corte de log só em janela menor; gatilho = distribuição multi-resolução). O feedback real do `target_h` virá quando integrar | Alta | L1.11-CLICKCB, L1.12-ELEMBOX | Média | ✅ Concluído | ✓ |
| L1.14-GLLOADER | LW8 · **P1** | C++23/Loucura | **1º marco da internalização, entregue 2026-07-09:** loader GL próprio **clean-room** (`glintfx/src/gl_loader.{h,c}`, 344 comandos GL 3.3 core) gerado por `tools/gen_glloader.py` a partir do registro público Khronos `gl.xml` (Apache-2.0, vendorizado em `glintfx/third_party/khronos/`) mais os headers MIT `glcorearb.h`/`khrplatform.h` já vendorizados; `glx_gl_load()` resolve via `glXGetProcAddress` -> `eglGetProcAddress` -> `dlsym` (0 = sucesso). Substitui `third_party/gl3w` nos 6 TUs mais os testes; `gl3w.c`/`gl3w.h` NÃO foram lidos durante a escrita (só `gl.xml`/headers Khronos mais doc pública de GetProcAddress). Suíte 100% verde nos 2 configs (35 testes GLFW=ON, 19 embed OFF, inclui o novo TDD `gl_loader_sanity`); `ldd`/`nm` sem lib/símbolo novo inesperado; `NOTICE`/README/AGENTS.md/docs atualizados. Plano-base em [`AUDIT_FIND.md` §5.3](AUDIT_FIND.md). Consome sementes AUD-TRANS-1 mais "internalização gl3w (LW8)". **Review adversarial `backend-engineer` APROVOU** (execução dos 2 configs, gerador byte-idêntico ao regenerar, clean-room confirmado: globals-nomeadas != union-posicional do gl3w). | Média | L1-ADR9 | Média | ✅ Concluído | ✓ |
| L1.17-FOCUS | LW10 ‖ · **P1** | C++23/API | **Entregue 2026-07-09 (`backend-engineer`):** `bool set_focus(const char* id)` + `bool clear_focus()` programáticos em `UiLayer`+`App` — host cujo MODELO é dono da seleção (GusWorld canon 1B contornou via data-binding; fecha a promessa do README). `set_focus` encapsula `Rml::Element::Focus(false)`; `clear_focus` encapsula `Context::GetFocusElement()`+`Element::Blur()`. **Correção vs. a hipótese original de "tabindex autorado" (confirmada lendo o source pinado do RmlUi 6.3):** `Focus()` se protege SÓ pela propriedade RCSS `focus` (default REGISTRADO `auto` — todo elemento é focável por padrão, `focus: none;` é a guarda real), NÃO por `tab-index` (só ordem de travessia Tab, sem efeito em `Focus()`). `clear_focus()` não zera o foco (RmlUi nunca reseta `Context::focus` pra nulo — inicializado no `root` na construção; `Blur()` reparenta pro pai) — semântica é "mover o foco pra longe de onde está", `true` idempotente quando nada relevante focado. Hardening: guard doc-não-carregado nos dois; `set_focus` também rejeita id nulo/vazio (AUD-TEC-5)/desconhecido/`focus:none`. TDD `focus_sanity` novo (dirige `Engine` direto + oráculo `Context::GetFocusElement()`, mesmo precedente do `document_reload_leak`), suíte 36/20 verde nos 2 configs. Docs: `docs/embed-integration.md` §5, `README.md`, `CHANGELOG.md` [Unreleased] atualizados. Sinergia com L1.15 (focus/blur observáveis fecham o ciclo de foco). Consome semente GAP-4 da INBOX. **Review adversarial `tech-lead` APROVOU** (premissa Focus≠tabindex confirmada no source pinado; auditoria-dominó dos guards limpa; encapsulamento OK; mutation spot-check com dente — inverteu `Focus()`, teste falhou). 1 cosmético registrado na INBOX. | Média | L2-EMBED | Baixa | ✅ Concluído | ✓ |
| L1.16-DOMRW | LW10 ‖ · **P2** | C++23/API | **Entregue 2026-07-09 (`backend-engineer`):** DOM read/write leve por id — setters imperativos `bool set_text(id, txt)`/`add_class`/`remove_class(id, cls)`/`set_property(id, prop, val)` (wrappers finos sobre `Rml::Element::SetInnerRML`/`SetClass`/`SetProperty`, assinaturas confirmadas no source pinado do RmlUi 6.3) + **leitura de volta do data-model** `get_number/string/bool(key, out)` (era write-only — two-way binding agora visível ao host, correto por construção: `DataModelConstructor::Bind` envolve o MESMO ponteiro da célula própria da glintfx). **Hardening crítico:** `set_text` escapa `& < > "` via `Rml::StringUtilities::EncodeRml` antes do `SetInnerRML` — texto nunca é markup, fecha o vetor de injeção de RML. `set_property` propaga o `false` de parse inválido do próprio RmlUi. Espelha o contrato dos 6 métodos por-id existentes (guard `!id &#124;&#124; !*id` AUD-TEC-5, retorno `bool`, `App`+`UiLayer` em paridade); `get_*` retorna `false` (out intocado) só quando a chave nunca foi ligada — legível antes E depois de `load()`. Consome sementes AUD-PUB-6(d)+(e) da INBOX. TDD `domrw_sanity` novo (dirige `Engine` direto, mesmo precedente do `focus_sanity`/`document_reload_leak`; inclui teste "mordida" de injeção de RML provando round-trip exato via `GetInnerRML()` + zero elementos filhos criados), suíte **37/21 verde nos 2 configs**. Docs: `docs/embed-integration.md` §15 (nova) + §5 (correção cosmética), `README.md`, `CHANGELOG.md` [Unreleased] atualizados. **Cosmético fechado junto:** correção da prosa "RmlUi nunca reseta focus pra nulo" (`bootstrap.hpp`/`embed-integration.md`/`CHANGELOG`) vs. `Context::OnElementDetach` — sem mudança de comportamento, só prosa (ver INBOX). Gate de encapsulamento (grep include-based) verificado manualmente, limpo. **Review adversarial `tech-lead` APROVOU** (37/21 verde; injeção de RML testada + mutation com dente — removeu o `EncodeRml`, o teste pegou; lifetime `get_string` OK/sem UAF; guards dominó nos 7 métodos × 2 fachadas; encapsulamento OK; cosmético `OnElementDetach` conferido no source). 1 achado não-bloqueante: falta teste de `get_*` após reload (lacuna preventiva, não regressão) — semeado. | Média | L2-EMBED | Média | ✅ Concluído | ✓ |
| L1.15-FORMEV | LW10 ‖ · **P3** | C++23/API | **Entregue 2026-07-09 (`backend-engineer`):** 5 callbacks de evento observável (Opção B da spec + HOVER, decisão do líder) — `set_change_callback(id,value)`, `set_submit_callback(id)`, `set_focus_callback(id)`, `set_blur_callback(id)`, `set_hover_callback(id,entered)`, paridade `App`+`UiLayer` (10 métodos). `change`/`submit` são listeners de fase bubble (cópia mecânica de `ScrollEventListener`); `value` do change é o próprio parâmetro `"value"` do RmlUi verbatim (já normalizado por tipo de controle); `submit` reporta o id do `<form>` (alvo real do `Submit` do RmlUi). `focus`/`blur` exigiram listener de fase-**CAPTURA** (`in_capture_phase=true`) — confirmado no source pinado que `Focus`/`Blur` não borbulham mas a fase de captura do RmlUi desce raiz→alvo incondicionalmente. `hover` usa um ÚNICO listener em `Mouseover` (Mouseout não é escutado) com `current_hover_id_` de dedup, evitando redisparo ao cruzar filhos sem id do mesmo container com id. **2 achados empíricos não previstos na spec, verificados e documentados nos doc-comments (`bootstrap.hpp`) + provados em teste:** (1) `Element::Focus()` num elemento JÁ focado é NO-OP verdadeiro (RmlUi dedupa por diferença de conjunto da cadeia de ancestrais, `Context::OnFocusChange`/`SendEvents` — nem `Blur` nem `Focus` disparam de novo); (2) uma ÚNICA mudança de foco pode disparar `focus_callback`/`blur_callback` MAIS DE UMA VEZ (RmlUi despacha um evento independente por ancestral recém-entrado na cadeia, não um único evento que borbulha) — documentado como "disparo múltiplo por transição", host deve tratar qualquer id não-vazio como sinal, não assumir 1 disparo por chamada. **Bug real corrigido antes do merge:** `Mouseover` sofre do MESMO fan-out de `Context::SendEvents` que Focus/Blur (não é um único disparo-que-borbulha); resolver o id a partir de `event.GetTargetElement()` tornaria o dedup do hover dependente de ordem/não-determinístico — corrigido resolvendo via `Context::GetHoverElement()` (atribuído ANTES do fan-out disparar, confirmado lendo `UpdateHoverChain`), tornando o dedup determinístico. TDD `form_events_sanity` novo (dirige `Engine` direto + `Rml::Context::ProcessMouseMove/ProcessMouseButtonDown/Up` como simulação de input, mesmo precedente do `focus_sanity`/`domrw_sanity`; cobre change/submit/focus-no-op/blur-limpo/hover-dedup/2× reentrância AUD-TEC-3 — submit e hover, este último exercitando o estado extra `current_hover_id_`), suíte **38/22 verde nos 2 configs**. Gate de encapsulamento (`tools/check_encapsulation.sh`) limpo. Docs: `docs/embed-integration.md` (nova seção "form/DOM events"), `README.md`, `CHANGELOG.md` [Unreleased] atualizados. Consome semente AUD-PUB-6(c) da INBOX. **Review adversarial `tech-lead` APROVOU** (38/22; 3 mutações mortas — incl. segfault comprovando o UAF sem functor-copy; auditoria-dominó dos 5 listeners limpa; observer puro sem StopPropagation; TODAS as claims verificadas contra o source pinado do RmlUi, zero alucinação). Nota: hover era fora-de-escopo na spec, incluído por decisão autônoma do orquestrador (líder pediu ganchos sonoro-relevantes hover/focus) — **CONFIRMADO retroativamente pelo líder 2026-07-10** + validado pelo GusWorld (vai migrar o hit-test manual pros callbacks). | Média | L2-EMBED | Média | ✅ Concluído | ✓ |
| TST-L1-STATIC | LW-TST | C++23/Testes | Job `lint-and-scan` (espelhado GitHub+Codeberg): `clang-tidy` (`clang-analyzer-*` gate duro, `modernize-*`/`cppcoreguidelines-*` reportado não-gateado -- ~87 achados pré-existentes, fora de escopo) + `cppcheck` (`glintfx/src/` só) + `tools/check_encapsulation.sh` -- gate real (não só doc) do include-based `(Rml&#124;glfw&#124;GLFW&#124;gl3w&#124;GL/&#124;stb&#124;SDL)`. Validado via `forgejo-runner exec`, verde | Alta | L1-API | Média | ✅ Concluído | — |
| TST-L1-SECRETS | LW-TST | C++23/Testes | `gitleaks` (histórico completo `fetch-depth: 0` + working tree) no job `lint-and-scan`, 2 remotos. 0 leaks em 117 commits | Média | — | Baixa | ✅ Concluído | — |
| TST-L1-DEPS | LW-TST | C++23/Testes | `grype` (`dir:.` scan) no job `lint-and-scan`, 2 remotos, `--fail-on high`. 0 vulnerabilidades hoje. **Ressalva documentada no CI:** sem cataloger p/ libs C++ header-only vendorizadas/fetchadas -- `AUD-L1-SUPPLY` (auditoria manual de versão-pinada) segue necessária, não substituída | Média | L1-BUILD | Baixa | ✅ Concluído | — |
| TST-L1-MEM | LW-TST | C++23/Testes | Gate de memória entregue: option `GLINTFX_SANITIZE` (ASan+UBSan+LSan) + suppression-files (`lsan_suppressions.txt` ruído GTK3/Wayland/fontconfig da máquina dev; `ubsan_suppressions.txt` só `vptr:ElementDocument` de terceiro) + nightly espelhado nos 2 remotos (cron 03:17 UTC + dispatch). Local: 18/18 ON + 8/8 OFF sanitizados limpos; `forgejo-runner exec` verde. Achado real de terceiro registrado na INBOX. Ver `TESTES.md` | Alta | L1-BACKEND, L2-EMBED | Média | ✅ Concluído | — |
| L1.18-COVCI | LW10 ‖ · **P3** | C++23/Infra | **Entregue 2026-07-10 (`devops-sre`):** cobertura de código self-hosted (decisão do líder 2026-07-04: llvm-cov, não Codecov). Option CMake `GLINTFX_COVERAGE` (só Clang, `-fprofile-instr-generate -fcoverage-mapping`, hard `FATAL_ERROR` sem Clang) instrumenta **por-alvo** via `glintfx_instrument_coverage()` — o alvo `glintfx` mais, via loop sobre `BUILDSYSTEM_TARGETS` no fim de `tests/CMakeLists.txt`, todo binário de teste; RmlUi (`_deps`)/`third_party/` ficam **fora** (diferente do `GLINTFX_SANITIZE` que instrumenta tudo de propósito). `OFF` por padrão = zero-efeito. Novo `tools/coverage_report.sh` (gêmeo local que o CI chama): build+`ctest` sob Xvfb com `LLVM_PROFILE_FILE` → `llvm-profdata merge` → `llvm-cov show`(HTML)+`export -summary-only`(%) → `--floor N` (gate)/`--update-badge` (JSON local). Job `coverage` novo nos 2 CIs (GitHub + Codeberg, `upload-artifact@v4`/`@v3` respectivamente — armadilha Forgejo confirmada), `--floor 75` (medido 83.8% em 2026-07-10, arredondado p/ baixo a múltiplo de 5 menos 5pp), CI **não** commita o badge. Badge README via `img.shields.io/endpoint` lendo `glintfx/coverage-badge.json` commitado. Validado com `forgejo-runner exec` escopado ao job `coverage` ANTES de subir. Ver `TESTES.md` `TST-L1-COV`. Consome semente INBOX "cobertura de código self-hosted". **Review adversarial `qa-engineer` APROVOU** (teste-com-dente: excluindo 19 testes a cobertura caiu 83.8%→37.1% e o gate FALHOU no piso 75% — métrica responsiva; exclusão de `_deps`/`third_party`/`tests` confirmada no índice HTML; badge schema shields OK; COVERAGE=OFF no-op; v4/v3 e cache separado corretos). 1 cosmético: warning benigno `llvm-cov mismatched data` (lib estática linkada por 38 binários) — documentar em `TESTES.md`. | Média | L1.2-CI | Média | ✅ Concluído | ✓ |
| TST-L1-DECODE | LW-TST | C++23/Testes | Robustez de decode de asset não-confiável (PNG/JPG/TGA malformado/truncado via stb_image + laço próprio de premultiply, bounds `w*h*4`): retorna fallback sem crash/corrupção. **Entregue junto do `AUD-L1-PARSE`** (2026-07-16, `16ab365`+`0f944e1`): corpus test `asset_decode_hostile_sanity` (5 casos de malformação de imagem + `img_huge_1tib`/`font_huge_1tib`, discriminação de guarda provada por mutation testing, não só rodada verde) roda sob a build ASan de `TST-L1-MEM`. Ver `TESTES.md` | Média | L1-BACKEND, TST-L1-MEM | Média | ✅ Concluído | ✓ |
| TST-L1-GLLEAK | LW-TST | C++23/Testes | Vazamento de recurso GPU/GL (textura/FBO/buffer/VAO/programa) no ciclo de `App`/`UiLayer` e load/unload repetido -- o que ASan não vê (handle é do driver, não heap). Contexto GL debug (`GL_KHR_debug`) ou accounting `glGen`↔`glDelete` por N ciclos. **Absorve** a cobertura do `GlStateGuard` (ex-INBOX). Endurance-lite (risco no host de longa duração: GusWorld). Pull. Ver `TESTES.md` | Média | L1-API, L2-EMBED | Média | ✅ Concluído (v0.11.2) | ✓ |
| TST-L1-CONTRACT | LW-TST | C++23/Testes | Contrato público de embed (`docs/embed-integration.md`) exercido como host real: **só API pública** (FetchContent + `find_package`), sem includes internos, nos 2 modos (GLFW=ON e embed-only OFF); `load`+`render()` num FBO + leitura de pixel; falha se a superfície quebrar/vazar tipo de terceiro. **Entrega o `consumer-example-embed/`** e o harness `test_host.hpp` que remove os includes `"../src/..."` da suíte (ex-INBOX). Pull. Ver `TESTES.md` | Média | L1.8-FINDPKG, L2-EMBED | Média | ✅ Concluído (v0.11.2) | ✓ |
| TST-L1-PRECI | LW-TST | C++23/Testes | Pré-CI local (`tools/preci.sh`): espelha o gate -- 2 configs (GLFW ON/OFF) + build + `ctest` sob Xvfb + os gates hoje ausentes do CI (STATIC/MEM/SECRETS). Local-first (soberania; offline exceto o FetchContent do RmlUi). **Entregue 2026-07-11** (`devops-sre`, `d07a849`, Onda 0 da reestruturação de CI): `tools/preci.sh` (modo rápido detecta a camada tocada vs `origin/main`; `--full` = 2 configs + `check_encapsulation` + gitleaks) + hook `.githooks/pre-push` opt-in (`git config core.hooksPath .githooks`). Validado: rápido/full verdes, detecção de camada correta, 39/39+22/22. **✅ Aceite do líder 2026-07-11** ("pode dar ok em TST-L1-PRECI") + `heavy` verde no `claudio` — promovido a Concluído. Ver `TESTES.md`. | Baixa | TST-L1-STATIC, TST-L1-SECRETS, TST-L1-MEM | Baixa | ✅ Concluído | ✓ |
| AUD-L1-PARSE | LW-AUD | C++23/Auditoria | **Entregue e re-verificada 2026-07-16** (`security-engineer`, `16ab365`+`0f944e1`): veredito **CONFORME** -- 0 CRÍTICO, 0 IMPORTANTE ABERTO, 1 COSMÉTICO (TOCTOU informativo, não bloqueia). Achou e fechou no mesmo ciclo 2 IMPORTANTE: (1) `LoadTexture` ganhou teto de 256MiB + guarda `INT_MAX`, mas o caminho de FONTE (`@font-face`, os 2 motores) não tinha teto próprio -- fechado com guarda ÚNICA e central em `BaseUrlFileInterface::Open()` (cobre imagem+fonte, os 2 motores, confirmado ao vivo forçando FreeType); (2) o corpus test `oversized` não discriminava a remoção da guarda sob mutation testing -- fechado com 2 casos novos (`img_huge_1tib`/`font_huge_1tib`, 2^40+1 bytes esparsos) que mutation-testam de verdade. Re-review independente por EXECUÇÃO (não confiou no relato do implementer): rebuild+53/53 sob ASan/UBSan, mutation testing refeito 2×, `SECURITY.md` corrigido pra versão suportada corrente. Relatório `docs/auditoria/AUD-L1-PARSE.md`. Ver `AUDITORIAS.md` | Média | TST-L1-DECODE, L1-BACKEND | Média | ✅ Concluído | ✓ |
| AUD-L1-SUPPLY | LW-AUD | C++23/Auditoria | **Entregue 2026-07-16** (`security-engineer`): veredito **CONFORME** -- 0 CRÍTICO/IMPORTANTE, 1 COSMÉTICO (o texto deste próprio item citava "gl3w vendorizados" -- **corrigido**: gl3w foi eliminado na v0.8.0/`L1.14-GLLOADER`, substituído pelo loader GL clean-room próprio `glintfx/src/gl_loader.{h,c}`). `grype dir:.` local 0 vulnerabilidades (bate com `TST-L1-DEPS`); RmlUi pin SHA `2cd28864…` sem advisory aplicável (osv.dev + GHSA); stb_image v2.30 tem 2 CVE reais de 2026 mas ambos escopados ao decoder GIF, código MORTO neste build (`STBI_ONLY_PNG/JPEG/TGA` define `STBI_NO_GIF` transitivamente, confirmado lendo o header); FreeType/GLFW/libGL do sistema, fora da responsabilidade de pin deste repo. Processo de advisory-no-bump documentado + **dobra o `AUD-L1-BUILD`** (flags de hardening pra empacotadores, ver linha abaixo). Relatório `docs/auditoria/AUD-L1-SUPPLY.md`. Ver `AUDITORIAS.md` | Média | TST-L1-DEPS, L1-BUILD | Baixa | ✅ Concluído | ✓ |
| AUD-L1-MEM | LW-AUD | C++23/Auditoria | **Entregue 2026-07-16** (`security-engineer`): veredito **CONFORME** -- 0 CRÍTICO/IMPORTANTE/COSMÉTICO nesta passagem. Julgamento downstream do gate noturno `TST-L1-MEM` (não re-executado): 12 supressões LSan legítimas (ruído GTK3/libdecor/Wayland do desktop, nenhuma toca código `glintfx::`, isolamento empírico confirmado pro padrão mais amplo) + 0 supressões UBSan (os 3 achados `vptr` históricos corrigidos NA RAIZ via patch de fonte rastreado do RmlUi, sem resíduo). Revisão dos recursos GL novos desde o item (`tint_program_` v0.7, `ripple_program_`+`backdrop_capture_tex_` v0.11/`L1.22-CAPTURE`): disciplina compile-lazy/dono-único/teardown-no-destrutor preservada, interação correta com o RAII do `GlStateGuard` pro estado de read-framebuffer, checados contra a forma dos 3 bugs reais históricos (leak stb, UAF do `bind_*`, UAF de reentrância do click-callback) -- nenhum recorreu. Relatório `docs/auditoria/AUD-L1-MEM.md`. Ver `AUDITORIAS.md` | Média | TST-L1-MEM, L2-EMBED | Baixa | ✅ Concluído | ✓ |
| AUD-L1-LICENSE | LW-AUD | C++23/Auditoria | **Entregue 2026-07-16** (`compliance-legal`): veredito **CONFORME** -- 0 CRÍTICO/IMPORTANTE, 1 COSMÉTICO (o texto deste próprio item citava "gl3w/stb domínio público" -- **corrigido**: `NOTICE` real na v0.11.0 já omite gl3w corretamente, só a prosa de catálogo deste item/`AUDITORIAS.md` ficou pra trás). Header SPDX presente em 100% do fonte auditado, incluindo o escopo estendido `tools/`/`demos/`/`cmake/`/CI YAML/todo `CMakeLists.txt`/`Makefile`/`Containerfile`. `NOTICE` completo e correto vs. o grafo real de deps (gl3w ausente, FreeType presente conforme o flip suave do ADR-0011). Zero resíduo de texto AGPL (só menções históricas legítimas ao relicenciamento). Toda dependência linkada/vendorizada é permissiva e compatível com o copyleft fraco por-arquivo da MPL-2.0. `LICENSE` íntegro (374 linhas, texto MPL-2.0 completo e inalterado). Checagem técnica, não parecer jurídico vinculante. Relatório `docs/auditoria/AUD-L1-LICENSE.md`. Ver `AUDITORIAS.md` | Baixa | L1-DOCS | Baixa | ✅ Concluído | ✓ |
| AUD-L1-BUILD | LW-AUD | C++23/Auditoria | **Resolvido por dobra 2026-07-16 -- decisão autônoma do Caetano/CTO, confirmar retroativamente com o líder:** dobrado em `AUD-L1-SUPPLY`+`AUD-L1-MEM`+`AUD-L1-PARSE` no corte mínimo, conforme sugerido pelo `security-engineer`. As flags de hardening de build (`-D_FORTIFY_SOURCE=3`, `-D_GLIBCXX_ASSERTIONS`, `-fstack-protector-strong`, `-Wl,-z,relro,-z,now`) viraram **recomendação documentada pra empacotadores/consumidores** na seção "AUD-L1-BUILD fold-in" de `docs/auditoria/AUD-L1-SUPPLY.md` -- NÃO um gate de CI deste repo (racional: a `glintfx` é distribuída como fonte via `FetchContent`/`add_subdirectory`; impor flags de linker/compilador unilateralmente ao consumidor seria regressão de correção pra alguns casos, não melhoria de segurança). Gap real de higiene de build fechado em paralelo: **`-Wall -Wextra` aplicado ao alvo `glintfx`** (`45209e5`, `target_compile_options(... PRIVATE ...)`, escopado só ao alvo, zero warning nos 17 sources autorais; RmlUi/backends e os 26 alvos de teste + 2 de demo deliberadamente fora desta passada -- ver `glintfx/CMakeLists.txt:518-540`). Ver `AUDITORIAS.md` | Baixa | L1.2-CI, L1-BUILD | Média | ✅ Concluído -- resolvido por dobra | ✓ |
| L1.7-BACKENDS | LW7 | C++23/Plataforma | Backends SDL/X11 (adiados da v1; só GLFW entregue) | Baixa | L1-BACKEND | Alta | ⏳ Pendente | — |
| L1-ADR9 | LW8 | C++23/Arquitetura | **Gate da internalização — feature aberta 2026-07-09 (roadmap ativo, Cósimo/CoS):** redigir `docs/adr/0009-internalization-boundary.md` (bilíngue, template dos ADRs existentes) com as 3 opções de fronteira do [`AUDIT_FIND.md` §5.1](AUDIT_FIND.md) (A: link estático incremental `libcore.a` com prefixo de símbolo — recomendação técnica do auditor · B: process-boundary/IPC · C: clean-room dentro da Camada 1, soberania em fase 2) e colher a decisão do líder via AskUserQuestion (A recomendada, C alternativa de menor risco). **OWD** — nenhum código de internalização antes deste ADR; sucede o modelo "não se linkam" do ADR-0006. Consome semente AUD-TRANS-5 (INBOX urgente). **✅ DECIDIDO 2026-07-09 = Opção A-faseada** (link estático `libcore.a`/prefixo `glx_`; padrão obrigatório casca-hosted+carne-C via `extern C` → torna `SOV-CXXRT` opcional; 4 gates: core-v0.1.0 fechado + `SOV-ALLOC` hard-blocker + regra de ownership-de-heap testável + piloto gl3w; **B descartada pro hot path** — GL context é per-process). Workgroup: software-architect+embedded (lentes) → caetano-cto (dossiê) → decisão do líder via AskUserQuestion → `docs/adr/0009-internalization-boundary.md` escrito (`a34425e`) + adendo no ADR-0006. Destrava `L1.14-GLLOADER` e a trilha de internalização. | Alta | REL-TAG | Baixa | ✅ Concluído | ✓ |
| L1-INTERNALIZE | LW8 | C++23/Loucura | Trilha de internalização clean-room (peças da Camada 1 → reescritas sobre a Camada 0). Pós-MVP. **Épico de fim-de-projeto:** independência clean-room das libs userspace (RmlUi/gl3w/FreeType/GLFW); fronteira irredutível = libGL/driver/kernel. **Detalhamento/ordem (2026-07-09):** gate `L1-ADR9` → `L1.14-GLLOADER` (P) → `SOV-ALLOC`/`SOV-FCONV`/`SOV-MATH` na Camada 0 → FreeType via `FontEngineInterface` (G) → RenderInterface de efeitos próprio (G) → RmlUi core (GG, endgame). Recomendação registrada do auditor: **NÃO internalizar GLFW** (embed-only já vive sem ele — `AUDIT_FIND.md` §5.5). **AUD-TRANS-2 (FreeType) decomposto pelo Caetano/CTO 2026-07-10** em 5 sub-fases (FT-F0..F4 abaixo); descoberta: `SOV-MATH` NÃO é pré-req (rasterizador em fixed-point 26.6 dispensa libm) e a `libcore.a` do ADR-0009 nunca foi exercitada (gl3w-piloto ficou dentro do glintfx). | Média | L1-DEMO | Alta | 💡 Decisão tomada | — |
| SOV-LIBCORE | LW8 · **FT-F0** | Camada 0/Internalização | **Fronteira física do ADR-0009 — 1ª peça do épico FreeType (atacada 2026-07-10):** alvo `libcore.a` no Makefile (flags freestanding, símbolos públicos prefixo `glx_`, headers `include/core/`) + **teste de mau-uso de ownership de heap** (gate 3 do ADR-0009: ponteiro de `glx_malloc`→`free()` da glibc deve falhar alto, não corromper) + `nm -g` só `glx_*` exportados. O gl3w-piloto (L1.14) NÃO exercitou a `libcore.a`; esta peça prova a mecânica `.a`+prefixo+consumo. Decisão alias-vs-rename = do agente (menor churn que satisfaça o ADR). **Implementado 2026-07-10** (`embedded-firmware-engineer`): decisão elegante = **`-Dmalloc=glx_core_malloc_impl`** etc. no build da `.a` (símbolos internos da Camada 0 viram `glx_core_*_impl`, os testes existentes que usam `malloc`/`memcpy` no build normal NÃO quebram) + `src/core_api.c`/`include/core/core.h` expõem `glx_malloc`/`glx_free`/`glx_realloc`/`glx_memcpy`/`glx_memset`/`glx_memmove`/`glx_memcmp` como front-door; `include/alloc_internal.h` separa a superfície. **Teste de heap-ownership com DETECÇÃO ATIVA** (melhor que só convenção): `alloc_owns_ptr()` valida se o ponteiro é de uma arena `mmap` da Camada 0 — `glx_free()` num ponteiro da glibc **aborta com exit 112**, não corrompe; consumer C++ hosted (`tests/libcore_consumer.cpp`) prova os 2 heaps separados. `tools/check_libcore_symbols.sh` (gate 4 nm) confirma só `glx_*` exportados. `make test` Camada 0 verde 18/18 (sem regressão). **Review adversarial `security-engineer` REPROVOU o 1º commit** (`decb2fb`) com 1 CRÍTICO (symbol-hijack: `str.c`/`conv.c`/`printf.c` vazavam `strlen`/`atoi`/etc. crus da `.a` — o gate 4 dava falso-positivo por lista-negra incompleta; provado hijack ao vivo) + 1 IMPORTANTE (`alloc_owns_ptr` sem check de alinhamento → `glx_free(p+8)` SIGSEGV em vez de sys_exit). **CORRIGIDO** (`40a74bc`): rename estendido pra 22 nomes (toda a família libc-shaped → `glx_core_*_impl`) + `check_libcore_symbols.sh` virou **whitelist positiva fail-secure**; `alloc_owns_ptr` valida alinhamento de header (`(hdr-base)%16==0`) + caso 3c no consumer. **Re-review APROVOU com dente**: reataque de hijack ao vivo confirma resolução dinâmica pra glibc (não estático do `.a`); whitelist testada negativo; resíduo (offset alinhado-16 dentro de bloco maior) honestamente documentado. bug lateral corrigido: `.gitignore` `core` engolia `include/core/`. **1ª peça do épico FreeType fechada — fronteira `libcore.a` provada.** | Alta | L1-ADR9, SOV-ALLOC | Média | ✅ Concluído | ✓ |
| SOV-SFNT | LW8 · FT-F1 | Camada 0/Internalização | Parser SFNT clean-room (specs OpenType/TrueType públicas, PROIBIDO ler FreeType): `cmap` 4/12, `glyf`/`loca` (outline quadrático + glyphs compostos — acentuados pt-br), `head`/`hhea`/`hmtx`. Carne C pura (blob→dado; I/O de arquivo é da casca). Oráculo = Open Sans + valores conhecidos via `ttx`/fontTools (só p/ gerar os esperados, nunca código). Hostil: tabela truncada / offset OOB / `loca` inconsistente / `numGlyphs` mentiroso → fail-clean, nunca UB. **Implementado 2026-07-10** (`embedded-firmware-engineer`): `src/sfnt.c`/`include/core/sfnt.h`, API `glx_sfnt_open`/`glx_sfnt_glyph_id`/`glx_sfnt_hmetrics`/`glx_sfnt_glyph_outline` — SEM alocação dinâmica (buffers do chamador, sem `glx_sfnt_close`), toda leitura via `bounds_ok` overflow-safe (`off>len &#124;&#124; need>len-off`), composto recursivo com teto `GLX_SFNT_MAX_COMPOSITE_DEPTH=8` (mata auto-referência). Oráculo: `fontTools` (não-FreeType) + leitura manual dos bytes crus com `struct`, cruzados — valores documentados no cabeçalho do `tests/test_sfnt.c` (sem script commitado). Fixture Open Sans real embutida via `ld -r -b binary` (link-time, sem syscall `open` — Camada 0 não tem uma) em `tests/fixtures/opensans_regular.ttf`; 2 fontes sintéticas hostis (`cmap` formato 12 não-BMP, glyph composto auto-referente) construídas à mão no teste. `make test` 12/12 verde (`test_sfnt` incluso) + `valgrind` limpo + `check-static` 0 achados + `make libcore`/`check_libcore_symbols.sh` verdes (`glx_sfnt_*` na whitelist, sem símbolo cru novo) + composto `á` (163) confere 47 pontos/3 contornos com offset (0,0)+(43,0), sem escala. **Review adversarial `security-engineer` (executou/UBSan+250k mutações) REPROVOU o 1º commit** (`1042dce`) com 1 CRÍTICO: `apply_component_transform` fazia `(int16_t)(fx±0.5f)` SEM clamp → UB (C 6.3.1.4) quando a coordenada transformada sai de int16, alcançável por composto ESPEC-LEGÍTIMO (translate-only, forma dos acentos pt-br); repro `fx=40000.5`→lixo `-25536` com `GLX_SFNT_OK`. **CORRIGIDO** (`ecf74b8`): `round_and_saturate_i16()` satura em `[INT16_MIN,INT16_MAX]` antes do cast + gate novo `make sanitize-sfnt` (harness hosted da MESMA `sfnt.c` sob ASan+UBSan) + teste `kSyntheticFontCompositeSaturate`. **Re-review APROVOU o CRÍTICO** (prova negativa reabre o UB exato; 100k parses byte-flip da Open Sans real ASan/UBSan limpos) e a **varredura-dominó** (a lição do projeto) achou 2 IMPORTANTES pré-existentes, **fechados** (`3d5e277`): (1) NaN-guard no helper (`v!=v→0`, latente/inalcançável hoje, defense-in-depth) + (2) **`parse_simple_glyph` truncava o acumulador `int32` de deltas sem clamp** (REACHABLE por fonte hostil, narrowing int→int implementation-defined = INVISÍVEL ao sanitizer → `saturate_i32_to_i16()` + teste de VALOR `kSyntheticFontSimpleDeltaOverflow` provando saturação vs wrap). `make test` verde + `sanitize-sfnt` 0 findings + `check-static` + `libcore` verdes; orquestrador re-verificou build+claims. | Alta | SOV-LIBCORE | Média | ✅ Concluído | ✓ |
| SOV-RAST | LW8 · FT-F2 | Camada 0/Internalização | **Rasterizador de outline (peça GG):** outline→bitmap 8-bit AA, fixed-point 26.6 (SEM libm — floor/ceil=shifts, achatamento de quadrática por desvio inteiro), scanline com cobertura fracionária por área, non-zero winding. Oráculo ANALÍTICO (retângulo/triângulo = cobertura exata previsível) + propriedades (soma cobertura ∝ área, simetria do 'O') — NÃO golden pixel-exato (flaky). **Implementado 2026-07-10** (`embedded-firmware-engineer`, `5d4af6e`): `src/raster.c`/`include/core/raster.h`, API `glx_rasterize_outline(...)` + `glx_raster_scratch_floats(w,h)`. Flattening De Casteljau em fixed-point 26.6 puro (int32, teste de flatness sem `sqrt` via cross-product, teto de profundidade 8) + cobertura analítica por área com sinal (Green, 1 passe, planos `area[]`/`cover[]`) + non-zero winding. Escala racional (`scale_num/scale_den`), Y-flip, scratch do chamador (zero alloc dinâmica), clip analítico a `[0,w)×[0,h)`. `nm -u` vazio (zero libm). Oráculo executado: retângulo meio-px→coluna de borda alpha 128 exato, triângulo≈área, buraco do 'O'=0 por winding. **Review adversarial `security-engineer` (EXECUTOU: 240k iterações de fuzz hostil + 10 cenários manuais sob ASan+UBSan, bitmap/scratch com redzone) APROVOU sem CRÍTICO/IMPORTANTE**: nenhum OOB-write possível (coords gigantes/negativas, stride≠w, w/h/stride inválidos, scratch subdimensionado → todos fail-clean ou clip), nenhum overflow/narrowing UB (saturate_fx int64+clamp ±16M), cobertura re-derivada correta (overlap satura 255, bowtie sem crash), degenerados limpos, clean-room confirmado (nada do ftgrays). 1 COSMÉTICO não-alcançável (`iabs32(INT32_MIN)` → INBOX). Gate novo `make sanitize-raster`. Orquestrador re-verificou build+gates+nm. | Média | SOV-SFNT | Alta | ✅ Concluído | ✓ |
| L1.19-FONTENG | LW8 · FT-F3 | C++23/Internalização | Casca `Rml::FontEngineInterface` própria (C++ hosted, glintfx — glue permitido pelo ADR-0009): banco de faces + cache glyph + **atlas shelf-packing** (textura via `RenderManager`) + `GenerateString`(quads premult)/`GetStringWidth`/`FontMetrics`; `PrepareFontEffects` no-op (nenhum consumidor). Consome parser+rast via `extern "C"`/handles opacos/`glx_*_free` (regra de heap-ownership). Atrás de option `GLINTFX_OWN_FONT_ENGINE=OFF`; `Rml::SetFontEngineInterface()` sobrepõe em runtime (mantém `RMLUI_FONT_ENGINE=freetype`, A/B sem rebuildar RmlUi). Aceite = suíte visual verde na config ON. **Implementado 2026-07-10** (`engine-graphics-programmer`, `26eefc3`): `glintfx/src/font_engine_own.{hpp,cpp}` — banco de faces (blob+`glx_sfnt_face`), cache de face-instância (chave face+px), **bake ávido** do atlas (ASCII + Latin-1 acentuado pt-br) via SOV-SFNT→SOV-RAST, shelf-packing, blit premult (branco×cobertura, casa com blend `GL_ONE,GL_ONE_MINUS_SRC_ALPHA`), textura preguiçosa via `Rml::CallbackTexture`. `GetStringWidth`/`GenerateString` compartilham `IterateGlyphs` (largura layout=mesh). A/B por `Rml::SetFontEngineInterface` no `Bootstrap::init` sob `#if GLINTFX_OWN_FONT_ENGINE` (option default OFF; `RMLUI_FONT_ENGINE` fica `freetype` — troca em runtime, sem rebuildar RmlUi). 2 bugs achados+corrigidos na impl: colisão `<limits.h>` Camada 0×stb_image (restrito via `set_source_files_properties`) + leak `CallbackTexture` no shutdown (ordem: `FontEngineOwn::Shutdown()` explícito antes de `Rml::Shutdown()`). **Review adversarial `qa-engineer` (EXECUTOU: rebuild ON/OFF/embed-only + ASan + probe UiLayer hostil) APROVOU a implementação** (sem crash/UAF atribuível, clean-room limpo, texto legível com acentos compostos, premult correto, robusto a CJK/emoji/UTF-8-quebrado degradando limpo) e achou 2 IMPORTANTES de COBERTURA de teste, **fechados** (`6d8710c`): (1) `fonteng_own_embed_sanity` novo (UiLayer, o caminho embed do GusWorld, fora do bloco GLFW) + fonte copiada no bloco embed; (2) oráculo por bbox real de `get_element_box(id)` (era `h/3`, media só cauda de descendente). ctest ON-GLFW 40/40, ON-embed 23/23, OFF 39/39. Orquestrador verificou registro do teste + flag. **Ressalva:** o CI ainda NÃO builda a config ON (só OFF/default é gated) — os testes `fonteng_own_*` rodam só localmente; a **job de CI pra `GLINTFX_OWN_FONT_ENGINE=ON` é obrigatória no `L1.20-FONTFLIP` (FT-F4)** quando a engine própria se aproximar de default. | Média | SOV-RAST, SOV-SFNT, SOV-LIBCORE | Alta | ✅ Concluído | ✓ |
| L1.20-FONTFLIP | LW8 · FT-F4 | C++23/Internalização | **O flip (go/no-go do líder, remove uma dep):** default da flag → ON, depois `RMLUI_FONT_ENGINE=none` + drop do link FreeType + `NOTICE`/`CHANGELOG` + goldens atualizados conscientemente + tag minor. Só após forno com CI dual nas 2 configs + idealmente 1 ciclo do GusWorld sobre a config própria. **DECISÃO DO LÍDER 2026-07-10: NÃO flipar agora — "Manter OFF, amadurecer".** A engine própria (F1-F3 entregues, funcional, review adversarial aprovou) fica **opt-in atrás de `GLINTFX_OWN_FONT_ENGINE=OFF`**; o FreeType maduro segue o default de produção. O objetivo do épico (provar a viabilidade da internalização clean-room do FreeType) está ENTREGUE. O flip é marco FUTURO (endgame sem prazo), condicionado a AMADURECIMENTO: (a) **kerning** (kern/GPOS) — **✅ FEITO 2026-07-10**: parser da tabela `kern` fmt 0 na Camada 0 (`glx_sfnt_kern`, `7d3d6d4`, review adversarial aprovou + fechou 1 MÉDIO de symbol-hijack por memset implícito `1432ac2`, gate `nm -u` endurecido) + o font engine APLICA no `IterateGlyphs` compartilhado (`6bf2cf7`, oráculo prova `width("AV")<width("A")+width("V")`); GPOS ainda semente; (b) **job de CI pra config ON** — **✅ FEITO** (`7fb28e5`, job `build-and-test-fonteng` GLFW=ON+embed-only, valida ON no servidor); (c) rodar **A/B em releases** + idealmente 1 ciclo do GusWorld sobre a engine própria — pendente; (d) hinting/subpixel-AA — pendente. Progresso de amadurecimento: (a)✅ (b)✅ (c)(d) em curso. **ONDA REABERTA pelo líder 2026-07-11**, branch `feat/ft-f4-fontflip` (escopo declarado: distribuição GERAL, não só GusWorld, ver memória `feedback_escopo_distribuicao_geral`). Entregues/commitados na branch: F1a A/B harness (`57fe173`, layout próprio bate FreeType <1%) + comparativo visual de nitidez (mostrou gap real em baixa DPI corpo pequeno, sem hinting) + cobertura 2A (`3b90573`, Latin-1 Supplement completo + pontuação tipográfica, î/em-dash resolvidos, ~199 codepoints) + hinting Y clean-room na Camada 0 (`df7f3c7`, `glx_hint_outline` grid-fit vertical, zero libm) + wiring do Y no motor (`7a2cfe3`, 5/5 fonteng verdes, Y necessário mas não empata FreeType). (c)(d) faseados: stem-darkening barato TESTADO e REPROVADO como release-default (`1746b80`, resultado NEGATIVO medido: curva de cobertura c^γ engorda a haste mas não fecha nitidez, é limitação geométrica -- haste split 0.5/0.5 só consolida via grid-fit no eixo X; ficou `own_font_engine_darken_enable()` OFF por padrão, maquinaria opcional togglável). **DECISÃO DO LÍDER 2026-07-11: encarar o eixo X COM GATE DE SAÍDA** (consumer-driven: GusWorld confirmou HUD/cockpit 9-13dp, vários 8-10dp, nítido CRÍTICO até ~150dpi Steam Deck — memória `feedback_gusworld_corpo_pequeno_fonte`). **O spike de gate (`engine-graphics-programmer`) achou a causa-raiz REAL: não é shape da haste, é FASE DE TELA fracionária.** O motor acumulava `pen`/`origin_y` em float por-glyph → cada glyph numa sub-fase sub-pixel (atlas GL_LINEAR re-borra); o FreeType usa advance/bearing INTEIROS → fase única → nítido. Resultado — **detector de haste (dancing stems) CANCELADO como desnecessário**, substituído por 2 fixes baratos de ~2 linhas cada, atrás de toggle: **(1) pen-snap eixo X** (`eab07df`, `own_font_engine_pensnap_bypass`): arredonda advance/kern/letter-spacing/offset_x a inteiro → empata/supera FreeType em `vstem_edge` em **14/15 células** (9-13dp × dp 1.0/1.5/2.0), 0 reversões (dancing morto), invariância largura==mesh preservada (control_gap 0.000 exato). **(2) vsnap eixo Y** (`4ec8193`, `own_font_engine_vsnap_bypass`): snapa `origin_y_26_6`(→múltiplo de 64)/`offset_y` a inteiro → a aresta que o `glx_hint_outline` já grid-fita para de ser re-borrada → empata o platô ~0.84 do FreeType em dp 1.5/2.0 inteiros + dp1.0 corpos 10-13 (resgata a catástrofe 10px 0.30→0.785, +0.485), sentinela X intacta. **Paridade X+Y com FreeType em corpo pequeno: 14/15 células.** Resíduo único **9px@dp1.0** investigado por spike focado: **limite fundamental, NÃO fix barato** (round-half refutado — snap de origem é invariante de fase; gap é overshoot de topo curvo o/c/e que o FreeType suprime via hinting de outline completo — classe do detector de haste já descartado) → **semente INBOX**. Gate de saída não precisou disparar (Plano B nunca acionado). Cada passo re-verificado pelo orquestrador (build limpo + 5/5 fonteng ctest). **Cobertura 2B (lazy bake on-demand) ENTREGUE 2026-07-14 (decisão autônoma do líder "modo automático"):** top-up lazy ADITIVO sobre o warm set ávido — qualquer codepoint que apareça e falte no mapa é bakeado sob demanda (mata o tofu de Grego/Cirílico/símbolos fora do Latin-1), atlas cresce por dobra geométrica (`GrowAtlas`, nunca move glyph existente), `GetVersion` dinâmico invalida a textura, negative-cache não re-tenta gid 0. Nitidez idêntica por construção (`RasterizeGlyph` extraído, DRY, reusa pen-snap/vsnap/hint) + invariância largura==mesh preservada (`EnsureGlyphs` antes do `IterateGlyphs` intacto). Impl `engine-graphics-programmer` (`7f119f1`), review adversarial que EXECUTA por agente distinto `qa-engineer` (`2712b5b`, `fonteng_lazy_sanity` cobre os 6 critérios: não-tofu Grego/Cirílico, crescimento sem corrupção via canários byte-idênticos, largura==mesh, negative-cache CJK, regressão zero, **sanitizer ASan+LSan+UBSan limpo**). 6/6 fonteng + 44/44 suíte verdes; orquestrador re-verificou build+claims. **Fix pré-flip (vazamento de memória, achado por review adversarial 2026-07-13/14, corrigido 2026-07-14, `c406b67`):** `FontEngineOwn::RegisterFace()` empilhava incondicionalmente um `LoadedFace` novo (cópia completa do blob, ~214KB pra Open Sans) a cada chamada, sem dedup — `Rml::LoadFontFace()` roda a cada `Context::LoadDocument()` por bloco `@font-face`, então recarregar o MESMO documento N vezes (navegação de tela, hot-reload — uso NORMAL, não hostil) vazava ~214KB×N (medido +213893 bytes/reload de VmRSS em 300 reloads); `FindBestFace()` sempre resolve pra face registrada primeiro, então toda duplicata pós-primeira era peso-morto nunca liberado antes do `Shutdown()`. Fix: dedup por `(family, style, weight-normalizado, blob-content-exato)` antes de parsear/alocar, espelhando `FontProvider::AddFace()`/`FontFamily::AddFace()` do source pinado do RmlUi (resultado "Duplicate" = sucesso). Como `FindBestFace()` passa a resolver sempre pro MESMO `LoadedFace*`, o cache de `FaceInstance` do `GetFontFaceHandle()` também estabiliza entre reloads — fecha blob E atlas, não só o blob. Teste novo `fonteng_dedup_sanity` (50 reloads, contador de debug `own_font_engine_debug_registered_face_count()` EXATO em 1, não tolerância) + verificação manual a 300 reloads confirmou VmRSS byte-a-byte idêntico do reload #50 ao #300 (delta +25MB é warm-up de heap/driver de UMA VEZ, não vazamento por-reload). 7/7 fonteng + 45/45 suíte completa verdes. **Review adversarial FORTE do motor inteiro (pré-flip) RODADO 2026-07-14 (`qa-engineer`, dossiê de prontidão):** estresse holístico de integração LIMPO sob ASan+LSan+UBSan (71 fontes hostis/malformadas no motor completo, 11 UTF-8 malformados, atlas growth ~896 codepoints, 20 ciclos de vida attach/load/render/shutdown — zero crash/UAF/UB/leak atribuível). Inventário de gaps vs FreeType-via-RmlUi achou **2 BLOQUEADORES do flip**: (#1) vazamento do `RegisterFace` — **✅ CORRIGIDO** (`c406b67`, acima); (#2) **sem fallback de fonte por-glyph** — `fallback_face=true` no `@font-face` é aceito mas nunca usado dentro de uma família resolvida (`FindBestFace` só troca face quando a FAMÍLIA não bate; RmlUi-FreeType itera `fallback_font_faces` glyph-a-glyph, `FontFaceHandleDefault.cpp:343`) → documento que depende de fallback (CJK/emoji ao lado de Latin) perde caracteres silenciosamente. **BLOQUEADOR #2 IMPLEMENTADO 2026-07-14** (`engine-graphics-programmer`, `957505b`, modo autônomo): fallback de fonte por-glyph, espelhando `FontFaceHandleDefault.cpp:343` do RmlUi. `FontEngineOwn::fallback_faces_` (índice em ordem de registro sobre `faces_`, populado por `RegisterFace()` no push fresco E no early-return de dedup — paridade com `FontFamily::AddFace()` do RmlUi, que anexa mesmo no outcome "Duplicate"); `RasterizeGlyph()` ganhou `const LoadedFace* src` (default `inst.face`, refactor mecânico confirmado neutro — 7/7 fonteng verdes ANTES do loop de fallback ser escrito) — **crítico: a escala usa `src->sfnt.units_per_em`, não a de `inst.face`** (cada arquivo de fonte tem seu próprio em-square: Open Sans 2048 vs. PixelOperatorMono 1600); `GlyphInfo::src_face` (novo campo) grava de qual face o glyph veio, warm set só grava a primária (nunca recorre a fallback, anti-OE); `BakeGlyph()` percorre `fallback_faces_` em ordem no gid==0 da primária, usa a primeira que resolve; `IterateGlyphs()` só aplica kern quando `prev->src_face == cur->src_face` (tabela `kern` clássica é indexada por gid DENTRO de um arquivo — gid de faces diferentes não se relaciona), escala do kern também derivada de `cur->src_face->sfnt.units_per_em`. Reusa 100% da maquinaria do atlas 2B (shelf/GrowAtlas/negative-cache/version); sem shaping/GPOS/bidi. Build ON limpo + 7/7 fonteng verdes confirmados 2x (pós-refactor `RasterizeGlyph`, pós-fallback completo). **Toca só `font_engine_own.{hpp,cpp}` — teste dedicado (`fonteng_fallback_sanity` ou similar) fica para o `qa-engineer` em onda separada; item segue `✅`, NÃO fecha o bloqueador nem habilita o flip sozinho.** Gaps ACEITÁVEIS/follow-up (não bloqueiam): hinting de bytecode, emoji/cor (CBDT/COLR), variable fonts, CFF/OTF cúbico (rejeitado limpo — restringe catálogo a TTF/glyf), `.ttc`, tabela `name`. NÃO-gaps (paridade real com o FreeType atual, confirmado lendo o RmlUi pinado): GPOS, kern-table ausente, subpixel/LCD, atlas-sem-eviction. **Bloqueador #2 (fallback de fonte por-glyph) ENTREGUE 2026-07-14 (modo autônomo):** impl `engine-graphics-programmer` (`957505b`) — quando a face primária não tem o gid, itera `fallback_faces_` (faces com `fallback_face=true`, ordem de registro, primeira que tem vence, não cai em si mesma) e bakeia o glyph da fallback no atlas da instância primária **escalado pela upem da FACE-FONTE** (não da primária), kern roteado por `src_face` (só intra-face), reusa a maquinaria 2B. Review adversarial que EXECUTA `qa-engineer` (`a81ca22`, fixture nova PixelOperatorMono CC0 Latin-only upem 1600 + Open Sans fallback): Grego (Ω/α) e ² aparecem via fallback (contra-prova sem fallback = branco, causal), mismatch de upem validado, ordem/negative-cache/largura==mesh/kern-cross-face/sanitizer OK, 10/10 fonteng + 48/48 suíte. **Bug de produção achado pelo review e CORRIGIDO** (`2f9d3fd`): `FindBestFace` comparava família case-sensitive mas o RmlUi passa em minúsculas (`ComputeFontFamily→ToLower`) → qualquer `@font-face` com maiúscula caía sempre na 1ª face (mis-render silencioso com 2+ famílias); fix = normalizar a família a minúsculas no `RegisterFace` (prova causal: repro maiúsculo resolve certo, contraprova revertida traz o bug de volta). **Superíndice validado renderizado** (`fonteng_sup_sanity`): `<span class="sup">` (`vertical-align:super`) eleva o dígito "2" 3.56px — solução do ² do GusWorld sem sprite (o PixelOperatorMono não tem o glifo ²/³/¹, confirmado por fontTools; tem × e todos os diacríticos pt-br). ****GusWorld deu luz verde pro flip SUAVE 2026-07-14** (validação Windows deles fechou): motor vira default MAS FreeType selecionável em runtime (SEM drop; reversível; rollback = toggle). Condição prévia deles: perf em máquina fraca. **Benchmark de perf entregue (`L1.20-PERFBENCH`, `919ae6f`, harness `fonteng_perf_bench` diagnóstico):** razão own/FreeType em bake/memória/layout-CPU/emit, HUD mutável a cada frame (anti-cache), PixelOperatorMono (GusWorld) + Open Sans (distribuição geral), 3 DPIs. Resultado: **PixelOperatorMono em paridade (flip seguro pro GusWorld); memória negligível; MAS Open Sans 165× mais lento no layout** — causa-raiz `glx_sfnt_kern` fazia varredura LINEAR sobre 18694 pares por par-de-glyph por frame. **FIX `FT-F4-KERN-BSEARCH` (`baeb15b`): busca binária** (pares ordenados por spec, chave `(left<<16)&#124;right`), postura (c) autônoma (assumir ordenação; bounds já pré-validados → fonte hostil degrada a miss cosmético, nunca UB/OOB); TDD com oráculo de equivalência dos 18694 pares vs linear-referência; review adversarial `security-engineer` EXECUTOU 7 ataques (desordenado/duplicata/nPairs-mentiroso/truncado/fuzz-250k/overflow-chave/equivalência) sob ASan+UBSan — zero UB/OOB/falso-positivo, aprovado; make test/sanitize-sfnt/test-negative/check-static + 10 fonteng verdes; orquestrador re-rodou o bench: **Open Sans layout own/ft 165×→1.42×** (gap fechado, motor viável pra distribuição geral). **O flip SUAVE (default ON + toggle runtime FreeType↔próprio, REVERSÍVEL) EM EXECUÇÃO, autorizado pelo líder 2026-07-14 -- Fase 1+2 entregues:** **Fase 1** (`ff0f0f7`): vendoriza a carne C do motor próprio em `glintfx/vendor/core/` (cópia estável, desacopla do dir `src/` da Camada 0) + `GLINTFX_OWN_FONT_ENGINE` vira default **ON** no `glintfx/CMakeLists.txt` -- motor próprio agora compila por padrão em AMBOS os motores no binário (`RMLUI_FONT_ENGINE=freetype` mantido, FreeType continua linkado). **Fase 2** (`6bfc2ff`, `engine-graphics-programmer`): API pública de seleção -- `enum class FontEngine { Own, FreeType }` novo (`glintfx/include/glintfx/font_engine.hpp`, nenhum símbolo `Rml::` vaza, confirmado por `tools/check_encapsulation.sh`) + campo `font_engine` (default `Own`) em `AppConfig`/`UiLayerConfig`, fixado na construção (sem setter runtime -- RmlUi só lê a `FontEngineInterface` uma vez); wiring em `Engine::attach()`/`Bootstrap::init()` com precedência `own_font_engine_ab_bypass()` (teste A/B interno) > `font_engine==Own` > fallback-com-aviso (`Rml::Log::LT_WARNING`, nunca crash) quando `Own` é pedido mas o build foi compilado com `GLINTFX_OWN_FONT_ENGINE=OFF`. Verificado: rebuild default limpo, 10/10 fonteng verdes, gate de encapsulamento OK, verificação manual scratch confirmando `UiLayer` constrói e `ok()==true` com `font_engine=Own` E `=FreeType` num build ON, e fallback-com-aviso confirmado num build isolado OFF. **Ainda pendente: Fase 3** (cobertura de teste formal do `qa-engineer` pra seleção pública -- os 10 fonteng existentes cobrem o motor, não a troca via config -- e o go/no-go final de tag minor) antes de considerar o flip SUAVE fechado. NÃO dropa o FreeType agora; o drop irreversível fica p/ muito depois. Sem mais bloqueadores técnicos.** Nada em produção mudou (glintfx `v0.9.1` publicada segue com o comportamento pré-flip; esta branch ainda não foi lançada). Branch `feat/ft-f4-fontflip` (github).  **FLIP SUAVE LANÇADO em v0.10.0 (2026-07-15).** Decisão do líder + 2 decisões de arquitetura dele: (A) seleção via campo `FontEngine font_engine = FontEngine::Own` no `App/UiLayerConfig` (fixo na construção, rollback = trocar o enum sem rebuild); (B) carne C da Camada 0 VENDORIZADA em `glintfx/vendor/core/` (default portável). 3 fases + release: Fase 1 vendorização + CMake default ON (`ff0f0f7`, `devops-sre`, prova de portabilidade: glintfx/ isolado sem `../src` compila), Fase 2 API pública `FontEngine` + wiring Bootstrap com fallback gracioso (`6bfc2ff`, `engine-graphics-programmer`), Fase 3 gate adversarial (`1ef8607`, `qa-engineer`: seleção pública funciona, **auditoria-dominó = ZERO oráculo quebrou**, matriz 4 cantos, fallback OK). Release `release-manager` (merge --no-ff sem conflitos, `e6030fb`, ADR-0011, CHANGELOG). Orquestrador re-verificou (default ON, suíte 49/49 na main) + fix de null-guard clang-tidy do CI (`49a4611`) + **tag `v0.10.0` push DUAL (codeberg+github), CI VERDE**. Motor próprio agora é o DEFAULT; FreeType linkado e selecionável via `font_engine=FontEngine::FreeType` (SEM drop — flip completo/drop fica pra futuro, ADR-0011). **ÉPICO COMPLETO E EM PRODUÇÃO.** Resta (do lado do GusWorld, sem pressa): ciclo de validação nos 3 DPIs pós bump pra v0.10.0. | Baixa | L1.19-FONTENG | Média | ✅ Concluído -- flip suave v0.10.0 (CI verde) | ✓ |
| A0-MODSKEL | FW-A0 | C++23/Build | Esqueleto modular de build da ADR-0015, sem API nova nem lógica C++/C tocada: `GLINTFX_MODULE_WINDOW` alias bidirecional de `GLINTFX_BACKEND_GLFW`; `GLINTFX_MODULE_APP` (gateia `app.cpp`+`RmlUi_Platform_GLFW.cpp`, FATAL_ERROR se app=ON/window=OFF); `GLINTFX_MODULE_IMAGE`/`GLINTFX_MODULE_TEXT` reservadas forçadas ON (FATAL_ERROR honesto em OFF); alvos OBJECT `glintfx_core`/`glintfx_image`/`glintfx_text`/`glintfx_window` + aliases `glintfx::*`, linkados PRIVATE no agregado `glintfx`; `CMakePresets.json` (`full`/`embed-only`). **Implementado 2026-07-19** (`bf7a53d`): build limpo + ctest 100% verde full (54/54) e embed-only (37/37), alias `GLINTFX_MODULE_WINDOW`↔`GLINTFX_BACKEND_GLFW` confirmado nos 2 sentidos, guardas FATAL_ERROR confirmadas, `tools/check_encapsulation.sh` OK, nenhum header público tocado. | Alta | ADR-0015 | Baixa | ✅ Concluído | — |
| A1-INPUT | FW-A1 | C++23/Input | Módulo input no `App` standalone (ADR-0015 (b) "input"; plano `docs/superpowers/plans/2026-07-19-framework2d-A1-input.md`): tradução `UiEvent`↔RmlUi sobe de `UiLayer::process_event` pra `Engine::process_event(ev,offx,offy)` (compartilhada, `UiLayer` mantém só o caso `Resize` local); captura física via 5 callbacks GLFW (`WindowGlfw::set_event_sink`) sobre funções PURAS testáveis sem janela (`glfw_event_translate.hpp`: mapa de tecla c/ `KP_ENTER`, mods explícitos, botão, **inversão de sinal do scroll** citando `RmlUi_Platform_GLFW.cpp:161-168`, **escala de cursor fb/win HiDPI** citando `:134-142`, UTF-8 encode); `App::process_event(const UiEvent&)` (canal sintético, paridade `UiLayer`, `Resize`=no-op documentado) + `App::set_frame_callback(fn(dt))` (hook de cena do jogo, GL corrente pós-clear pré-UI, `Engine::render_standalone` ganha `scene_hook` com `GlStateGuard` defendendo o passe de UI de estado GL deixado pelo hook; `App::snapshot()` também roteado pelo hook via helper `Impl::render_frame` compartilhado, mesma disciplina AUD-PUB-1-TWIN). Testes novos: `glfw_event_translate_sanity` (unit puro, sem janela, cobre os 2 gotchas de fórmula com asserção dedicada), `app_process_event_smoke` (clique sintético + focus+Enter emulado + Resize no-op), `app_frame_callback_smoke` (contador+dt sanos + perna de pixel provando composição sob a UI). Suíte **57 GLFW=ON / 37 embed** (+3/+0 vs. baseline A0 54/37), ambos os configs verdes sob Xvfb, `tools/check_encapsulation.sh` OK (nenhum tipo GLFW/Rml cruza `app.hpp`; `glintfx_window` ganhou include dir PRIVATE pro header público `ui_event.hpp` que `window_glfw.hpp` agora inclui). Limitação honesta registrada: disparo FÍSICO dos trampolins GLFW (tecla/mouse real) não é exercitável sob Xvfb sem XTest — mitigado por trampolins de 1 linha sobre funções 100% testadas + inspeção do reviewer. Implementer `backend-engineer`; **review adversarial (`qa-engineer`) APROVADO** (57/57+37/37 re-executado; 3 mutações mataram teste -- scroll/HiDPI/delegação; risco nº1 = vazamento de estado GL do hook MITIGADO por prova: probe agressivo muda blend/cull/depth/stencil/viewport no hook e o pixel da UI sai correto, o `GlStateGuard` restaura antes do `Context::Render()`). ✅ Lançado em `v0.12.0` (merge PR #4, `f57a17e`, CI dual verde, tag dual). | Alta | A0-MODSKEL | Média | ✅ Concluído (v0.12.0) | ✓ |
| I18N-1 | FW-I18N1 | C++23/i18n | Módulo i18n (ADR-0015 (b) "i18n", fatia mínima recomendada em `docs/superpowers/plans/2026-07-19-framework2d-i18n-module.md`, executada em paralelo à trilha A2+, branch `feat/i18n`): `GLINTFX_MODULE_I18N` (default ON, **real** ON/OFF — não reservado como `IMAGE`/`TEXT` no A0 — depende só de `core`, zero GL/janela/RmlUi, `glintfx_i18n` OBJECT sem NENHUMA dep de link) + `glintfx::I18n` (`glintfx/i18n.hpp`, sem pImpl — nenhum tipo de terceiro pra esconder). **Catálogo próprio zero-dep** (decisão do líder no doc de opções, não JSON): texto UTF-8, `[locale]`/`chave = valor`/`#comentário`, escapes mínimos `\\`/`\n`/`\t`, linha malformada = pula+conta (nunca aborta, `I18nLoadResult{ok,entries_loaded,lines_skipped}`) — formato documentado com exemplo no cabeçalho do header. **Plural CLDR-lite en/pt** (regras citadas do gráfico CLDR v48, fonte em `i18n.cpp`: en `one: i=1,v=0`; pt `one: i=0..1` — a divergência real testada por contagem em `i18n_plural_sanity`, não só presença), sub-chaves `.one`/`.other`, placeholder `#`→dígitos da contagem. **Troca de locale em runtime** sem recriar (`set_locale`), **ordem de resolução em 5 passos** (locale exato → idioma-base → fallback exato → idioma-base do fallback → chave crua) documentada e testada passo-a-passo. Alimenta a UI só via `bind_string`/`set_string` já existentes (zero acoplamento novo `i18n`→`ui`). 3 testes novos, PUROS (sem GL/janela/Xvfb, fora do bloco `GLINTFX_BACKEND_GLFW`, mesma classe do `glfw_event_translate_sanity`): `i18n_catalog_sanity`, `i18n_lookup_fallback_sanity`, `i18n_plural_sanity`. Suíte **60 GLFW=ON / 40 embed** (+3/+3 vs. baseline A1 57/37), ambos verdes sob Xvfb (60/60 e 40/40, `-j1` — a `-j4` mostrou 8 falhas de contenção GL/Xvfb em testes PRÉ-EXISTENTES não relacionados a i18n, reproduzido não-flaky em `-j1`); `tools/check_encapsulation.sh` OK; `-DGLINTFX_MODULE_I18N=OFF` confirmado remover código+símbolos (`nm libglintfx.a` sem ocorrência de `I18n`). Implementer `backend-engineer`; **review adversarial (`qa-engineer`) APROVADO** (mutações matam o teste no boundary n=0 do plural pt e no fallback; atomização provada `OFF`→sem símbolo; CLDR v48 conferido) + **2 achados reais fechados antes do merge** (UB de `negation of LLONG_MIN` em `plural_category_for` → abs seguro via unsigned, UBSan-limpo; `load_catalog_file` distinguir diretório de arquivo-vazio via `peek()`) + fix de `cppcheck` lint (shadowFunction do param `locale` + laços crus → STL, CI-red do PR #5 pego pelo gate). **✅ Lançado em `v0.13.0`** (merge PR #5, `e5ac06b`, CI dual verde, tag dual). | Média | A0-MODSKEL | Média | ✅ Concluído (v0.13.0) | ✓ |
| FX-CARVE-1 | S2 (plano `docs/superpowers/plans/2026-07-20-framework2d-A3-audio-e-carving-uifx.md`) | C++23/Build+FX | Carving do fx AUTORAL da glintfx pra fora da `ui` (ADR-0015 (c), fronteira honesta narrowed em vez de removida): os programas GLSL "glintfx-tint"/"glintfx-ripple" + o despacho `CompileShader`/`RenderShader`/`ReleaseShader` (`GlintfxShaderHandle`) + a maquinaria de captura de backdrop (`ArmBackdropCapture`/`EnsureBackdropCaptured`, ADR-0010/ADR-0012), movidos VERBATIM de `render_gl3.cpp` pra `src/fx/effects_gl3.cpp` (novo, classe `Gl3FxHook`), atrás de uma costura seam nova `src/fx_hook.hpp` (`FxHook` abstrato, `CreateGl3FxHook()`); `Gl3RenderInterface` ganha `std::unique_ptr<FxHook> fx_` (construído sob `#if GLINTFX_MODULE_FX` no ctor, invariante fx=ON→todo handle é wrapper / fx=OFF→todo handle é cru, nunca misto) + método `arm_backdrop(...)` que substitui a chamada direta a `ArmBackdropCapture`. `bootstrap.cpp` gateia os 3 `#include` de decorator + os 3 membros do `Impl` + o bloco de construção/registro sob o mesmo `#if`. CMake: `option(GLINTFX_MODULE_FX ON)` (mesmo padrão OBJECT-lib de `glintfx_i18n`/`glintfx_window`), remove os 3 `decorator_*.cpp` de `_glintfx_sources`, `glintfx_fx` OBJECT (effects_gl3.cpp + os 3 decorators; PRIVATE RmlUi::Core+glloader; PUBLIC include public+binary; PRIVATE `${_rml_backends}`), fold condicional no agregado; `config.hpp.in` ganha `GLINTFX_MODULE_FX`; `CMakePresets.json` ganha preset `lean-ui` (`GLINTFX_MODULE_FX=OFF`). Nota "ui/fx: NOT introduced in A0" do CMakeLists.txt atualizada — só "ui" segue reservada, fx tem gate real agora. **Auditoria-dominó (achado não previsto no plano original):** `echo_sanity` (L1.21-ECHO) NÃO estava na lista dos "5 testes de fx" do plano mas é construído inteiramente sobre `decorator: image-tint(...)` (confirmado via grep em `echo_scene.rcss`) — gateado junto (6 testes fx no total: `polygon_sanity`/`polygon_gradient_sanity`/`image_tint_sanity`/`echo_sanity`/`ripple_sanity`/`ripple_stress_sanity`, todos BYTE-INALTERADOS). Segundo achado: `asset_decode_hostile_sanity` (não-fx, sempre compilado) dependia de `image_tint_fixture.png` copiado só pelos blocos agora-gateados de `image_tint_sanity`/`ripple_stress_sanity` — corrigido com uma cópia `configure_file` incondicional dedicada, comentada, antes do alvo (regressão silenciosa em lean-ui que teria escapado do plano). Baseline pré-carve capturado via `git stash` (HEAD `main`, pré-FX-CARVE-1) antes de qualquer edição de source, suíte completa **60/60 verde** sob Xvfb -- prova de referência pixel-equivalente. Ver relatório do agente pra logs dos 4 gates (full/lean-ui/embed/lint) + prova `nm`. **Review adversarial (`qa-engineer`) APROVADO** (regressão visual provada por diff byte-idêntico dos 6 testes fx + prova `nm` 0-símbolos em lean-ui; carve = só transformação mecânica de costura `this->`→`ri.`, zero mudança algorítmica/GL). **✅ Lançado em `v0.14.0`** (PR #6, merge `b4b1d45`, CI dual verde incl. Windows/MSVC + ASan 63/63). | Alta | A0-MODSKEL | Média | ✅ Concluído (v0.14.0) | ✓ |
| A3-AUDIO | S1 (plano `docs/superpowers/plans/2026-07-20-framework2d-A3-audio-e-carving-uifx.md`) | C++23/Audio | Módulo audio (ADR-0015 (b) "audio"), mesma classe de independência do i18n: `GLINTFX_MODULE_AUDIO` (default ON, real ON/OFF, depende só de `core`) + `glintfx::Audio` (`glintfx/include/glintfx/audio.hpp`, pimpl, zero tipo miniaudio no header) sobre o [miniaudio](https://github.com/mackron/miniaudio) `0.11.25` vendorizado e pinado (tag+SHA+data+SHA-256 em `third_party/miniaudio/README.md`). **RAII determinístico** (a dor de lifecycle do GusWorld, corrigida por construção): `shutdown()` idempotente destrói TODO `ma_sound` ANTES do `ma_engine_uninit()`; destrutor chama `shutdown()`; toda operação antes de `init()`/depois de `shutdown()` retorna `false`/`0`; re-init pós-shutdown suportado; `SoundId` monotônico nunca reusado na vida da instância. `load_sound()` faz decode SÍNCRONO completo (`MA_SOUND_FLAG_DECODE`) — real-time-safe por construção, zero callback nosso na audio thread (WAV/FLAC/MP3 de graça; OGG adiado, precisa `stb_vorbis`). `set_volume`/`set_master_volume`: `!isfinite`/negativo rejeitado mantendo o anterior, clamp `[0,1]`. `AudioConfig::null_backend` para CI headless (backend `ma_backend_null`, contexto próprio). Rule-of-5 explícito (copy+move `=delete`, achado do clang-tidy `cppcoreguidelines-special-member-functions` fechado antes do build). **Achado crítico (fora do escopo original):** heap-use-after-free GENUÍNO no próprio miniaudio 0.11.25 (`ma_resource_manager_data_buffer_node_acquire`, epílogo desreferencia `pDataBufferNode` já `ma_free()`'d no ramo de falha síncrona — dispara em TODO `load_sound()` de arquivo ausente/hostil), capturado ao vivo sob ASan standalone (fora do CMake, antes da fase B), isolado de código glintfx com repro mínimo, corrigido com patch de 1 linha (guarda `result == MA_SUCCESS &&`) documentado inline + em `third_party/miniaudio/README.md` (mesma disciplina de divulgação do patch RmlUi em `patches/README.md`; reportar upstream pendente de autorização do líder). 3 testes novos PUROS (sem GL/Xvfb, `null_backend=true`, fora dos blocos GLFW/xvfb em `tests/CMakeLists.txt`, espelho do i18n): `audio_lifecycle_sanity` (estado init/shutdown/idempotência/re-init/init-2x), `audio_playback_sanity` (WAV PCM16 gerado EM CÓDIGO sem fixture binária; load/play/stop/volume+hardening), `audio_hostile_sanity` (WAV truncado header/data, lixo aleatório, vazio, data-size mentiroso, `nullptr` → sempre `0`/`false`, nunca crash). Wiring completo: `config.hpp.in` (`GLINTFX_MODULE_AUDIO`), `glintfx.hpp` (include condicional de `audio.hpp`, mesmo padrão do i18n), OBJECT lib `glintfx_audio` (Threads::Threads+CMAKE_DL_LIBS+m PRIVATE — miniaudio faz dlopen de ALSA/PulseAudio em runtime, zero dep de link rígida — mesmo espírito do glloader; `-w` só no `miniaudio_impl.c` via `set_source_files_properties`), fold no agregado, `NOTICE` (entrada miniaudio MIT-0/domínio público + nota do patch), `CHANGELOG.md` `[Unreleased]`. **Gates fase B (orquestrador re-verificou):** build full limpo (`clang`/`clang++`, `-DGLINTFX_BUILD_TESTS=ON`) sem warning; suíte completa **63/63 verde** sob Xvfb `-j1` (60 baseline pré-audio + 3 novos); leg `-DGLINTFX_MODULE_AUDIO=OFF` compila limpo, os 3 testes de áudio AUSENTES do ctest (60 registrados), prova `nm libglintfx.a &#124; grep -Ei "glintfx.*Audio&#124;ma_engine&#124;ma_sound_init"` = **0 matches** (full = **114 matches**); `clang-tidy -p <build> glintfx/src/*.cpp` (invocação idêntica ao job `lint-and-scan`): `audio.cpp` = **0 achados `clang-analyzer-*`** (confirmado 3x isolado) — só `modernize-*`/`cppcoreguidelines-*` não-gateados (mesma classe de ruído sempre-verde do resto do arquivo); os 2 achados `clang-analyzer-security.ArrayBound` do run full-project vêm de `bootstrap.cpp`/`decorator_image_tint.cpp`/`decorator_polygon.cpp` (RmlUi `TypeConverter.inl`, pré-existente, NÃO relacionado a A3-AUDIO, confirmado isolando cada TU); `cppcheck` (invocação idêntica ao yml, `--project=compile_commands.json`, `--error-exitcode=1`) = **0 achados**. Sem `lean-ui`/`embed-only`/ASan-container nesta fatia (fora do escopo pedido pra fase B, ficam com S3 (matrix completa: full 63/63, lean-ui 57/57, audio-off 60/60, embed 43/43, **ASan-f42 63/63 zero-findings**). **Review adversarial (`qa-engineer`) APROVADO** (desmentiu o falso-alarme do S1 sobre o mutation-test — a suíte PEGA o teardown errado; patch UAF do miniaudio validado real revertendo→reproduzindo→reaplicando; 1 achado não-bloqueante do ponto cego do ASan documentado inline). **✅ Lançado em `v0.14.0`** (PR #6, merge `b4b1d45`, CI dual verde incl. Windows/MSVC — o job win-atoms pegou 1 bug real de portabilidade `wchar_t`/`char` do `filesystem::path`, corrigido). | Alta | A0-MODSKEL | Média | ✅ Concluído (v0.14.0) | ✓ |
| A2-GAMEPAD | FW-A2 (plano `glintfx/docs/superpowers/plans/2026-07-21-framework2d-A2-gamepad.md`) | C++23/Input | Módulo gamepad (ADR-0015 (b) "gamepad", refinado pelo **ADR-0016** — átomo próprio `GLINTFX_MODULE_GAMEPAD`, `GLINTFX_MODULE_INPUT` segue reservado pro backend teclado/mouse futuro): `glintfx::Gamepads` (`glintfx/include/glintfx/gamepad.hpp`) via evdev CRU — **zero dependência nova** (sem libSDL/libudev/libevdev) — depende SÓ de `core`, mesma classe de independência do audio/i18n; **Linux-only por natureza** (gate CMake força OFF fora de Linux, `message(STATUS ...)`, não FATAL). 3 camadas: **decode** puro (`gamepad_decode.{hpp,cpp}`, reassemblagem de `input_event` de 24B, `SYN_DROPPED`, normalização por absinfo), **mapping** puro (`gamepad_mapping.{hpp,cpp}`, GUID formato SDL via CRC16 do nome, derivação de índice compatível-SDL, parser hostile-safe do `gamecontrollerdb.txt`, default kernel-spec), **device+API pública** (`gamepad.cpp`, scan+ioctls+EACCES gracioso+hotplug `inotify` `IN_CREATE`/`IN_ATTRIB`/`IN_DELETE` — `IN_ATTRIB` pega a corrida de permissão do udev — `poll()` por frame). 8 pads máx (`kMaxPads`), superfície CRUA completa exposta além do layout normalizado (decisão do líder: não restringir aos botões padrão), `load_mappings_file/text` pra override em runtime, `SYN_DROPPED` resincronizado via `EVIOCGKEY`/`EVIOCGABS`. **Banco de mapeamento vendorizado** (D6, subconjunto Linux do `SDL_GameControllerDB` — 728 entradas, commit `8d9fefd7b8`, licença zlib — `glintfx/third_party/gamecontrollerdb/`, gerador `tools/gen_gamepad_db.py` no padrão offline do `gen_glloader.py`, emite `gamecontrollerdb_linux.inc` ~200 KB de `.rodata` quando ON; **D6 e o átomo próprio `GLINTFX_MODULE_GAMEPAD`/ADR-0016 (D1) CONFIRMADOS RETROATIVAMENTE pelo líder em 2026-07-21**, liberando o fechamento como `v0.15.0`). Execução em 4 agentes (`backend-engineer` decode+mapping onda 1 ‖ `devops-sre` dado+docs+ADR onda 1 paralelo ‖ `backend-engineer` device+API onda 2 serializada ‖ `qa-engineer` review adversarial que executa onda 3). Doc `docs/gamepad.md` (contrato, permissões grupo `input`/regra udev, degradação graciosa, mapeamento, limites honestos: sem rumble/FF — D9 sem deadzone imposta, D10 sem integração `App` ainda, D11 sem preset novo). CI: zero mudança de workflow — glob recursivo `src/**/*.cpp` do `lint-and-scan` + cppcheck via `compile_commands.json` cobrem `src/gamepad*.cpp` automaticamente; `windows-atoms.yml` INTOCADO (path-filters explícitos não casam nenhum arquivo novo, harness `tests/win-atoms/CMakeLists.txt` compila lista explícita sem gamepad, gate `CMAKE_SYSTEM_NAME` desliga o módulo limpo num build Windows hipotético) — **verificação de conformidade confirmada pelo Agente C (devops-sre), sem alterar nenhum workflow.** Onda 1 (Agente C: dado vendorizado + gerador + doc + ADR-0016 + NOTICE/CHANGELOG/TODO + verificação CI) ‖ onda 1 paralela (Agente A: decode+mapping+CMake) ‖ onda 2 (Agente B: device+API pública) **todas entregues**; onda 3 (Agente D: review adversarial que executa) **APROVADO** (1 achado MAJOR de `cppcheck` corrigido e re-verificado). Review adversarial (Agente D) pegou 1 MAJOR de `cppcheck` (`useStlAlgorithm`) + 1 achado de CI no PR (**falso-positivo do `gitleaks` em `docs/gamepad.md`**, resolvido com reword + `.gitleaksignore` — o gitleaks escaneia o histórico inteiro), ambos corrigidos e re-verificados verdes pelo orquestrador. **✅ Lançado em `v0.15.0`** (PR #7, merge `a934ea7`, CI dual verde). | Alta | A0-MODSKEL | Média | ✅ Concluído (v0.15.0) | ✓ |
| FX-CARVE-2 | — | C++23/Build+FX | **ADIADA (decisão 2026-07-21, parecer do CTO, ver adendo da [ADR-0015](docs/adr/0015-framework-2d-atomized-architecture.md)).** Completaria o recorte do fx AUTORAL pra fora da `ui` removendo, no preset `lean-ui` (`GLINTFX_MODULE_FX=OFF`), também os shaders de filtro do backend UPSTREAM do RmlUi (`Backends/RmlUi_Renderer_GL3.cpp`). **Reenquadramento crítico:** esses "filtros" incluem `linear-gradient`/`radial-gradient`/`mask-image` — estilização RCSS PADRÃO, não efeito autoral da glintfx; removê-los de um preset chamado lean-*ui* seria regressão de feature de UI, não atomização (a FX-CARVE-1 já traçou a fronteira certa: `fx` = autoral). Nenhuma via boa: (a) RmlUi sem option nativa pra desligar filtros (renderer monolítico, ~10 shaders eager em `Gfx::CreateShaders()`); (b) `PATCH_COMMAND` seria fork de ~1000 linhas re-resolvido a cada bump (o "fork silencioso" que `patches/README.md` proíbe); (c) interceptar na camada glintfx (~50 linhas) economiza 0 bytes (shaders continuam linkados). ROI não fecha: ~25-30 KB (< 1% de binário stripped) + ~3ms de init, contra manutenção perpétua de fork + risco de regressão visual em todos os filtros. **Gatilhos de reabertura:** (i) consumidor real do `lean-ui` com requisito de footprint com número (hoje nenhum; GusWorld usa fx FULL); (ii) upstream do RmlUi aceitar uma option de build mínimo (`RMLUI_GL3_MINIMAL` ou similar) — caminho sustentável, via feature-request, se (i) disparar. | Baixa | FX-CARVE-1 | Média | 💡 Decisão tomada (adiada) | — |
| A4-WINMODES | FW-A4 (plano `glintfx/docs/superpowers/plans/2026-07-21-framework2d-window-modes.md`) | C++23/App | Módulo de modo de janela no `glintfx::App` standalone, alvo `v0.16.0` (`UiLayer`/embed INTOCADO -- o host é dono da janela lá, ADR-0008): enum próprio `WindowMode` (`Windowed`/`Maximized`/`FullscreenDesktop`/`FullscreenExclusive`, `include/glintfx/window_mode.hpp`, sem tipo de terceiro) + `AppConfig::window_mode` (modo inicial) + runtime `App::set_window_mode()`/`window_mode()`/`get_window_size()`. **Origem consumer-driven (GusWorld, via o bus):** janela maximizada invadia a barra de tarefas do KDE -- causa-raiz NOSSA (`App` não tinha NENHUM tratamento de modo de janela). `Maximized` = `GLFW_MAXIMIZED` (hint pré-create/`glfwMaximizeWindow` runtime), o WM aplica os próprios struts de painel DE GRAÇA (nunca dimensionamos pro monitor à mão) -- é O fix. `FullscreenDesktop` cobre tudo, painel incluso, BY DESIGN (borderless no video mode atual do monitor); `FullscreenExclusive` troca o video mode de verdade pro mais próximo de `width`×`height`. `window_mode()` é query VIVA derivada do window system (`glfwGetWindowMonitor`/`glfwGetWindowAttrib(GLFW_MAXIMIZED)`), não eco do último pedido (o usuário pode maximizar/restaurar pelo botão do WM). Runtime switch ENTRA na fatia (D1, table-stakes de jogo/toggle de menu) com máquina de transição que salva/restaura geometria windowed (D7). Ctor do `App` passa a atachar com o tamanho REAL do framebuffer pós-`create()` (não mais `cfg.width/height` cru), já que janela `Maximized`/fullscreen tem framebuffer≠cfg no frame 0 (D6, de quebra fecha descasamento latente de HiDPI). Fail-high (D5): enum inválido/`!ok()` → `false` no-op; `AppConfig.window_mode` inválido → fallback `Windowed` documentado, `create()` nunca falha por isso. **Limites honestos documentados, não escondidos:** Wayland -- `FullscreenExclusive` é EMULADO pelo compositor (sem mode-set real, comportamento GLFW documentado), posição de restauração best-effort (D10); só monitor primário nesta fatia (D2, multi-monitor → INBOX). Testabilidade partida headless×manual: sob Xvfb não há WM/painel, então "maximize respeita a work area" NÃO é verificável por máquina -- o teste headless (`tests/app_window_mode_smoke.cpp`, parametrizado por env var, 5 corridas: 4 modos + `invalid_config`) trata o caso `maximized` como tolerante (aceita `Maximized` OU `Windowed`); o real é provado por smoke manual numa sessão KDE/GNOME real (roteiro em `docs/window-modes.md`). `tests/app_window_mode_transitions_smoke.cpp` cicla os 4 modos + re-pedido no-op + enum inválido. Execução em 3 agentes (Onda 1: `backend-engineer` código+testes ‖ `technical-writer` docs/CHANGELOG/TODO, posse disjunta; Onda 2: `qa-engineer` review adversarial que executa + smoke manual). Doc `docs/window-modes.md` (os 4 modos, distinção Maximized×FullscreenDesktop em destaque, notas de plataforma, roteiro de verificação manual). CI: zero mudança de workflow (`windows-atoms.yml` INTOCADO -- App/janela fora do harness win-atoms). **Review adversarial (`qa-engineer`) rodou o smoke na sessão KDE/Wayland REAL e pegou 1 MAJOR que o Xvfb não pega** (race onde a janela travava maximizada pra sempre — a guarda de no-op confiava num attrib assíncrono do GLFW); corrigido (livro-razão síncrono `dispatched_mode_`, `mode()` público segue vivo/D4) + 1 race de compositor auto-pego (`settle` limitado) + guarda D5 de width negativo (era pré-existente). **✅ Lançado em `v0.16.0`** (PR #8, merge `fb15102`, CI dual verde, 77/77 GLFW=ON + smoke 3/3 real). Sementes INBOX: multi-monitor, resizable/borderless, cobertura do D6. | Alta | A0-MODSKEL | Média | ✅ Concluído (v0.16.0) | ✓ |
| A4-EMOJI | FW-A4 (plano `glintfx/docs/superpowers/plans/2026-07-22-framework2d-A4-emoji-colr.md`) | C+C++ / font engine | Emoji colorido **COLRv0** (vetorial) no motor de fonte próprio clean-room. Parser `COLR` v0 + `CPAL` na **Camada 0 soberana zero-libc** (`src/sfnt.c`/`include/core/sfnt.h`, `glx_sfnt_colr_layers`/`glx_sfnt_cpal_rgba`, mesma postura de hardening do `kern`, 7 casos hostis C1, espelho vendor byte-idêntico) + bake colorido no `font_engine_own` (atlas já era RGBA8 → sem mudança de shader/render; `is_color`, vértice branco `(a,a,a,a)`, skip VS15/16, prova de pixel). Review adversarial (S3) rodou 8 mutações de COLR/CPAL no motor completo sob ASan+UBSan no container f42 (zero crash/UAF/leak) + 50 reloads; resolveu que o timeout de `fonteng_own_sanity`/`app_*` era **ambiental** (acúmulo de Xvfb, 78/78 em sessão fresca). **Escopo: cobre Twemoji-classe (COLRv0), NÃO a Noto moderna** (COLRv1/CBDT adiados com gatilho, decisão do líder). **✅ Lançado em `v0.17.0`** (yolo autônomo, S1→S2→S3, PR + tag por julgamento). Sementes INBOX: **COLRv1** (gradientes/transforms, mini-renderizador vetorial, adiado -- decisão do líder 2026-07-22, ver `glintfx/docs/superpowers/plans/2026-07-22-programa-completo.md` §6) + **CBDT/CBLC** (bitmap, cobre Noto, stb já vendorizado -- confirmado na Onda 9 do mesmo plano, gated ao go do líder) + **shaping ZWJ/tom-de-pele/bandeira** (precisa GSUB, adiado, mesmo plano). Os outros 2 achados do S3 **entregues na Onda 1 do PROGRAMA-COMPLETO** (`QW-COLRDOC`/`QW-MKDEP`, `90d325a`, PROG-1 · ✅ **Pendente verificação**): doc-comment da bisecção COLR refinado (`src/sfnt.c`, distingue corrupção por-registro de corrupção por-campo) + `-MMD -MP` no Makefile da Camada 0 (header-dependency tracking nas duas árvores de objeto). | Média | A0-MODSKEL | Alta | ✅ Concluído (v0.17.0) | ✓ |
| AUDIO-F2 | FW-AUDIO2 (plano `glintfx/docs/superpowers/plans/2026-07-22-framework2d-audio-F2.md`) | C++23/Audio | Quatro comportamentos consumer-driven (GusWorld, migrando do miniaudio direto pro `glintfx::Audio`) somados ao módulo audio (`A3-AUDIO`), retrocompatíveis em nível de fonte: **loop gapless** (`play(id, loop)`/`set_looping(id, loop)`, só na instância PRIMÁRIA), **fade in/out** (`play(id, loop, fade_in_s)`/`stop(id, fade_out_s)` via o fader nativo do miniaudio -- reset obrigatório de stop/fade agendado ANTES do restart, senão um `stop` velho mata a reprodução nova em silêncio; `is_playing` continua `true` de propósito durante um fade-out), **polifonia de SFX** (`play_oneshot(id)`, cópia via `ma_sound_init_copy`, fire-and-forget, nunca loopa, reap preguiçoso só de dentro do próprio `play_oneshot` -- sem timer/thread de fundo, não corre com `shutdown()` --, teto de 32 cópias vivas por id com voice-stealing da mais antiga), e **`is_playing`/`voice_count`** (novos, `const`, consideram primária + cópias vivas). `set_volume`/`stop_all` estendidos às cópias. Hardening: `fade_in_s`/`fade_out_s` validados ANTES do cast pra ms (a classe de bug da auditoria-dominó), rejeitados se não-finito/negativo, clampeados em `[0, 3600]` s. **Dobra também** um fix de portabilidade Windows/MSVC no mesmo release: os 9 `target_compile_options(... -Wall -Wextra)` do `glintfx/CMakeLists.txt` quebravam o `cl.exe` (`D8021`) desde ~v0.16.0 -- regressão real pega pelo CI Windows do consumidor GusWorld, não pelo nosso; corrigido com generator expression `$<$<NOT:$<CXX_COMPILER_ID:MSVC>>:-Wall;-Wextra>` nos 9 pontos + job de CI novo `windows-msvc-build` (builda o `CMakeLists.txt` PRÓPRIO no `windows-latest`/MSVC via vcpkg pinado, cobre 6 dos 9 alvos -- `glintfx_window`/GLFW fica de fora por `GLINTFX_BACKEND_GLFW=OFF` deliberado, `gamepad` já é Linux-only). Doc `docs/audio.md` (as 4 features + contrato de threading + aviso de migração ODR: consumidor com `MINIAUDIO_IMPLEMENTATION` própria DEVE apagar a TU ao ligar `GLINTFX_MODULE_AUDIO=ON`, senão símbolo `ma_*` duplicado/erro de link). Execução em 3 agentes (onda única: `backend-engineer` implementação + 5 testes/2 CMakeLists ‖ `qa-engineer` review adversarial que EXECUTA -- ASan/LSan, mutação de reap/shutdown, UAF/leak com loop+fade+cópias vivos no shutdown, floats hostis, spam de oneshot, spot-check das chamadas miniaudio contra a definição vendorizada (incl. armadilha do parâmetro com nome errado na declaração) ‖ `technical-writer` docs/release/relay). Bump de versão `0.17.0`→`0.18.0` no CMake (`version()` acompanha, classe de bug da v0.2.5). **Review adversarial (`qa-engineer`) APROVADO** (5/5 sob ASan+LSan+UBSan; 4 mutações — o cap de vozes e o hardening de fade são pegos pelos testes; probes extras de UAF/leak limpos: dtor sem shutdown, double-shutdown, re-init, mid-fade). **Dobrado com o fix de portabilidade MSVC** (regressão que bloqueava o consumidor): guard de `-Wall -Wextra` nos 9 targets (D8021) + **`NOMINMAX`/`WIN32_LEAN_AND_MEAN` em escopo de diretório** (a varredura dominó achou **21 sites de min/max em 4 arquivos** — `font_engine_own.cpp` sozinho tem 14 — provando que o fix sistêmico era o certo) + **job de CI novo `windows msvc build`** que builda o CMakeLists PRINCIPAL no MSVC, fechando o gap que deixou a regressão passar (o `win-atoms` só compila fontes de átomos puros). O job novo pegou o 2º bug MSVC na 1ª execução. **✅ Lançado em `v0.18.0`** (PR #10, merge `c5f2c3b`, CI dual verde incl. MSVC). | Alta | A3-AUDIO | Média | ✅ Concluído (v0.18.0) | ✓ |
| LOGTHR-1 | Onda 2 (plano `glintfx/docs/superpowers/plans/2026-07-22-onda2-input-host.md`, seção D8, brief 7.2) | C++23/Observabilidade | **Dedup/throttle do flood de warning do RmlUi na camada `SystemInterface` do glintfx, nos dois modos (App e embed)** -- achado do consumidor (GusWorld): `Rml::SystemInterface::LogMessage` imprime "No font face defined. Missing 'font-family' property. On element ..." (`ElementText.cpp:86`, upstream do RmlUi, PINADO -- não remendado) uma vez por elemento de texto por passe de layout re-formatado; medido em milhares de linhas numa sessão headless de 60s. Fatia do implementer B (agente `backend-engineer` DIFERENTE do implementer A de HOSTIN-1..5), rodou depois de A (colisão de 1 linha em `app.cpp`). Novo `src/log_dedup.hpp` (`class LogDedup`, PURO -- zero `#include` de RmlUi/GLFW, testável isolado como `input_state.hpp`/`glfw_event_translate.hpp`): `should_print(type, message, is_error, out_line) → bool` com política determinística -- 1ª ocorrência de um par (type,message) sempre imprime verbatim; repetidas são suprimidas; sumário reemitido nas contagens EXATAS 10/100/1000/... ("`<mensagem> (repeated Nx, suppressed)`"); `is_error` (LT_ERROR/LT_ASSERT no ponto de chamada) IGNORA o dedup incondicionalmente (nunca suprimido, nunca rastreado); tabela com teto LRU de 256 entradas distintas (defesa contra stream hostil de mensagens sempre-diferentes, nunca cresce sem limite). Dois pontos de override, ambos delegando ao mesmo helper puro (a tradução `Rml::Log::Type`→`int`+`is_error` acontece no override, não no helper): `SystemClock::LogMessage` (embed, `src/system_clock.hpp`) e novo `src/system_glfw_dedup.hpp` (`class SystemInterfaceGlfwDedup`, subclasse fina de `SystemInterface_GLFW` upstream, só acrescenta o override; `app.cpp` troca sua única linha de construção pra este tipo -- GLFW=ON only). Testes: novo `log_dedup_sanity` (PURO, registrado INCONDICIONALMENTE como `input_state_sanity`: 1ª-impressão, supressão 2..9, fronteira 10/100/1000 exata -- 20/30 NÃO disparam --, independência entre mensagens e entre types com o mesmo texto, bypass de `is_error` mesmo através de 50 repetições sem poluir a tabela de uma msg não-erro com o mesmo texto, teto LRU sob 10 mil mensagens hostis distintas com capacidade 8, mensagem vazia/1MB seguras); novo `log_throttle_embed_sanity` (integração, roda nos 2 configs via `${_embed_dep}` como `ui_layer_sanity`: cena `log_throttle_scene.rml` SEM font-family em lugar nenhum com label ligado a um contador de data-model mutado a cada iteração -- um label ESTÁTICO não reproduz o flood real, confirmado empiricamente ao escrever o teste, já que RmlUi só reformata texto dirty -- update()+render() 100x, stderr capturado via `dup`/`freopen`/`dup2` (só-POSIX, seguro porque .cpp de teste nunca compila no job MSVC, que roda com `GLINTFX_BUILD_TESTS=OFF`), assert ≤10 linhas). **Prova numérica da redução** (medida ao vivo, bypass temporário do dedup revertido antes do commit): mesmo loop de 100 frames produz **200 linhas cruas SEM o fix** vs **3 linhas COM o fix** (1ª ocorrência + sumário@10 + sumário@100) -- reducão de ~98,5%. Suíte **84/84 GLFW=ON / 56/56 embed** sob Xvfb (+2/+2 vs. baseline HOSTIN-1..5 82/54); `tools/check_encapsulation.sh` OK (nenhuma mudança de API pública, LOGTHR-1 é só `src/`); `clang-tidy -p glintfx/build-lint` em `app.cpp`+`system_clock.cpp` = 0 achados `clang-analyzer-*` (só `modernize-*`/`cppcoreguidelines-*` pré-existentes não-gateados); `cppcheck` (invocação idêntica ao `lint-and-scan`) = 0 achados nos arquivos tocados; `gitleaks detect --source .` limpo; `clang-format-diff`/`clang-format --dry-run --Werror` limpos (arquivos novos + linhas tocadas de `app.cpp`/`system_clock.hpp`). MSVC-safe: `log_dedup.hpp`/`system_clock.hpp` (compilados pelo job MSVC embed, `GLINTFX_BUILD_TESTS=OFF`) usam só stdlib portável (`<list>`/`<unordered_map>`/`<string>`), zero `min`/`max` cru, zero `std::filesystem::path::c_str()`; `system_glfw_dedup.hpp` é GLFW=ON only (não compilado por aquele job, mas mantido portável por disciplina); os `.cpp` de teste usam `dup`/`dup2`/`fileno` (só-POSIX), seguro pois não são compilados pelo job MSVC. Implementer `backend-engineer`; review adversarial (`qa-engineer`) e re-verificação do orquestrador **AINDA PENDENTES** nesta branch. | Alta | HOSTIN-1..5 | Média | ✅ | — |
| HOSTIN-1..5 | Onda 2 (plano `glintfx/docs/superpowers/plans/2026-07-22-onda2-input-host.md`) | C++23/Input | **Canal de input físico pro host + ciclo de vida da janela no `App` standalone** (bloqueio de produção do GusWorld: sem isto o jogo não lia WASD/mouse físicos, só linkando GLFW por conta própria). Fatia do implementer A (agente `backend-engineer`); LOGTHR-1/DOC-HOSTIN são de outros agentes da mesma onda, ainda não mesclados nesta branch. **HOSTIN-1** (canal de ESTADO): `enum class Key` estendido APPEND-ONLY (D1, ~92 entradas -- A-Z/Digit0-9/F1-F12/modificadores L-R/locks/pontuação/World1-2/numpad -- + sentinela `Count`, valores antigos `None..PageDown` congelados por `static_assert`) + `src/input_state.hpp` (struct pura `InputState`, fail-high, zero GLFW) + tap nos `handle_key`/`handle_mouse_button`/`handle_cursor_pos` de `WindowGlfw` (estado atualizado ANTES do sink) + `App::is_key_down`/`is_mouse_button_down`(`[0,7]`, D3, SEM restringir a 3 botões -- teto honesto do GLFW documentado, mouse de 12 botões é seed `SEED-MOUSE9PLUS`)/`get_cursor_pos`. **HOSTIN-2**: `enum class KeyAction{Press,Release,Repeat}` + `App::set_key_callback` (mesmo tap, cópia-antes-de-invocar AUD-TEC-3). **HOSTIN-3**: `App::request_close()` (bypassa o veto, D6). **HOSTIN-4**: `App::set_close_request_callback` (veto só do close INICIADO PELO USUÁRIO; decisão pura extraída `glfw_decide_window_close()` em `glfw_event_translate.hpp`, testável isolada). **HOSTIN-5**: `set_window_focus_callback`/`set_window_iconify_callback`/`is_window_focused`/`is_window_iconified` (prefixo `window_` pra não colidir com o foco de elemento DOM já existente; honestidade headless sob Xvfb documentada). **D10 preservado**: roteamento `UiEvent`→RmlUi INALTERADO -- só o subconjunto pré-existente de 14 teclas de nav é encaminhado à UI (`glfw_key_is_ui_forwardable()`, testado). Layout ABNT2 (D4, requisito do líder): tokens são de POSIÇÃO física (WASD agnóstico de layout); `ç` chega como posição `Semicolon`; o `/` extra do ABNT2 (scancode 97) é lacuna conhecida do GLFW, documentada + seed `SEED-SCANCODE`. Testes: `glfw_event_translate_sanity` estendido (tabela completa + static_asserts de congelamento + `glfw_key_is_ui_forwardable` + `glfw_decide_window_close`), novo `input_state_sanity` (puro, roda nos 2 configs, hostil: Key/botão fora de faixa nunca UB), novo `app_input_query_smoke` (Xvfb: fresh App false/(0,0), `process_event` sintético NÃO vaza pro estado físico -- D5 --, `request_close()`→`running()==false`→`run()` retorna sem travar, callbacks registráveis pré/pós `load()`). **Downgrade honesto registrado** (válvula de escape do próprio plano): o e2e do veto via `XSendEvent WM_DELETE_WINDOW` real NÃO foi implementado (Xvfb não tem WM pra gerar o close de verdade); o seam puro (`glfw_decide_window_close`) cobre a lógica, o ponta-a-ponta fica pra perna manual de QA em compositor aninhado (seção 4.3 do plano). Suíte **82/82 GLFW=ON / 54/54 embed** sob Xvfb, `tools/check_encapsulation.sh` OK (zero `GLFW_*` público), `clang-format`/`clang-tidy` (`clang-analyzer-*`, o gate duro)/`cppcheck`/`gitleaks` limpos. Implementer `backend-engineer`; review adversarial (`qa-engineer`) e re-verificação do orquestrador **AINDA PENDENTES** nesta branch. | Alta | A1-INPUT | Média | ✅ | — |
| DOC-HOSTIN | Onda 2 (plano `glintfx/docs/superpowers/plans/2026-07-22-onda2-input-host.md`, brief 7.3) | Docs | **Documentação das três fatias de contrato da Onda 2**, escrita por `technical-writer` (agente distinto dos dois implementers), bilíngue EN→PT, com `arquivo:linha` em cada claim: (1) **`set_frame_callback` hospedando renderer GL PRÓPRIO do host** -- suportado e validado em produção pelo consumidor (GusWorld: renderer próprio + loader GL do host, sem conflito com o gl3w interno, dt p95 20,8 µs em 8633 frames); o contrato de restauração de estado GL **não é duplicado**, só referenciado de `engine.hpp` (fonte única); loader GL é trabalho do host porque os símbolos GL do glintfx são internos ([ADR-0013](docs/adr/0013-gl-symbol-boundary.md)). (2) **Nota honesta de vsync** em `render()`/`run()` (`app.hpp:246` EN / `:254` PT): a glintfx **não chama `glfwSwapInterval` em lugar nenhum** do próprio `src/` (verificado por grep pelo writer E re-verificado pelo orquestrador -- as duas únicas ocorrências do símbolo no repo são exatamente essas linhas de doc dizendo que ele não é chamado); quem governa é o default do driver/compositor; setter fica pro `WM-EXTRAS`. **Nenhuma promessa de sincronização foi feita.** (3) **Coexistência `Gamepads` + `App`** (`docs/gamepad.md`, seção curta): ordem de init livre (o `Gamepads` é evdev puro e não toca GLFW; o `App` é dono do `glfwInit`), `poll()` no mesmo ponto do frame step. (4) **Seção 22 nova do `docs/embed-integration.md`** cobrindo a API de HOSTIN-1..5 com tabela de referência por método e os **limites honestos preservados**: teto de 8 botões de mouse (limitação do GLFW, não nossa), lacuna do scancode 97 do ABNT2, e canal sintético (`process_event`) separado do físico (D5). (5) Rascunho da entrada `v0.19.0` no `CHANGELOG.md`. Zero em-dash introduzido em prosa (verificado: 0 em `CHANGELOG.md`/`embed-integration.md`/`gamepad.md`). Review adversarial (`qa-engineer`) valida a verificabilidade de cada claim (brief 7.4 item 5). **Correção pós-review (Major achado pelo `qa-engineer`, re-verificado pelo orquestrador):** as 11 citações `app.hpp:linha` da seção 22 estavam off (o writer computou os números ANTES de inserir os próprios doc-comments no mesmo commit e não re-grepou depois); corrigidas contra `git grep` no HEAD. **Mata-a-classe:** `tools/check_doc_line_refs.sh` novo (bash puro, sem PCRE) -- abre cada arquivo citado num formato `` `símbolo` (`arquivo:linha`) `` ou o atalho duplo `` `sím1` / `sím2` (`arquivo:N`/`M`) ``, e falha se o símbolo não estiver na linha citada; escopo deliberadamente estreito (citações com prosa entre o símbolo e a citação são puladas, não adivinhadas); `docs/superpowers/` e `docs/auditoria/` excluídos da varredura (planos vivos e auditorias datadas/versão-alvo fixa, não feitos pra rastrear o HEAD). Plugado em `tools/preci.sh` (roda nos dois modos, rápido e `--full`, gate barato sem build). Provado: 34 ok/0 falha na doc corrigida; 22 falhas (11×EN/PT) numa cópia com os números revertidos. **Achado dominó, fora do escopo desta fatia, corrigido por ser mecânico e barato:** o próprio verificador achou 2 citações pré-existentes e não-relacionadas já desatualizadas em `docs/embed-integration.md` (`CMakeLists.txt:187`→`:1248`, drift puro de número; e a claim de `SetViewport(w,h)`/`glViewport` na seção 1, que ficou tecnicamente errada desde a v0.2.5 -- a chamada direta a `glViewport` foi removida, `SetViewport` hoje tem 4 args -- corrigida citando `render_gl3.cpp:594` e o próprio comentário do código em `:582-593`); e achou drift real, não corrigido (fora do job deste gate), em `docs/auditoria/AUD-L1-MEM.md`/`AUD-L1-SUPPLY.md` (citam `render_gl3.cpp`/`CMakeLists.txt` pré-refactor FX-CARVE-1) -- reportado ao líder/orquestrador, não remendado por um technical-writer sem revisão de engenharia. | Média | HOSTIN-1..5 | Baixa | ✅ | -- |
| D2D-1A | Onda 3 (plano `glintfx/docs/superpowers/plans/2026-07-23-onda3-draw2d-d2d1.md`, decisão D3, brief 8.1) | C++23/Arquitetura | **Carve do decode de imagem -- costura interna compartilhada ui↔Draw2D, zero mudança de comportamento** (fatia do implementer A, `backend-engineer`; roda ANTES do implementer B de D2D-1B, que depende desta costura e colide em `tests/CMakeLists.txt`). Achado honesto nomeado pelo próprio ADR-0017: o "átomo image" não tinha fronteira própria -- decode+premultiply+teto de 256 MiB viviam fundidos dentro de `Gl3RenderInterface::LoadTexture` (`render_gl3.cpp`), e `GLINTFX_MODULE_IMAGE` segue flag reservada forçada ON (não muda nesta fatia). NOVO `src/image_decode.hpp` (`glintfx::decode_premultiplied_rgba(const unsigned char* data, size_t len) -> DecodedImage{ok,width,height,rgba}`, PURO -- zero `#include` de GL/RmlUi, testável isolado como `input_state.hpp`/`log_dedup.hpp`): teto próprio `kMaxImageDecodeBytes` (256 MiB, `static_assert <= INT_MAX`) checado ANTES de tocar `data`, `stbi_load_from_memory` forçando 4 canais, premultiply in-place (`R=R*A/255` etc.) copiado pro `std::vector` de retorno (1 cópia extra deliberada -- ver o próprio comentário do header pro racional de custo, load de asset não é caminho por-frame). **Divisão de guarda explícita:** a guarda de tamanho de arquivo PRÉ-leitura (`Seek`/`Tell` antes de alocar, evita ler um asset hostil pra memória) fica em `render_gl3.cpp` (precisa de `Rml::FileInterface`, que o helper não pode depender); o teto próprio do helper é redundante-mas-inalcançável no call site do `LoadTexture` hoje e é a PRIMEIRA linha de defesa pra qualquer outro chamador que pule direto ao helper (o `load_texture` do Draw2D, D2D-1B). `Gl3RenderInterface::LoadTexture` reconectado como caller fino sobre o helper (fallback TGA INTOCADO; toda falha de decode -- incluindo a agora-morta guarda 2/2 de INT_MAX, dobrada num `static_assert` em vez de um 2º branch em runtime -- cai no mesmo fallback pré-existente). **Comportamento observável idêntico:** a suíte pré-existente (`texture_png_alpha`, `asset_decode_hostile_sanity`) é o guarda de regressão, verde sem alteração. NOVO `tests/image_decode_sanity.cpp` (PURO, registrado incondicionalmente como `input_state_sanity`/`log_dedup_sanity`, roda nas DUAS configs): decode válido PNG 2x2 (4 fixtures geradas via PIL, `image_decode_rgba_2x2.png`) com premultiply verificado POR VALOR byte-exato (opaco no-op, transparente com RGB≠0 zerado -- o próprio caso do bug de halo --, meio-alpha com arredondamento derivado à mão, branco opaco); decode válido JPEG 4x4 sólido (`image_decode_solid.jpg`) com RGB por tolerância (com perda mesmo em qualidade 100) e A==255 exato; corpus hostil (`png_truncated`/`garbage`/`huge_dims`, re-derivado -- binário de teste diferente de `asset_decode_hostile_sanity.cpp`, sem header compartilhado) entregue DIRETO ao helper (prova que o PRÓPRIO helper rejeita, não só algum load de documento ao redor); teto próprio do helper discriminado independente do `kMaxAssetFileBytes` do `render_gl3.cpp` (buffer real pequeno + `len` reivindicado 1 byte acima do teto, seguro pois a guarda roda antes de tocar `data`); null-data/zero-len. **Suíte 85/85 GLFW=ON / 57/57 embed** sob Xvfb (+1/+1 vs. baseline 84/56); `tools/check_encapsulation.sh` OK (mudança é só `src/`+`tests/`, zero header público tocado); `tools/check_doc_line_refs.sh` OK (achado dominó fora de escopo, corrigido por ser mecânico e barato: o próprio carve deslocou linhas de `render_gl3.cpp` citadas por `docs/embed-integration.md` seção 1 -- `:594`→`:565`, `:582-593`→`:553-564` -- E por `docs/adr/0017-draw2d-module.md` -- `:184`→`:196`, 4 citações EN/PT -- ambos corrigidos); `cppcheck`/`clang-tidy -p build (clang-analyzer-*)` = 0 achados nos arquivos tocados (ruído pré-existente do `stb_image.h` vendorizado, não deste carve); `gitleaks detect` limpo; `clang-format --dry-run --Werror` limpo nas linhas tocadas (violações pré-existentes fora do diff, não deste carve). MSVC-safe: `image_decode.hpp` é consumido por `render_gl3.cpp` (sempre compilado, inclusive no job MSVC embed) -- só stdlib portável (`<climits>`/`<cstddef>`/`<memory>`/`<vector>`), zero `min`/`max`, zero `filesystem::path::c_str()`, zero header só-Unix; `tests/image_decode_sanity.cpp` não é compilado pelo job MSVC (`GLINTFX_BUILD_TESTS=OFF` lá, mesma convenção de LOGTHR-1), mas já é portável por disciplina (`<fstream>`/stdlib puro, zero POSIX). Zero em-dash introduzido (verificado via grep no conteúdo novo/editado). Implementer `backend-engineer`; review adversarial (`qa-engineer`) e re-verificação do orquestrador **AINDA PENDENTES** nesta branch. | Alta | ADR-0017 | Média | ✅ | — |
| D2D-1B | Onda 3 (plano `glintfx/docs/superpowers/plans/2026-07-23-onda3-draw2d-d2d1.md`, decisões D1-D11, brief 8.2) | C++23/Draw2D | **O módulo Draw2D em si -- sprites (carregar textura + `draw_sprite` + batching interno por-textura) sobre um pipeline GL 3.3 próprio, a única fatia G do programa** (fatia do implementer B, `backend-engineer` DIFERENTE do implementer A; depende da costura de D2D-1A). `option(GLINTFX_MODULE_DRAW2D ON)` (`glintfx/CMakeLists.txt:570`), OBJECT lib `glintfx_draw2d` (só `src/draw2d.cpp`; `sprite_batch.hpp` é header-only incluído por ele; `glintfx/CMakeLists.txt:1265`, PRIVATE `glloader`+`third_party/stb`, ZERO `RmlUi::Core`) dobrada condicionalmente no agregado; `GLINTFX_MODULE_DRAW2D` em `config.hpp.in` + include guardado em `glintfx.hpp` (mesmo padrão do audio/gamepad). NOVO `include/glintfx/draw2d.hpp` (`RectF`/`ColorF`/`Texture2d`/`Draw2d`, `class Draw2d` em `:196` -- contrato LITERAL da seção 4 do plano, pImpl move-only, ZERO tipo de terceiro) + NOVO `src/sprite_batch.hpp` (`class SpriteBatch` em `:75`, PURO -- zero GL, batching policy D4/vertex-gen D5/tint-clamp D8/validação-NaN-antes-de-matemática D10, testável headless) + NOVO `src/draw2d.cpp` (`struct Draw2d::Impl` em `:130` -- execução GL do que o batcher decidiu: shader textured-quad único, VAO/VBO orphaned por flush D2, blend premultiplicado `GL_ONE`/`GL_ONE_MINUS_SRC_ALPHA`, D9 seta program+VAO+blend+textura a CADA flush e NUNCA restaura, registry de textura id+geração D7). **D2 verificado em código, não suposto** (a exigência do plano): `glx_gl_load()` (`glintfx/src/gl_loader.c:830`/`:897`) resolve e sobrescreve os mesmos ponteiros toda chamada, sem guarda de estado -- genuinamente idempotente, `Draw2d::init()` chama sem guard extra. **Achado de correção real além do plano literal:** o design original de `Texture2d` (id+geração só) NÃO garantia rejeição de handle de OUTRA instância `Draw2d` -- id+geração podiam coincidir numericamente entre registries independentes; adicionado 3º campo `owner_` (endereço do `Draw2d::Impl` emissor, type-erased, `glintfx/src/draw2d.cpp:442`) tornando a rejeição de handle estrangeiro uma GARANTIA por construção, não uma coincidência -- achado e fechado ANTES do commit, não pelo review. `destroy_texture()` sempre zera o handle do chamador (D7 literal), inclusive em foreign/tampered. `Draw2d` é null-safe em TODO método após um move (mais estrito que o contrato UB-exceto-`ok()` do `App`, decisão deliberada já que não custa nada aqui). NOVOS testes: `sprite_batch_sanity.cpp` (PURO, incondicional -- acumulação por-textura, flush em troca/capacidade/end, geração de vértice com sentinela+clamp de `src_px`, clamp de tint, NaN/Inf ANTES de matemática, dst degenerado no-op silencioso, negativo é legal, fora-de-bracket, begin aninhado, stream hostil de 100k desenhos limitado em memória via dreno a cada chamada); `draw2d_render_sanity.cpp` (fixture WindowGlfw só, sem UiLayer -- 4 checks com TGA feito-à-mão de cor exata conhecida, incl. 2 deltas D8 CALCULADOS À MÃO, não "parece certo"); `draw2d_ui_coexist_sanity.cpp` (**o fixador do risco central D9** -- UM frame sprite→`ui_layer.render()`, OUTRO frame ui→sprite, `:229-233` e `:242-246`, regiões UI/sprite/neutra NÃO-sobrepostas, achado+corrigido um bug de flip vertical do próprio readback do teste -- `glReadPixels` é bottom-up, `RectF`/dst é top-down, mesma armadilha do comentário de `decorator_ripple.cpp` -- ANTES de reportar green); `draw2d_hostile_sanity.cpp` (corpus hostil re-derivado -- ausente/nulo/vazio/lixo/TGA com `image_type` inválido/oversized-256MiB -- handle destruído/adulterado/ESTRANGEIRO de uma 2ª instância `Draw2d` real, `begin(0,0)`, begin aninhado, draw fora de bracket, shutdown() duplo explícito+destrutor, destruir `Draw2d` com textura viva); `app_draw2d_smoke.cpp` (GLFW=ON, Draw2d dentro de `App::set_frame_callback`, `Draw2d::init()` chamado DEPOIS do `App` já construído -- prova ponta-a-ponta do D2 -- achado+corrigido um `app.update()` faltando antes de `snapshot()`, mesma exigência de `app_frame_callback_smoke.cpp`). **Suíte 90/90 GLFW=ON / 61/61 embed** sob Xvfb isolado (+5/+4 vs. baseline pós-A 85/57); `tools/check_encapsulation.sh` OK; `tools/check_doc_line_refs.sh` OK (achado dominó: a inserção do bloco CMake deslocou `docs/embed-integration.md:101`/`:107` de `CMakeLists.txt:1248`→`:1310` pra `RMLUI_GL3_CUSTOM_LOADER`, corrigido); **dois `nm` confirmados**: `nm libglintfx.a &#124; grep -E ' [BbDd] gl[A-Z]'` = **0 matches** nas 3 configs (ON/embed/draw2d-OFF, ADR-0013 intacto); com `-DGLINTFX_MODULE_DRAW2D=OFF` = **0 matches** de `draw2d&#124;SpriteBatch` (com ON = 349 matches). MSVC-safe: `draw2d.hpp`/`draw2d.cpp`/`sprite_batch.hpp` sem `std::min`/`max`, sem `std::filesystem::path::c_str()` (usa overload `ifstream(const char*)`, D7), sem header só-Unix; `-Wall`/`-Wextra` via generator expression (mesmo padrão dos módulos irmãos). Zero em-dash introduzido. **Nota operacional, fora do escopo mecânico desta fatia:** 4 invocações diretas de `bash tools/preci.sh` (sem o prefixo de isolamento `env -u WAYLAND_DISPLAY XDG_RUNTIME_DIR=...`) durante a verificação abriram janela real na sessão do líder -- causa raiz é `tools/preci.sh:181` não isolar `XDG_RUNTIME_DIR` na própria chamada de `xvfb-run`, achado reportado ao orquestrador, fix em andamento por outro agente (`devops-sre`) em `tests/run_xvfb.cmake`; toda validação DESTA fatia (os dois configs + os dois `nm` + encapsulamento + citações) rodou via `ctest`/binário direto com o prefixo canônico completo, isolado. Implementer `backend-engineer`; review adversarial (`qa-engineer`) e re-verificação do orquestrador **AINDA PENDENTES** nesta branch. | Alta | D2D-1A | Alta | ✅ | — |
| QA-XVFBISO | Onda 3 (incidente 2026-07-23) | Infra de teste | **Isolamento de `XDG_RUNTIME_DIR` no gate de teste -- fecha o caminho pelo qual teste de janela vazava pra sessão VIVA do líder.** Incidente real: o líder reportou *"tem telas abrindo e fechando várias vezes com efeitos do glintfx"* enquanto a Onda 3 rodava; na semana anterior, uma rajada equivalente **travou o touchpad dele até precisar reboot**. **Causa-raiz, em três pontos de entrada**: (1) `glintfx/tests/run_xvfb.cmake` **documentava** a ressalva (o `wl_display_connect(NULL)` do libwayland cai no socket embutido `"wayland-0"` **dentro do `$XDG_RUNTIME_DIR`** quando a env var some, e esse socket é o da sessão real) mas **só fazia `unset(ENV{WAYLAND_DISPLAY})`** -- proteção pela metade, que é pior que nenhuma porque parece completa; (2) `tools/preci.sh` lançava `xvfb-run -a ctest` **sem isolamento próprio**, confiando no ambiente do chamador -- foi o caminho de fato usado no incidente (4 invocações de `bash tools/preci.sh` sem prefixo, cada uma rodando ~25-30 testes que criam janela); (3) `tools/coverage_report.sh` tinha o mesmo furo. **Fix**: cada ponto de entrada cria o próprio `XDG_RUNTIME_DIR` falso e vazio via `mktemp -d` (0700, um por invocação, sem corrida sob `ctest -j`), de modo que **não exista socket `wayland-0` pro fallback achar**, e remove no fim. **Prova mecânica** (`strace -f -e trace=connect` no `window_smoke`, com `WAYLAND_DISPLAY`/`XDG_RUNTIME_DIR` REAIS herdados, reproduzindo o cenário do incidente): `connect(.../glintfx-xvfb-runtime.XXXX/wayland-0) = -1 ENOENT` seguido de `connect(@/tmp/.X11-unix/X105) = 0`, **zero** ocorrências de `/run/user` no trace inteiro. **Bug pego dentro da própria correção** (harness isolado com `cmake`/`ctest` mockados e `xvfb-run` real falhando de propósito): a 1ª versão limpava via `trap ... RETURN`, que **NÃO dispara** quando `set -e` aborta pela falha do próprio comando -- o dir ficava órfão exatamente no **caminho de falha de teste**, o mais comum; trocado por captura explícita de `rc` + `rm` incondicional + `return`. No `coverage_report.sh` (escopo de topo, não função) o `trap ... EXIT` **sim** dispara sob `set -e`, verificado com o mesmo harness -- as duas mecânicas diferentes estão documentadas no comentário de cada script. Verificação do orquestrador: parse com balanceamento de parênteses do `tests/CMakeLists.txt` -- **87 testes reais, 66 pelo wrapper, 21 fora e TODOS puros** (nenhum abre janela), portanto o conserto do wrapper já fechava o vazamento na prática; os dois scripts foram fechados assim mesmo por defesa em profundidade, pelo princípio canonizado nesta onda: **o isolamento pertence a quem EXECUTA, não a quem chama**. Suíte 90/90 GLFW=ON e 61/61 embed verdes contra o wrapper corrigido; `bash -n` e `shellcheck` limpos. Agente `devops-sre` (nenhum dos implementers da onda). **Não provado** (declarado, não escondido): `bash tools/preci.sh` ponta-a-ponta não foi re-executado, pra não disparar uma 3ª rajada de janela na sessão do líder. | Alta | — | Média | ✅ | — |
| DOC-D2D | Onda 3 (plano `glintfx/docs/superpowers/plans/2026-07-23-onda3-draw2d-d2d1.md`, brief 8.3) | Docs | **`docs/draw2d.md`** (contrato público completo do átomo Draw2D) + cross-refs + rascunho do CHANGELOG `v0.20.0`, escrito por `technical-writer` (agente distinto dos implementers), bilíngue EN→PT, cada claim com `arquivo:linha` **verificado pelo gate** `tools/check_doc_line_refs.sh`. Cobre: espaço de coordenadas (px de tela, origem top-left, y pra baixo, **mesmo espaço** do `UiEvent`/`get_element_box`); semântica de premultiply e a **fórmula exata do tint** (D8); a **lista enumerada do estado GL tocado** mais o **contrato D9** (o Draw2D seta tudo a cada flush e **não restaura nada**; no `App` o frame hook restaura, no embed é do host, igual ao GL cru dele); receitas de `App` (dentro do frame hook, sob a UI) e de embed (ordem livre); e os dois achados do implementer que mudam o que o consumidor precisa saber: **handle de textura não é intercambiável entre instâncias** de `Draw2d` (validação por dono, `draw2d.hpp:174`) e **`glx_gl_load()` é idempotente** (verificado em `gl_loader.c:830` e `:897`), logo o host **não precisa ordenar a init**. **Limites declarados, não escondidos**: sem câmera nem rotação até `D2D-2`; sem orçamento de performance até `D2D-3`; MSVC **compila mas não executa** em CI; llvmpipe implica teste **estatístico, nunca pixel-exato**; `load_texture` **não** honra `set_asset_base_url` (semente `SEED-D2D-BASEURL`). | Média | D2D-1B | Baixa | ✅ | — |
| DOC-D2D | Onda 3 (plano `glintfx/docs/superpowers/plans/2026-07-23-onda3-draw2d-d2d1.md`, brief 8.3) | Docs | **Contrato público do Draw2D**, escrito por `technical-writer` (agente distinto dos dois implementers, roda APÓS o B pras assinaturas reais), bilíngue EN→PT, com `arquivo:linha` em cada claim: (1) NOVO `docs/draw2d.md` (referência/how-to completo -- ciclo de vida, contrato de coordenadas D5 com cross-ref à seção 10 do `embed-integration.md`, tabela de API, fórmula EXATA do tint D8, a lista enumerada de estado GL tocado no estilo de `gl_state.hpp` + o contrato D9 completo -- Draw2D seta tudo, não restaura nada, App restaura por você, embed é responsabilidade do host --, receitas App/embed, handles de textura D7 incl. o achado do implementer B de rejeição por-dono, batching D4, superfície fail-high D10, idempotência do `glx_gl_load()` D2, e os limites declarados: sem câmera/rotação até D2D-2, sem perf budget até D2D-3, MSVC só compila, llvmpipe só estatístico, `load_texture` não honra `set_asset_base_url`). (2) Seção 23 nova em `docs/embed-integration.md` (coexistência Draw2D+`UiLayer`, cross-ref pro D9) + entrada na lista "See also". (3) Um parágrafo de cross-ref bilíngue no doc-comment do `set_frame_callback` (`glintfx/include/glintfx/app.hpp`) apontando pro `docs/draw2d.md` -- a inserção deslocou a própria assinatura do método de `:490` pra `:506`, todas as citações pré-existentes desse número (seção 21 do embed-integration.md, 2×) e as novas do draw2d.md (4×) corrigidas pro número novo no mesmo commit. (4) Rascunho da entrada `v0.20.0` no `CHANGELOG.md` (Added: D2D-1B+D2D-1A; Fixed: QA-XVFBISO citando o achado do `devops-sre` em `run_xvfb.cmake` sem tocar o arquivo; Documentation: DOC-D2D) com os downgrades declarados junto. Zero em-dash introduzido em prosa (verificado: 0 em `docs/draw2d.md`/`docs/embed-integration.md`/`CHANGELOG.md`/`app.hpp`, só ocorrências pré-existentes fora do diff). **Correção pré-commit (achada por mim mesmo, não pelo review):** duas citações `draw_sprite()` apontavam pra linha do CHECK de dono (`:507`) em vez da definição da função -- o próprio `tools/check_doc_line_refs.sh` pegou (`draw_sprite` não está na linha 507); trocadas pro atalho de citação dupla `` `draw_sprite()` / `destroy_texture()` (`:499`/`446`) ``, apontando as duas pras definições reais. `tools/check_doc_line_refs.sh`: **72 ok / 0 falha / 1 skip** (o skip é pré-existente, não-relacionado -- `docs/adr/0010-image-tint-luminance-key.md`, header do RmlUi vendorizado ausente neste checkout). Não toquei em `tools/` nem em `glintfx/tests/run_xvfb.cmake` (outro agente, `devops-sre`, ainda com o arquivo modificado e não commitado) nem nas linhas D2D-1A/D2D-1B desta tabela. Não rodei `tools/preci.sh` (buraco de isolamento conhecido, ainda sob conserto) -- só o gate de citação, direto. Review adversarial (`qa-engineer`) e re-verificação do orquestrador **AINDA PENDENTES** nesta branch. | Média | D2D-1B | Baixa | ✅ | — |
| D2D-2A | Onda 4 (plano `glintfx/docs/superpowers/plans/2026-07-23-onda4-draw2d-camera.md`, seções 3-4, brief 8.2; ADR-0017 Addendum grava Q1=(c)/Q2=(2a)) | C++23/Draw2D | **Matemática pura do D2D-2 (câmera + transform por-sprite) -- fonte única, ZERO GL** (fatia do implementer A, `backend-engineer`; roda antes do implementer B de D2D-2B, que consome estes arquivos e colide em `tests/CMakeLists.txt`). NOVO `glintfx/src/transform2d.hpp` (PURO, `namespace glintfx::draw2d_detail`): `world_to_screen`/`screen_to_world` (D12 literal, ancorado no centro, totais -- D16, input inválido devolve o ponto de entrada inalterado, nunca NaN pra fora), `camera_from_world_rect` (D17, fit por largura, hostil → `Camera2d{}`), `compute_sprite_corners` (D18, pivô normalizado, ordem escala→rotação sobre o pivô), predicados `is_finite` (D20, primeira guarda pré-conta) e um `rotate(v,t)` único reusado por câmera E sprite. `include/glintfx/draw2d.hpp` ganha EXATAMENTE a seção 4 do plano: `Vec2F`/`Camera2d`/`SpriteTransform` (tipos públicos, zero terceiro), as 3 free functions declaradas (definidas fora de linha em draw2d.cpp, D21 -- fatia do B), `set_camera`/`reset_camera`/overload de `draw_sprite` com `SpriteTransform` DECLARADOS (não implementados aqui); reenquadre do comentário D5 do cabeçalho ("px de tela" vira "o caso sem câmera", Q1 (c)). NOVO `glintfx/tests/transform2d_sanity.cpp` (PURO, incondicional -- roda nas DUAS configs, mesma classe de `sprite_batch_sanity.cpp`): valores computados à mão do D12 (offset/zoom 2x-0.5x/rotação pi/2 exata/combinado, incl. perna inversa de `screen_to_world` que pina o sinal `+cam.rotation` independente do round-trip), round-trip `world→screen→world` numa grade de pontos × câmeras, wrap `t` vs `t+2π`, totalidade D16 (zoom 0/negativo/NaN/Inf de câmera e ponto/viewport ≤0, com helper `same_or_nan` pra comparar NaN preservado), D17 (fit válido + corpus hostil), matemática de cantos D18 (rotação pi/2 permuta cantos exatamente, pivôs (0,0) vs (1,1), escala negativa espelha), equivalência EXATA D19 (`SpriteTransform{}` identidade reproduz os 4 cantos do próprio `dst` bit-a-bit -- igualdade de float exata legítima aqui, valores escolhidos representáveis sem arredondamento), overflow D20 (escala finita × dst finito que multiplica além do float, capturado por `is_finite(SpriteCorners)`). **Achado de colisão de nome via ADL:** `world_to_screen`/`screen_to_world`/`camera_from_world_rect` têm o MESMO nome nos dois namespaces (`glintfx::` público declarado e `glintfx::draw2d_detail::` interno definido); um `using`+chamada não-qualificada no teste é genuinamente ambíguo (ADL acha os dois via `Camera2d`/`Vec2F`, ambos `glintfx::`) -- resolvido com 3 wrappers locais (`w2s`/`s2w`/`fit_cam`) que chamam a versão `draw2d_detail::` totalmente qualificada, o mesmo padrão que o `draw2d.cpp` do B precisa usar dentro da própria definição fora-de-linha das free functions públicas (senão o mesmo ambiguity existe lá). **Suíte 92/92 GLFW=ON / 63/63 embed** sob Xvfb isolado (+1/+1 vs. baseline pós-D2D-1B 91/62); `tools/check_encapsulation.sh` OK (zero tipo de terceiro nos novos tipos públicos); `nm libglintfx.a \| grep -E ' [BbDd] gl[A-Z]'` = 0 matches (ADR-0013 intacto); `clang-format --dry-run --Werror` limpo (achado pré-commit: `transform2d.hpp` reprovou 1x, corrigido com `clang-format -i` antes do commit); MSVC-safe (só `<cmath>`, zero header Unix-only, zero `min`/`max` cru). **Achado dominó, fora do escopo mecânico desta fatia:** os edits no `draw2d.hpp` deslocaram 30 citações `draw2d.hpp:NNN` que `docs/draw2d.md` já carregava (`tools/check_doc_line_refs.sh` confirma) -- LISTADAS no relatório ao orquestrador, NÃO corrigidas aqui por ser fatia do `technical-writer` (DOC-D2D2, roda depois do B). **Colisão operacional registrada, não desta fatia:** um `git reset --hard` de outro agente nesta mesma working tree (não-isolada) descartou a 1ª rodada destas duas edições em arquivo tracked antes do commit; reaplicadas idênticas após verificação (os 2 arquivos NOVOS, untracked, sobreviveram). Implementer `backend-engineer`; review adversarial (`qa-engineer`) e re-verificação do orquestrador **AINDA PENDENTES** nesta branch. | Alta | ADR-0017 (Addendum) | Média | ✅ | — |
| D2D-2B | Onda 4 (plano `glintfx/docs/superpowers/plans/2026-07-23-onda4-draw2d-camera.md`, seções 3-6, brief 8.3) | C++23/Draw2D | **Plumbing do D2D-2 (câmera + transform por-sprite) sobre a matemática pura do D2D-2A** (fatia do implementer B, `backend-engineer` DIFERENTE do A; consome `transform2d.hpp` sem reimplementar NENHUMA fórmula). `glintfx/src/sprite_batch.hpp` ganha `SpriteBatch::draw_quad(texture_id, tex_w, tex_h, corners[4], src_px, tint)` (D19, PURA): mesma política de bracket/troca-de-textura/capacidade do `draw_sprite()` existente (D4), mesmo sentinela+clamp de `src_px` (D5) e clamp de `tint` (D8) via os MESMOS helpers `resolve_src()`/`clampf()`, mas os vértices vêm dos cantos do chamador (`emit_quad_corners()`, irmão de `emit_quad()`) -- o caminho `draw_sprite()`/`emit_quad()` EXISTENTE fica byte-a-byte intocado (git diff vazio nesse trecho). `glintfx/src/draw2d.cpp`: `set_camera`/`reset_camera` (D15 fail-high -- membro não-finito ou `zoom<=0` MANTÉM o estado anterior, log dedup'd; D22 -- `shutdown()` reseta a câmera, estado vive em `Impl::has_camera`/`Impl::camera`), o overload `draw_sprite(tex,dst,src_px,tint,SpriteTransform)` (D20: `transform` validado ANTES de qualquer conta, `dst.w<=0\|\|dst.h<=0` silencioso inalterado, cantos via `compute_sprite_corners()` do A, COM câmera cada canto projetado por `world_to_screen()` POR CANTO, guarda pós-conta sobre os cantos FINAIS -- pós-projeção, cobrindo overflow tanto da conta de canto quanto da própria projeção de câmera --, `flip_h`/`flip_v` do D18 implementados como reordenação de slot de canto entregue a `draw_quad` -- mesma UV por slot, footprint de tela inalterado, verificado por leitura de pixel), e as 3 free functions públicas `world_to_screen`/`screen_to_world`/`camera_from_world_rect` DEFINIDAS aqui (D21, fora de linha, delegando com chamada qualificada `draw2d_detail::...` -- evita a mesma ambiguidade de ADL que os testes do A já documentam). O caminho `draw_sprite` de 4 args fica INALTERADO (camera ou não -- é o contrato literal que o doc-comment do A já grava, "absence of diff" da Q1 (c)). `glintfx/CMakeLists.txt`: `project(VERSION 0.21.0)`. NOVO `glintfx/tests/draw2d_camera_render_sanity.cpp` (readback GL, 7 checks, tolerância ESTATÍSTICA nunca pixel-exata): offset de câmera (pan/afasta/volta), zoom 2x (footprint dobrado), rotação de câmera pi/2 num checker 4-cores assimétrico (`rot(v,-cam.rotation)`), `SpriteTransform.rotation` pi/2 SEM câmera (`rot(v,+rotation)`, permutação DIFERENTE da câmera -- prova que é a rotação PRÓPRIA do sprite), `flip_h`/`flip_v` SEM câmera (2 checks -- LACUNA fechada por esta fatia: D18 promete flip mas nem A nem o plano tinham teste algum pra flip em lugar nenhum, já que a implementação mora no D2D-2B), e troca `set_camera`→`reset_camera` NO MEIO do bracket sobre UMA textura só (D13 -- SpriteBatch nunca vê a câmera, não pode fisicamente flushar numa troca; a prova de flush-count é estrutural por inspeção + `test_same_texture_accumulates`, não por readback). Tolerância mais larga (`kTolRotated=24`, vs. `kTol=8` dos checks axis-aligned) para os 4 checks rotacionados/espelhados -- ruído de interpolação llvmpipe medido empiricamente MAIOR num quad não-axis-aligned mesmo num ponto de amostra longe de fronteira de texel, direção sempre batendo com a expectativa derivada à mão, nunca um quadrante errado. EXTEND `glintfx/tests/draw2d_hostile_sanity.cpp`: `set_camera`/`reset_camera`/overload de transform em pré-init e movido-de (safe no-op); seção nova de câmera hostil com PROVA POR RENDER (NaN em cada membro/Inf/zoom 0/zoom negativo → estado ANTERIOR provadamente mantido -- desenha antes/depois e compara, já que `Camera2d` não tem getter, `SEED-D2D-CAMGETTER`); overload de transform com campos hostis (NaN rotation/origin_x, Inf scale_x → draw pulado, sem crash, ainda usável depois); `shutdown()`/`init()` reseta a câmera (D22) via instância ISOLADA (evita invalidar `tex_real` que o resto do arquivo reusa). EXTEND `glintfx/tests/sprite_batch_sanity.cpp`: equivalência EXATA D19 (`draw_quad` com cantos-de-identidade == `draw_sprite`'s próprio caminho, vértice-a-vértice bit-exato -- a metade do pin que faltava, a do A cobre só a matemática de canto), mesma política D4/D5/D8 espelhada, corpus hostil (NaN/Inf/fora-de-bracket/bracket-degenerado). **Achado dominó fechado nesta fatia:** `flip_h`/`flip_v` do D18 tinham ZERO teste em qualquer lugar (nem A, nem o plano listavam um pure-test possível, já que a UV-swap mora no draw2d.cpp) -- fechado com os 2 checks de readback acima, não deixado como lacuna silenciosa. **Doc-refs corrigidos** (regra "quem desloca conserta"): 2 citações duplicadas em `docs/draw2d.md` (linhas 193/463 do doc, ambas citando `draw2d.cpp:518`/`463`) deslocadas pelos meus edits em `draw2d.cpp` -- atualizadas para `:535`/`:480` (mais `:459`→`:476` da citação de `load_texture()`), `tools/check_doc_line_refs.sh` 0 falhas após o fix (a prosa do doc em si -- seções de câmera/transform -- é fatia do `technical-writer`, DOC-D2D2, não tocada aqui). **Suíte 93/93 GLFW=ON / 64/64 embed** sob Xvfb isolado (+1/+1 vs. baseline pós-D2D-2A) + leg ASan limpa (7 binários relevantes, zero leak/UB); `tools/check_encapsulation.sh` OK; `tools/check_doc_line_refs.sh` 0 falhas; os DOIS `nm` (ADR-0013 zero `gl[A-Z]` cru nos dois builds; `GLINTFX_MODULE_DRAW2D=OFF` remove TODOS os símbolos novos, inclusive as 3 free functions públicas -- confirmado 0 matches); `clang-format --dry-run --Werror` limpo (achados corrigidos com `-i` antes do commit: 1 linha em `draw2d.cpp`, 1 em `draw2d_camera_render_sanity.cpp`, 3 em `draw2d_hostile_sanity.cpp`); MSVC-safe por inspeção (grep confirma zero `min`/`max` cru, zero header Unix-only, zero `filesystem::path::c_str()` novo -- compilação real no MSVC não verificada localmente, sem toolchain Windows neste ambiente). Implementer `backend-engineer`; review adversarial (`qa-engineer`) e re-verificação do orquestrador **AINDA PENDENTES** nesta branch. | Alta | D2D-2A | Média | ✅ | — |
| DOC-D2D2 | Onda 4 (plano `glintfx/docs/superpowers/plans/2026-07-23-onda4-draw2d-camera.md`, brief 8.4) | Docs | **Câmera + `SpriteTransform` documentadas em `docs/draw2d.md`**, escrito por `technical-writer` (agente distinto dos dois implementers, roda após o B pras assinaturas/linhas reais), bilíngue EN→PT, `arquivo:linha` em cada claim. (1) Reenquadre da seção de coordenadas D5: "px de tela, top-left, y-down" vira o CASO SEM CÂMERA (default, comportamento v0.20.0 literalmente intocado, Q1 (c) da Adenda da ADR-0017) -- com link direto pro anchor do adendo. (2) Seção nova "Camera": `Camera2d` (âncora CENTRO, `Camera2d{}` NÃO é identidade -- destacado como o custo aceito da Q2 (2a)), as fórmulas D12 verbatim (`rot(v,t)` + `world_to_screen`/`screen_to_world`), `set_camera`/`reset_camera` (validação D15 -- estado anterior mantido em input hostil --, câmera sticky/CPU-side/D13, troca no meio do bracket não quebra batching, com snippet de código do padrão mundo+HUD num bracket só), totalidade D16 dos helpers puros, `camera_from_world_rect` (D17, fit por largura). (3) Seção nova "SpriteTransform": pivô normalizado default centro, ordem literal escala→rotação sobre o pivô (D18, com aviso de que é a 1ª linha que o mutation testing ataca), flips como troca de UV do sub-rect RESOLVIDO, scale negativo legal, compatibilidade D19 (ausência de diff no caminho de 4 args + equivalência exata), fail-high D20 em duas etapas (membro finito antes da conta, guarda pós-conta de canto sem gêmeo na v0.20.0 por design). (4) Tabela de API com as 6 linhas novas (`set_camera`/`reset_camera`/overload de transform/3 free functions). (5) Limites declarados atualizados: sem `draw_text` até `D2D-TEXT`, sem primitivas/perf até `D2D-3`, shake/suavização/limites de câmera são do consumidor (seed `SEED-D2D-CAMANIM`), sem getters de câmera (seed `SEED-D2D-CAMGETTER`), fit só por largura (seed `SEED-D2D-LETTERBOX`). (6) `docs/embed-integration.md` seção 23: duas frases de cross-ref (a câmera é CPU-side, zero estado GL novo, não muda o contrato de coexistência). (7) Rascunho da entrada `[Unreleased]` do `CHANGELOG.md` (Added: D2D-2A+D2D-2B citando a Adenda da ADR-0017 verbatim; Documentation: DOC-D2D2) com os downgrades declarados junto. `tools/check_doc_line_refs.sh`: **94 ok / 0 falha / 1 skip** (o skip é pré-existente, não-relacionado -- `docs/adr/0010-image-tint-luminance-key.md`, header do RmlUi vendorizado ausente neste checkout), rodado ANTES e DEPOIS de todo o bloco de edits. Não toquei em `glintfx/src/`, `glintfx/tests/`, nem nas linhas D2D-2A/D2D-2B desta tabela (fatia dos implementers). Não rodei `tools/preci.sh` completo (build pesado, fora do escopo mecânico de uma fatia de doc) -- só o gate de citação, direto, duas vezes. Review adversarial (`qa-engineer`) e re-verificação do orquestrador **AINDA PENDENTES** nesta branch. | Média | D2D-2B | Baixa | ✅ | — |
| D2D-3A | Onda 5 (plano `glintfx/docs/superpowers/plans/2026-07-23-onda5-draw2d-primitives.md`, seções 3-4, brief 8.1; decisões D23-D32) | C++23/Draw2D | **Geometria pura + superfície pública do D2D-3 (primitivas não-texturizadas/layers/scissor/bbox) -- fonte única, ZERO GL** (fatia do implementer A, `backend-engineer`; roda antes do implementer B de D2D-3B, que consome estes arquivos e colide em `tests/CMakeLists.txt`). **Achado dominó corrigido de graça:** o parágrafo "WHAT THIS SLICE DOES NOT SHIP" do `draw2d.hpp` (EN+PT) ainda listava câmera/rotação/escala/flip/`screen_to_world` como NÃO entregues -- doc-drift pré-existente da Onda 4 (D2D-2A/2B já entregaram isso), achado ao tocar o mesmo arquivo e corrigido no mesmo commit (não é regressão desta fatia), com a lista atualizada pros seeds da Onda 5 (`SEED-D2D-SHAPES`/`LINECAPS`/`LAYER-TEXSORT`/`WORLDCLIP`/`BBOX-THRESH`). NOVO `glintfx/src/primitives2d.hpp` (PURO, `namespace glintfx::draw2d_detail`): `compute_line_corners()`/`is_degenerate_line()` (D25 literal -- normal `n=(-u.y,u.x)`, butt caps, guarda `a==b`/`thickness<=0` ANTES do sqrt/divisão, sem caminho de divisão-por-zero por construção), `compute_outline_strips()`/`is_degenerate_outline()` (D26 literal -- 4 faixas sem sobreposição, clamp de colapso `t=min(thickness,min(w,h)/2)`, a MESMA fórmula clampada ladrilha `dst` exatamente no caso `2t>=min(w,h)` sem ramo de caso especial, provado por identidade algébrica no teste), `map_scissor_to_gl()` (D28 literal -- `glScissor(x,target_h-(y+h),w,h)`, extensões w/h clampadas a não-negativo, origem x/y intocada), `is_finite(ColorF)`/`is_finite(OutlineStrips)` (predicados novos, reusando `is_finite(Vec2F)`/`is_finite(RectF)` de `transform2d.hpp` -- fonte única, sem redefinição). `glintfx/src/image_decode.hpp` ganha `compute_content_bbox()` (D29 -- bbox do menor retângulo com `alpha>0`, espaço de texel top-left/y-down igual ao `src_px`, `alpha==1` conta, totalmente-transparente/dado nulo/dimensão<=0 -> `found=false`; devolve a PRÓPRIA struct `ContentBbox`, não o `TextureBbox` público, porque este header não pode depender de `<glintfx/draw2d.hpp>` -- `render_gl3.cpp` também o usa e não tem nada a ver com Draw2D); `decode_premultiplied_rgba()` **zero mudança** (verificado por `git diff`, só a adição no fim do arquivo). `include/glintfx/draw2d.hpp` ganha EXATAMENTE a seção 4 do plano: `struct TextureBbox`, `draw_filled_rect` (2 overloads), `draw_filled_quad`, `draw_line`, `draw_rect_outline`, `set_layer`, `set_scissor`/`reset_scissor`, `texture_content_bbox` -- todos DECLARADOS com doc-comment bilíngue completo citando D23-D29 (espaço ATIVO/D24, fail-high/D10 estendido, arming do D27, aviso ALTO de scissor-vazando-pro-host-em-embed do D28), **NADA implementado em `.cpp`** (fatia do B). NOVO `glintfx/tests/primitives2d_sanity.cpp` (PURO, incondicional, roda nas DUAS configs): D25 valores à mão (linha horizontal E diagonal 3-4-5 assimétrica -- só a diagonal pega normal com EIXO trocado --, mata-mutação `h` vs `thickness`, `is_degenerate_line()` antes de qualquer divisão, demonstração de ORDEM de guarda -- finitude→degenerescência→conta, provando por construção que nenhum caminho de divisão-por-zero é alcançado --, guarda pós-conta de overflow com endpoint próximo de `FLT_MAX` -- achado e corrigido ANTES do commit: a 1ª tentativa com `a`/`b`/`thickness` da ordem de `1e30` não overflowava de verdade, `len`/`u` cancelavam via subtração exata e o resultado ficava finito; a versão corrigida usa `a.y=b.y=3e38f` com offset perpendicular de `5e37f` que estoura `FLT_MAX` na SOMA, não na multiplicação); D26 faixas escolhidas à mão + NÃO-SOBREPOSIÇÃO (AABB par-a-par) + COBERTURA EXATA (área de união == `2t(w+h)-4t²`, identidade algébrica) + fronteira `2t==min(w,h)` vs. `2t>min(w,h)` produzindo as MESMAS faixas clampadas; D29 arrays RGBA forjados (cantos `(0,0)`/`(w-1,h-1)`, diagonal, all-transparent, all-opaque, `alpha==1` conta, input inválido total); D28 mapeamento `glScissor` computado à mão em DOIS retângulos fora-do-centro (mata-mutação de y-flip) + clamp de extensão w/h negativa. Bloco próprio em `tests/CMakeLists.txt` (incondicional, gateado só por `GLINTFX_MODULE_DRAW2D`, mesmo padrão de `transform2d_sanity`/`sprite_batch_sanity`). **Suíte 94/94 GLFW=ON / 65/65 embed** sob Xvfb isolado (+1/+1 vs. baseline pós-v0.21.0 93/64); `tools/check_encapsulation.sh` OK; os DOIS `nm` (ADR-0013 zero `gl[A-Z]` cru nos 3 builds -- ON/embed/`GLINTFX_MODULE_DRAW2D=OFF`; com `MODULE_DRAW2D=OFF` = 0 matches de `draw2d\|SpriteBatch\|primitives2d`, com ON = 366 matches); `cppcheck`/`clang-tidy -p (clang-analyzer-*)` = 0 achados nos 3 arquivos tocados/novos (ruído pré-existente do `stb_image.h` vendorizado, não deste item); `-Wall -Wextra` limpo; `clang-format --dry-run --Werror` limpo (achado pré-commit: `primitives2d.hpp`/`primitives2d_sanity.cpp`/`image_decode.hpp` reprovaram, corrigidos com `-i`, suíte re-verificada verde depois); MSVC-safe por inspeção (zero `std::min`/`max` cru -- ternários, como `sprite_batch.hpp::clampf`; zero header Unix-only; zero `filesystem::path::c_str()`). **`tools/check_doc_line_refs.sh`: 45 citações `docs/draw2d.md`→`draw2d.hpp:NNN` quebradas** (meus edits deslocam linha; a fatia de conserto é do `technical-writer`, DOC-D2D3, roda depois do B) -- lista completa reportada ao orquestrador, **NÃO consertada aqui** por instrução explícita do brief. Implementer `backend-engineer`; review adversarial (`qa-engineer`) e re-verificação do orquestrador **AINDA PENDENTES** nesta branch. | Alta | ADR-0017 (Addendum) | Média | ✅ | — |
| D2D-3B | Onda 5 (plano `glintfx/docs/superpowers/plans/2026-07-23-onda5-draw2d-primitives.md`, seções 3-6, brief 8.2; decisões D23-D32) | C++23/Draw2D | **Plumbing do D2D-3 (primitivas/layers/scissor/bbox) sobre a geometria pura do D2D-3A** (fatia do implementer B, `backend-engineer` DIFERENTE do A; consome `primitives2d.hpp`/`compute_content_bbox` sem reimplementar NENHUMA fórmula). `glintfx/src/sprite_batch.hpp` ganha `flush_pending()` (D27/D28, ADITIVO, ~4 linhas -- finaliza a corrida corrente, mantém o bracket aberto; **`git diff` do arquivo mostra SÓ esse método**, zero linha removida/alterada em qualquer outro membro). NOVO `glintfx/src/layer_queue.hpp` (PURO, zero GL, `namespace glintfx::draw2d_detail`): `LayerQueue::push()`/`drain_grouped()` -- stable-sort por `layer` (`std::stable_sort`, camada igual = ordem de submissão) DEPOIS agrupamento em corridas maximais por scissor idêntico (`scissor_equal()`, agrupamento acontece na ordem ORDENADA, não na de push -- pinado por um mata-mutação dedicado), cap `kMaxCommands=262144` fail-high (`PushResult::CapExceeded`, descarta sem crescer). `glintfx/src/draw2d.cpp`: textura branca 1x1 premultiplicada no `init()` via `create_white_texture()` (D23, slot NORMAL do registry id 1, falha → `init()` false); `Impl::project_corners()`/`Impl::submit_quad()` (helpers NOVOS, D24 -- projeta por câmera quando setada, revalida finitude, ou enfileira em `layer_queue` quando `layer_armed` ou streama via `batch.draw_quad()`+`drain_ready()`); os 5 métodos de primitiva (`draw_filled_rect` x2, `draw_filled_quad`, `draw_line`, `draw_rect_outline` -- geometria via `primitives2d.hpp` do A, ordem de guarda literal finito→degenerado→projeção→pós-conta); `set_layer()` (D27: arma bufferizado, drena o streaming pendente via `flush_pending()`+`drain_ready()` ANTES de armar); `set_scissor()`/`reset_scissor()` (D28: validação D15, `w/h<=0` legal "clipa tudo", mudança em streaming força `flush_pending()`+drain sob o scissor VELHO); `texture_content_bbox()` (D29: `TextureSlot::bbox` cacheado no `load_texture()`, validação D7 completa no getter); `Impl::replay_layer_queue()` (chamado no `end()` ANTES das 2 linhas existentes `batch.end()`/`drain_ready()`, que ficam INTOCADAS -- cada grupo aplica o próprio scissor e força `flush_pending()`+`drain_ready()`); o ÚNICO diff comportamental em código pré-existente é a linha de scissor do `set_gl_state_for_draw()` (`glDisable` incondicional vira condicional D28 -- **confirmado por `git diff`: essa é a ÚNICA linha `-` no arquivo inteiro**, todo o resto é adição pura, inclusive os overloads de `draw_sprite()` existentes que ganham um `if (impl_->layer_armed)` no TOPO, mas nenhuma linha pré-existente foi removida/reescrita). **Tensão nomeada com o brief:** a regra "overloads de `draw_sprite` ficam byte-idênticos" do D31 colide com o próprio contrato JÁ PUBLICADO em `draw2d.hpp` pelo A ("a camada CORRENTE marca todo `draw_sprite()`/primitiva") -- resolvida com a inserção MÍNIMA possível (branch novo, zero linha pré-existente tocada), não com reformulação; flagada aqui pro orquestrador confirmar retroativamente. NOVO `glintfx/tests/layer_queue_sanity.cpp` (PURO, incondicional): ordem estável dentro de uma camada, camadas negativas/intercaladas ascendentes, agrupamento de scissor DEPOIS da ordenação (não antes -- mata-mutação dedicado com 2 grupos esperados vs. 4 se agrupasse antes), cap 262144 com stream de 300k, sobrevivência exata de campos no dreno (câmera-no-momento-da-chamada). NOVO `glintfx/tests/draw2d_primitives_render_sanity.cpp` (readback GL, ESTATÍSTICO): fórmula D8 sobre o texel branco, contorno alpha-0.5 sem double-blend (canto==borda), linha triplicando de banda sob zoom de câmera 3 (delta test), rect preenchido rotacionado sob câmera pi/2 (overload de transform), scissor em DOIS retângulos fora-do-centro nas duas metades y + scissor vazio clipando tudo, camadas em ordem invertida vs. ordem de chamada (cores hand-derived clara e distinguível), sprites+primitivas intercalados com ordem de submissão embaralhada (sobrevivência de troca-de-textura no replay ordenado). EXTEND `glintfx/tests/draw2d_hostile_sanity.cpp`: as 11 novas superfícies públicas em pré-init E movido-de (mesma lição cppcheck do PROG-1), corpus NaN/Inf pras 5 primitivas, `set_scissor()` hostil com prova por render (estado anterior mantido), `set_layer()` durante bracket degenerado, stream de 300k comandos em modo bufferizado (cap segura, ASan limpo), `texture_content_bbox()` com handle obsoleto/zerado/estrangeiro. EXTEND `glintfx/tests/draw2d_ui_coexist_sanity.cpp`: +1 caso de scissor deixado ATIVO propositalmente antes de `ui.render()` -- prova que `UiLayer` NÃO é afetado (o próprio `GlStateGuard`, `gl_state.hpp:68/72/129/131`, restaura scissor box+enable) enquanto o passe de sprite anterior FOI clipado de verdade (prova que o scissor era real, não no-op). `glintfx/CMakeLists.txt`: `project(VERSION 0.22.0)`. **Suíte 96/96 GLFW=ON / 67/67 embed** sob Xvfb isolado (+2/+2 vs. baseline pós-D2D-3A 94/65) + leg ASan (5 binários relevantes rodados sob `GLINTFX_SANITIZE=ON`, zero leak/UB -- suíte ASan completa não rodada por limite de 1-build-pesado-por-vez desta máquina); `tools/check_encapsulation.sh` OK; os DOIS `nm` (ADR-0013 zero `gl[A-Z]` cru nos 2 builds; `GLINTFX_MODULE_DRAW2D=OFF` = 0 matches de `Draw2d\|draw2d_detail\|LayerQueue\|LayerCommand\|submit_quad\|SpriteBatch` via c++filt, com `ON` = 581); `cppcheck` (`--enable=warning,performance,portability`) = 0 achados nos 4 arquivos tocados/novos (ruído pré-existente do `stb_image.h` vendorizado, não deste item); `clang-format --dry-run --Werror` limpo (achados pré-commit em `draw2d.cpp`/`layer_queue.hpp`/2 arquivos de teste, corrigidos com `-i`, suíte re-verificada verde depois nas duas configs); MSVC-safe por inspeção (zero `std::min`/`max` cru, zero header Unix-only, código novo só `<algorithm>`/`<cstdint>`/`<vector>` -- compilação MSVC real não verificada localmente, sem toolchain Windows neste ambiente). **`tools/check_doc_line_refs.sh`: 4 citações quebradas em `docs/draw2d.md`** (linhas 336 e 778, cada uma citando `draw2d.cpp:535`→`draw_sprite` e `draw2d.cpp:480`→`destroy_texture`, deslocadas pelos meus edits) -- LISTADAS pro orquestrador, **NÃO consertadas aqui** por instrução explícita do brief (fatia do `technical-writer`, DOC-D2D3, roda depois). Implementer `backend-engineer`; review adversarial (`qa-engineer`) e re-verificação do orquestrador **AINDA PENDENTES** nesta branch. | Alta | D2D-3A | Alta | ✅ | — |
| PERF-D2D3 | Onda 5 (plano `glintfx/docs/superpowers/plans/2026-07-23-onda5-draw2d-primitives.md`, seção 5 item 5, brief 8.3; decisão D30) | Performance/Draw2D | **Orçamento de performance declarado do Draw2D** (fatia do `performance-engineer`, papel DISTINTO do `qa-engineer` adversarial; roda após o D2D-3B, que precisava do módulo construído). NOVO `glintfx/tests/draw2d_perf_budget.cpp`, incondicional (gateado só por `GLINTFX_MODULE_DRAW2D`), rodando nas DUAS configs sob a mesma fixture `${_embed_dep} glloader`/Xvfb de `draw2d_render_sanity.cpp`. Três métricas, cada uma imprimindo `GLINTFX_PERF <chave>=<valor>`: **(a)** `pure_batcher_quads_per_s` -- 100k quads pelo `SpriteBatch` puro (zero GL); **(b)** `layered_streaming_ratio` -- o MESMO stream de 100k comandos por `LayerQueue::push()`→`drain_grouped()`→replay via `SpriteBatch::draw_quad()` (a MESMA sequência do `Impl::replay_layer_queue()` de `draw2d.cpp`), reportado como razão de tempo sobre (a); **(c)** `e2e_10k_sprite_1k_prim_median_ms` -- bracket real de 10k sprites (1 textura) + bracket real de 1k `draw_filled_rect`, sob GL 3.3 vivo em Xvfb/llvmpipe, `glFinish()` antes do relógio parar, mediana de 30 frames (2 de aquecimento descartados). **Estabilização de ruído (achado ao vivo nesta onda):** uma corrida única de 100k quads leva só ~20-30ms -- curto o bastante pra jitter de agendador/turbo-boost mover a razão (b) em ±15-20% execução a execução (3 corridas seguidas mediram 2,06/2,23/1,94 antes da estabilização); a métrica (b) agora roda 8 repetições de (a)+(b) NAS COSTAS UMA DA OUTRA (descarta a 1ª como aquecimento), a razão do gate é a MEDIANA das razões PAREADAS por repetição (não razão-de-medianas). **Gate em dois níveis (D30, literal):** com `GLINTFX_PERF_STRICT=1` (env setada SÓ no job `fonteng` do `.forgejo/workflows/heavy.yml`, runner `claudio` -- a UNICA linha nova naquele arquivo), (a)/(c) falham se cruzarem a própria baseline em mais de 1,5x; sem a env, só um teto de sanidade 10x falha. O gate de razão (b) `<= 2,0x` é **incondicional em todo lugar** (relativo-à-máquina, D30). **Baselines MEDIDAS, não inventadas** (regra da casa): `pure_batcher_quads_per_s`=8 500 000, `e2e_10k_sprite_1k_prim_median_ms`=5,0 -- medidas por mim em 2026-07-23 no container de dev sandboxed desta sessão (Fedora 44, Intel Core i5-12500H 12ª geração, 16 núcleos, 31 GiB RAM), **NÃO o runner `claudio` literal** (máquina física que este agente não alcança) -- flagado no próprio comentário de cabeçalho do teste como baseline PROVISÓRIA, a trocar pelos números da 1ª execução real no `claudio`. **ACHADO REAL, não bug do teste:** a razão (b) mede ~3,4-3,6x nesta máquina, de forma reprodutível (5+ corridas, ambas configs) -- ACIMA do teto de 2,0x do D30. Causa-raiz isolada com uma sonda avulsa: `LayerQueue::drain_grouped()` (`glintfx/src/layer_queue.hpp`) move structs `LayerCommand` inteiras (~96 B, a própria estimativa do D27) no `std::stable_sort` pra reordenar por uma chave `int layer` só, dominando ~60% do custo do caminho em camadas; `draw2d.cpp`'s `Impl::replay_layer_queue()` roda a MESMA `drain_grouped()`, então não é artefato de benchmark. O teste entrega o gate CONFORME DESENHADO (não afrouxado em silêncio) -- **decisão pendente do dono do D2D-3B/D30**: otimizar `LayerQueue` (ordenar uma chave leve, não a struct inteira) ou revisar o teto de 2,0x. `docs/draw2d.md` ainda não cita `draw2d_perf_budget.cpp` (a fatia do `technical-writer`, DOC-D2D3, pega os números deste relatório). **Suíte confirmada verde nas DUAS configs** (só `draw2d_perf_budget` falha, no achado do gate (b), como projetado -- ON 96/97 + OFF 67/68, único outro "falha" foi `app_resize_smoke` por contenção de Xvfb de um processo órfão meu, confirmado flaky/não-relacionado ao re-rodar isolado, 100% verde); `tools/check_doc_line_refs.sh`: 94 ok/0 falha/1 skip pré-existente, intocado; `clang-format --dry-run --Werror` limpo (`-i` aplicado uma vez); MSVC: o job `windows-msvc-build` builda SÓ o alvo agregado `glintfx` (`GLINTFX_BUILD_TESTS=OFF`) -- nenhum arquivo de teste, deste nem de nenhum outro, é sequer compilado lá, então a preocupação de MSVC-safety do brief não se aplica a este arquivo na prática (confirmado lendo `.github/workflows/ci.yml`). Não toquei em `layer_queue.hpp`/`draw2d.cpp`/outros arquivos de src, nem nas linhas D2D-3A/D2D-3B desta tabela. Implementer `performance-engineer`; review adversarial (`qa-engineer`) e re-verificação do orquestrador **AINDA PENDENTES** nesta branch. | Alta | D2D-3B | Média | ✅ | — |
| DOC-D2D3 | Onda 5 (plano `glintfx/docs/superpowers/plans/2026-07-23-onda5-draw2d-primitives.md`, brief 8.4) | Docs | **Primitivas/layers/scissor/bbox/perf do D2D-3 documentadas em `docs/draw2d.md`**, escrito por `technical-writer` (agente distinto dos 3 implementers, roda após o B pras assinaturas/linhas reais; pega os números do relatório do `performance-engineer`). Conferi CADA assinatura/linha citada direto em `glintfx/include/glintfx/draw2d.hpp`/`glintfx/src/draw2d.cpp`/`glintfx/src/primitives2d.hpp`/`glintfx/src/layer_queue.hpp`/`glintfx/src/gl_state.hpp` (não copiei do plano cegamente -- achei e corrigi 1 desvio real: o plano cita `draw2d.cpp:284` pro `set_gl_state_for_draw`/linha de scissor, a linha real é `358`/`363-374`). Bilíngue EN→PT, sem em-dash em prosa nova. (1) 4 seções novas em `docs/draw2d.md` (inseridas entre "SpriteTransform" e "API reference", espaço-ATIVO/D24 primeiro): **"Primitives"** (D23 textura branca 1x1 no `init()`, D24 espaço-ATIVO+fórmula D8 sobre o texel branco, linha D25 com tampas butt, contorno D26 com as 4 faixas SEM sobreposição+clamp de colapso+espessura escalando com zoom por projeção -- o caso de uso do consumidor citado como saindo da regra geral, sem API dedicada); **"Layers"** (D27 opt-in default streaming v0.21.0 literal, stable sort com garantia de ordem-de-submissão em empate, cap 262144, armar no meio do bracket drena o streaming ANTES, reset no `end()`, "layers ordenam SÓ dentro do bracket" citando ADR-0017 eixo 3); **"Scissor"** (D28 SEMPRE px de tela, fórmula `glScissor(x, target_h-(y+h), w, h)` verbatim citando `primitives2d.hpp:272`, `w/h<=0` legal "clipa tudo", muda-no-meio força flush em CONTRASTE nomeado com a câmera D13, e o aviso de severidade alta de embed com receita `reset_scissor()` + citação verificada `gl_state.hpp:68/72/129/131`); **"Bbox de conteúdo de textura"** (D29 `alpha>0`, espaço do `src_px`, computado uma vez no `load_texture()`, `found==false` nos dois casos). (2) Tabela de API ganha as 10 linhas novas (EN+PT) com `arquivo:linha` reais. (3) Seção D9 (contrato de estado GL) ATUALIZADA: a linha "Scissor test" vira CONDICIONAL (era `glDisable` incondicional) + parágrafo novo declarando a mudança de contrato em voz alta, nos dois idiomas. (4) Seção **"Performance" NOVA** (D30) com os 3 números REAIS lidos do cabeçalho de `glintfx/tests/draw2d_perf_budget.cpp` (não inventados): baseline `pure_batcher_quads_per_s`=8 500 000, baseline `e2e_10k_sprite_1k_prim_median_ms`=5,0, teto `layered_streaming_ratio`=4,0x (**achado ao ler o fonte**: o teto do plano era 2,0x, mas o teste já documenta um achado real de ~3,4x medido com clang + reversão de uma otimização + revisão do teto pro líder pra 4,0x em 2026-07-23 -- documentei o número REAL do código, não o do plano, e citei a proveniência provisória/máquina-sandbox explicitamente). (5) "Limites declarados" atualizado: removida a linha obsoleta "D2D-3, promovida, próxima onda" (já entregue), adicionadas as 6 sementes reais (`SEED-D2D-SHAPES`/`LINECAPS`/`LAYER-TEXSORT`/`WORLDCLIP`/`BBOX-THRESH` + nota de que os tetos de 4096-quad/262144-comando seguem limites de memória). (6) "Relacionados" ganha `primitives2d.hpp`/`layer_queue.hpp`/link do plano da Onda 5. (7) `docs/embed-integration.md` seção 23: parágrafo novo de cross-ref (EN+PT) sobre o D2D-3 tocar o contrato de coexistência pela 1ª vez, apontando pra receita de scissor. (8) Rascunho da entrada `[0.22.0]` do `CHANGELOG.md` (Added: D2D-3A+D2D-3B; Changed: a linha de contrato D9; Performance: PERF-D2D3 com o achado real do ratio e a decisão do líder de revisar o teto; Documentation: DOC-D2D3) -- downgrades declarados junto. `tools/check_doc_line_refs.sh`: **1 falha encontrada e corrigida antes do commit** (citação PT `` `LayerQueue::drain_grouped()` (`layer_queue.hpp:206`) `` -- o símbolo mais próximo do parêntese não batia com a linha 206, que é o `std::stable_sort` de dentro da função, não a própria definição de `drain_grouped` (linha 203); reordenei a frase pra citar `std::stable_sort` no lugar certo, mesmo padrão que a versão EN já usava) -- **119 ok / 0 falha / 1 skip pré-existente** (rodado ANTES e DEPOIS de todo o bloco de edits, inclusive depois do fix). Não toquei em `glintfx/src/`, `glintfx/tests/`, nem nas linhas D2D-3A/D2D-3B/PERF-D2D3 desta tabela (fatia dos implementers). Não rodei `tools/preci.sh` completo (build pesado, fora do escopo mecânico de uma fatia de doc, e o QA está com o token do build pesado) -- só o gate de citação, direto, duas vezes. Review adversarial (`qa-engineer`) e re-verificação do orquestrador **AINDA PENDENTES** nesta branch. | Média | D2D-3B, PERF-D2D3 | Baixa | ✅ | — |
| D2D-TEXT-T1A | Onda 6 (plano `glintfx/docs/superpowers/plans/2026-07-24-onda6-draw2d-text.md`, seção 4, T1a; ADR-0018) | C++23/Draw2D | **A costura de RASTER do texto do Draw2D -- fonte única, ZERO GL, ZERO RmlUi** (fatia T1a, `backend-engineer` #1; T1b, agente distinto, começa em paralelo assim que o 1º commit desta fatia congela a interface de avanço, arquivos disjuntos `text_layout.*`). NOVOS `glintfx/src/text_raster.hpp`/`.cpp` (TX2 (a), construído DIRETO sobre `glx_sfnt_open`/`glyph_id`/`hmetrics`/`glyph_outline`/`kern`/`glx_rasterize_outline`/`glx_hint_outline` já vendorizados -- NÃO reuso da casca `FontEngineOwn`, ADR-0018 decisão (a)): decode UTF-8 (`decode_utf8_codepoint`, política U+FFFD "subparte máxima" -- overlong/surrogate/acima-de-U+10FFFF/truncado/lead inválido todos tratados, nunca engole byte de sequência não-quebrada); `TextFace` (ciclo de vida sobre `glx_sfnt_open`, POSSUI cópia própria do blob, teto 64 MiB TX6); `glyph_advance_px`/`kern_px`/`metrics_px` (SEMPRE crus, sem snap -- ver a nota "DIVISÃO AVANÇO/BAKE" do próprio header pro porquê wrap/align não podem depender de câmera); `GlyphAdvanceSource` (**interface CONGELADA no 1º commit da fatia, sozinha** -- struct de ponteiro de função que o `text_layout.hpp` do T1b consome, zero acoplamento a `TextFace`/`glx_sfnt_*`, compile-time enforced) + `make_advance_source`/`AdvanceContext`; `plan_glyph_bake`/`bake_glyph_into`/`glyph_bake_scratch_floats` (bake outline→`glx_hint_outline` (só vsnap Y, quando pedido)→`glx_rasterize_outline` em scratch de quem chama, geometria de fonte única entre as duas funções -- nunca podem discordar, mesma disciplina TX17; teto próprio `kMaxGlyphBitmapDim=4096` por-glifo, independente do teto de página de atlas do TX3/TX4, que é do T2). **TX9 -- desacoplamento de CMake:** `glintfx/CMakeLists.txt` linha do `if(GLINTFX_OWN_FONT_ENGINE)` que seta `_glx_core_src_dir`/`_glx_core_include_dir` alargada pra `if(GLINTFX_OWN_FONT_ENGINE OR GLINTFX_MODULE_DRAW2D)` (`CMakeLists.txt:688`) -- confirmado por `git diff` que SÓ a condição do `if` mudou (nenhuma outra linha estrutural); `GLINTFX_OWN_FONT_ENGINE` INTOCADO, segue estritamente o seletor de motor da ui (`glintfx_text`, `CMakeLists.txt:1051-1056`, inalterado). Como o `glintfx_draw2d` final consome estes mesmos objetos sem colisão de símbolo duplicado contra o `glintfx_text` quando as DUAS flags estão ON (o default) é trabalho do T2 -- fora de escopo desta fatia. NOVO `glintfx/tests/text_raster_sanity.cpp` (PURO, gateado só por `GLINTFX_MODULE_DRAW2D`, **DELIBERADAMENTE não linka `glintfx`** -- compila `sfnt.c`/`raster.c`/`hint.c` DIRETO no executável via as mesmas variáveis do TX9, provando o desacoplamento standalone: builda e passa com `-DGLINTFX_OWN_FONT_ENGINE=OFF`, confirmado ao vivo). **Valores à mão (TX17, gate raster)**: fixtures `demos/showcase/assets/OpenSans-Regular.ttf` (`size_px=16`, escala EXATA 1/128) e `PixelOperatorMono.ttf` (`size_px=100`, escala EXATA 1/16 -- monoespaçada, TODO glifo advance==50.0px, SEM tabela `kern`), oráculo derivado via `fontTools` lendo `head`/`hhea`/`hmtx`/`kern` direto e multiplicado à mão no papel (nunca chamando o próprio código sob teste) -- gids/advances/kerns/métricas de linha testados com `==` exato (sem epsilon, toda fração escolhida representável em IEEE754), incl. par sem entrada de kern (`kern(a,a)==0`) DISTINTO de fonte sem tabela `kern` nenhuma (PixelOperatorMono), bake de `'A'` com prova de tinta não-zero + guarda de plano descasado, `' '` como glifo zero-tinta válido, corpus hostil de UTF-8 (overlong 2/3-byte, truncado, continuação solta, lead inválido, surrogate U+D800, acima de U+10FFFF -- 10 casos com `{codepoint, consumed}` derivados à mão), piso hostil de `TextFace::open`/`glyph_advance_px`/`kern_px` (nulo/zero-len/size_px não-finito ou `<=0`/gid fora de faixa), e o contrato `GlyphAdvanceSource` exercitado ponta-a-ponta (trampolins batem com `TextFace` direto, face/size_px inválidos produzem source `!ok()` com todo ponteiro nulo). **Suíte 98/98 GLFW=ON / 69/69 embed** sob Xvfb, linha LITERAL do CI (clang, sem `CMAKE_BUILD_TYPE`) nas duas configs, mais uma 3ª config isolada `-DGLINTFX_OWN_FONT_ENGINE=OFF -DGLINTFX_MODULE_DRAW2D=ON -DGLINTFX_BACKEND_GLFW=OFF` (só pra provar o TX9, `text_raster_sanity` sozinho, PASS) e uma 4ª configure-only `-DGLINTFX_MODULE_DRAW2D=OFF` (confirma que nada quebrou quando o átomo inteiro está OFF). `tools/check_doc_line_refs.sh`: **2 falhas achadas e corrigidas antes do commit** (meus ~25 linhas inseridas no `CMakeLists.txt` deslocaram `docs/embed-integration.md:101`/`:107` -- citação de `RMLUI_GL3_CUSTOM_LOADER` apontava pra linha errada -- e os 4 números de linha que a própria `docs/adr/0018-draw2d-text.md` cita pro gate ainda-não-implementado, `664`→`688`/`1026-1031`→`1051-1056`, sem mudar a prosa histórica "as of this ADR", que segue verdadeira) -- **120 ok / 0 falha / 1 skip pré-existente**, rodado ANTES e DEPOIS. `clang-format -i` aplicado (achado pré-commit real: espaçamento de `public:`/`private:` e comentário de fim-de-linha). `-fsyntax-only`/`cppcheck` limpos nos 2 arquivos novos. Implementer `backend-engineer` #1 (T1a); T1b (`text_layout.hpp/.cpp`, wrap/align TX15/TX16) segue em paralelo, agente distinto; T2 (integração `Draw2d`) e review adversarial (`qa-engineer`) **AINDA PENDENTES**. | Alta | ADR-0018 | Média | 🔍 Pendente verificação | — |
| L2-BRAINSTORM | V2W1 | v2/Produto | **Porta de entrada da v2 — CONCLUÍDA.** Brainstorm bigtech (CPO+ux-ui-designer) + verdito de integração GusWorld (software-architect) + [ADR-0008](docs/adr/0008-embed-guest-mode.md) (embed mode) + [spec da v2](docs/superpowers/specs/2026-06-30-glintfx-v2-design.md) (aprovado pelo líder) | Alta | v1 fechada | Média | ✅ Concluído | ✓ |
| L2-EMBED | V2W2 | v2/Arquitetura | **Keystone (F1) — ENTREGUE:** embed/guest mode `UiLayer` (anexa ao contexto GL do host, compose-only, eventos injetados+gamepad, `GlStateGuard`). SDD T1–T6, suíte 10/10, review final internal-auditor PRONTO; mergeada na main, **tag `v0.2.0`**. **OWD** | Alta | L2-BRAINSTORM | Alta | ✅ Concluído | ✓ |
| L2-COMPONENTS | V2W3 | v2/UI | **F2 (pull incremental, 2026-07-01):** tokens-first + component library de UI de jogo (button/panel/dialog/menu/label, efeitos como modificadores) + `glintfx::ui` opt-in. Plano pronto **no branch `feat/v2-f2-components`** (arquivo `docs/superpowers/plans/2026-06-30-glintfx-v2-f2-components.md`, **não mergeado no `main`** — 404 aqui), branch parada — só inicia com demanda real confirmada (não agendada) | Média | L2-EMBED | Alta | ⏳ Pendente | — |
| L2-GUSWORLD | V2W4 | v2/Integração | **F3:** GusWorld adota o glintfx via embed (ADR-010) no cockpit — **cockpit limpo de contorno (confirmado 2026-07-03):** data-model + `set_click_callback` + `:hover` nativo, tudo direto. A v0.2.5 matou 147 linhas do `battle_cockpit_pills` (hit-test manual que espelhava geometria RCSS à mão), red/green de equivalência passou. `get_element_box` também em uso (cursor do hover-selftest). Único aberto = `GAP-2b` (lado glintfx, sem demanda). Esforço no repo GusWorld | Média | L2-EMBED | Alta | ✅ Concluído | ✓ |

## INBOX (descobertas não priorizadas)

**Auditoria `AUDIT_FIND.md` — MAJORITARIAMENTE RESOLVIDA (frescor 2026-07-10): sem item URGENTE acionável.** Dos 41 achados: 16 corrigidos na `v0.4.1`; generalização de API na `v0.5.0` (`AUD-PUB-1/3/4` + `clang↔CI`); cobertura embed na `v0.6.0` (`AUD-TEC-7`); `AUD-MEM` (local+global) resolvido; `AUD-C0-2/4` resolvido; e o épico `AUD-TRANS-*` virou a trilha de internalização (FT-F0..F3 entregues, `core-v0.3.0`). **Resta apenas:** `AUD-TEC-6` (consolidação breaking de API) ADIADO conscientemente pelo líder (candidato a `v1.0`), e `AUD-PUB-2/6` remanescentes = sementes pull (sem 2º consumidor que as exija). Detalhe histórico completo abaixo:
- **Auditoria de distribuição pública + memória (2026-07-07)**: relatório completo em [`AUDIT_FIND.md`](AUDIT_FIND.md) (read-only, Caetano/CTO). **41 achados** (40 originais + `clang↔CI`). Veredito: preocupação do líder confirma-se PARCIALMENTE — fronteira pública exemplar, o problema é o que o GusWorld nunca exercitou. **✅ 16 RESOLVIDOS e publicados na `v0.4.1`** (2026-07-07) via workflow de 3 agentes Sonnet + review adversarial + ASan + embed-only: robustez `AUD-TEC-1..5` (incl. 1 Critical UAF de reentrância provado sob ASan), Camada 0 `AUD-C0-1/3/5`, docs/licença `AUD-PUB-7/8/9/10`, version-drift `AUD-TEC-8/9`, frescor `AUD-TEC-10`/`AUD-C0-6`. **⏳ RESTAM as decisões do líder** (não autoexecutadas): generalização de API `AUD-PUB-1` (bug real de resize do `App`)/`2/3/4/5/6`, consolidação breaking `AUD-TEC-6`, cobertura embed `AUD-TEC-7`, `AUD-TRANS-*`/ADR-0009 (ver item abaixo), memória GLOBAL `AUD-MEM-3/4/5/7` + reorg local `AUD-MEM-1/2/6/8`, micro-decisões `AUD-C0-2/4` (✅ RESOLVIDAS 2026-07-09 — poda syscall + adendo ADR-0002, ver banner de estado), e o achado `clang↔CI`.
  - **Decisões do líder (2026-07-07) — ✅ itens 1,2,4 EXECUTADOS na v0.5.0; item 3 adiado:** (1) `AUD-PUB-1` → corrigir **Opção A** (automático: `App::render()` cacheia `(w,h)` e chama `set_viewport` interno quando muda, ~5 linhas, zero API nova). (2) `AUD-PUB-3/4` → **implementar (será v0.5.0 — adição de API pública = minor pela política SemVer)**: `enum Key` **completo** (+Delete/Home/End/PageUp/PageDown) + novo `set_click_info_callback(ClickInfo{id, button, x, y, double_click})` mantendo o callback só-id atual intacto (aditivo, non-breaking). `AUD-PUB-2/6` (input injection no `App`, eventos RmlUi genéricos change/submit/focus, setters imperativos, get do data-model, load_font runtime, cursor, clipboard) = sementes INBOX. **Nota (feedback GusWorld 2026-07-07):** v0.4.1 confirmada drop-in (1508/1508 verde, zero regressão); o consumidor pediria o MÍNIMO (só `button` + PageUp/Down; lê `SDL_Keycode` direto e resolve coords via SDL pro resto) — o líder escolheu o COMPLETO conscientemente, priorizando completude da lib de distribuição pública sobre consumer-minimalismo. (3) `AUD-TRANS-*`/ADR-0009 + `AUD-TEC-6` → **registrar e ADIAR** (transição = endgame sem prazo; consolidação breaking = candidata v0.5.0/v1.0). (4) `clang↔CI` → **alinhar o CI pra clang** (setar `CC`/`CXX=clang` nos 2 workflows, validar com `forgejo-runner exec` antes de subir). Recomendações do orquestrador ainda pendentes de execução (não decididas nesta rodada): reorg memória local `AUD-MEM-1/2/6/8`, `AUD-TEC-7` (teste data-model no path embed), anexar `AUD-C0-2/4` às ondas `AUD-*`/`REL-TAG` da Camada 0; bullets de memória GLOBAL `AUD-MEM-3/4/5/7` a aprovar 1 a 1. **STATUS v0.5.0 (2026-07-07):** (1) resize ✅ (`8fc76be` render + `08ad844` snapshot gêmeo achado pelo review); (2) `enum Key` completo ✅ (`240776e`) + `set_click_info_callback`/`ClickInfo` ✅ (`589b0f4`) com `button` **FUNCIONAL pros 3 botões** (esquerdo via `Click` nativo; right/middle via `Mousedown`+`Mouseup` sintetizado, `5bee559`; `double_click` esquerdo-apenas — limitação nativa RmlUi); (4) CI clang ✅ (`3d38423`, validado com `forgejo-runner exec`, clang 18). (3) ADR-0009/`AUD-TEC-6` adiados. Review adversarial (sem achados) + ASan/embed limpos. `AUD-PUB-2/6` → sementes (abaixo).
- **Plano de Transição — Camada 1 rodando sozinha sobre a Camada 0**: seção dedicada em [`AUDIT_FIND.md` §5](AUDIT_FIND.md#5-plano-de-transição--camada-1-rodando-sozinha-sobre-a-camada-0-aud-trans-) (achados `AUD-TRANS-0..5`), detalhamento técnico do épico `L1-INTERNALIZE` (LW8, linha 92 desta tabela). **`AUD-TRANS-5` é o gate**: decisão de fronteira entre camadas (ADR-0006 hoje diz "não se linkam, fronteira é o processo" — incompatível com internalização real), 3 opções registradas (A: link estático incremental, recomendada tecnicamente · B: process-boundary/IPC · C: clean-room em 2 fases), precisa de `AskUserQuestion` ao líder + um ADR-0009 ANTES de qualquer código. Ordem sugerida pós-decisão: gl3w (P, quick win) → `SOV-ALLOC`/`SOV-FCONV` na Camada 0 → FreeType via `FontEngineInterface` (G) → RenderInterface de efeitos próprio (G) → RmlUi core (GG, endgame sem prazo). Recomendação: **não internalizar GLFW** (o build embed-only já vive sem ele desde v0.2.1). **→ Formalizado 2026-07-09 (roadmap ativo, Cósimo/CoS):** gate = **`L1-ADR9`** (LW8), 1º marco = **`L1.14-GLLOADER`** (LW8), pré-requisitos Camada 0 = **`SOV-ALLOC`/`SOV-FCONV`** (W15). `SOV-MATH`/`SOV-SYS2`/`SOV-CXXRT` seguem sementes (pull, condicionais ao ADR-0009).

- **`D2D-BLOCO` -- ordem canônica do Draw2D até ser adotável, com escopo de DISTRIBUIÇÃO GERAL** (**DECISÃO DO LÍDER 2026-07-23, corrigindo a formulação do orquestrador**): a pergunta que eu fiz (*"atender ou não o que o GusWorld pede?"*) estava **errada de origem**. A régua já estava canonizada e eu deveria ter aplicado em vez de perguntar -- verbatim do líder: *"já expliquei um milhão de vezes que ele é consumidor principal, mas o framework é pra distribuir. LOGO, o que gusworld pede é prioridade, MAS NÃO é pra fazer APENAS o que ele pede."* **Os dois eixos são separados e ambos valem ao mesmo tempo:** (1) **PRIORIDADE (o QUÊ, e a ordem)** -- vem do consumidor principal: as 4 lacunas medidas sobem no programa, na ordem de valor que **eles** declararam (`câmera > texto > preenchido/contorno > bbox`); (2) **ESCOPO de cada peça (o QUANTO, e a barra)** -- é de **distribuição geral**, NÃO o subconjunto que o GusWorld usa. Consequência concreta, peça a peça: **`draw_text`** não é o atlas ASCII 32..126 deles -- é o que um framework distribuído precisa (UTF-8, o **motor de fonte próprio clean-room que já temos** da Camada 0/`L1.20-FONTFLIP`, kerning, e a decisão de como conviver com o texto da `ui` -- exige ADR); **`draw_rect_outline`** não é só a espessura em unidade de mundo que eles pediram, é a primitiva completa; **`texture_content_bbox`** genérico (e barato: o dado já está na textura que carregamos); **câmera** já estava planejada genérica (`D2D-2`: origem/rotação/escala + `Camera2d` + `screen_to_world`). **Ordem resultante**: `D2D-2` (câmera, onda seguinte, já era a próxima) → **`D2D-3` SOBE da onda 8** (traz `filled rect`/linha/layers/scissor, que cobre 29+19 usos deles) + **`D2D-TEXT` NOVO** (o maior por uso, 44, e o de escopo mais pesado -- **exige ADR próprio** antes de qualquer linha) + **`D2D-BBOX` NOVO** (pequeno, barato, nasceu de bug real de playtest deles) → **só então** retoma SVG / gamepad-finish / window-modes-finish (com o `WM-VSYNC` aprovado) / audio-stream / emoji / docs-wiki. **Anti-viés registrado**: o risco desta reordenação é virar "implementar o renderer do GusWorld"; o antídoto é o eixo (2) -- cada peça sai genérica e documentada para qualquer consumidor, e a régua de qualidade continua sendo público amplo (memória `feedback_escopo_distribuicao_geral`). | Alta | D2D-1B | Alta |
- **`D2D-GAPS` -- as 6 primitivas que faltam pro Draw2D "desenhar um JOGO", medidas no consumidor** (feature · **consumer-driven GusWorld 2026-07-23, contagem de uso REAL no código deles, não estimativa** · **gatilho JÁ DISPARADO** · **decisão de roadmap PENDENTE DO LÍDER**): resposta deles à v0.20.0 -- **vão bumpar o pin (aditiva, risco zero) mas NÃO vão adotar o Draw2D agora**. Motivo, e é estrutural: **o Draw2D desenha sprite em coordenada de TELA; o jogo inteiro deles desenha em coordenada de MUNDO, com câmera.** Das 6 primitivas da `IRenderer` deles (`platform/include/gus/platform/render2d/i_renderer.hpp`), o Draw2D cobre **1**: (a) **`begin_frame(camera_world, w, h)` -- 19 usos -- NÃO temos**; (b) **`draw_text` -- 44 usos, o MAIOR número da tabela -- NÃO temos** (eles têm atlas de glifos próprio sobre FreeType, `font_atlas.hpp`, 32..126 em bitmap grayscale com métricas/UV); (c) **`draw_filled_rect` -- 29 usos -- NÃO**; (d) `draw_textured_rect` -- 22 usos -- **~sim** (`draw_sprite`), mas em px de tela; (e) **`draw_rect_outline` com espessura em unidade de MUNDO -- 19 usos -- NÃO** (não sai de graça: a espessura escala com a câmera); (f) **`texture_content_bbox` -- 5 usos -- NÃO**: devolve a caixa dos pixels NÃO-transparentes de uma textura; **nasceu de bug real de playtest** (retrato de NPC com muita margem transparente fazia a hitbox derivada do canvas disparar o diálogo com o jogador visivelmente longe) -- **barato do nosso lado (o dado já está na textura que carregamos), caro do lado do consumidor**. **Ordem de valor declarada por eles**: `câmera > texto > preenchido/contorno > bbox de conteúdo` -- *"as duas primeiras são as que mudam a categoria da ferramenta"*. **A câmera já é o `D2D-2`** (onda seguinte do programa) -- mas eles registram que **para eles não é conveniência, é pré-requisito de qualquer adoção**. **`draw_text` NÃO está no programa hoje** -- é a maior lacuna por uso e a decisão mais pesada (implica carga de fonte + atlas no átomo Draw2D, e existe tensão com o motor de fonte próprio da Camada 0 e com o texto da `ui`). **O que eles explicitamente NÃO pedem**: reimplementar o renderer deles -- a régua do líder deles é *"genérico-reutilizável vai pro glintfx; regra do jogo fica no GusWorld"*, e **as 6 primitivas são todas genéricas, nenhuma tem regra de jogo dentro**; se existirem aqui, o `platform/render2d` inteiro deles vira candidato a sumir. **Por que não adotam só o `draw_sprite`**: dois caminhos de desenho no mesmo quadro, um com câmera e outro sem -- *"complexidade sem ganho"*. | Alta | D2D-1B | Alta |
- **`GP-WINDOWS` -- backend de gamepad para Windows (XInput/GameInput) atrás da mesma API pública** (feature · **consumer-driven GusWorld 2026-07-23, pedido com gatilho declarado pelo líder deles** · **gatilho: o porte Windows do GusWorld entrar na fila, pós-v1.0.0 deles**): o módulo `Gamepads` é fachada sobre **evdev**, com `#error` guard fora de Linux (escolha coerente e documentada -- evdev não tem substituto portável). Consequência para o consumidor: a **F4 deles** (trocar a casca SDL própria pelo `glintfx::App`) **remove o `SDL_Gamepad`, que é hoje o único caminho cross-platform de gamepad deles** -- ao adotar o App mode, **perdem gamepad no Windows**, não por bug nosso, por natureza da dependência. **NÃO bloqueia a F4** (a v1.0.0 do GusWorld é **Linux-only** por decisão do líder deles; Windows é pós-v1). Pedido: backend Windows atrás da **mesma API pública**, para o `#error` virar **seleção de backend por plataforma**. Nota: **o nosso próprio job MSVC já antecipa isto em comentário** (documenta que a lista de alvos muda "quando o suporte a gamepad chegar no Windows") -- o caminho já estava previsto no CI, agora tem consumidor e gatilho. Se aparecer um segundo consumidor pedindo antes do porte, o caso fica mais forte. | Média | A2-GAMEPAD | Alta |
- **`CI-MSVCGLFW` -- job MSVC ganha perna GLFW=ON, matrix com a perna OFF que já existia** (infra · **subido de onda por decisão do líder 2026-07-23, rodou em paralelo ao Draw2D (Onda 3)** · item **já existia no programa, Onda 7**, esforço P/M, risco baixo, CI-only · **🔍 Pendente verificação -- entregue 2026-07-23** (`devops-sre`, branch `main`, commit local não pushado)): o consumidor (GusWorld) perguntou explicitamente se o job MSVC valida `GLINTFX_BACKEND_GLFW=ON` -- resposta era NÃO (o job só rodava OFF, embed-only), e essa lacuna decidia uma escolha de arquitetura do lado deles (o CTO deles propôs manter uma segunda casca SDL viva por uma onda inteira só pra se proteger dela). **Entregue:** `.github/workflows/ci.yml`, job `windows-msvc-build` virou `strategy: matrix: backend: [ON, OFF]` (`fail-fast: false`, mesmo padrão do `build-and-test`) -- a perna **OFF ficou byte-a-byte inalterada** em comportamento (mesmo cache key, mesmos passos, nunca regride, é o modo de produção real do GusWorld hoje); a perna **ON é nova**: `vcpkg install glfw3:x64-windows` pinado na mesma tag do freetype (`2026.06.24`), cache key própria (`freetype-glfw3`, não colide com a da OFF), `-DGLINTFX_BACKEND_GLFW=ON`, build dir próprio (`build-msvc-ON` vs `build-msvc-OFF`), `glintfx_link_smoke` roda nas duas pernas. **Correção honesta do próprio enunciado do item:** a formulação "9/9 alvos" (`programa-completo.md` linhas 71/185) está ERRADA -- `GLINTFX_MODULE_GAMEPAD` é forçado OFF em QUALQUER `CMAKE_SYSTEM_NAME` não-Linux INCONDICIONALMENTE (evdev não tem substituto portável), independente de `GLINTFX_BACKEND_GLFW`, então o gamepad NUNCA builda no Windows hoje, nem ON nem OFF. **O teto real da perna ON é 8 de 9 alvos** (acrescenta `window` sobre os 7 que a OFF já builda: core/image/text/i18n/fx/audio/draw2d); o 9º depende do `GP-WINDOWS` (linha acima desta tabela, backend XInput/GameInput, gatilho próprio). Comentário de cabeçalho do job (EN+PT) reescrito para carregar este teto honesto -- é a fonte que o orquestrador cita ao consumidor, e não deve dizer 9/9 até o `GP-WINDOWS` fechar. **NÃO VERIFICADO LOCALMENTE** (sem toolchain Windows/MSVC nesta máquina, igual a perna OFF quando foi autorada): YAML validado por `python3 -c "yaml.safe_load(...)"` (sintaticamente correto) e leitura cruzada com `glintfx/CMakeLists.txt` (`find_package(glfw3 REQUIRED)` já gateado em `GLINTFX_BACKEND_GLFW`, alvo `glfw` já usado em `target_link_libraries(glintfx_window ...)`, `RmlUi_Platform_GLFW.cpp` já condicional -- nenhuma mudança em `CMakeLists.txt` foi necessária), mas **compilar de verdade sob MSVC com GLFW linkado é território genuinamente inexplorado** -- o primeiro sinal real é a própria primeira execução de CI. Se quebrar, a resposta é reportar o erro exato (não forçar verde) -- ver o comentário do job. Não toquei em `glintfx/include/glintfx/draw2d.hpp`, `glintfx/src/transform2d.hpp`, `glintfx/src/sprite_batch.hpp`, `glintfx/src/draw2d.cpp` nem `glintfx/tests/transform2d_sanity.cpp` (outro agente no Draw2D, arquivos disjuntos confirmados). | Média | — | Baixa |
- **`SEED-D20-ISOLATE` -- a guarda pós-conta do D20 não é provada isoladamente pela API pública** (verificação · MINOR 1 do review adversarial da Onda 4, 2026-07-23 · **gatilho: refactor que mexa em `Draw2d::draw_sprite(..., SpriteTransform)` ou no `all_finite` do batcher**): o `qa-engineer` **removeu inteiramente** o guard `if (!draw2d_detail::is_finite(final_corners))` de `glintfx/src/draw2d.cpp:684-688`, rebuildou, e os 4 binários de render/hostil **passaram 100%**. **Não é bug**: o `SpriteBatch::draw_quad()` (`glintfx/src/sprite_batch.hpp:206`) tem o **próprio** `all_finite(corners)` de defesa-em-profundidade, que absorve o caso e rejeita o draw -- **nenhum NaN chega na GPU**, a propriedade de segurança está coberta pela redundância. O que fica errado sem o guard do D20 é só a **mensagem de log** (diria "src_px/tint" em vez de "overflow de canto computado"). **Decisão do orquestrador: NÃO consertar agora** -- mexer no caminho de produção por uma string de log é churn com risco maior que o benefício. A cobertura da camada pura existe (`transform2d_sanity::test_d20_overflow_guard` exercita `compute_sprite_corners()`+`is_finite()` direto); o que falta é o teste **através da API pública real**. Se um refactor futuro remover a guarda, ninguém percebe -- por isso o gatilho é "alguém mexer nesses dois pontos". | Baixa | D2D-2B | Baixa |
**Ativos (com gatilho real):**
- **`GATE-PRECOMMIT` -- gates baratos ANTES do commit (shift-left), não só no fim da onda** (infra · **DECISÃO DO LÍDER 2026-07-23, aprovada com dado medido** · **🔍 Pendente verificação -- entregue 2026-07-23** (`devops-sre`, branch `feat/onda3-draw2d`, commit local não pushado): `tools/precommit.sh` (os 3 gates: cppcheck em `glintfx/src/*.cpp|*.hpp` staged com fallback `--project`/per-arquivo, `clang-format-diff` no diff staged, `check_doc_line_refs.sh` condicional por relevância) + wrapper fino `.githooks/pre-commit` (mesmo padrão de `.githooks/pre-push` → `tools/preci.sh`). Provado nos dois sentidos com arquivos descartáveis staged (removidos, `git status` limpo ao fim): bloqueio por cppcheck (`duplicateExpression`+`unreadVariable`) + `clang-format-diff` juntos em 96ms; bloqueio isolado de `check_doc_line_refs.sh` em 1654ms (gate condicional, só dispara quando staged toca doc/arquivo citado); passagem limpa em 80ms (arquivo novo, exercita o fallback per-arquivo do cppcheck); commit que não toca `glintfx/src/` nem `docs/` em 21ms (os 3 gates pulam). **Correção pós-teste ao vivo do líder de equipe (mesmo dia)**: o gate condicional de `check_doc_line_refs.sh` usava grep de SUBSTRING crua do basename -- staged `TODO.md` (quase todo commit desta casa toca, pela própria convenção "citar o ID") casava com toda MENÇÃO crua de "TODO.md" nos docs (nenhuma é citação de verdade), transformando o caso comum em caminho caro: **4108ms medido** num commit realista (`TODO.md` + `.cpp`). Corrigido: o grep agora exige a FORMA exata de citação que o próprio `check_doc_line_refs.sh` parseia (basename imediatamente seguido de `:<dígitos>`, ex. `app.hpp:325`), não a menção crua -- confirmado `grep -E 'TODO\.md:[0-9]'` = zero ocorrências em toda `docs/`, então `TODO.md` para de disparar o gate sozinho, enquanto citações reais (`app.hpp:325` etc.) continuam disparando (verificado). Não toquei em `tools/check_doc_line_refs.sh` (gate de CI intocado, sem risco de regressão nele). **Reteste no caso realista via `git commit` real** (não só invocação direta do script): `TODO.md` + `.cpp` novo staged, hook disparou pelo `.git/hooks/pre-commit` (symlink), **77ms** -- bem abaixo da meta de 500ms do líder. Commit de sonda desfeito (`git reset --soft` + unstage + `gio trash`), árvore limpa.

`core.hooksPath` -- **correção da minha leitura anterior, o líder já ativou e não há conflito**: o `~/.claude/githooks/_chain.sh` (mecanismo global de frescor) já delega ao `.git/hooks/<nome>` LOCAL quando executável -- então a rota certa nesta máquina é symlink (`ln -s ../../.githooks/pre-commit .git/hooks/pre-commit`), não trocar `core.hooksPath`. Já ativado pelo líder (`.git/hooks/pre-commit` symlinkado); `.git/hooks/pre-push` ainda não (lacuna pré-existente, TST-L1-PRECI segue não enganchado nesta clone). Documentado nos cabeçalhos de `tools/precommit.sh`, `.githooks/pre-commit`, `.githooks/pre-push` e no `AGENTS.md` (duas rotas de ativação: clone simples vs. `core.hooksPath` global já reivindicado). · gatilho JÁ DISPARADO): o líder observou que *"cada feat tá cada vez mais demorada"* e a medição confirmou -- Onda 3 com **4 commits de `fix`/`style` contra 3 de `feat`/`test`** (mais correção que construção), e **8h34** contra 5h14 da Onda 2. **Onde o desperdício estava** (as 3 rodadas evitáveis, mais da metade causadas pelo próprio orquestrador): (1) commit de formatação deslocou linhas e quebrou 4 citações de doc porque o `check_doc_line_refs.sh` foi rodado ANTES de formatar, não depois; (2) o commit seguinte trouxe código de teste novo que nunca passou pelo `clang-format-diff`, derrubando o `TST-L1-FORMAT` no release-prep; (3) um **null-deref real** (`draw2d.cpp:378`, `impl_->log_warn()` dentro do bloco `if (!impl_ || ...)` -- crash em objeto movido-de) só apareceu no **CI**, custando build de ~3min + espera, quando o `cppcheck` local o pegaria em segundos. **Custo medido do gate proposto: `cppcheck` num arquivo = 0,079s** -- praticamente grátis. **Escopo**: hook `pre-commit` (a infra já existe, há `.githooks/pre-push`) rodando **só nos arquivos staged** -- `cppcheck` (mesma invocação do CI, `--error-exitcode=1`), `clang-format-diff` nas linhas tocadas, e `check_doc_line_refs.sh` **quando o commit tocar arquivo citado por doc**. O caro (suíte completa, ASan, build MSVC) **continua no fim** -- o `preci.sh` modo fast já mira ~2min e não deve virar pre-commit. **Princípio**: descobrir em 10 segundos em vez de 10 minutos, multiplicado por quantas vezes acontecer. Nasceu da mesma família de lições desta semana (`QA-XVFBISO`: o isolamento pertence a quem executa, não a quem chama; aqui: a verificação pertence ao momento do commit, não ao fim da onda). | Alta | — | Baixa |
- **`WM-VSYNC` -- controle de vsync + consulta da taxa do monitor** (feature · **consumer-driven, pedido do GusWorld 2026-07-23** · **DECISÃO DO LÍDER 2026-07-23: escopo aprovado** · gatilho JÁ DISPARADO, entra na fila rápida): `set_vsync(bool)` / `set_swap_interval(int)` sobre `glfwSwapInterval` + `get_monitor_refresh_hz()` pro host ler a taxa real. **Escopo escolhido pelo líder entre 3 opções**; o **cap arbitrário por software** (ex.: 60 numa tela de 144 via espera no loop) ficou **FORA de propósito** -- `sleep` tem jitter e costuma gerar micro-travamento pior que o problema, e exigiria spin-wait híbrido pra ficar decente; vira semente se alguém pedir. **Custo baixo**: `glfwGetVideoMode` já é usado em `glintfx/src/window_glfw.cpp:141` (caminho de fullscreen, com o guard D5 pra monitor sem modo reportado) e o swap está isolado em `WindowGlfw::swap()` (`window_glfw.cpp:225`). **Estado atual, declarado desde a v0.19.0**: a glintfx **não chama `glfwSwapInterval` em lugar nenhum** -- quem governa é o default do driver/compositor, o que faz o comportamento **variar por máquina**, exatamente o que uma framework deve tornar explícito. **Ponto técnico que motivou o corte**: "fps = taxa do monitor" **É** vsync (interval 1 sincroniza com 60/144/240 sem precisar saber o número); saber o número importa só pra meia-taxa, HUD ou ajuste de simulação. **Nota de origem**: o pedido nasceu de um diagnóstico ERRADO do consumidor (leram 142 fps e concluíram "sem vsync", assumindo tela de 60 Hz; o monitor do líder é **144 Hz**, então 142 fps = 98,8% da taxa = vsync JÁ ativo, zero quadro descartado). O sintoma não existia, mas **o pedido continua legítimo por outro motivo**: dar controle explícito ao dev em vez de depender do default do ambiente. Pertence ao `WM-EXTRAS` (fechamento do átomo de janela); com o pedido do consumidor, sobe de "completude" pra fila rápida. | Alta | — | Baixa |
- **`SEED-D2D-POSTUI` -- frame hook PÓS-UI no `App` (sprites POR CIMA da UI em standalone)** (feature · semente da Onda 3, 2026-07-23 · **gatilho: pedido de consumidor real**): o ADR-0017 fechou **ordem = ordem de chamada, sem config nova** (decisão do líder). No `App`, desenhar dentro do frame hook põe o sprite **sob** a UI, que é o padrão que o GusWorld já roda em produção; no embed o host escolhe livremente. Um hook **pós-UI** daria sprite por cima da UI em standalone, mas é **API especulativa** enquanto ninguém pedir (YAGNI, registrado no próprio ADR).
- **`SEED-D2D-BASEURL` -- `load_texture` honrando `set_asset_base_url`** (feature · semente da Onda 3, 2026-07-23 · **gatilho: pedido de consumidor**): hoje o `set_asset_base_url` prefixa caminho relativo de asset **da UI**, mas o `Draw2d::load_texture` **não** o consulta -- quem usa Draw2D passa caminho por conta própria. Assimetria **documentada** em `docs/draw2d.md`, não escondida.
- **`SEED-D2D-TEXFILTER` -- filtro por textura (nearest-neighbour pra pixel-art)** (feature · semente da Onda 3, 2026-07-23 · **gatilho: pedido de consumidor**): o default gravado é `GL_LINEAR`, que borra arte de pixel quando escalada. Consumidor de pixel-art vai querer `GL_NEAREST` por textura. Barato de acrescentar quando houver demanda concreta.
- **`SEED-UINPUT` -- fechar os 2 itens e2e de janela que a injeção XTest não conseguiu provar** (verificação · pendência honesta da Onda 2, 2026-07-22 · **gatilho: uma nova rodada de QA manual de janela/input, ou qualquer dúvida sobre o comportamento de close/layout em teclado real**): a perna manual da Onda 2 rodou em KWin aninhado com isolamento provado e fechou **3 de 5** itens com evidência limpa -- tecla física → `is_key_down`, **veto de close DENY ponta-a-ponta** (via `ClientMessage` ICCCM `WM_DELETE_WINDOW` real, o mesmo protocolo do clique no X: janela continuou aberta, processo vivo) e focus/iconify (via ações de WM). **Ficaram inconclusivos 2:** (a) o caminho ALLOW do veto + o bypass do `request_close()` e (b) o layout **ABNT2** (`setxkbmap br`: WASD posicional, `ç` na posição `Semicolon`, scancode 97). **Causa é de AMBIENTE, não de produto:** injeção de teclado por `xdotool`/XTest é **flaky neste stack** (KWin aninhado-em-Wayland + Xwayland interno) -- entrega intermitente mesmo com foco X11 confirmado (`getactivewindow` sempre certo), funcionou 1 vez e falhou nas 5-6 seguintes sem mudança de setup; com `XTestFakeKeyEvent` cru houve um toggle **duplo numa única chamada** (suspeita não-confirmada de auto-repeat do X11, `xset q` = "auto repeat: on"). **A lógica desses 2 itens já tem cobertura unitária determinística e limpa sob Xvfb** (`glfw_decide_window_close`, 4 asserções em `glintfx/tests/glfw_event_translate_sanity.cpp`) -- o gap é só a confirmação visual e2e, por isso **não bloqueou a tag**. Caminho recomendado pra fechar: **injeção por `uinput` bruto**, que bypassa X11/XTest inteiro, em vez de insistir neste stack. O que se mostrou **confiável** no aninhado: ações de WM (`windowminimize`/`windowactivate`) e `ClientMessage` ICCCM. Receita validada e as 3 armadilhas de invocação (a flag `--windowed` que NÃO existe, o env isolado que é só do app e não do compositor, e o `GLFW_PLATFORM` que não é env var -- a certa é `XDG_SESSION_TYPE=x11`) estão no `AGENTS.md` e na memória `feedback_nunca_stress_janela_sessao_viva`.
- **`SEED-SCANCODE` -- consulta de tecla por SCANCODE (`is_scancode_down`)** (feature · semente da Onda 2, 2026-07-22 · **gatilho: um consumidor esbarrar numa tecla sem nome no nosso enum**, o caso concreto conhecido é o `/` extra do teclado **ABNT2, scancode 97**, que o GLFW não mapeia pra nenhum `GLFW_KEY_*`): hoje `App::is_key_down` só responde por posições que existem no `enum class Key`; teclas físicas fora dessa tabela são invisíveis pro host. A saída seria um canal paralelo por scancode cru, que não polui o enum público nem quebra o append-only. **Lacuna documentada, não escondida** (`docs/embed-integration.md` §22 e doc-comment de `app.hpp`). Sem gatilho real ainda: o GusWorld usa WASD, que é agnóstico de layout por ser posicional.
- **`SEED-MOUSE9PLUS` -- botões de mouse ≥ 8 por canal lateral evdev** (feature · semente da Onda 2, 2026-07-22 · **gatilho: um consumidor precisar do 9º botão**): o teto de 8 botões de `is_mouse_button_down` **é do GLFW** (`GLFW_MOUSE_BUTTON_LAST == 7`), não uma escolha nossa -- e o líder registrou explicitamente que existem mouses de 12 botões e que a framework não deve restringi-los (memória `feedback_input_multibutton_teclados`). O caminho seria um canal evdev lateral (o mesmo já usado pelo módulo `gamepad`, que é evdev puro), não um patch no GLFW. Teto honesto documentado no doc-comment em vez de silenciosamente truncado.
- **`SEED-UIKEYFWD` -- encaminhar as teclas estendidas pra UI (RmlUi) pra atalhos** (feature · semente da Onda 2, 2026-07-22 · **gatilho: demanda real de UI**): D10 da Onda 2 preservou INTACTO o roteamento `UiEvent`→RmlUi -- só o subconjunto pré-existente de 14 teclas de navegação é encaminhado à UI (`glfw_key_is_ui_forwardable()`, testado). As ~92 posições novas alimentam **só** o canal de estado físico do host. Ampliar o encaminhamento é mudança de comportamento da UI (atalhos, captura de foco) e exige demanda concreta -- não se faz "porque agora dá".
- **`SEED-MOUSEEDGE` -- callback de borda pra botão de mouse** (feature · semente da Onda 2, 2026-07-22 · **gatilho: pedido do consumidor**): a Onda 2 entregou callback de **borda** pra teclado (`set_key_callback` + `KeyAction{Press,Release,Repeat}`) mas **só polling de estado** pra botão de mouse (`is_mouse_button_down`). A assimetria é deliberada (o consumidor pediu teclado; mouse por polling atendeu o caso dele) e está documentada. Fechar a simetria é barato quando alguém precisar.
- **L1.14-GLLOADER-WIN — codepath Windows no loader GL gerado** (feature · consumer-driven, blocker de build Windows do GusWorld · 🔍 **Pendente verificação**, implementação entregue 2026-07-14): `glintfx/src/gl_loader.c` (gerado por `tools/gen_glloader.py`, trilha `L1.14-GLLOADER`) resolvia ponteiros GL só via `dlopen`/`dlsym` (`<dlfcn.h>`), inexistente no Windows — travava qualquer build Windows do consumidor. Fix vive **no gerador** (não no `.c` à mão): `SOURCE_PREAMBLE` bifurca o include (`<windows.h>` vs `<dlfcn.h>`); `LOAD_FN_TEMPLATE` bifurca `#ifdef _WIN32 … #else … #endif` em torno de typedefs/estado/`glintfx_resolve_symbol`/`glx_gl_load` — POSIX fica **byte-idêntico** no `#else` (só adições, `git diff` confirma). Codepath Win32 espelha o padrão GLAD `GLAD_PLATFORM_WIN32`: `wglGetProcAddress` (typedef `WINAPI`/`__stdcall`, cobre GL≥1.2+extensões) com guarda de sentinela (`NULL`/`0x1`/`0x2`/`0x3`/`-1` tratados como falha) → fallback `GetProcAddress(opengl32.dll, name)` (cobre GL 1.0/1.1). `opengl32.dll` via `LoadLibraryA` (nunca linkada, `-lopengl32` evitado — kernel32 implícito). Sem `FreeLibrary` no teardown (espelha a ausência de `dlclose` no POSIX). `gl_loader.h` **inalterado** (diff vazio). Regenerado + build Linux 100% limpo + `ctest -R gl_loader` verde (não-regressão). Impl `engine-graphics-programmer` (`918cb5b`). **Gate adversarial `qa-engineer` + re-verificação do orquestrador 2026-07-14 PASSOU nos 4 grupos:** (1) não-regressão Linux ON (39/39) + OFF/embed (22/22) verdes; (2) `git diff` prova POSIX byte-idêntico + `.h` vazio; (3) **cross-compile Windows REAL em container efêmero `podman run --rm fedora:42` + MinGW-w64** (host limpo, nada instalado no host, `--rm` sem resíduo) → `x86_64-w64-mingw32-gcc -Wall -Wextra -c gl_loader.c` compila limpo (sem o erro `dlfcn.h`, sem warning de ABI), `file` → **`x86-64 COFF object file, 368 symbols`** (PE/COFF Windows válido) — re-executado pelo orquestrador (`REVERIFIED_OK`); (4) inspeção do branch Win32 (sentinelas/`WINAPI`/ordem/sem-FreeLibrary/idempotência) OK. **Fecha o blocker do GusWorld.** Segue 🔍 até o consumidor bumpar o pin e revalidar o cross-compile do `app/` inteiro dele (então `✅`).
- **glintfx — encaminhamento de text-input (`SDL_EVENT_TEXT_INPUT` → `Context::ProcessTextInput`) no `UiLayer`** (feature · semente consumer-driven GusWorld 2026-07-10 · **gatilho: o GusWorld implementar o campo de digitação do SAVE-NOMEAR**): hoje o `UiLayer::process_event` injeta `UiEvent` (mouse) + `Key` (navegação), mas NÃO há caminho pro host repassar CARACTERES digitados (texto Unicode composto) ao contexto RmlUi — necessário pra `<input type="text">` funcional. RmlUi tem `Context::ProcessTextInput(Character)`/`ProcessTextInput(String)`. Expor um `UiEvent::Type::TextInput` (ou método `process_text_input(const char* utf8)`) que chama isso. Confirmado pelo dev do GusWorld que v0.9.0 cobre o resto do pedido; este é o próximo pull quando ele fizer o SAVE-NOMEAR. Cross-ref: a família de callbacks `L1.15-FORMEV` (change dispara quando o input muda) + `L1.16-DOMRW` (`set_text`/`get_string` pra ler/escrever o valor). Puxar quando o gatilho chegar. **Contrato detalhado pelo dev do GusWorld 2026-07-10 (pré-mapeado pro pull — NÃO especificar no ar, é receita de retrabalho):** além de `process_text_input(utf8)`→`ProcessTextInput`, o pull precisa fechar: (a) ciclo `SDL_StartTextInput`/`SDL_StopTextInput` (quando ligar/desligar a captação — provável amarração ao foco de um `<input>`); (b) edição: backspace/delete/caret/seleção; (c) paste, max-length; (d) **CRÍTICO — IME/composição**: acento morto e teclado composto (pt-br: `~`+`a`=`ã`); o SDL entrega texto JÁ composto no `SDL_EVENT_TEXT_INPUT`, mas há o `SDL_EVENT_TEXT_EDITING` (pré-composição) a decidir se/como repassar. A spec só fecha contra o campo real do SAVE-NOMEAR (deferido no GusWorld: PI10, baixa prioridade, pré-cond. mocks no Brave). **Consumidor confirma (2026-07-10): a lib está NO PONTO, sem gap real além deste** — scroll (v0.4) + focus (v0.8) + som/DOM-rw (v0.9) cobrem tudo; o único hand-rolled restante é o hit-test de hover pra SFX, que o `set_hover_callback` mata na migração. **Adiamento CONFIRMADO pelo líder 2026-07-22** (Onda 0 do PROGRAMA-COMPLETO, `glintfx/docs/superpowers/plans/2026-07-22-programa-completo.md` §6): o gatilho segue o mesmo (GusWorld iniciar o SAVE-NOMEAR) -- decisão fechada, não reabrir sem o gatilho.
- **glintfx — log inconsistente nos guards de entrada** (`QW-GUARDLOG`, PROG-1 Onda 1 · Minor · achado mini-review v0.3.0 · ✅ **Concluído (v0.18.1)** (implementação entregue 2026-07-22)): `set_dp_ratio` loga `LT_WARNING` ao rejeitar input inválido, mas `load(nullptr)` e `set_viewport(w/h<=0)` rejeitavam SILENCIOSAMENTE. Fail-secure preservado nos 3, mas um host que loga só via o dp_ratio. **Padronizado:** `Engine::set_viewport` (`engine.cpp`) e o guard `load(nullptr)` (`bootstrap.cpp`) agora logam `LT_WARNING`; o pre-guard próprio de `UiLayer::set_viewport` (as 2 sobrecargas, `ui_layer.cpp`) -- que intercepta ANTES de alcançar o ponto comum do Engine -- também ganhou log próprio, senão o caminho real de embed (o mais usado) continuaria silencioso. Nenhuma semântica mudou (só log adicionado); verificado por probe manual (`load(nullptr)`/`set_viewport(0,0)`/`set_viewport` 5-arg inválido/`set_dp_ratio(NaN)`, os 4 avisos disparam, retorno de rejeição inalterado) + suíte completa 80/80 (GLFW=ON) + 53/53 (embed-only) verde.
- **CI/infra — RmlUi não compila do ZERO no runner sem `-include sys/syscall.h`** (Watch-item · achado ao adicionar a job `build-and-test-fonteng` 2026-07-10 · **✅ MITIGADO** `8780022`): o `libstdc++ 14` do `ubuntu-latest` + clang bate em `atomic_wait.h: SYS_futex undeclared` ao compilar `FreeTypeInterface.cpp` do RmlUi do zero (gcc puxa `<sys/syscall.h>` transitivo, clang não). Era uma **bomba-relógio mascarada por cache**: as jobs antigas viviam de `_deps` cacheado e nunca recompilavam o RmlUi; a job nova (cache miss) expôs. Mitigado com `env CXXFLAGS: "-include sys/syscall.h"` nas 3 jobs que compilam (build-and-test, build-and-test-fonteng, coverage) nos 2 workflows. **Não reproduz no Fedora local** (libstdc++ do sistema não tem o bug) → validação real = CI no push. Se o runner atualizar o toolchain e resolver o bug upstream, a flag vira inócua (pode remover). Vigiar.
- ~~**Camada 0 (raster) — `iabs32(INT32_MIN)` sem guarda**~~ (`QW-IABS32`, PROG-1 Onda 1 · Cosmético · achado review adversarial `SOV-RAST`/FT-F2 2026-07-10 · ✅ **Concluído (v0.18.1)** (implementação entregue 2026-07-22, `90d325a`)): `iabs32` (`src/raster.c:141`) fazia `-v` sem guardar `v==INT32_MIN` (UB de overflow assinado) -- não-alcançável hoje (`dx`/`dy` de `flatten_quad` já `saturate_fx`-limitados a ±16M), defesa em profundidade, não fix de bug vivo. **Fechado:** guarda `if (v==INT32_MIN) return INT32_MAX;` adicionada (static, `src/raster.c`). Provada por sonda hosted-companion nova (`tests/probe_iabs32.c`, `make probe-iabs32`, `-fsanitize=undefined`): revertendo a guarda, o UBSan aborta ao vivo com "negation of -2147483648 cannot be represented" -- confirmado manualmente, guarda restaurada. Gates verdes: `make build/test/check-static/test-negative/test-mem`, `nm -u` limpo em todo binário.
- ~~**Camada 0 — coredump/DrKonqi do abort ESPERADO em `libcore_consumer`**~~ (`QW-NOCORE`, PROG-1 Onda 1 · item extra achado pelo líder monitorando o journal, 2026-07-22 · ✅ **Concluído (v0.18.1)** (implementação entregue 2026-07-22, `4df519c`)): `tests/libcore_consumer.cpp` (gate 3b do ADR-0009) faz `std::free()` num ponteiro do `glx_malloc` de propósito, provando que a glibc rejeita o mau-uso alto via `abort()` -- teste PASSANDO, não crash real. Sem tratamento, cada `make libcore-test` gerava um SIGABRT ao vivo + tempestade de conexão do `drkonqi-coredump-launcher`. **Fechado:** 2 camadas no filho forkado antes de `fn()` -- (1) `setrlimit(RLIMIT_CORE, {0,0})` (necessário mas NÃO suficiente sozinho: suprime o arquivo de core mas o `core_pattern` do kernel ainda dispara o pipe pro systemd-coredump em qualquer sinal fatal não-capturado); (2) `sigaction(SIGABRT, ...)` fecha a lacuna (sinal CAPTURADO nunca é "não-capturado" do ponto de vista do kernel) -- `_exit(128+sig)` preserva o contrato que o pai observa (`WIFEXITED`+`exit!=0`, aceito via OR com `WIFSIGNALED`, nenhuma asserção mudou). Prova: `coredumpctl list --since` e `journalctl` sem NENHUMA entrada nova pro `libcore_consumer` pós-fix (antes: `ANOM_ABEND` + par de serviços drkonqi). Gates completos da Camada 0 verdes (build/test/check-static/test-negative/test-mem/libcore-test); `nm -u` limpo nos binários freestanding.
- **glintfx — release hygiene: bump do `project(VERSION)` no CMake** (Processo · achado v0.3.0): o `project(VERSION)` ficou travado em `0.2.4` da v0.2.4 até a v0.2.5 (bump esquecido), então `version()` reportava errado. Corrigido na v0.3.0 + `app_smoke` agora compara com `GLINTFX_VERSION` (não hardcoda). CHECKLIST DE RELEASE: sempre bumpar `glintfx/CMakeLists.txt` `project(VERSION)` ANTES da tag (é a única fonte da verdade do `version()`).
- **glintfx — CI do Codeberg preso em `waiting` na tag v0.3.1** (Infra/observação · achado 2026-07-04, runs #5279687/#5279690 do `codeberg-medium`): os 2 runs disparados pelo push (`main` + tag `v0.3.1`, commit `c4ea201`) ficaram em `status: waiting` sem nunca iniciar (`Started`/`Stopped` em epoch), por tempo além do histórico normal (~2min). Não bloqueou a release (líder decidiu seguir — GitHub CI confirmou `success` na mesma tag, verificação local 29/29+15/15+ASan/UBSan também limpa). Gatilho de revisitar: se o padrão se repetir em releases futuras, investigar status do runner `codeberg-medium`/pool compartilhado do Codeberg Actions. **REINCIDIU 2026-07-09** (F1/W12, `core-ci` run #19, sha `eb3c854`): mesmo `waiting` sem iniciar. GitHub verde + `forgejo-runner exec` local (mesmo container) verde ⇒ não bloqueia. Padrão agora confirmado 2×; o runner `codeberg-medium` do Actions do Codeberg parece não pegar jobs deste repo de forma confiável — candidato a investigar (habilitar runner próprio? checar Settings→Actions) OU aceitar o GitHub como gate primário e o Codeberg como espelho best-effort.
- ~~**glintfx — 2ª causa UBSan (`WidgetScroll`) não reproduzida no toolchain do reviewer**~~ (`VF-WIDGETSCROLL`, PROG-1 Onda 1 · Minor · achado review GLINTFX-SCROLL-1, 2026-07-07) **OBSOLETADA -- ✅ riscada 2026-07-22, COM PROVA**: as 2 supressões novas (`vptr:*WidgetScroll.cpp*`/`vptr:*RemoveEventListener*`) documentavam a mesma causa raiz do `vptr:ElementDocument` (ordem de teardown recursivo do RmlUi). Essa causa raiz foi **corrigida na fonte** pelo patch `glintfx/patches/rmlui-2cd28864-teardown-ub.patch` (v0.9.1, 2026-07-10) -- as 3 entradas de suppression (incluindo as 2 do `WidgetScroll`) foram **REMOVIDAS** de `glintfx/tests/ubsan_suppressions.txt` (arquivo hoje vazio de entradas), validado na hora com a suíte completa (`GLFW=ON`/`OFF`) rodando limpa sob UBSan, zero achado `vptr:*` (`glintfx/patches/README.md`). **Evidência adicional coletada nesta onda:** todo run agendado do workflow `nightly-sanitize` (GitHub Actions, `halt_on_error=1` no UBSan -- um achado real reprovaria o job) desde a tag `v0.9.1` (2026-07-10 20:13) até hoje é **`success`** -- 12 execuções consecutivas, runs `29075102488` (2026-07-11, primeira pós-v0.9.1) a `29895602911` (2026-07-22), zero falha, zero achado de sanitizer. Riscada com prova dupla (patch + histórico de CI); não é mera suposição de obsolescência.
- **glintfx — jitter de ~3px em clique dentro de container `overflow-y:auto`** (Minor · achado review GLINTFX-SCROLL-1, pré-existente/RmlUi terceiro): um clique de mouse dentro de um container scrollável desloca `scroll_top` em ~3px por si só (reproduzido também na cena original do autor). Não é regressão desta feature — sempre existiu, só ficou observável agora que `get_element_scroll_top()` existe. Sem gatilho de urgência; documentar em `docs/embed-integration.md` se algum consumidor reportar confusão.
- **raiz — `out.ppm` (1.6 MB) artefato de render solto na raiz** (Housekeeping · achado AUD-C0-6, corrigido de imprecisão: NÃO está commitado — é untracked/gitignored, não polui o repo): decisão do líder = `gio trash out.ppm` se não for mais útil. Sem impacto no git.
- **AUDIO-STREAM -- streaming de áudio (`MA_SOUND_FLAG_STREAM`)** (semente registrada no plano `AUDIO-F2`, 2026-07-22 · **sem demanda bloqueante confirmada pelo consumidor**): `load_sound()` faz decode síncrono completo em RAM (`MA_SOUND_FLAG_DECODE`); uma faixa de música longa paga o custo total de RAM na carga. `MA_SOUND_FLAG_STREAM` decodificaria sob demanda, útil pra música longa sem o custo full-RAM -- puxar quando um consumidor real precisar (GusWorld hoje não bloqueia nisso).
- **windows-msvc-build -- cobertura GLFW/`glintfx_window` faltando no job MSVC** (achado do próprio job `AUDIO-F2`/S3-CI, 2026-07-22 · **degrade deliberado, documentado no próprio job**): o job novo builda o `CMakeLists.txt` principal no MSVC com `GLINTFX_BACKEND_GLFW=OFF` (evita um segundo port vcpkg-do-source pro `glfw3`), então `glintfx_window`/`RmlUi_Platform_GLFW.cpp` seguem sem cobertura de portabilidade MSVC (o único dos 9 alvos não exercitado ali). Fechar exigiria `vcpkg install glfw3` no job + tirar o `GLINTFX_BACKEND_GLFW=OFF` -- puxar se o gap incomodar ou se um consumidor Windows precisar do `App` standalone (GLFW) via este exato CMake.

- **Camada 0 — harness `test:` usa `< /dev/null` global** (Minor · achado review B7 · gatilho: se/quando o runner de teste da Camada 0 for redesenhado): o `make test` redireciona stdin de `/dev/null` p/ todo teste (determinismo, evita `echo_stdin` travar). Trade-off: um teste futuro que precise de conteúdo específico de stdin fica bloqueado no harness automático. Decidir fixture de stdin por-teste (ex.: `tests/<name>.stdin` opcional) se surgir a necessidade. Sem item-alvo planejado (o `C1` já shipou).
- **Camada 0 — `mini_printf` short-write silencioso** (Minor · achado review E2 · sem gatilho ativo, YAGNI): o `mini_flush` reseta o scratch mesmo em erro/short-write do `sys_write` (não faz retry, não sinaliza falha ao chamador além de `total` menor). Decisão consciente documentada no header (ADR-0002 `-errno` cru, sem retry). Endurecer só se um consumidor real precisar de garantia de escrita completa.
- **Camada 0 — `%g` no `mini-printf`** (semente · registrado no escopo do `SOV-FCONV`, 2026-07-09 · **sem demanda, NÃO implementado de propósito**): `%g` exigiria formatação em notação científica (estilo `%e`, escolha fixo/exponencial pela magnitude + poda de zeros à direita) que o `ftoa` atual NÃO oferece (é ponto-fixo apenas) — não "saiu de graça" do trabalho de `ftoa`/`atof`, então ficou de fora por escopo explícito da tarefa. Pré-requisito: um `ftoa`-variante ou `dtoa_sci` próprio com notação exponencial (`%e`), zero libm, mesma disciplina de teste (round-trip + fronteiras). Puxar só com necessidade real (harness/printf hoje não pede).
- ~~**`.github/workflows/ci.yml`**~~ (`QW-XKB`, PROG-1 Onda 1 · Minor · ✅ **Concluído (v0.18.1)** (implementação entregue 2026-07-22)): `libxkbcommon-dev` ausente (presente no Forgejo; implícito por transitividade no `ubuntu-latest`). **Fechado:** `libxkbcommon-dev` adicionado nos 4 blocos `install deps` de `.github/workflows/ci.yml` (`build-and-test`, `build-and-test-fonteng`, `lint-and-scan`, `coverage`) -- paridade com `.forgejo/workflows/ci.yml`/`heavy.yml`. YAML validado (`python3 -c "import yaml..."` + `yamllint`, zero erro, só os warnings de line-length pré-existentes no arquivo inteiro).
- ~~**Em-dashes pré-existentes**~~ (`QW-EMDASH`, PROG-1 Onda 1 · gatilho: qualquer varredura de doc · ✅ **Concluído (v0.18.1)** (implementação entregue 2026-07-22)): em doc-comments de `ui_layer.hpp`. **Fechado:** as 14 ocorrências trocadas por `--`; diff comment-only confirmado (`git diff`), zero token de código tocado. `grep -c "—" glintfx/include/glintfx/ui_layer.hpp` = 0.
- ~~**FONTENG — paridade de x-height em 9px@dp1.0**~~ (`QW-FONT9DOC`, PROG-1 Onda 1 · semente da onda `L1.20-FONTFLIP` 2026-07-11 · **limite fundamental, sem fix barato** · ✅ **Concluído (v0.18.1)** (doc entregue 2026-07-22)): após pen-snap+vsnap empatarem o FreeType em 14/15 células de corpo pequeno, o único resíduo é 9px@dp1.0 no eixo Y (`xe_body` own ~0.42 vs FreeType 0.85). Spike focado (`engine-graphics-programmer`) provou que **não é arredondamento** (round-half/floor/ceil são invariantes de fase — byte-idênticos) nem ancoragem de zona: o gap é o **overshoot dos topos curvos** (o/c/e, x-height física ~4px) que o FreeType suprime via hinting completo de outline. Fechá-lo exige **hintar a curva no topo redondo** (mesma classe do detector de haste/grid-fit de outline já descartado por caro/arriscado) — não é snap-Y. **Fechado (documentação, o fix de CÓDIGO segue adiado):** parágrafo honesto novo em `docs/embed-integration.md` §17 (bilíngue), cobrindo o fato do spike (overshoot, não arredondamento), os números (~0.42 vs ~0.85) e as 2 saídas pro consumidor (corpo ≥10px em dp1.0, ou flip runtime pra `FontEngine::FreeType`). Puxar o fix de código só se um consumidor reclamar de 9px especificamente; senão dormante (o GusWorld tem paridade nos outros 14 pontos). Detalhe: memória `feedback_gusworld_corpo_pequeno_fonte`.
- **L1.21-ECHO — sprite-eco (fantasma translúcido, status-driven)** (consumer-driven GusWorld 2026-07-15 · **ENTREGUE como receituário, zero feature de código**): 🔍 how-to em `docs/effects.md` (`231bdd8`) + teste `echo_sanity` que PROVA por pixel readback (`285f4d9`, eco translúcido+tint azulado via `opacity`+`image-tint`, toggle via API DOM `add_class`/`remove_class`, RCSS puro, 50/50 GLFW ON+OFF). **✅ ESCOPO FECHADO pelo dev 2026-07-15:** é o caso glintfx — no modo vigente (HUD externo, ADR-009 do GusWorld) o cockpit inteiro (painel, retrato do ator, pips, menu) é RmlUi; o "ator" que o eco acompanha = o retrato no cockpit = elemento glintfx. Vão pelo receituário (`docs/effects.md`): cópia do retrato com `opacity:0.35` + tint azulado + fade `@keyframes`, ligado/desligado via `add_class`/`set_property`; afterimage = classe extra com `@keyframes` translate+fade. Se um dia meterem uma camada de sprite host-drawn na arena, ELA é do host — mas o cockpit atual é 100% o caminho RML. **Nada mais da lib; item entregue.**
- **L1.22-WAVE — screen-wave (distorção de tempo, transiente)** (consumer-driven GusWorld 2026-07-15 · **É HOST-SIDE por arquitetura, não in-lib**): 🔍 descoberta do CTO: glintfx em embed/compose-only NÃO lê o FBO 0 do host (`RmlUi_Renderer_GL3.cpp:887-920`; layer própria composta por cima), então um ripple que refrata a CENA DE BATALHA não tem a textura dentro da lib — `emit_vfx` REJEITADO (não teria o que refratar + quebra o princípio "sem API imperativa de efeito"). A lib entrega os ganchos (todos já no ar): timing (callbacks de evento), epicentro (`get_element_box`), + **shader GLSL de refração radial de referência** em `docs/embed-integration.md` §19 (`231bdd8`). **✅ CONFIRMADO pelo dev 2026-07-15:** assumem host-side (têm o render target + contexto GL), pegaram o GLSL de referência, ligam nos 3 ganchos, **RETIRARAM o pedido de `emit_vfx`**. Item entregue (a lib não faz o pixel; faz os ganchos). **SEMENTE dormante (Opção B):** captura do FBO0→textura in-lib + decorator `ripple` (via `RenderShader` override como o image-tint, sem tocar os membros privados do backend) — só refrataria conteúdo PRÓPRIO do glintfx (painéis do cockpit), NÃO a cena do host; puxar só se o líder quiser um shimmer sobre a UI glintfx. Detalhe: plano do Caetano nesta sessão. **→ SEMENTE Opção B CONSTRUÍDA 2026-07-16 (a pedido do líder "opção b agora") na branch `feat/backdrop-ripple`:** captura de FBO0→`backdrop_capture_tex_` (`ArmBackdropCapture` incondicional em `begin_frame_compose`, zero GL; `EnsureBackdropCaptured` faz a captura real on-demand DENTRO do ramo `is_ripple` de `RenderShader`, idempotente — padrão arm/ensure que torna o custo-zero-quando-inativo ESTRUTURAL) + decorator `ripple(...)` (custom props `ripple-origin-x/-y/-phase/-strength/-width`, animável por `@keyframes`; via `RenderShader` override como o image-tint, NÃO toca membro privado do backend); `GlStateGuard` estendido pra salvar/restaurar `GL_READ_FRAMEBUFFER`+`GL_READ_BUFFER`. ADR-0012. **Review adversarial (implementer≠reviewer≠orquestrador) PASSOU:** fix de cold-start (`43cdcfa`) validado por teste diferencial (`ripple_stress_sanity`: 4 elementos pós-ripple bit-exatos com vs sem captura mid-frame) + mutation testing; suíte **52/52** (re-verificada pelo orquestrador). Branch pushada (dual). **✅ LANÇADO em v0.11.0 (2026-07-16)** — go/no-go do líder ("automatize isso, pode fazer") + escolha "abrir PR primeiro" (PR #1, ci 6/6 verde). Gate pesado pré-merge fechado (ASan+UBSan+LSan da suíte completa **52/52** em `/var/tmp`); merge `--no-ff` (`58e18f7`); tag `v0.11.0` push dual; CI de main+tag+core-ci **verde** (`heavy.yml`/ASan roda no Forgejo). SEM demanda de consumidor (o GusWorld foi host-side) — capability de catálogo, decisão consciente do líder.
- **L1.22-CAPTURE / L1.22-WAVE — 2 achados dormantes do review** (`QW-RIPPLEAPP`+`QW-PROOF4`, PROG-1 Onda 1 · ✅ **Concluído (v0.18.1)** (implementação entregue 2026-07-22)): (1) **ripple morto silencioso em modo `App` standalone** (não-embed): `begin_frame()` standalone nunca chama `arm_backdrop`/`ArmBackdropCapture`, então o decorator `ripple` num `App` (sem host) fica permanentemente inerte. PRÉ-EXISTENTE e conceitualmente esperado (backdrop só existe quando um host é dono da cena), mas merecia um log de aviso ou nota na doc pública do `App`. **Fechado (`QW-RIPPLEAPP`):** aviso `LT_WARNING` ÚNICO (não por-frame, guardado por instância) em `Gl3RenderInterface::CompileShader` (`render_gl3.cpp`) quando `"glintfx-ripple"` compila sem o caminho compose-only (`arm_backdrop`) ter armado o backdrop nesta instância -- discrimina App standalone (nunca arma) de UiLayer/embed (arma sempre antes) sem tocar o módulo fx; doc-comment bilíngue novo em `app.hpp::load()`. (2) **lacuna de cobertura no Proof 4 do `ripple_sanity`** (qa): checar `GL_READ_FRAMEBUFFER_BINDING==0` pós-render não discriminava "restaurou" de "a captura deixou 0 por coincidência" (falso-negativo demonstrado por mutation testing); o `ripple_stress_sanity` (security) já cobria com FBO sentinela não-zero. **Fechado (`QW-PROOF4`):** Proof 4 de `ripple_sanity.cpp` adotou o mesmo idioma de FBO-sentinela não-zero (bind ANTES do render, checa restauração AO sentinela DEPOIS) -- fecha o ponto cego, verificado localmente (sentinel_read_fbo=3 lido de volta corretamente); mutation testing formal (mutar o restore do `GlStateGuard` pra confirmar que ESTE Proof 4 agora reprova) é o passo de review adversarial ainda pendente (Agente D/onda de review).
- **GAP-2b FBO custom do host** (metade não ativada do GAP-2 original · **ADIADO, sem demanda confirmada**): permitir compor num FBO do host (≠ FBO 0). A metade "origem de viewport" do GAP-2 original foi ativada na v0.2.5 (`L1.13-VPORIGIN`, ver Concluídos abaixo); o alvo de composição continua sempre FBO 0 -- hardcoded no backend RmlUi, não muda com este item. GusWorld compõe no FBO 0 hoje, sem demanda real por FBO custom.
- **AUD-PUB-2/6 — gaps de API genérica** (sementes da auditoria de distribuição pública 2026-07-07 · **sem demanda confirmada, modo pull**; o GusWorld não precisa de nenhum hoje, lê SDL direto): `AUD-PUB-2` injeção de input no `App` (`process_event` só no `UiLayer` hoje; `App` é runner opaco). `AUD-PUB-6`: unload/hide/show; múltiplos documentos/overlay modal; eventos RmlUi além de click (change/submit/focus/blur — formulários inertes pro host); setters imperativos por id (`set_text`/`add_class`/`set_property`); leitura de volta do data-model (`get_number/string/bool`, hoje write-only); `load_font` em runtime; cursor callback (`SetMouseCursor` não repassado); clipboard callbacks. Detalhe em `AUDIT_FIND.md` (AUD-PUB-2/6). Puxar só com 2º consumidor real. **→ Parcialmente formalizadas 2026-07-09 (roadmap ativo):** (c) eventos change/submit/focus/blur → **`L1.15-FORMEV`**; (d) setters imperativos + (e) get do data-model → **`L1.16-DOMRW`** (ambas LW10). **Seguem sementes pull:** `AUD-PUB-2` (input no `App`), unload/hide/show, múltiplos documentos (+ limite de 1 `create_data_model`), `load_font` runtime, cursor callback, clipboard callbacks.
- **glintfx — `image-tint()` preservação de saturados mais dura** (semente DORMANTE · follow-up do luminance-key entregue na v0.7.0 · **sem demanda — o dial de threshold bastou pro GusWorld**): a fidelidade de saturados hoje é `(1-sat)` linear (aproximada, tunável subindo `image-tint-threshold`). Se um 2º consumidor com ouro de luminância problemática precisar de corte DURO por saturação, endurecer: curva `(1-sat)^k` ou prop separada `image-tint-saturation-threshold`. Detalhe no adendo do [ADR-0010](docs/adr/0010-image-tint-luminance-key.md). Puxar só com gatilho real. **Reavaliada 2026-07-09 na abertura do roadmap ativo: segue DORMANTE** (o dial de threshold bastou pro único consumidor; sem gatilho).
- **UA-stylesheet: recompile redundante por load** (Minor · **sem payoff -- inerente à mesclagem via API pública pós-load**): quando o documento tem `<link>`/`<style>` próprio, a stylesheet original é compilada no `LoadDocument` e descartada quando o `Bootstrap::load()` a substitui pela mesclada.
- **UB `vptr:ElementDocument` no RmlUi upstream** (achado do TST-L1-MEM 2026-07-02 · gatilho: próximo bump do pin do RmlUi, ou reportar upstream): `Element::SetOwnerDocument` → `owner_document->GetContext()` durante `~ElementDocument()` (`Element.cpp:2128`, pin `2cd28864`) — chamada de membro virtual com a parte derivada já destruída. 100% reproduzível em todo load+unload de documento; UB estrito mas sem corrupção observada. NÃO é código glintfx (não subclassamos `Rml::Element*`). Suprimido cirurgicamente em `glintfx/tests/ubsan_suppressions.txt` (`vptr:ElementDocument`) com rationale; qualquer OUTRO vptr segue gateado. Candidato a issue upstream no mikke89/RmlUi. **→ Consumidor (GusWorld) topou no MESMO UB 2026-07-10** (mesmas linhas `Element.cpp:2123/2128/2190`, mesmo pin) rodando o app embed sob UBSan. Triado (confirmado upstream puro — a ordem de teardown da árvore é 100% interna do RmlUi, glintfx não subclassa nem pode evitar) e **RESOLVIDO com supressões recomendadas documentadas pro consumidor** em `docs/embed-integration.md` §18 (`95fef49`) — as 3 entradas (`vptr:ElementDocument` + par `WidgetScroll`/`RemoveEventListener`) + como consumir via `UBSAN_OPTIONS=suppressions`. Busca web (2026-07-10) confirmou **SEM issue público** no mikke89/RmlUi (master idêntico ao pin — upstream NÃO corrigiu). **→ RESOLVIDO NA RAIZ 2026-07-10 (decisão do líder: resolver de fato, depois reportar problema+solução):** `backend-engineer` desenvolveu e VALIDOU sob UBSan (prova positiva+negativa) um **patch do RmlUi core** — são **2 UBs distintos**: (1) `~ElementDocument()` `=default` (corpo novo desanexa a subárvore enquanto o doc é válido) + (2) `~Element()` liberava `meta`/`WidgetScroll` DEPOIS de `children.clear()` (reorder antes). Patch versionado `glintfx/patches/rmlui-2cd28864-teardown-ub.patch` (+README), **aplicado via CMake `PATCH_COMMAND` idempotente** (`3c0e845`) → build `-DGLINTFX_SANITIZE=ON` **SEM supressões = 0 findings vptr** (prova); as 3 supressões REMOVIDAS. Doc `embed-integration.md` §18 reconciliada + crédito/atribuição RmlUi (README/NOTICE/wiki) pro report upstream (`5535905`). **✅ REPORTADO UPSTREAM 2026-07-10: [mikke89/RmlUi#968](https://github.com/mikke89/RmlUi/issues/968)** (issue com problema + solução + link pro nosso patch + contexto glintfx/GusWorld + nota pessoal do líder; postado pelo líder — o PAT do MCP não cria issue em repo de terceiro, 403). Lançado na **v0.9.1**. **✅ CONFIRMADO PELO GusWorld 2026-07-10: v0.9.1 consumida, `vptr:*` 9→0, 650 testes verdes.** Achado de integração do consumidor (documentado na §18, `7756198`): host que declara a PRÓPRIA cópia do RmlUi ANTES do glintfx → o `PATCH_COMMAND` do glintfx NÃO alcança (FetchContent first-population-wins) → o host aplica o mesmo patch na Declare dele (GusWorld fez, funcionou). **Combinado do drop:** quando o mikke89 merjar (#968) + bumparmos o pin, **avisar o GusWorld** (ele larga a cópia vendorizada + PATCH_COMMAND no mesmo movimento). **Leak LSan secundário RESOLVIDO** (GusWorld diagnosticou com `fast_unwind_on_malloc=0`/`print_module_map=1`): é do **driver NVIDIA `libEGL_nvidia`/GLVND + JIT do Mesa/llvmpipe**, one-time na criação do 1º contexto GL — NÃO é glintfx nem RmlUi, nada a suprimir do nosso lado (nota na §18).
- ~~**AUD-L1-QUALITY — god-class/complexidade/dead-code da Camada 1**~~ ✅ **CONSUMIDA 2026-07-19** (semente · levantada na consolidação da `LW-AUD` 2026-07-16, `internal-auditor` · puxada na Onda 1 do branch `chore/l1-quality` por Caetano/CTO): o catálogo `AUD-L1-*` cobre supply-chain/parsing-hostil/memory-safety/licença mas NÃO qualidade-de-código/dívida-técnica (escopo do `tech-lead`, não do `security-engineer`/`compliance-legal`). 3 arquivos já passam de 1000 linhas e são candidatos naturais a uma 1ª passada: `glintfx/src/font_engine_own.cpp` (1727L, o motor de fonte próprio inteiro — bake/atlas/kern/fallback/hint concentrados numa classe), `glintfx/src/bootstrap.cpp` (1500L), `glintfx/src/render_gl3.cpp` (1349L, cresceu a cada decorator novo — polygon/tint/ripple). Não é bloqueio de release (nenhum achado de segurança/correção pendurado nisso); é dívida a inventariar (métricas de complexidade ciclomática, candidatos a extração de classe/arquivo, dead-code) antes do `v1.0` fechar a API. **Veredito** (`docs/auditoria/AUD-L1-QUALITY.md`): **CONFORME COM RESSALVAS** — os 3 arquivos-manchete **NÃO** são god-classes (55–74% do volume é a documentação bilíngue obrigatória do próprio repo; medidos em código, 469–577L cada, responsabilidade única, funções ≤77L de código/aninhamento ≤5); zero dead-code (7 candidatos do cppcheck, todos falso-positivo, cross-checados contra testes/demos/API pública); 2 achados IMPORTANTE de duplicação real — 11 sítios verbatim de subida-de-ancestral em `bootstrap.cpp` e andaime de quad GL copiado entre os 2 decorators com shader — **ambos corrigidos** (`0f29879` extrai `AncestorIdOf`+`find_element`; `d6ee6ba` extrai `decorator_gl_quad.hpp`). Nenhum split de arquivo recomendado (anti-over-engineering explícito, justificado por arquivo no relatório).
- ~~**glintfx — `clang-format` no `preci.sh`**~~ ✅ **CONSUMIDA 2026-07-19** (semente · achado na consolidação da `LW-AUD` 2026-07-16 · puxada na Onda 2 do `chore/l1-quality`): `tools/preci.sh` rodava build+ctest+encapsulamento+gitleaks (`--full`) mas não formatava/checava formatação consistente (`.clang-format` não existia no repo). **`.clang-format` novo criado** (`ColumnLimit:0`, evita quebra feia de declaração) **+ gate `clang-format-diff` no `preci.sh`** (gateia-só, não reformata automaticamente) — `95a09bc` (config+gate) + `1f8c831` (conforma as linhas da Onda 1 ao `.clang-format` novo).
- ~~**glintfx — estender `-Wall -Wextra` aos 26 alvos de teste autorais**~~ ✅ **CONSUMIDA 2026-07-19** (semente · follow-up do gap de higiene do CONTRACT §10.4 fechado na `LW-AUD` só pro alvo `glintfx` · puxada na Onda 2 do `chore/l1-quality`): `45209e5` (`devops-sre`, consolidação `LW-AUD` 2026-07-16) tinha aplicado `-Wall -Wextra` só ao alvo de biblioteca `glintfx`, deliberadamente fora de escopo os 26 alvos de teste + 2 de demo. **2ª passada entregue** (`86a4039`): `-Wall -Wextra` estendido aos alvos de teste/demo autorais (RmlUi/backends fetchados seguem fora, mesmo racional documentado em `glintfx/CMakeLists.txt`), zero warning novo.
- **AUD-L1-GLSYM — colisão de símbolo do loader GL com o `libGL` do host** (✅ achado E fixado na onda `LW-TST` 2026-07-19, lançado em `v0.11.2` · **HIGH, afeta o consumidor real GusWorld**): a onda de teste de contrato (consumidor externo embed-only chamando GL pelo nome cru) reproduziu ao vivo um **SIGSEGV dentro de `glClearColor`** — `libglintfx.a` exportava os 344 ponteiros do loader gerado (`tools/gen_glloader.py`) com o **nome EXATO das funções GL 3.3 como símbolo BSS global** (`nm` → `B glClearColor` em `gl_loader.c.o`); um host que chame `glClear`/etc pelo nome cru e linke o `.a` antes do `libGL` resolve pro slot BSS não-inicializado → crash. Vazamento de fronteira via **símbolo**, invisível ao `check_encapsulation.sh` (grep de include). **FIX (aprovado pelo líder, glx_ prefix alinhado ao ADR-0009, `8ed7862`):** o gerador emite `glx_<cmd>` + `#define <cmd> glx_<cmd>` no header privado (call sites internos inalterados; string do getprocaddress segue crua). Verificado por 3 ângulos: `nm` do orquestrador (0 crus, 344 `glx_`), + **review adversarial APROVADO** (`qa-engineer`, mutation testing que EXECUTA: TU externo host-style crashou pré-fix no slot BSS `glClearColor` / rodou limpo pós-fix resolvendo `U glClearColor`→`libOpenGL.so`; `#define` provado load-bearing — removê-lo quebra o build; suíte 55/55; zero vazamento público). [ADR-0013](docs/adr/0013-gl-symbol-boundary.md). GusWorld avisado no bus com workaround. ✅ Concluído — lançado em `v0.11.2` (2026-07-19, merge PR #3, CI dual verde, tag dual).
- **AUD-C0-PICVAR — variante `-fpic` da `libcore.a` pra consumidor hosted que exige PIE** (semente · levantada pelo achado 🟠 do `AUD-ABI-Δ` 2026-07-19, onda 4 de re-auditoria delta da Camada 0, formalizada no adendo do [ADR-0009](docs/adr/0009-internalization-boundary.md#addendum-2026-07-19--pieaslr-cost-of-the--fno-pic-archive--custo-pieaslr-do-archive--fno-pic) · **sem gatilho urgente, deferida por escopo**): `libcore.a` é compilada `-fno-pic` (contrato freestanding da Camada 0, ADR-0005) — linkar num consumidor C++ hosted derruba o binário-consumidor inteiro pra `ET_EXEC` em silêncio, perdendo ASLR no processo inteiro (não só nos object files linkados), confirmado via `readelf -h build/bin/libcore_consumer` (`Type: EXEC`, não `DYN`). Não é corrupção de memória (16/16 checks do `libcore_consumer` passam) — 🟠, não 🔴 — mas importa porque o destino declarado da `libcore.a` inclui parsers de input hostil (SFNT/raster/hint, já membros do archive). Uma variante `libcore_pic.a` (`-fpic`) resolveria pra um consumidor que exija PIE. **Deliberadamente NÃO construída agora**: nenhum consumidor real hoje exige PIE (`libcore_consumer` é harness de teste; o glintfx ainda não linkou nenhum módulo casca-hosted/carne-C em produção) — construir/manter uma 2ª variante de archive contra demanda hipotética seria scope creep à frente do consumidor (cf. `feedback_cadencia_releases_consumidor`/`feedback_escopo_distribuicao_geral`). Puxar no dia em que um consumidor hosted real da `libcore.a` exigir PIE. **CONFIRMADO NÃO SERÁ FEITO pelo líder 2026-07-22** (Onda 0 do PROGRAMA-COMPLETO, `glintfx/docs/superpowers/plans/2026-07-22-programa-completo.md` §6 -- o CTO contra-argumentou "tudo" da INBOX especificamente neste item e o líder manteve a deferição; Onda 10/"completude opcional" caiu do roadmap).
- **A2-GAMEPAD — rumble/force-feedback (`EV_FF`)** (semente · escopo explicitamente FORA da fatia A2-GAMEPAD, `docs/superpowers/plans/2026-07-21-framework2d-A2-gamepad.md` seção 7 · **sem demanda confirmada**): o átomo gamepad não expõe nenhuma API de força tátil nesta fatia — só leitura de botão/eixo. Implementar exigiria abrir o fd em `O_RDWR` (hoje `O_RDONLY`) + `EVIOCSFF`/`EVIOCRMFF`/write de `struct ff_effect`, superfície nova de permissão (RW no device, não só R) e API pública nova (`play_rumble(pad, strength, duration)` ou similar). Puxar só com pedido real de consumidor.
- **A2-GAMEPAD — helper de deadzone** (semente · decisão consciente D9 do plano A2-GAMEPAD: "sem deadzone imposta" · **sem demanda confirmada**): `axis()`/`raw_axis()` devolvem o valor normalizado CRU por `absinfo`, sem threshold algum — decisão deliberada (deadzone é política de jogo, não do framework, documentado em `docs/gamepad.md`). Se um padrão repetido de "todo consumidor reimplementa o mesmo clamp radial/axial" aparecer, um helper opcional (`glintfx::apply_deadzone(x, y, threshold)`, função livre, sem estado, fora do `Gamepads`) seria a próxima fatia natural — sem imposição no caminho `axis()` em si.
- **A2-GAMEPAD — integração `App`/`AppConfig`** (semente · decisão consciente D10 do plano A2-GAMEPAD: "sem acoplamento no App nesta fatia" · **sem demanda confirmada**): hoje o consumidor cria seu próprio `glintfx::Gamepads` e chama `poll()` dentro do `set_frame_callback` do A1-INPUT — zero risco de regressão no `App` existente. Uma conveniência nível-`AppConfig` (ex.: `AppConfig::enable_gamepads = true` + `App::gamepads()` acessando uma instância auto-possuída) é fatia futura só se um consumidor pedir; a via manual já cobre o caso de uso sem gambiarra.
- **A4-WINMODES — multi-monitor / `monitor_index`** (semente · decisão consciente D2 do plano `2026-07-21-framework2d-window-modes.md`: "primary monitor only nesta fatia" · **sem demanda confirmada**): `App::set_window_mode`/`AppConfig::window_mode` sempre miram `glfwGetPrimaryMonitor()`; não há API de enumeração de monitores nem `monitor_index`. Expor um índice sem enumeração seria meia-API — enumerar monitores é fatia própria. Puxar só com pedido real de consumidor (jogo que precise escolher display num setup multi-tela).
- **A4-WINMODES — `resizable`/borderless-windowed/min-max size/callback de mudança de modo/vsync** (semente · decisão consciente D11 do plano `2026-07-21-framework2d-window-modes.md` · **sem demanda confirmada**): nenhum tem consumidor confirmado hoje; cada um seria superfície pública nova por conta própria (`resizable` trava/libera o resize do WM; borderless-windowed é janela sem borda NÃO-fullscreen; um callback de mudança de modo notificaria o host quando o WM alterar o modo por fora, ex. botão de restaurar da barra de título; vsync/refresh-rate control é orthogonal ao `WindowMode`). Puxar item a item, só com pedido real.
- ~~**App sem encaminhamento de roda do mouse**~~ (`VF-APPWHEEL`, PROG-1 Onda 1 · semente pré-existente, reafirmada no plano A4-WINMODES) **DESATUALIZADA -- ✅ riscada 2026-07-22**: a semente dizia que `glintfx::App` não tinha NENHUM caminho de `UiEvent::Type::MouseWheel`, mas o A1-INPUT (v0.12.0) já havia entregado a tradução física de scroll (`glfw_event_translate.hpp`) + o caso `MouseWheel` de `Engine::process_event`, compartilhado por `App::process_event` -- a semente só nunca tinha sido PROVADA por teste de valor. Provado: `app_process_event_smoke.cpp` estendido com um `UiEvent::Type::MouseWheel` sintético sobre um `#scroller` (mesma geometria 900px/100px de `scroll_scene.rcss`) + assert de VALOR via `get_element_scroll_top` (não só "não crashou") -- vermelho se o delta não aplicar. Verde localmente (80/80 GLFW=ON). Riscagem final (✅) pendente da onda de review adversarial do orquestrador.

**Promovidos/absorvidos na reconciliação (2026-07-01 · não re-adicionar):**

- Gate de encapsulamento (unificar a regex `(Rml|glfw|gl3w|GL/|stb|SDL)`) → sub-tarefa de **`TST-L1-STATIC`** (LW-TST).
- `version()` no build embed-only → **`L1.9-VERSEMBED`** (LW5).
- Doc de moved-from + `UiEvent::Type` default → **`L1.10-APIDOC`** (LW5).
- `consumer-example-embed/` → entregue por **`TST-L1-CONTRACT`** (LW-TST).
- Includes relativos `"../src/..."` na suíte → harness `test_host.hpp` de **`TST-L1-CONTRACT`** (LW-TST).
- Cobertura do `GlStateGuard` → **`TST-L1-GLLEAK`** (LW-TST) + auditoria **`AUD-L1-MEM`** (LW-AUD).

**Concluídos (histórico):**

- ~~**LW-AUD — onda de auditoria formal da Camada 1 (`AUD-L1-SUPPLY`/`PARSE`/`MEM`/`LICENSE`/`BUILD`)**~~ ✅ **FECHADA 2026-07-16** (`security-engineer` + `compliance-legal` audit, review adversarial que EXECUTA + `internal-auditor` consolidou o dossiê): 5/5 itens do catálogo `AUD-L1-*` resolvidos — 4 vereditos **CONFORME** sem achado bloqueante (`SUPPLY`/`MEM`/`LICENSE` sem CRÍTICO/IMPORTANTE; `PARSE` com 2 IMPORTANTE achados E fechados no mesmo ciclo, `16ab365`+`0f944e1`, re-verificados por mutation testing refeito de forma independente, não confiando no relato do implementer) + `BUILD` resolvido por dobra (decisão autônoma do Caetano/CTO, ver linha própria). Hardening real entregue no caminho: teto central de 256MiB em `BaseUrlFileInterface::Open()` cobrindo imagem+fonte (fecha o gap de DoS por alocação no `.rcss @font-face` que os guards só-de-imagem tinham deixado aberto) + corpus test discriminante `asset_decode_hostile_sanity` + `-Wall -Wextra` no alvo `glintfx` (`45209e5`, zero warning nos 17 sources autorais). Consolidação: `docs/auditoria/README.md` (índice mestre novo, sumário executivo + achados-por-severidade) + `docs/RELEASE_CHECKLIST.md` novo (gate de 5 itens pré-tag, motivado por 3 drifts reais já ocorridos: `SECURITY.md` travado em `0.4.x`, prosa "gl3w vendorizado" desatualizada em `AUDITORIAS.md`/`TODO.md` por 3 releases, `CONTRIBUTING.md` "5 testes" relíquia da v0.1.x) + housekeeping de ~2.3GB de builds órfãos (`/var/tmp`/`build*` scratch, limpeza do orquestrador). Sementes puxadas pra INBOX (não bloqueiam): `AUD-L1-QUALITY` (god-class/dead-code, pull antes da v1.0), `clang-format` no `preci.sh`, estender `-Wall -Wextra` aos 26 alvos de teste. Nenhum 🔴 CRÍTICO em lugar nenhum do dossiê.
- ~~**Releases pós-2026-07-11 (6 tags, todas CI dual verde)**~~ ✅ **v0.9.2** (blocker Windows do GusWorld — `L1.14-GLLOADER-WIN`, codepath `#ifdef _WIN32` no loader GL gerado, cross-compile MinGW validado em container efêmero host-limpo) · **v0.10.0** (flip suave `L1.20-FONTFLIP` — motor de fonte próprio vira DEFAULT, FreeType selecionável em runtime sem drop, ADR-0011) · **v0.11.0** (backdrop-ripple `L1.22-WAVE`/`L1.22-CAPTURE` — captura FBO0 + `decorator: ripple(...)` da UI própria, arm/ensure custo-zero, ADR-0012, review adversarial + ASan 52/52). Detalhe por-item nos bullets da INBOX acima (marcados ✅/🔍) + `CHANGELOG.md`. **VFX consumer-driven fechadas:** `L1.21-ECHO` sprite-eco (receituário RCSS puro, zero feature) + `L1.22-WAVE` screen-wave (host-side, GLSL de referência entregue) + parecer de ancoragem HUD do action-clock (host-drawn, GusWorld confirmou, zero demanda pro glintfx).
- ~~**CI — reestruturar workflows p/ a política canônica do líder**~~ **✅ COMPLETA (Ondas 0-4) 2026-07-11** (decisões i-A/ii-A/iii-A/iv-A/v-A ratificadas via AskUserQuestion). **Ondas 0-3 (impl):** (0) `tools/preci.sh` + hook `pre-push` opt-in (`TST-L1-PRECI`, `d07a849`); (1) imagem `tools/ci/Containerfile.f42` + `.forgejo/workflows/heavy.yml` (ASan `sanitize` + `fonteng` no runner local `claudio`, `runs-on: docker` + `container: fedora:42`, trigger PR/tag/dispatch), `fac7b70`; (2) triggers GitHub → push main + tags + PR, `cc77fa7`; (3) Codeberg voluntário encolhido a 1 job de paridade + `nightly.forgejo` removido (subsumido pelo heavy), `97f38da`; fix `SYS_futex`/clang no fonteng (`6225ae9`). **`heavy` VERDE em run real no claudio (#53, 4/4 jobs)**; GitHub CI verde. Achados via política-(e)-reproduzir-local: libasan/libubsan faltando no Fedora + SYS_futex é clang-específico (afeta fonteng, não sanitize/gcc). Ordem github>claudio>codeberg = convenção documentada (AGENTS.md/`feedback_ci_policy_canonica`); "waiting no Codeberg não segura release". **Onda 4 (docs) ENTREGUE 2026-07-11 (`technical-writer`):** auditoria de frescor read→edit reconciliou o resíduo pós-poda — gotcha do Codeberg no `AGENTS.md` (afirmava "matriz ON/OFF"; corrigido p/ 1 job GLFW=ON de paridade mínima) + descrição do pipeline de CI e `TST-L1-COV` no `TESTES.md` (job `coverage` agora GitHub-only; recalibração num só workflow; fallback→estado real; três-superfícies `github>claudio>codeberg` com ponteiro pro `AGENTS.md`). README já reconciliado na Onda 3 (badges GitHub/Codeberg/Coverage OK, sem badge inventado pro `claudio` self-hosted) — não tocado (anti-over-doc). Drift de contagem de testes (26/13 vs 31/16 vs 38/22 vs 39/22 espalhado) NÃO tocado: fora do escopo da poda de CI, é auditoria de frescor separada.
- ~~Drenados no reorder 2026-07-10~~ ✅: internalização FreeType **FT-F0..F3** (`SOV-LIBCORE`/`SOV-SFNT`/`SOV-RAST`/`L1.19-FONTENG`, tag `core-v0.3.0`, cada peça com review adversarial que executou — pegou CRÍTICO de UB float→int16 no SFNT + symbol-hijack no LIBCORE); **kerning** (parser `kern` `glx_sfnt_kern` + aplicação no font engine, `7d3d6d4`/`6bf2cf7`, review pegou 1 MÉDIO de `memset` implícito → gate `nm -u` endurecido `1432ac2`); **job de CI da config ON** (`build-and-test-fonteng`, `7fb28e5`); mitigação da bomba-relógio **RmlUi `SYS_futex`** no CI (`8780022`). Removidos da INBOX por resolvidos: `clear_focus` doc-precision (L1.16-DOMRW), divergência doc↔CI do compilador (clang alinhado v0.5.0), cobertura self-hosted (→L1.18-COVCI), `GAP-4 set_focus` (→L1.17-FOCUS).
- ~~glintfx — luminance-key tint de decorator~~ 🔍 **entregue na v0.7.0** (decorator próprio `image-tint()`, shader `glintfx-tint` injetado no `Gl3RenderInterface` sem tocar o RmlUi core, [ADR-0010](docs/adr/0010-image-tint-luminance-key.md)). Consumer-driven GusWorld; **validado em produção 2026-07-09** (1517/1517 drop-in, isola runa/ouro/pedra, ouro `rgb(218,170,96)` L=0.69 zera no threshold 0.70). Follow-up dormante de saturação → semente ativa acima.
- ~~glintfx — `id=""` (string vazia) aceito onde deveria falhar~~ ✅ **RESOLVIDO** (AUD-TEC-5, `70cf07f`, 2026-07-07): guard `|| !*id` nos 6 métodos por-id; `element_box_sanity`/`scroll_sanity` cobrem `""`. Auditoria dominó prevista.
- ~~GAP-2 origem de viewport~~ 🔍 **entregue na v0.2.5** (`L1.13-VPORIGIN` -- `UiLayer::set_viewport(x,y,w,h,target_h)`, letterbox, pedido real GusWorld aprovado pelo líder 2026-07-02). **FBO custom do host segue em aberto** -- ver `GAP-2b` em Ativos (só a origem mudou; o alvo de composição continua sempre FBO 0, `docs/embed-integration.md` seção 8).
- ~~GAP-1 `dp_ratio`~~ ✅ **entregue na v0.2.2** (`set_dp_ratio` + `dp_ratio` no config, UiLayer+App).
- ~~GAP-3 base-url override~~ ✅ **entregue na v0.2.2** (`set_asset_base_url` via `BaseUrlFileInterface`, UiLayer+App).
- ~~DOC-README (achado `/bigtech` 2026-07-01, Minor): README/AGENTS/getting-started apontavam `v0.2.3`/`v0.1.0`~~ ✅ **resolvido em c5a2072** -- sincronizados p/ `v0.2.4` (badge, `GIT_TAG` EN+PT, "produto ativo", status do AGENTS, FetchContent do tutorial); varredura do repo concluída. Follow-ups só-versão sinalizados (fora do escopo estrito): a data `2026-06-30` de "Current release" (a v0.2.4 saiu 2026-07-01), a linha "v0.1.0 is honest" no README e o snapshot datado do `CLAUDE.md` (versão soldada a data/contagem de testes).
