# AUD-L1-PARSE — Hostile Asset Parsing Audit / Auditoria de Parsing de Asset Hostil

- **Date / Data:** 2026-07-16 (original review); **re-review 2026-07-16 same day, commit `0f944e1`**.
- **Auditor:** security-engineer (independent adversarial review; implementer was `backend-engineer`, commits `16ab365` then `0f944e1`). Read-only on the reviewed commits' own diffs; product code was mutated ONLY as disposable, reverted experiments (mutation testing) — final `git diff` against `0f944e1` is empty.
- **Scope item / Item da TODO.md:** `AUD-L1-PARSE` (LW-AUD). Per `AUDITORIAS.md` row 19, scope is explicitly "decode de imagem malformada/hostil (PNG/JPG/TGA em `render_gl3.cpp::LoadTexture`) **e de `.rml`/`.rcss`**... DoS por alocação gigante", and per row 34 "o único DoS realista (imagem gigante) está em `AUD-L1-PARSE`".
- **Commits under review / Commits sob revisão:** `16ab365` — "fix(glintfx): AUD-L1-PARSE -- teto de 256MiB + guarda INT_MAX em LoadTexture" (image-only guards); `0f944e1` — "fix(glintfx): AUD-L1-PARSE -- teto central de tamanho cobre @font-face, corpus discrimina de verdade" (follow-up closing both IMPORTANTE findings below). Branch `chore/lw-aud`.
- **Verdict / Veredito:** **CONFORME — 0 CRÍTICO, 0 IMPORTANTE ABERTO, 1 COSMÉTICO.** `0f944e1` closes both IMPORTANTE findings from the `16ab365` review, independently re-verified below by execution (not by trusting the implementer's own commit-message report): (IMPORTANTE #1, font-path cap) a single central guard in `glintfx::BaseUrlFileInterface::Open()` now protects `.rcss` `@font-face` loading through BOTH font engines (glintfx's own `FontEngineOwn` and RmlUi's default FreeType `FontProvider`), confirmed live by forcing `FontEngine::FreeType` at runtime against a hostile huge font file and by mutation-testing the guard's removal (reproduces the exact ASan `allocation-size-too-big` abort the original review predicted, through the real `@font-face` → `LoadFontFace` → `LoadFile`/`Open` call chain); the image-specific guards in `render_gl3.cpp` (`16ab365`) were kept as a second, independently-sufficient defense layer (confirmed by a second mutation round: disabling the image guard alone still leaves the central `Open()` guard catching the same hostile image). (IMPORTANTE #2, tautological test) two new corpus cases (`img_huge_1tib`, `font_huge_1tib`, both `2^40+1`-byte sparse files) now genuinely discriminate guard presence/absence — confirmed by mutation testing myself, not by re-reading the implementer's transcript. No regression on legitimate assets (real font via both engines, real large image, ordinary `.rml`/`.rcss` — the untouched 51/53 of the suite). `AUD-L1-PARSE`'s "Estado Auditado" column may move to `✓`.

## Scope / Escopo

**EN:** (1) Re-verify the two size guards added to `Gl3RenderInterface::LoadTexture` (`render_gl3.cpp`) — cap boundary correctness, whether they actually prevent the DoS they claim to, whether the "do not delegate to the base loader" design decision is justified by the base loader's real behaviour. (2) Re-run the committed `asset_decode_hostile_sanity` corpus test under the project's ASan/UBSan sanitizer build and confirm it passes for real. (3) Mutation-test the guard itself — remove it, confirm the corpus test (and, if it doesn't, a purpose-built probe) actually detects the regression. (4) Search for sibling decode entry points that bypass the guarded `LoadTexture` — per this audit item's own scope, `.rml`/`.rcss` parsing, not just image decode.

**PT:** (1) Reverificar as duas guardas de tamanho acrescentadas ao `Gl3RenderInterface::LoadTexture` (`render_gl3.cpp`) — corretude da fronteira do cap, se de fato previnem o DoS que alegam prevenir, se a decisão de design "não delegar pro loader da base" é justificada pelo comportamento real do loader base. (2) Rerodar o corpus test commitado `asset_decode_hostile_sanity` sob o build com sanitizer (ASan/UBSan) do projeto e confirmar que passa de verdade. (3) Mutation-testar a própria guarda — removê-la, confirmar que o corpus test (e, se não, uma sonda própria) de fato detecta a regressão. (4) Buscar pontos de entrada de decode irmãos que escapem do `LoadTexture` guardado — conforme o próprio escopo deste item de auditoria, parsing de `.rml`/`.rcss`, não só decode de imagem.

**Files reviewed / Arquivos revisados:** `glintfx/src/render_gl3.cpp` (the guarded `LoadTexture`), `glintfx/tests/asset_decode_hostile_sanity.cpp`, `glintfx/tests/CMakeLists.txt`, `SECURITY.md`; upstream (pinned) `build-san/_deps/rmlui-src/Backends/RmlUi_Renderer_GL3.cpp` (base `LoadTexture` fallback, to verify the commit's own claim), `build-san/_deps/rmlui-src/Source/Core/FileInterface.cpp` (default `LoadFile`), `build-san/_deps/rmlui-src/Source/Core/FontEngineDefault/FontProvider.cpp` (upstream font loader), `build-san/_deps/rmlui-src/Source/Core/StyleSheetParser.cpp` (`ParseFontFaceBlock`, confirms the `@font-face` → `LoadFontFace` call site); `glintfx/src/font_engine_own.cpp` (glintfx's own font engine, default since `L1.20-FONTFLIP`).

## Method / Método

**EN:** Rebuilt `glintfx/build-san` (pre-existing `GLINTFX_SANITIZE=ON`, `Debug`) from the exact `16ab365` tree (`TMPDIR=/var/tmp`, never `/tmp`), ran the full 53-test suite under Xvfb + ASan/UBSan (53/53 PASS, including `asset_decode_hostile_sanity`). Wrote two disposable, non-committed probe executables (added as temporary `add_executable`/`add_test` blocks in `tests/CMakeLists.txt`, deleted and the CMakeLists edit reverted before finishing — final `git status`/`git diff` against `16ab365` is clean): `aud_review_boundary_probe.cpp` (exact-boundary cases `cap-1`/`cap`/`cap+1`/`0`, later extended with a `2^40`-byte sparse case) and `aud_review_regress_probe.cpp` (a real ~12.4 MiB legit PNG, generated via PIL, to confirm the cap does not regress legitimate large assets). Performed **mutation testing**: commented out guard 1/2 (`if (len > kMaxAssetFileBytes) {...}` short-circuited to `false && ...`), rebuilt, re-ran both the committed corpus test and the boundary probe, then reverted the mutation byte-for-byte (`cp` from a saved original) and rebuilt again to confirm the tree is back to `16ab365`'s exact state. Read the upstream RmlUi source (pinned commit, vendored under `build-san/_deps/rmlui-src/`) line-by-line to independently verify the commit message's own claim about the base loader's unguarded `new byte[buffer_size]`. Grepped the whole `glintfx/src/` tree for other `LoadTexture`/`LoadFontFace`/`FileInterface::Open` call sites to check for decode paths bypassing the new guard, per this audit item's explicit `.rml`/`.rcss` scope.

**PT:** Reconstruído `glintfx/build-san` (`GLINTFX_SANITIZE=ON` pré-existente, `Debug`) a partir da árvore exata do `16ab365` (`TMPDIR=/var/tmp`, nunca `/tmp`), rodada a suíte completa de 53 testes sob Xvfb + ASan/UBSan (53/53 PASS, incluindo `asset_decode_hostile_sanity`). Escritas duas sondas descartáveis, não-commitadas (acrescentadas como blocos `add_executable`/`add_test` temporários em `tests/CMakeLists.txt`, apagadas e o edit do CMakeLists revertido antes de terminar — `git status`/`git diff` final contra `16ab365` está limpo): `aud_review_boundary_probe.cpp` (casos de fronteira exata `cap-1`/`cap`/`cap+1`/`0`, depois estendido com um caso esparso de `2^40` bytes) e `aud_review_regress_probe.cpp` (um PNG legítimo real de ~12,4 MiB, gerado via PIL, para confirmar que o cap não regride assets grandes-mas-legítimos). Executado **mutation testing**: a guarda 1/2 comentada (`if (len > kMaxAssetFileBytes) {...}` curto-circuitada para `false && ...`), rebuild, corpus test commitado e a sonda de fronteira rerodados, depois a mutação revertida byte-a-byte (`cp` de um original salvo) e novo rebuild pra confirmar que a árvore voltou ao estado exato do `16ab365`. Lido o source do RmlUi upstream (commit pinado, vendorizado sob `build-san/_deps/rmlui-src/`) linha a linha pra verificar independentemente a alegação da própria mensagem de commit sobre o `new byte[buffer_size]` sem guarda do loader da base. Feito grep na árvore inteira de `glintfx/src/` por outros pontos de chamada de `LoadTexture`/`LoadFontFace`/`FileInterface::Open` pra checar caminhos de decode que escapem da guarda nova, conforme o escopo explícito de `.rml`/`.rcss` deste item de auditoria.

## Findings / Achados

### CRITICAL / CRÍTICO

None found. / Nenhum encontrado.

### IMPORTANT / IMPORTANTE

**Status: BOTH RESOLVED in `0f944e1` (2026-07-16), independently re-verified by execution — see "Re-review (`0f944e1`)" section below. Table below is the historical record as found against `16ab365`, kept for the record.** / **Status: AMBOS RESOLVIDOS no `0f944e1` (2026-07-16), re-verificados de forma independente por execução — ver a seção "Re-review (`0f944e1`)" abaixo. A tabela abaixo é o registro histórico como encontrado contra `16ab365`, mantida pro registro.**

| # | File:line / Arquivo:linha | Finding / Achado |
|---|---|---|
| 1 | `glintfx/src/font_engine_own.cpp:697,725` (both `FontEngineOwn::LoadFontFace(const String&,...)` overloads) + upstream `build-san/_deps/rmlui-src/Source/Core/FontEngineDefault/FontProvider.cpp:90-104` | **EN:** The exact unbounded-allocation DoS class this commit closes for `<img>` (`bad_alloc` from an oversized/hostile asset file propagating uncaught out of `LoadTexture`, across the RmlUi virtual-call boundary, into the host) is still wide open on the **font** path. `StyleSheetParser::ParseFontFaceBlock` (`build-san/_deps/rmlui-src/Source/Core/StyleSheetParser.cpp:561`) calls `Rml::LoadFontFace(src, family, style, weight, is_fallback, face_index)` for every `@font-face` block in an `.rcss` stylesheet — reachable from any document/stylesheet the host loads, exactly the `.rml`/`.rcss` half of this audit item's own declared scope (`AUDITORIAS.md` row 19). **Default engine** (glintfx's own, `FontEngine::Own`, default since `L1.20-FONTFLIP`): `FontEngineOwn::LoadFontFace` calls `Rml::GetFileInterface()->LoadFile(file_name, data)` — resolves to the DEFAULT (unoverridden) `Rml::FileInterface::LoadFile` (`build-san/_deps/rmlui-src/Source/Core/FileInterface.cpp:19-37`, glintfx's own `BaseUrlFileInterface` only overrides `Open`/`Read`, not `LoadFile`/`Length`), which does `out_data = String(length, 0)` **unconditionally, no cap, before any content is inspected**. **`FreeType` engine** (selectable at runtime via `AppConfig::font_engine`/`UiLayerConfig::font_engine`): `FontProvider::LoadFontFace(file_name,...)` does `auto buffer_ptr = UniquePtr<byte[]>(new byte[length])` — the SAME unguarded pattern the commit's own message identifies as the DoS vector in the base TGA loader, just in the font loader instead. Reproduced empirically by adversarial mutation-testing methodology on the analogous image path (see "Verified" section below) — a `String(length,0)`/`new byte[length]` with `length` derived straight from an attacker/asset-controlled `SEEK_END` and no upper bound is precisely the pattern that crashed the process under ASan (`allocation-size-too-big`) for the image case; there is no reason to expect a different outcome for a hostile/huge font file, and nothing in either font-loading code path inspects `length` before allocating. **Impact:** an `@font-face { src: url(...) }` pointing at an oversized or maliciously-reported-size font asset (any document/stylesheet an embedding host loads from a source it does not fully trust — the same threat model `docs/embed-integration.md` and this very commit's own rationale assume for images) can abort the host process via an uncaught `std::bad_alloc`/`std::length_error`, exactly the DoS class `AUD-L1-PARSE` exists to close, on a vector this commit does not touch. **Recommendation:** apply the same two-guard pattern (`kMaxAssetFileBytes` cap checked before allocation, `return false`/`fallback_face`-appropriate failure, no silent delegation to an unguarded path) to both `FontEngineOwn::LoadFontFace(const String&,...)` overloads (glintfx's own code, straightforward) and, if the `FreeType` engine selection remains supported, either wrap/guard `FontProvider::LoadFontFace` at the call site or gate it behind the same `FileInterface::Length` pre-check before calling into it. **PT:** A mesma classe de DoS por alocação sem teto que este commit fecha pro `<img>` (`bad_alloc` de um arquivo de asset hostil/grande-demais propagando sem captura pra fora do `LoadTexture`, cruzando a fronteira de virtual-call do RmlUi, até o host) continua totalmente aberta no caminho de **fonte**. `StyleSheetParser::ParseFontFaceBlock` chama `Rml::LoadFontFace(...)` pra todo bloco `@font-face` de uma folha `.rcss` — alcançável de qualquer documento/stylesheet que o host carregue, exatamente a metade `.rml`/`.rcss` do próprio escopo declarado deste item de auditoria. **Motor default** (próprio da glintfx, `FontEngine::Own`, default desde `L1.20-FONTFLIP`): `FontEngineOwn::LoadFontFace` chama `Rml::GetFileInterface()->LoadFile(...)` — resolve pro `Rml::FileInterface::LoadFile` DEFAULT (não sobrescrito), que faz `out_data = String(length, 0)` **incondicionalmente, sem teto, antes de qualquer inspeção de conteúdo**. **Motor `FreeType`** (selecionável em runtime): `FontProvider::LoadFontFace` faz `new byte[length]` — o MESMO padrão sem guarda que a própria mensagem do commit identifica como o vetor de DoS no loader TGA da base, só que no loader de fonte. Reproduzido empiricamente pela mesma metodologia de mutation testing aplicada ao caminho análogo de imagem (ver seção "Verificado" abaixo) — um `String(length,0)`/`new byte[length]` com `length` vindo direto de um `SEEK_END` controlado por atacante/asset e sem teto superior é precisamente o padrão que crashou o processo sob ASan (`allocation-size-too-big`) no caso da imagem; não há motivo pra esperar resultado diferente pra um arquivo de fonte hostil/enorme, e nada em nenhum dos dois caminhos de carregamento de fonte inspeciona `length` antes de alocar. **Impacto:** um `@font-face { src: url(...) }` apontando pra um asset de fonte de tamanho enorme ou com tamanho falsamente reportado (qualquer documento/stylesheet que um host embarcador carregue de uma fonte não totalmente confiável -- o mesmo modelo de ameaça que `docs/embed-integration.md` e o próprio racional deste commit assumem pra imagens) pode abortar o processo host via `std::bad_alloc`/`std::length_error` sem captura, exatamente a classe de DoS que `AUD-L1-PARSE` existe pra fechar, num vetor que este commit não toca. **Recomendação:** aplicar o mesmo padrão de duas guardas (teto `kMaxAssetFileBytes` checado antes da alocação, falha `return false`/apropriada a `fallback_face`, sem delegação silenciosa pra caminho sem guarda) às duas sobrecargas de `FontEngineOwn::LoadFontFace(const String&,...)` (código próprio da glintfx, direto) e, se a seleção do motor `FreeType` continuar suportada, ou embrulhar/guardar `FontProvider::LoadFontFace` no ponto de chamada ou fazer o gate dele atrás da mesma pré-checagem de `FileInterface::Length` antes de entrar nele. |
| 2 | `glintfx/tests/asset_decode_hostile_sanity.cpp:32-45` (`oversized` case, `kAssetCapBytes + 1`) | **EN:** Mutation testing (guard 1/2 disabled, see "Verified" section) shows the committed corpus test's `oversized` case **does not go red** when the guard it is meant to protect is removed — it stays green because `kAssetCapBytes + 1` (256 MiB + 1 byte) is small enough that, even with zero cap, `std::vector<unsigned char> buf(len)` allocates successfully, `stbi_load_from_memory` fails to decode the all-zero content, and the function falls back to the base TGA loader, which also fails gracefully (`"Texture file size is smaller than TGAHeader"`-class rejection) — the SAME outcome the guard produces, just reached the slow way. The test is provably **not tautological in general** (it correctly asserts the guard's log line format is exercised, and the fast-rejection behaviour is directly observable in the log at the correct boundary — see "Verified" below) but it IS tautological **for its stated purpose of regression-proofing the DoS guard specifically**: a future refactor that silently drops or breaks guard 1/2 would NOT be caught by this test, because the chosen size never actually stresses the allocator. The real DoS this guard exists to prevent only manifests at genuinely large nominal sizes (empirically confirmed: a `2^40`-byte sparse file crashes the process with the guard removed, ASan pinpointing `render_gl3.cpp:539`, and is instantly rejected with the guard present — see "Verified" below). **Recommendation:** add a sixth corpus case using a much larger nominal size (e.g. the same `2^40`-byte sparse-file technique already used for `oversized`, costing ~0 real disk) specifically to prove the guard prevents an actual allocation attempt, not just that a modest over-cap file degrades gracefully either way. **PT:** O mutation testing (guarda 1/2 desabilitada, ver seção "Verificado") mostra que o caso `oversized` do corpus test commitado **não fica vermelho** quando a guarda que ele deveria proteger é removida — permanece verde porque `kAssetCapBytes + 1` (256 MiB + 1 byte) é pequeno o bastante pra que, mesmo com teto zero, `std::vector<unsigned char> buf(len)` aloque com sucesso, `stbi_load_from_memory` falhe ao decodificar o conteúdo todo-zero, e a função caia no fallback do loader TGA da base, que também falha graciosamente (rejeição classe `"Texture file size is smaller than TGAHeader"`) — o MESMO resultado que a guarda produz, só que alcançado pelo caminho lento. O teste **não é tautológico em geral** (afirma corretamente o formato da linha de log da guarda ao ser exercitado, e o comportamento de rejeição rápida é diretamente observável no log na fronteira correta -- ver "Verificado" abaixo), mas É tautológico **para seu propósito declarado de blindar contra regressão especificamente a guarda de DoS**: um refactor futuro que silenciosamente derrube ou quebre a guarda 1/2 NÃO seria pego por este teste, porque o tamanho escolhido nunca de fato estressa o alocador. O DoS real que esta guarda existe pra prevenir só se manifesta em tamanhos nominais genuinamente grandes (confirmado empiricamente: um arquivo esparso de `2^40` bytes crasha o processo com a guarda removida, ASan apontando exatamente `render_gl3.cpp:539`, e é instantaneamente rejeitado com a guarda presente -- ver "Verificado" abaixo). **Recomendação:** acrescentar um sexto caso de corpus usando um tamanho nominal bem maior (ex.: a mesma técnica de arquivo esparso de `2^40` bytes já usada pro `oversized`, custando ~0 disco real) especificamente pra provar que a guarda previne uma tentativa de alocação de fato, não só que um arquivo modestamente acima do cap degrada graciosamente de qualquer jeito. |

### COSMETIC / COSMÉTICO

| # | File:line / Arquivo:linha | Finding / Achado |
|---|---|---|
| 1 | `render_gl3.cpp:542-546` and `:586-589` (the two `return RenderInterface_GL3::LoadTexture(dims, source)` delegating fallbacks, short-read and decode-failure paths) | **EN:** Both delegating fallback paths re-derive their own file size independently (the base loader re-`Open()`s and re-`Seek`/`Tell`s the same path) rather than reusing the already-validated `len`. In the extremely narrow window between this function's own size check and the base loader's re-check, a file that grows on disk (attacker-controlled mount/symlink target swapped mid-request, or a legitimately growing log-like file used as an asset by mistake) could theoretically be re-measured larger than `kMaxAssetFileBytes` by the base loader's own unguarded path — the exact regression this commit's own "DELIBERATELY DOES NOT delegate" comment on guard 1/2 explicitly reasons about for the size-cap path, but this TOCTOU is a distinct (and much narrower) route to the same base-loader code. Requires an adversarial filesystem/mount, not just an adversarial file — outside the realistic threat model this fix targets (a static asset file), and not a new issue introduced by this commit (the base loader's re-`Open` pattern is upstream, unconditional, unrelated to whether this subclass calls it once or the guard exists at all). Documented for the líder's awareness, not blocking. **PT:** Os dois caminhos de fallback que delegam re-derivam o próprio tamanho de arquivo de forma independente (o loader da base reabre e re-faz `Seek`/`Tell` no mesmo path) em vez de reusar o `len` já validado. Na janela extremamente estreita entre a checagem de tamanho desta função e a re-checagem do loader da base, um arquivo que cresce em disco (mount/symlink-alvo controlado por atacante trocado no meio da requisição, ou um arquivo tipo-log crescendo legitimamente usado por engano como asset) poderia teoricamente ser re-medido maior que `kMaxAssetFileBytes` pelo caminho sem guarda do próprio loader da base -- exatamente a regressão que o comentário "DELIBERATELY DOES NOT delegate" deste próprio commit já raciocina explicitamente sobre pro caminho do teto de tamanho, mas este TOCTOU é uma rota distinta (e bem mais estreita) pro mesmo código do loader da base. Exige um sistema de arquivos/mount adversarial, não só um arquivo adversarial -- fora do modelo de ameaça realista que este fix mira (um arquivo de asset estático), e não é um problema novo introduzido por este commit (o padrão de reabrir do loader da base é upstream, incondicional, independente de esta subclasse chamá-lo uma vez ou de a guarda existir). Documentado pra consciência do líder, não bloqueia. |

## Verified conformant, with evidence / Verificado conforme, com evidência

**1. Build + full suite, sanitizers on / Build + suíte completa, sanitizers ligados** — `glintfx/build-san` (`GLINTFX_SANITIZE=ON`, pre-existing config, rebuilt clean from `16ab365`), full suite under Xvfb: **53/53 PASS**, including `asset_decode_hostile_sanity` (`red_count=2472 > 500` post-hostile-corpus real-PNG decode oracle, matching `texture_png_alpha.cpp`'s own threshold). Two tests (`scroll_sanity`, `fonteng_dedup_sanity`) flaked once each under `-j4`/`-j2` parallel execution, unrelated subsystems (scroll geometry, font-engine memory trend, neither touches `LoadTexture`); both PASS cleanly in isolation on re-run — consistent with the project's own documented Xvfb/GLFW parallel-execution noise class (`tests/run_xvfb.cmake`'s own header comment on Wayland-socket races), not a regression from this commit.

**2. Cap boundary is exact and inclusive at 256 MiB / Fronteira do teto é exata e inclusiva em 256 MiB** — Boundary probe (`cap-1`=268435455B, `cap`=268435456B, `cap+1`=268435457B, `0`B) confirms: `cap-1` and `cap` are BOTH allowed through (no rejection log line, buffer allocated, decode attempted, fails gracefully as non-image content — expected, these are zero-filled sparse files, not valid images); `cap+1` is rejected **instantly**, log line `"...is 268435457 bytes, exceeding the 268435456 byte cap -- refusing to load."` fires before any `Seek`/`Read` of the bulk content. Matches the guard's own `len > kMaxAssetFileBytes` (strict greater-than) semantics exactly.

**3. The base loader's fallback really is unguarded — the commit's own claim, verified / O fallback do loader da base realmente não tem guarda -- alegação do próprio commit, verificada** — Read `build-san/_deps/rmlui-src/Backends/RmlUi_Renderer_GL3.cpp:1190-1213` (`RenderInterface_GL3::LoadTexture`) directly: `Rml::UniquePtr<byte[]> buffer(new byte[buffer_size]);` at line 1211, with `buffer_size` coming straight from an unchecked `Tell(file_handle)` at line 1200 — **unconditional, no upper bound whatsoever**. Confirms the commit's central design decision (guards 1/2 `return false` directly instead of falling through to this function) is correct: delegating on cap-exceeded would silently defeat the guard for exactly the assets it exists to reject.

**4. Mutation testing proves the guard is real and necessary / Mutation testing prova que a guarda é real e necessária** — Disabled guard 1/2 (`if (false && len > kMaxAssetFileBytes)`), rebuilt: (a) the committed `asset_decode_hostile_sanity` corpus test still PASSES (see IMPORTANT #2 above — this is the tautology finding, not evidence the guard is unnecessary); (b) a purpose-built probe with a `2^40`-byte (1 TiB) sparse fixture reproduces the exact DoS the guard exists to prevent — **AddressSanitizer aborts the process** with `allocation-size-too-big`, stack trace pinpointing `Gl3RenderInterface::LoadTexture` at `render_gl3.cpp:539` (`std::vector<unsigned char> buf(len)`), reached via the real call chain `Bootstrap::load → Rml::Context::LoadDocument → ElementDocument::UpdateLayout → ... → Gl3RenderInterface::LoadTexture` — i.e. an ordinary `ui.load()` on a document referencing the hostile asset, no special API needed. Reverted the mutation byte-for-byte, rebuilt, re-ran: the same 1 TiB case is now rejected **instantly** with the guard's log line, `ui.ok()` stays true, no crash. Final `git diff`/`git status` against `16ab365` confirmed clean (mutation fully reverted, no probe files left in the tree).

**5. No regression on legitimate large assets / Sem regressão em assets legítimos grandes** — A real ~12.4 MiB PNG (2400x1500, random-noise content chosen to compress poorly and stay well clear of a trivial small fixture) decodes correctly through the guarded path (`ok_load=true`, `get_element_box` finds the element at its styled 64dp size, `ui.ok()=true`, no ASan/UBSan report) — the 256 MiB cap is comfortably generous, ~20x headroom over this test asset and matching the commit's own stated rationale (an 8192x8192 RGBA8 texture, already past most GPUs' `GL_MAX_TEXTURE_SIZE`, is 256 MiB raw).

**6. No other image-decode entry point bypasses the guard / Nenhum outro ponto de entrada de decode de imagem escapa da guarda** — Grepped `glintfx/src/` for `stbi_load`/`LoadTexture`: the only call site is the one guarded by this commit (`Gl3RenderInterface::LoadTexture`, `render_gl3.cpp:477`); `decorator_image_tint.cpp` reuses the RmlUi texture database (`RenderManager::LoadTexture`), which itself routes back through the same guarded override — no separate stb_image entry point exists in this codebase.

## Re-review (`0f944e1`) — IMPORTANTE #1/#2 closed, independently re-verified / IMPORTANTE #1/#2 fechados, re-verificado de forma independente

**EN:** The `backend-engineer` fixed both IMPORTANT findings in a follow-up commit, `0f944e1`, on the same branch. This section documents an **independent re-validation by execution** (not a re-read of the commit message) — re-built `build-san` from `0f944e1`, re-ran the full suite, and personally re-ran mutation testing rather than trusting the implementer's own transcript, per the coordinator's explicit instruction.

**Fix design (IMPORTANTE #1):** instead of duplicating a size guard inside each font engine, `0f944e1` adds a **single** guard to `glintfx::BaseUrlFileInterface::Open()` (`glintfx/src/base_url_file_interface.hpp`) — the one place every consumer of `Rml::GetFileInterface()` converges on before reading a byte, since `Rml::SetFileInterface(&impl_->file_iface)` installs this exact class globally (`glintfx/src/bootstrap.cpp:965`, confirmed by reading). `Open()` now runs both fopen attempts (base-URL-prefixed and raw-path fallback) through a shared `fopen_capped()` helper: `fseek`/`ftell`/`fseek` to measure size, `> kMaxFileBytes` (256 MiB, same value as `render_gl3.cpp`'s own constant) closes and returns `nullptr` — the same failure convention as a normal `fopen()` error, so every existing caller's pre-existing `if (!handle)` fail-secure path fires for free, no caller-side code changed. The image-specific guards added in `16ab365` were **kept intact** in `render_gl3.cpp`, deliberately, as a second independent layer (documented rationale: an embedding host could call `Rml::SetFileInterface()` itself after `Bootstrap::load()` and swap out the capped interface — a real possibility in embed mode, per the class's own comment).

**Independent verification performed / Verificação independente realizada:**

**1. Code read confirms the FreeType path really does converge on `Open()` (not a path that escapes the global interface) / Leitura de código confirma que o caminho FreeType de fato converge no `Open()` (não escapa da interface global)** — Re-read `build-san/_deps/rmlui-src/Source/Core/FontEngineDefault/FontProvider.cpp:90-104` (`FontProvider::LoadFontFace(const String& file_name, ...)`): `FileInterface* file_interface = GetFileInterface(); FileHandle handle = file_interface->Open(file_name);` — a direct virtual call through the globally-installed interface, exactly `glintfx::BaseUrlFileInterface`, exactly the guarded `Open()`. No `LoadFile()` involved on this path at all (confirming the fix's own rationale for why a `LoadFile()`-only guard would have missed FreeType).

**2. Live confirmation, FreeType engine forced at runtime, not just the default / Confirmação ao vivo, motor FreeType forçado em runtime, não só o default** — Wrote a disposable probe (`aud_review_freetype_probe.cpp`, deleted + `tests/CMakeLists.txt` edit reverted before finishing) that constructs `UiLayer` with `.font_engine = glintfx::FontEngine::FreeType` explicitly and loads a document with `@font-face { src: "<2^40+1-byte sparse file>"; }`. Result: `BaseUrlFileInterface::Open: '...' is 1099511627777 bytes, exceeding the 268435456 byte cap -- refusing to open.` fires, immediately followed by RmlUi's own upstream message `Failed to load font face from ..., could not open file.` (the exact expected `Open()`-returns-`NULL` failure path in `FontProvider::LoadFontFace`) — `ok_load=1` (document still loads, font degrades gracefully), `ui.ok()=1` post-render, no crash. Confirms the guard protects the FreeType engine specifically, live, not just by inference from the default-engine test case.

**3. Mutation testing redone myself, round 1 (central guard) / Mutation testing refeito por mim, rodada 1 (guarda central)** — Commented out the `size > kMaxFileBytes` check in `fopen_capped()` (`base_url_file_interface.hpp`), rebuilt `build-san`. Result: `asset_decode_hostile_sanity`'s `font_huge_1tib` case **aborts under ASan** — `allocation-size-too-big`, `SUMMARY: ... FileInterface.cpp:27 in Rml::FileInterface::LoadFile`, full stack trace through the REAL call chain `FontEngineOwn::LoadFontFace → Rml::FileInterface::LoadFile → Rml::LoadFontFace → StyleSheetParser::ParseFontFaceBlock → ElementDocument::ProcessHeader → ... → Bootstrap::load → Engine::load → UiLayer::load`, reached by an ordinary `ui.load()` — exactly the DoS the original review predicted, reproduced independently. Reverted the mutation byte-for-byte (`cp` from a saved original), rebuilt: `font_huge_1tib` rejected instantly again, `git diff` against `0f944e1` confirmed clean.

**4. Mutation testing redone myself, round 2 (layered-defense claim) / Mutation testing refeito por mim, rodada 2 (alegação de defesa em camadas)** — With the central guard restored, disabled `render_gl3.cpp`'s OWN image guard (guard 1/2 from `16ab365`) instead, rebuilt. Result: `img_huge_1tib` is STILL rejected — but now the log line is `BaseUrlFileInterface::Open: ... refusing to open` (not `LoadTexture: ... refusing to load`), confirming `render_gl3.cpp`'s own guard code was genuinely unreachable/disabled and the CENTRAL `Open()` guard alone caught it — independently validating the "central guard alone suffices for images too" claim, not just the "central guard closes the font gap" claim. Reverted, rebuilt, `git diff` clean.

**5. Full suite, sanitizers on / Suíte completa, sanitizers ligados** — `build-san` (`GLINTFX_SANITIZE=ON`) rebuilt clean from `0f944e1`: **53/53 PASS** under Xvfb, including `asset_decode_hostile_sanity`'s all 7 cases (5 image-malformation + `img_huge_1tib` + `font_huge_1tib`) and `fonteng_engine_select_sanity` (loads a real font, `assets/OpenSans-Regular.ttf`, through BOTH `FontEngine::Own` and `FontEngine::FreeType`, renders real text, compares ink coverage — direct regression proof the central cap does not break legitimate font loading on either engine). The same `scroll_sanity`/`fonteng_dedup_sanity` pair flaked once under `-j3` parallel execution (unrelated subsystems, same known Xvfb/GLFW parallel-noise class documented in the original review) and PASS cleanly in isolation on re-run — not a regression from `0f944e1`.

**6. No regression on ordinary documents / Sem regressão em documentos comuns** — The other 51 tests in the suite all load ordinary `.rml`/`.rcss` documents and real assets (well under 256 MiB) through the now-guarded `Open()` with zero change in behaviour — the cap sits far above any legitimate asset these tests (or the project's own stated rationale: an 8192×8192 RGBA8 texture at 256 MiB raw) exercise.

**Final state / Estado final:** `git diff`/`git status` against `0f944e1` clean (all mutations reverted, no probe files left in the tree, only the pre-existing untracked `docs/auditoria/*.md` files from sibling audits remain untracked).

**PT:** O `backend-engineer` corrigiu os dois achados IMPORTANTE num commit de follow-up, `0f944e1`, na mesma branch. Esta seção documenta uma **re-validação independente por execução** (não uma releitura da mensagem de commit) — `build-san` reconstruído a partir do `0f944e1`, suíte completa rerodada, e mutation testing re-executado pessoalmente em vez de confiar no transcript do próprio implementer, conforme instrução explícita do coordenador.

**Design do fix (IMPORTANTE #1):** em vez de duplicar uma guarda de tamanho dentro de cada motor de fonte, o `0f944e1` acrescenta UMA ÚNICA guarda em `glintfx::BaseUrlFileInterface::Open()` (`glintfx/src/base_url_file_interface.hpp`) — o único ponto onde todo consumidor de `Rml::GetFileInterface()` converge antes de ler um byte, já que `Rml::SetFileInterface(&impl_->file_iface)` instala exatamente esta classe globalmente (`glintfx/src/bootstrap.cpp:965`, confirmado por leitura). O `Open()` agora roda as duas tentativas de fopen (prefixada por base-URL e fallback de path bruto) por um helper `fopen_capped()` compartilhado: `fseek`/`ftell`/`fseek` pra medir o tamanho, `> kMaxFileBytes` (256 MiB, mesmo valor da constante própria de `render_gl3.cpp`) fecha e retorna `nullptr` — a mesma convenção de falha de um erro normal de `fopen()`, então o caminho fail-secure `if (!handle)` já existente de todo chamador dispara de graça, nenhum código do lado chamador mudou. As guardas específicas de imagem acrescentadas no `16ab365` foram MANTIDAS intactas em `render_gl3.cpp`, deliberadamente, como uma segunda camada independente (racional documentado: um host embarcador poderia chamar `Rml::SetFileInterface()` por conta própria depois de `Bootstrap::load()` e trocar a interface com teto — uma possibilidade real em embed mode, conforme o próprio comentário da classe).

**Verificação independente realizada:** ver os seis itens EN acima (espelhados aqui em conceito) — leitura confirmando que o FreeType converge no `Open()`; confirmação AO VIVO forçando `FontEngine::FreeType` contra fonte hostil de 1 TiB (rejeitada instantaneamente, sem crash); mutation testing rodada 1 (guarda central removida → `font_huge_1tib` aborta sob ASan na cadeia real `FontEngineOwn::LoadFontFace → Rml::FileInterface::LoadFile → ... → Bootstrap::load`; reposta → verde, `git diff` limpo); mutation testing rodada 2 (guarda de imagem de `render_gl3.cpp` removida, guarda central intacta → `img_huge_1tib` ainda pego, agora pela guarda central sozinha, confirmando que ela sozinha basta pra imagem também); suíte completa 53/53 sob ASan/UBSan incluindo `fonteng_engine_select_sanity` (fonte real via os DOIS motores, prova direta de não-regressão); sem regressão nos outros 51 testes que carregam documentos/assets comuns.

**Estado final:** `git diff`/`git status` contra `0f944e1` limpo (todas as mutações revertidas, nenhum arquivo de sonda restante na árvore, só os `docs/auditoria/*.md` pré-existentes não-versionados de auditorias irmãs permanecem untracked).

## Conclusion / Conclusão

**EN:** The two guards `16ab365` adds to `Gl3RenderInterface::LoadTexture` are **correct, boundary-exact, and independently proven necessary** — mutation testing both confirms the guard closes a real, reproducible process-abort DoS (1 TiB sparse asset, ASan-verified crash-then-fix) and confirms the commit's own stated rationale for not delegating to the base loader (verified against the actual pinned upstream source, not just the commit message's claim). The original review found two IMPORTANT gaps on top of that sound image-only fix: the font-loading path (`@font-face`, both engines) had no cap of its own, squarely inside `AUD-L1-PARSE`'s own declared `.rml`/`.rcss` scope; and the committed corpus test's `oversized` case did not actually discriminate the guard's removal under mutation testing. **Both are now closed in `0f944e1`**, re-verified here independently by execution rather than by trusting the implementer's report: a single, well-placed central guard in `BaseUrlFileInterface::Open()` closes the font gap for both font engines (confirmed live, including with FreeType explicitly forced), the existing image guards remain as a genuinely independent second layer (confirmed by mutating each layer separately), the two new `2^40+1`-byte corpus cases genuinely discriminate guard presence/absence (confirmed by mutation testing performed by this reviewer, not by re-reading the implementer's transcript), and no regression was found on legitimate assets through either font engine or on ordinary documents. Zero CRITICAL, zero IMPORTANT open, one COSMETIC (TOCTOU note, informational, not blocking, unchanged from the original review). `AUD-L1-PARSE`'s "Estado Auditado" column may move to `✓` (aprovado).

**PT:** As duas guardas que o `16ab365` acrescenta ao `Gl3RenderInterface::LoadTexture` são **corretas, com fronteira exata, e provadas necessárias de forma independente** — o mutation testing confirma tanto que a guarda fecha um DoS real e reproduzível de abort de processo (asset esparso de 1 TiB, crash verificado por ASan e depois fechado) quanto confirma o racional do próprio commit pra não delegar ao loader da base (verificado contra o source real do upstream pinado, não só a alegação da mensagem de commit). A review original achou duas lacunas IMPORTANTES por cima desse fix de imagem sólido: o caminho de carregamento de fonte (`@font-face`, os dois motores) não tinha teto próprio nenhum, bem dentro do escopo `.rml`/`.rcss` declarado do próprio `AUD-L1-PARSE`; e o caso `oversized` do corpus test commitado não discriminava de fato a remoção da guarda sob mutation testing. **Os dois agora estão fechados no `0f944e1`**, re-verificados aqui de forma independente por execução em vez de confiar no relato do implementer: uma única guarda bem posicionada em `BaseUrlFileInterface::Open()` fecha a lacuna de fonte pros dois motores (confirmado ao vivo, inclusive com FreeType forçado explicitamente), as guardas de imagem existentes permanecem como uma segunda camada genuinamente independente (confirmado mutando cada camada separadamente), os dois novos casos de corpus de `2^40+1` bytes discriminam de fato a presença/ausência da guarda (confirmado por mutation testing feito por esta revisora, não por reler o transcript do implementer), e nenhuma regressão foi achada em assets legítimos por nenhum dos dois motores de fonte nem em documentos comuns. Zero CRÍTICO, zero IMPORTANTE aberto, um COSMÉTICO (nota de TOCTOU, informativa, não bloqueia, inalterada da review original). A coluna "Estado Auditado" de `AUD-L1-PARSE` pode passar a `✓` (aprovado).
