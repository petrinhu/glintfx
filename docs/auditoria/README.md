# Audit dossier — master index / Dossiê de auditoria — índice mestre

> **EN:** Consolidated by `internal-auditor`. This index is the entry point for an external auditor: what was audited, when, by whom, and the verdict. Individual reports are the source of truth; this file only indexes and summarizes them. Governance/method: [`AUDITORIAS.md`](../../AUDITORIAS.md) (root). Pre-tag process: [`docs/RELEASE_CHECKLIST.md`](../RELEASE_CHECKLIST.md).
> **PT:** Consolidado pelo `internal-auditor`. Este índice é o ponto de entrada para um auditor externo: o que foi auditado, quando, por quem, e o veredito. Os relatórios individuais são a fonte de verdade; este arquivo só indexa e resume. Governança/método: [`AUDITORIAS.md`](../../AUDITORIAS.md) (raiz). Processo pré-tag: [`docs/RELEASE_CHECKLIST.md`](../RELEASE_CHECKLIST.md).

## Scope / Escopo

**EN:** This repository has two independent stacks under audit — Layer 0 (`loucura_c_asm`, pure C+ASM, zero libc, HEAD post-Onda-4 delta re-audit, `core-v0.4.0`-candidate — untagged, `REL-TAG` is a líder go/no-go, not yet exercised) and Layer 1 (`glintfx`, C++/RmlUi/GL3 library, `v0.11.1` tagged, branch `chore/l1-quality` HEAD carries a code-quality pass + a symbol-boundary fix pending the `v0.11.2` tag decision). Layer 0's catalog is `AUD-ABI`/`AUD-SEC` (ABI conformance + security) — Onda 4 re-issued both as **delta** re-audits (`AUD-ABI-Δ`/`AUD-SEC-Δ`) covering everything that landed after the original `core-v0.1.0` pass (free-list allocator, float conversion, `libcore.a` boundary, SFNT/raster/hint). Layer 1's catalog is `AUD-L1-*` (supply-chain, hostile-asset parsing, memory-safety, license) plus two items sourced outside the formal audit catalog from the `chore/l1-quality` branch: `AUD-L1-QUALITY` (a code-quality pass, `tech-lead`-equivalent scope) and `AUD-L1-GLSYM` (a `HIGH`-severity finding from the `TST-L1-CONTRACT` test wave, not a security-engineer audit pass — see its own row below for why it is not mapped onto the 🔴/🟠/🟢 scale). The full catalog rationale (why these five formal chapters and not more — network/authN/crypto/PII are explicitly out of scope for a desktop rendering library) lives in `AUDITORIAS.md`.

**PT:** Este repositório tem duas stacks independentes sob auditoria — Camada 0 (`loucura_c_asm`, C+ASM puro, zero libc, HEAD pós-re-auditoria-delta da Onda 4, `core-v0.4.0`-candidata — não-taggeada, `REL-TAG` é go/no-go do líder, ainda não exercido) e Camada 1 (`glintfx`, biblioteca C++/RmlUi/GL3, `v0.11.1` taggeada, HEAD do branch `chore/l1-quality` carrega uma passada de qualidade de código + um fix de fronteira de símbolo pendentes da decisão de tag `v0.11.2`). O catálogo da Camada 0 é `AUD-ABI`/`AUD-SEC` (conformidade ABI + segurança) — a Onda 4 reemitiu os dois como re-auditorias **delta** (`AUD-ABI-Δ`/`AUD-SEC-Δ`) cobrindo tudo que pousou depois da passada original do `core-v0.1.0` (alocador free-list, conversão float, fronteira `libcore.a`, SFNT/raster/hint). O catálogo da Camada 1 é `AUD-L1-*` (supply-chain, parsing de asset hostil, memory-safety, licença) mais dois itens sourced fora do catálogo formal de auditoria a partir do branch `chore/l1-quality`: `AUD-L1-QUALITY` (uma passada de qualidade de código, escopo equivalente a `tech-lead`) e `AUD-L1-GLSYM` (um achado de severidade `HIGH` da onda de teste `TST-L1-CONTRACT`, não de uma passada de auditoria do `security-engineer` — ver a linha própria abaixo pro porquê de não ser mapeado na escala 🔴/🟠/🟢). O racional completo do catálogo (por que estes cinco capítulos formais e não mais — rede/authN/cripto/PII estão explicitamente fora de escopo para uma lib de render desktop) vive no `AUDITORIAS.md`.

## Executive summary / Sumário executivo

**EN:** All 8 formal audit chapters closed to date have a **CONFORME** or **CONFORME-COM-RESSALVAS** verdict — none FAILED, none open without disposition. Zero 🔴 CRITICAL findings remain open anywhere in the formal dossier. Four 🟠 IMPORTANTE findings were found and closed within their own audit wave, all independently re-verified by execution (mutation testing / negative-proof redone by the reviewer, not trusted from the implementer's report) before the verdict was recorded: two in `AUD-L1-PARSE` (`LW-AUD`, 2026-07-16), and two new ones from **Onda 4's delta re-audit of Layer 0** (2026-07-19) — `AUD-ABI-Δ`'s PIE/ASLR cost of the `libcore.a` archive (resolved by an accepted-cost addendum to ADR-0009, not a code fix) and `AUD-SEC-Δ`'s `atof` fractional-mantissa overflow (resolved by a code fix that itself needed a second round — the first fix, `27efe3f`, introduced a leading-zero regression caught live by the adversarial reviewer; the corrected fix, `c789a7a`, passed a second adversarial review, 26 cases cross-checked against glibc, 0 divergence). Outside the formal 🔴/🟠/🟢 catalog scale, one `HIGH`-labelled finding surfaced from a test wave rather than an audit pass — `AUD-L1-GLSYM` (a real host-crashing symbol collision, reproduced live and fixed with an adversarially-reviewed mutation-testing pass) — see its own catalog row for why it is reported honestly rather than force-mapped onto the audit scale. A separate, non-security code-quality pass (`AUD-L1-QUALITY`, `tech-lead`-equivalent scope, outside `AUDITORIAS.md`'s security/compliance catalog by design) found 0 god-classes/dead-code and 2 IMPORTANTE-equivalent duplication findings, both fixed in the same wave. Five 🟢 COSMÉTICO findings remain from the original six chapters (unchanged, non-blocking), plus one new one from the Onda 4 delta (`syscall2`'s comment-clarity note, same class as the original report's #1). `AUD-L1-BUILD` (build-hardening gate) was dispensed by an autonomous CTO decision, folded into `AUD-L1-SUPPLY` as a packager-facing recommendation rather than a repo-side CI gate (rationale: `glintfx` ships as source, so imposing linker/compiler hardening flags unilaterally would be a correctness regression for some consumers, not a security improvement) — see that report's "AUD-L1-BUILD fold-in" section. **Readiness for an external auditor: ready, pending two go/no-go tag decisions reserved for the líder** (`core-v0.4.0` for Layer 0, `v0.11.2` for Layer 1) — neither is blocked by an open 🔴, both are blocked only by the líder's own merge/tag call. The `RELEASE_CHECKLIST.md` created in the `LW-AUD` pass gives the process to keep this state from drifting silently release to release.

**PT:** Os 8 capítulos formais de auditoria fechados até hoje têm veredito **CONFORME** ou **CONFORME-COM-RESSALVAS** — nenhum REPROVADO, nenhum aberto sem disposição. Zero achados 🔴 CRÍTICO seguem abertos em qualquer lugar do dossiê formal. Quatro achados 🟠 IMPORTANTE foram encontrados e fechados dentro da própria onda de auditoria, todos re-verificados de forma independente por execução (mutation testing / prova negativa refeita pelo revisor, não confiada no relato do implementer) antes do veredito ser registrado: dois no `AUD-L1-PARSE` (`LW-AUD`, 2026-07-16), e dois novos da **re-auditoria delta da Camada 0 na Onda 4** (2026-07-19) — o custo PIE/ASLR do archive `libcore.a` do `AUD-ABI-Δ` (resolvido por um adendo de custo-aceito na ADR-0009, não um fix de código) e o overflow de mantissa fracionária do `atof` do `AUD-SEC-Δ` (resolvido por um fix de código que precisou de uma 2ª rodada — o 1º fix, `27efe3f`, introduziu uma regressão de zeros-à-esquerda pega ao vivo pelo revisor adversarial; o fix corrigido, `c789a7a`, passou por um 2º review adversarial, 26 casos cruzados contra a glibc, 0 divergência). Fora da escala formal 🔴/🟠/🟢 do catálogo, um achado rotulado `HIGH` surgiu de uma onda de teste em vez de uma passada de auditoria — `AUD-L1-GLSYM` (uma colisão de símbolo real que crasha o host, reproduzida ao vivo e corrigida com uma passada de mutation testing revisada adversarialmente) — ver a linha própria do catálogo pro porquê de ser reportado com honestidade em vez de forçado na escala de auditoria. Uma passada separada, não-segurança, de qualidade de código (`AUD-L1-QUALITY`, escopo equivalente a `tech-lead`, fora do catálogo de segurança/compliance do `AUDITORIAS.md` por desenho) achou 0 god-class/dead-code e 2 achados equivalentes-a-IMPORTANTE de duplicação, ambos corrigidos na mesma onda. Cinco achados 🟢 COSMÉTICO permanecem dos seis capítulos originais (inalterados, não-bloqueantes), mais um novo do delta da Onda 4 (a nota de clareza de comentário da `syscall2`, mesma classe do achado #1 do relatório original). O `AUD-L1-BUILD` (gate de hardening de build) foi dispensado por decisão autônoma do CTO, dobrado no `AUD-L1-SUPPLY` como recomendação voltada a empacotadores em vez de gate de CI do próprio repo (racional: a `glintfx` é distribuída como fonte, então impor flags de hardening de linker/compilador unilateralmente seria uma regressão de correção para alguns consumidores, não uma melhoria de segurança) — ver a seção "AUD-L1-BUILD fold-in" daquele relatório. **Prontidão para auditor externo: pronto, pendente de duas decisões de tag go/no-go reservadas ao líder** (`core-v0.4.0` pra Camada 0, `v0.11.2` pra Camada 1) — nenhuma bloqueada por um 🔴 aberto, ambas bloqueadas só pela decisão de merge/tag do próprio líder. O `RELEASE_CHECKLIST.md` criado na passada `LW-AUD` dá o processo para este estado não desalinhar silenciosamente release a release.

## Findings by severity / Achados por severidade

| Severidade | Total | Abertos | Detalhe |
| :--- | :---: | :---: | :--- |
| 🔴 CRÍTICO | 0 | 0 | Nenhum em nenhum relatório do dossiê formal. (Fora da escala formal: `AUD-L1-GLSYM`, achado `HIGH` de onda de teste — não contado aqui, ver linha própria do catálogo e a nota do sumário executivo.) |
| 🟠 IMPORTANTE | 4 | 0 | 2 em `AUD-L1-PARSE` (font-path cap ausente + corpus test tautológico, `LW-AUD` 2026-07-16); fechados no mesmo ciclo (`16ab365`→`0f944e1`), re-verificados por execução independente. 2 novos da Onda 4 (2026-07-19, delta Camada 0): `AUD-ABI-Δ` (custo PIE/ASLR do archive `libcore.a`, resolvido por adendo à ADR-0009, `927738a`) e `AUD-SEC-Δ` (overflow de mantissa fracionária do `atof`, resolvido por fix + re-fix + 2º review adversarial, `27efe3f`→`c789a7a`). (Também fora da escala: 2 achados equivalentes-a-IMPORTANTE do `AUD-L1-QUALITY` — duplicação em `bootstrap.cpp`/decorators, fechados na mesma onda, `0f29879`/`d6ee6ba` — não contados aqui por serem de um catálogo de qualidade-de-código, não de segurança/compliance.) |
| 🟢 COSMÉTICO | 6 | 2 | `AUD-ABI` (1, comentário `syscall.asm:33`), `AUD-SEC` (3, trade-offs deliberados documentados), `AUD-L1-PARSE` (1, TOCTOU informativo, ver julgamento abaixo), `AUD-ABI-Δ` (1 novo, comentário `syscall.asm:36`/`syscall2`, mesma classe do #1 original). Os 2 nits de frescor "gl3w" (`AUD-L1-SUPPLY`/`AUD-L1-LICENSE`) e o doc-drift de contagem de teste do `nightly.yml` (`AUD-L1-MEM`) foram corrigidos na consolidação `LW-AUD` — não contam mais como abertos. |

## Catalog / Catálogo

### Layer 0 — `loucura_c_asm` (core-v0.4.0-candidate)

| ID | Report / Relatório | Date / Data | Auditor | Verdict / Veredito | Findings | Estado Auditado |
| :--- | :--- | :--- | :--- | :--- | :--- | :---: |
| `AUD-ABI` | [`AUD-ABI.md`](AUD-ABI.md) | 2026-07-09 | embedded-firmware-engineer | **CONFORME** | 0🔴 0🟠 1🟢 | ✓ |
| `AUD-SEC` | [`AUD-SEC.md`](AUD-SEC.md) | 2026-07-09 | security-engineer | **CONFORME** | 0🔴 0🟠 3🟢 | ✓ |
| `AUD-ABI-Δ` | [`AUD-ABI.md`](AUD-ABI.md#delta-re-audit-2026-07-19--re-auditoria-delta-2026-07-19) §"Delta re-audit 2026-07-19" | 2026-07-19 | embedded-firmware-engineer (mesmo auditor, continuidade de método) | **CONFORME-COM-RESSALVAS → resolvido** — 1 IMPORTANTE achado e resolvido (`927738a`, adendo ADR-0009) | 0🔴 1🟠 (resolvido) 1🟢 | ✓ |
| `AUD-SEC-Δ` | [`AUD-SEC.md`](AUD-SEC.md#delta-re-audit-2026-07-19--re-auditoria-delta-2026-07-19) §"Delta re-audit 2026-07-19" | 2026-07-19 | security-engineer | **CONFORME-COM-RESSALVAS → resolvido** — 1 IMPORTANTE achado e resolvido (`27efe3f`→`c789a7a`, 2º review adversarial aprovou) | 0🔴 1🟠 (resolvido) 0🟢 novo | ✓ |

**Nota sobre a Onda 4 (delta):** ambos os capítulos delta cobrem tudo que pousou na Camada 0 **depois** da auditoria original de 2026-07-09 (`core-v0.1.0`) e nunca tinha sido re-emitido no dossiê formal — o alocador free-list reescrito, `ftoa`/`atof`, `%f`, a fronteira `libcore.a` (ADR-0009), o parser SFNT (`kern`/bsearch), o rasterizador e o hinter (`src/hint.c`, auditado pela 1ª vez, gate `sanitize-hint` novo). Escopo completo: [`AUD-C0-PLAN.md`](AUD-C0-PLAN.md). Nenhum achado 🔴 CRÍTICO; os 2 🟠 foram encontrados E fechados dentro da própria onda (não ficaram abertos entre o relatório e esta consolidação). `REL-TAG core-v0.4.0` segue como decisão go/no-go do líder, não bloqueada por achado técnico aberto.

### Layer 1 — `glintfx` (v0.11.1, HEAD do branch `chore/l1-quality` pendente da tag `v0.11.2`)

| ID | Report / Relatório | Date / Data | Auditor | Verdict / Veredito | Findings | Estado Auditado |
| :--- | :--- | :--- | :--- | :--- | :--- | :---: |
| `AUD-L1-SUPPLY` | [`AUD-L1-SUPPLY.md`](AUD-L1-SUPPLY.md) | 2026-07-16 | security-engineer | **CONFORME** | 0🔴 0🟠 1🟢 (frescor "gl3w" — corrigido na consolidação `LW-AUD`) | ✓ |
| `AUD-L1-PARSE` | [`AUD-L1-PARSE.md`](AUD-L1-PARSE.md) | 2026-07-16 (re-review mesmo dia, `0f944e1`) | security-engineer | **CONFORME** — 2 IMPORTANTE fechados + re-verificados | 0🔴 0🟠 1🟢 (TOCTOU, informativo) | ✓ |
| `AUD-L1-MEM` | [`AUD-L1-MEM.md`](AUD-L1-MEM.md) | 2026-07-16 | security-engineer | **CONFORME** | 0🔴 0🟠 0🟢 | ✓ |
| `AUD-L1-LICENSE` | [`AUD-L1-LICENSE.md`](AUD-L1-LICENSE.md) | 2026-07-16 | compliance-legal | **CONFORME** | 0🔴 0🟠 1🟢 (frescor "gl3w" — corrigido na consolidação `LW-AUD`) | ✓ |
| `AUD-L1-BUILD` | dispensado — dobrado em `AUD-L1-SUPPLY` §"fold-in" | 2026-07-16 | Caetano/CTO (decisão autônoma) | **Resolvido por dobra** — recomendação para empacotadores, não gate de CI | n/a | ✓ (ver `TODO.md`) |
| `AUD-L1-QUALITY` | [`AUD-L1-QUALITY.md`](AUD-L1-QUALITY.md) | 2026-07-19 | Caetano/CTO (passada de qualidade, escopo `tech-lead` — fora do catálogo de segurança/compliance por desenho, ver `AUDITORIAS.md`) | **CONFORME COM RESSALVAS → resolvido** — 0 god-class, 0 dead-code, 2 duplicações reais achadas e fechadas (`0f29879`, `d6ee6ba`), 4 cosméticos | 0🔴 2 dup (resolvidas) 4 cosm. | ✓ |
| `AUD-L1-GLSYM` | sem relatório dedicado — achado + fix documentados em [`TODO.md`](../../TODO.md) (INBOX, "AUD-L1-GLSYM") + [ADR-0013](../adr/0013-gl-symbol-boundary.md) | 2026-07-19 | onda de teste `TST-L1-CONTRACT` (achado); review adversarial `qa-engineer` (fix) | **Achado e corrigido na mesma onda** — colisão de símbolo BSS (`glClearColor` etc., 344 nomes) com o `libGL` do host, SIGSEGV reproduzido ao vivo em consumidor externo; fix = prefixo `glx_` (`8ed7862`) | `HIGH` (fora da escala 🔴/🟠/🟢 — ver nota abaixo) | ✓ |

**Nota sobre `AUD-L1-GLSYM`:** este achado não veio de uma passada do catálogo formal `AUD-L1-*` (que já estava fechado desde a `LW-AUD`) — surgiu da onda de teste de contrato de consumo (`TST-L1-CONTRACT`, um binário host-style externo linkando `libglintfx.a` e chamando GL pelo nome cru), então foi rotulado `HIGH` pela equipe que o achou em vez de classificado na escala 🔴/🟠/🟢 do `AUDITORIAS.md`. Aplicando a definição de severidade do próprio manual ("CRÍTICO — corrompe memória/estado... ou é explorável"), este `internal-auditor` concorda que o achado teria sido 🔴 CRÍTICO se avaliado pelo catálogo formal (SIGSEGV real por ponteiro de função não-inicializado em BSS) — mas, como não passou pelo processo de auditoria formal (sem relatório com achados/método/veredito próprios), este índice registra o rótulo original (`HIGH`) em vez de reclassificar retroativamente sem esse processo. Encontrado E corrigido dentro da mesma sessão de trabalho, com review adversarial que reproduziu o crash pré-fix e confirmou limpo pós-fix (mutation testing, não apenas leitura) — não fica em aberto no dossiê. Pendente apenas a decisão de tag `v0.11.2` (go/no-go do líder).

## Judgment on the one residual COSMÉTICO / Julgamento sobre o único COSMÉTICO residual

**EN:** `AUD-L1-PARSE`'s TOCTOU note (the base RmlUi loader's fallback re-derives file size independently of the already-validated cap check) requires an adversarial filesystem/mount, not just an adversarial file, and is not a regression introduced by this audit's own fix — it is a pre-existing, narrow, upstream-code property outside this fix's realistic threat model (a static asset file). The re-review explicitly kept it as `✓` (aprovado), not `⚠`; this index concurs — it is informational, not a release blocker, and does not warrant downgrading `AUD-L1-PARSE`'s Estado Auditado.

**PT:** A nota de TOCTOU do `AUD-L1-PARSE` (o fallback do loader RmlUi da base re-deriva o tamanho de arquivo independentemente da checagem de teto já validada) exige um sistema de arquivos/mount adversarial, não só um arquivo adversarial, e não é uma regressão introduzida pelo próprio fix desta auditoria — é uma propriedade pré-existente, estreita, de código upstream fora do modelo de ameaça realista deste fix (um arquivo de asset estático). A re-review manteve explicitamente `✓` (aprovado), não `⚠`; este índice concorda — é informativo, não bloqueia release, e não justifica rebaixar o Estado Auditado do `AUD-L1-PARSE`.

## Cross-cutting findings fixed in this consolidation / Achados transversais corrigidos nesta consolidação

**EN:** Three of the individual reports independently flagged the same class of nit — planning-document prose (`TODO.md`/`AUDITORIAS.md`) lagging behind a real code change (gl3w removed in v0.8.0, replaced by glintfx's own clean-room GL loader) — each explicitly noting it was outside that deliverable's write scope. Fixed here as part of consolidation (see `TODO.md`/`AUDITORIAS.md` diffs, this branch): both catalog rows now describe the real dependency (glintfx's own `gl_loader.{h,c}`), not the removed third-party `gl3w`. A fourth, unrelated doc-drift (the `nightly.yml` workflow comment naming a stale test count, "18 GLFW=ON / 8 embed", noted inside `AUD-L1-MEM.md` itself as non-blocking) is left as-is — it is a comment inside a CI file, out of this pass's DOCS-only scope, and does not affect the gate's actual behaviour (`ctest` runs whatever is registered, not what a comment says).

**PT:** Três dos relatórios individuais sinalizaram independentemente a mesma classe de nit — prosa de documento de planejamento (`TODO.md`/`AUDITORIAS.md`) atrasada em relação a uma mudança real de código (gl3w removido na v0.8.0, substituído pelo loader GL clean-room próprio da glintfx) — cada um notando explicitamente que estava fora do escopo de escrita daquele entregável. Corrigido aqui como parte da consolidação (ver diffs de `TODO.md`/`AUDITORIAS.md`, este branch): as duas linhas de catálogo agora descrevem a dependência real (`gl_loader.{h,c}` próprio da glintfx), não o `gl3w` de terceiro removido. Um quarto doc-drift, não relacionado (o comentário do workflow `nightly.yml` citando uma contagem de teste desatualizada, "18 GLFW=ON / 8 embed", anotado dentro do próprio `AUD-L1-MEM.md` como não-bloqueante) foi deixado como está — é um comentário dentro de um arquivo de CI, fora do escopo só-DOCS desta passada, e não afeta o comportamento real do gate (`ctest` roda o que estiver registrado, não o que um comentário diz).

## What changed since the last consolidation / O que mudou desde a última consolidação

**EN:** This is the dossier's first master index — the six individual reports above (`AUD-ABI`/`AUD-SEC` from `core-v0.1.0`'s W12 wave, `AUD-L1-*` from the `LW-AUD` wave on `v0.11.0`) previously existed without a unifying index. Also new in this pass: `-Wall -Wextra` applied to the `glintfx` target (commit `45209e5`, zero warnings on our own 17 authored sources; RmlUi backend files and the 26 test + 2 demo targets deliberately left uninstrumented — see the rationale comment at `glintfx/CMakeLists.txt:518-540`) and `docs/RELEASE_CHECKLIST.md` (see that file). An INBOX seed for a future `AUD-L1-QUALITY` (god-class/complexity/dead-code review, e.g. `font_engine_own.cpp` at 1727 lines) was registered in `TODO.md` — not yet an audit, a pull candidate before v1.0.

**PT:** Este é o primeiro índice mestre do dossiê — os seis relatórios individuais acima (`AUD-ABI`/`AUD-SEC` da onda W12 do `core-v0.1.0`, `AUD-L1-*` da onda `LW-AUD` na `v0.11.0`) existiam antes sem um índice unificador. Também novo nesta passada: `-Wall -Wextra` aplicado ao alvo `glintfx` (commit `45209e5`, zero warnings nos nossos 17 sources autorais; arquivos de backend do RmlUi e os 26 alvos de teste + 2 de demo deliberadamente deixados sem instrumentar — ver o comentário de racional em `glintfx/CMakeLists.txt:518-540`) e o `docs/RELEASE_CHECKLIST.md` (ver esse arquivo). Uma semente de INBOX para um futuro `AUD-L1-QUALITY` (revisão de god-class/complexidade/dead-code, ex.: `font_engine_own.cpp` com 1727 linhas) foi registrada no `TODO.md` — ainda não uma auditoria, candidata a pull antes da v1.0.

## Onda 4 (2026-07-19) — Layer 0 delta re-audit + Layer 1 quality pass / re-auditoria delta da Camada 0 + passada de qualidade da Camada 1

**EN:** Branch `chore/l1-quality` (local, not pushed — 15+ commits over `main`'s `f461703`). Two independent tracks landed and are consolidated together here per the orchestrator's brief:

- **Layer 0 delta re-audit** (`AUD-C0-PLAN.md`, planned by Caetano/CTO, executed by `embedded-firmware-engineer` + `security-engineer`): the dossier's `AUD-ABI`/`AUD-SEC` chapters, dated 2026-07-09 (`core-v0.1.0`), had never been re-issued despite the Layer-0 surface roughly doubling since (free-list allocator rewrite, `ftoa`/`atof`, `%f`, the `libcore.a` boundary from ADR-0009, SFNT `kern`/bsearch, and `src/hint.c` — the last never audited at all, no sanitizer target existed). Onda 4 closes that gap with `AUD-ABI-Δ`/`AUD-SEC-Δ` (both **CONFORME-COM-RESSALVAS**, one 🟠 each, both found-and-resolved within the wave — see catalog rows above) plus a new `tests/sanitize_hint.c` gate (closes the last "hosted-companion sanitizer" gap among the SFNT/raster/hint trio).
- **Layer 1 quality pass** (`AUD-L1-QUALITY`, Caetano/CTO plan + `backend-engineer`/`engine-graphics-programmer` implementation): the INBOX seed raised by the `LW-AUD` consolidation (2026-07-16) was pulled. Verdict: the three headline-sized files (`font_engine_own.cpp`/`bootstrap.cpp`/`render_gl3.cpp`) are **not** god-classes (55–74% of their bulk is the repo's own mandated bilingual doc-comments); zero dead code; two genuine duplication pockets found and fixed (`0f29879`, `d6ee6ba`). Alongside it, two lower-stakes build-hygiene items pulled from the same `LW-AUD` INBOX were closed: `.clang-format` + a gate in `preci.sh` (`95a09bc`/`1f8c831`) and `-Wall -Wextra` extended to the 26 authored test targets (`86a4039`) — both marked resolved in `TODO.md`'s INBOX (struck through) as part of this consolidation.
- **`AUD-L1-GLSYM`** (test-wave finding, not a formal audit — see its own catalog row and note above): a real SIGSEGV in a host consumer, root-caused to 344 GL loader symbols exported as raw-named BSS globals colliding with the host's own `libGL`. Fixed with a `glx_` prefix (`8ed7862`) and [ADR-0013](../adr/0013-gl-symbol-boundary.md), reviewed adversarially with mutation testing that reproduced the crash pre-fix.
- **`AUD-C0-PICVAR`** (new INBOX seed, from `AUD-ABI-Δ`'s finding + the ADR-0009 addendum): a `-fpic` variant of `libcore.a` for PIE-requiring hosted consumers, deliberately deferred — no real consumer requires PIE today. See `TODO.md` INBOX.

**Reconciliation performed in this consolidation:** `TODO.md`'s INBOX seeds consumed by this wave (`AUD-L1-QUALITY`, `clang-format` no `preci.sh`, `-Wall -Wextra` nos alvos de teste) struck through with their closing commits; `AUD-C0-PICVAR` added as a new seed; nothing promoted to `✅` (per the freshness convention, `✅` is reserved for post-tag — both `core-v0.4.0` and `v0.11.2` remain the líder's own go/no-go, not yet exercised as of this consolidation).

**PT:** Branch `chore/l1-quality` (local, sem push — 15+ commits sobre o `f461703` da `main`). Duas trilhas independentes pousaram e são consolidadas juntas aqui conforme o brief do orquestrador:

- **Re-auditoria delta da Camada 0** (`AUD-C0-PLAN.md`, planejada por Caetano/CTO, executada por `embedded-firmware-engineer` + `security-engineer`): os capítulos `AUD-ABI`/`AUD-SEC` do dossiê, datados de 2026-07-09 (`core-v0.1.0`), nunca tinham sido re-emitidos apesar da superfície da Camada 0 praticamente dobrar desde então (reescrita do alocador free-list, `ftoa`/`atof`, `%f`, a fronteira `libcore.a` da ADR-0009, `kern`/bsearch do SFNT, e `src/hint.c` — este último nunca auditado, nenhum alvo sanitizer existia). A Onda 4 fecha essa lacuna com `AUD-ABI-Δ`/`AUD-SEC-Δ` (ambos **CONFORME-COM-RESSALVAS**, um 🟠 cada, ambos achados-e-resolvidos dentro da onda — ver linhas de catálogo acima) mais um gate novo `tests/sanitize_hint.c` (fecha a última lacuna de "sanitizer hosted-companion" entre o trio SFNT/raster/hint).
- **Passada de qualidade da Camada 1** (`AUD-L1-QUALITY`, plano do Caetano/CTO + implementação `backend-engineer`/`engine-graphics-programmer`): a semente de INBOX levantada na consolidação `LW-AUD` (2026-07-16) foi puxada. Veredito: os três arquivos-manchete (`font_engine_own.cpp`/`bootstrap.cpp`/`render_gl3.cpp`) **não** são god-classes (55–74% do volume deles é a documentação bilíngue obrigatória do próprio repo); zero dead code; dois bolsões genuínos de duplicação achados e corrigidos (`0f29879`, `d6ee6ba`). Junto, dois itens de menor porte de higiene de build puxados da mesma INBOX `LW-AUD` foram fechados: `.clang-format` + gate no `preci.sh` (`95a09bc`/`1f8c831`) e `-Wall -Wextra` estendido aos 26 alvos de teste autorais (`86a4039`) — ambos marcados resolvidos na INBOX do `TODO.md` (riscados) como parte desta consolidação.
- **`AUD-L1-GLSYM`** (achado de onda de teste, não uma auditoria formal — ver a linha própria do catálogo e a nota acima): um SIGSEGV real num consumidor host, causa-raiz em 344 símbolos do loader GL exportados como globais BSS de nome cru colidindo com o `libGL` próprio do host. Corrigido com prefixo `glx_` (`8ed7862`) e [ADR-0013](../adr/0013-gl-symbol-boundary.md), revisado adversarialmente com mutation testing que reproduziu o crash pré-fix.
- **`AUD-C0-PICVAR`** (semente nova de INBOX, do achado do `AUD-ABI-Δ` + o adendo da ADR-0009): uma variante `-fpic` da `libcore.a` pra consumidores hosted que exigem PIE, deliberadamente deferida — nenhum consumidor real exige PIE hoje. Ver INBOX do `TODO.md`.

**Reconciliação feita nesta consolidação:** sementes de INBOX do `TODO.md` consumidas por esta onda (`AUD-L1-QUALITY`, `clang-format` no `preci.sh`, `-Wall -Wextra` nos alvos de teste) riscadas com seus commits de fechamento; `AUD-C0-PICVAR` acrescentada como semente nova; nada promovido a `✅` (conforme a convenção de frescor, `✅` é reservado pra pós-tag — tanto `core-v0.4.0` quanto `v0.11.2` seguem como go/no-go do próprio líder, ainda não exercidos nesta consolidação).
