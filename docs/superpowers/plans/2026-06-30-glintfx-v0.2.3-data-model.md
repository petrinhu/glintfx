# glintfx v0.2.3 — data-model (data-binding) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Expor o **data-binding do RmlUi** por uma API pública limpa em `glintfx::UiLayer` e `glintfx::App` (sem vazar tipos RmlUi), para o consumidor (GusWorld/cockpit) ligar valores vivos: HP/AP/Mana (números), nome do ator / verbo / alvo (strings), estado (bool) e o **log de combate** (lista de strings). Funciona nos dois modos (embed + standalone), via `Engine`/`Bootstrap`. **v0.2.3**, aditivo: sem impacto quando o consumidor não usa.

**Architecture:** O RmlUi liga variáveis por **ponteiro** (`DataModelConstructor::Bind(name, T*)`) e o ponteiro precisa **sobreviver** ao modelo; tudo deve ser registrado **antes** do `LoadDocument` (o documento referencia `data-model="hud"` + `{{var}}` / `data-for`). Como a fachada é key-value (o consumidor passa valores por chave, não ponteiros), o glintfx **possui** o armazenamento estável de cada chave (uma célula por chave, endereço estável) e faz `Bind` uma vez; em `set_*` muta a célula e marca a variável suja (`DataModelHandle::DirtyVariable`), refletida no próximo `update()`. Um `DataBinder` interno encapsula isso, dono pelo `Engine`. Nenhum tipo RmlUi aparece nos headers públicos.

**Tech Stack:** C++ (CMake ≥3.16), RmlUi @`2cd28864ae25ed345b70598751703a5433b12356`, gl3w vendorizado, OpenGL 3.3, FreeType. Testes sob Xvfb.

**Achados de verificação (fonte pinado `glintfx/build/_deps/rmlui-src/Include/RmlUi/Core/`):**
- `Context::CreateDataModel(name, type_register=nullptr, allow_missing_variables=false)` → `DataModelConstructor` (`Context.h:261`); `GetDataModel`/`RemoveDataModel` (`:266,:275`).
- `DataModelConstructor::Bind<T>(name, T* ptr)` liga uma variável a um **ponteiro** (`DataModelHandle.h:41-46`); escalares **arithmetic + String são builtin** (não precisam de `RegisterScalar` — `DataModelHandle.h:119-120`). `bool`/`int`/`double`/`Rml::String` ligam direto.
- Listas: `RegisterArray<Container>()` (`DataModelHandle.h:90-91`) registra `std::vector<T>`; depois `Bind(name, &vector)`. O `value_type` (aqui `Rml::String`) já é builtin.
- Atualização: `DataModelConstructor::GetModelHandle()` → `DataModelHandle`; `DirtyVariable(name)`/`DirtyAllVariables()` (`DataModelHandle.h:18-20`); refletido no próximo `Context::Update()`.
- **Ciclo de vida:** `CreateDataModel` + todos os `Bind` **antes** de `LoadDocument`. `Rml::String` == `std::string`.

**Referências:** `glintfx/src/{engine,bootstrap,ui_layer,app}.cpp`, headers públicos, `docs/embed-integration.md`, `docs/superpowers/specs/2026-06-30-glintfx-v2-design.md`.

## Global Constraints

(Cada task herda implicitamente esta seção.)

- **Plataforma:** Linux x86-64. **C++:** alvo `cxx_std_23`, piso C++17.
- **Licença:** `SPDX-License-Identifier: MPL-2.0` no topo de todo arquivo novo. © 2026 Petrus Silva Costa.
- **Idioma:** identificadores en-intl; comentários/doc-comments bilíngues (en, depois pt).
- **Encapsulamento (gate por grep):** nenhum tipo de RmlUi/GLFW/GL/gl3w em `include/glintfx/*.hpp`. A API de data-model usa só `const char*`, `double`, `bool`, `size_t`.
- **Não-regressão:** os **13 testes** atuais seguem **verdes** sem mudar expectativa.
- **Zero impacto no default:** o data-model só é criado se o consumidor chamar `create_data_model`. Sem isso, `Engine`/`Bootstrap` rodam exatamente como hoje.
- **Disponível nos dois modos:** `UiLayer` (embed) e `App` (standalone) expõem a MESMA API, ambas encaminhando ao `Engine`.
- **Testes de GL:** sob `xvfb-run` via `tests/run_xvfb.cmake`.

---

## API escolhida (decisão de porta-de-mão-única)

Superfície pública (neutra), em `UiLayer` e `App`:

```cpp
// --- declaração (chamar ANTES de load) -----------------------------------
bool create_data_model(const char* name);                 // cria o modelo no contexto
bool bind_number(const char* key, double initial = 0.0);  // liga uma célula numérica
bool bind_string(const char* key, const char* initial = "");
bool bind_bool  (const char* key, bool initial = false);
bool bind_list  (const char* key);                        // lista de strings (log)

// --- atualização (chamar DEPOIS de load) ---------------------------------
void set_number(const char* key, double value);
void set_string(const char* key, const char* value);
void set_bool  (const char* key, bool value);
void set_list  (const char* key, const char* const* items, size_t count); // substitui a lista inteira
```

**Trade-offs pesados e por que esta forma:**
- **Key-value (escolhida) vs struct (`RegisterStruct` + Bind de um struct do host):** struct exige o consumidor definir um tipo C++, mantê-lo vivo, e **vazaria** o tipo na borda; key-value mantém o header livre de tipos RmlUi e dá uma superfície uniforme e neutra de linguagem (bom para um futuro binding C). Custo: bookkeeping interno (um registro de células), escondido no `Impl`. **Vale.**
- **Declarar-antes-do-load (escolhida) vs dinâmico (bind a qualquer hora):** o RmlUi **exige** `Bind` antes de `LoadDocument` (as data-views do documento são compiladas no load contra o modelo existente); ligar variável depois do load **não** religa as views já compiladas. Logo o "dinâmico pós-load" briga com o motor e seria frágil. A ergonomia é preservada por uma ordem clara (`create_data_model` → `bind_*` → `load` → `set_*`), validada em runtime (declarar após `load` retorna `false`).
- **Setters tipados explícitos (`set_number/string/bool`) vs `set_data(...)` sobrecarregado:** sobrecarga `set_data(key,double)` vs `set_data(key,bool)` tem **ambiguidade** com literais inteiros (`set_data(key,0)`); nomes explícitos eliminam o footgun e ficam C-ABI-friendly. **Escolhidos.**
- **`set_list` substitui a lista inteira (escolhida) vs push/clear:** substituição é atômica, sem estado parcial; para um log o host mantém seu próprio ring buffer e empurra as N linhas correntes por frame. Menos métodos, mais robusto.
- **`UiLayerConfig.data_model` (rejeitado):** ordenar via campo de config esconde o constraint "antes do load"; uma chamada explícita `create_data_model` torna a ordem óbvia. Config fica mínimo.

---

## File Structure

```
glintfx/
  CMakeLists.txt                       # + src/data_binder.cpp na lib
  include/glintfx/
    ui_layer.hpp                       # + API de data-model (neutra)
    app.hpp                            # + API de data-model (neutra)
  src/
    data_binder.hpp / data_binder.cpp  [NOVO]  # células estáveis + DataModelConstructor/Handle
    engine.hpp / engine.cpp            # + create_data_model/bind_*/set_* → DataBinder
    bootstrap.hpp / bootstrap.cpp      # expõe context() (já existe) p/ o DataBinder
    ui_layer.cpp / app.cpp             # encaminham a API ao Engine
  tests/
    data_model_smoke.cpp   [NOVO]      # T1: create+bind+load+render sem crash (UiLayer e App)
    data_model_scalar.cpp  [NOVO]      # T2: set_number/string muda o render (2 snapshots diferem)
    data_model_list.cpp    [NOVO]      # T3: bind_list + set_list (data-for) muda o render
    ui/hud.rml, ui/log.rml [NOVO]      # docs de teste com {{var}} e data-for
    CMakeLists.txt                     # registra os novos testes
docs/
  embed-integration.md                 # T4: nova seção "Data model"
CHANGELOG.md                           # T4: entrada v0.2.3
```

---

### Task 1: API de data-model + ciclo de vida (`DataBinder` no Engine; fachadas UiLayer/App)

**Files:**
- Create: `glintfx/src/data_binder.hpp`, `glintfx/src/data_binder.cpp`, `glintfx/tests/data_model_smoke.cpp`, `glintfx/tests/ui/hud.rml`
- Modify: `glintfx/src/engine.hpp/.cpp`, `glintfx/include/glintfx/ui_layer.hpp`, `glintfx/include/glintfx/app.hpp`, `glintfx/src/ui_layer.cpp`, `glintfx/src/app.cpp`, `glintfx/CMakeLists.txt`, `glintfx/tests/CMakeLists.txt`

**Interfaces:**
- `glintfx::DataBinder` (interno): `bool create(Rml::Context*, const char* name)`; `bool bind_number/bind_string/bind_bool/bind_list(...)`; `void set_number/set_string/set_bool/set_list(...)`. Possui as células estáveis + o `DataModelConstructor` + `DataModelHandle`.
- `Engine`: encaminha as 9 chamadas ao `DataBinder` (usando `boot.context()`), com guards de ordem (`create`/`bind` só antes de `load`; tracking de `loaded_`).
- `UiLayer`/`App`: os 9 métodos públicos neutros encaminham ao `Engine`.

- [ ] **Step 1: `DataBinder` (células estáveis + binding)**

`data_binder.hpp` (forward-decls; sem vazar nada — é header interno):
```cpp
// SPDX-License-Identifier: MPL-2.0
#pragma once
namespace Rml { class Context; }
#include <cstddef>
namespace glintfx {
class DataBinder {
public:
  DataBinder(); ~DataBinder();
  DataBinder(const DataBinder&) = delete; DataBinder& operator=(const DataBinder&) = delete;
  bool create(Rml::Context* ctx, const char* name);   // CreateDataModel + RegisterArray<vector<String>>
  bool bind_number(const char* key, double initial);
  bool bind_string(const char* key, const char* initial);
  bool bind_bool  (const char* key, bool initial);
  bool bind_list  (const char* key);
  void set_number(const char* key, double v);
  void set_string(const char* key, const char* v);
  void set_bool  (const char* key, bool v);
  void set_list  (const char* key, const char* const* items, std::size_t n);
private:
  struct Impl; Impl* impl_;   // mapas de unique_ptr<double/string/bool/vector<string>> + ctor/handle
};
}
```
`data_binder.cpp` (inclui `<RmlUi/Core.h>`): `Impl` guarda `DataModelConstructor ctor; DataModelHandle handle; bool created=false;` + `std::map<std::string, std::unique_ptr<double>> nums; ...strings(Rml::String); ...bools; ...lists(std::vector<Rml::String>)`.
- `create`: `impl_->ctor = ctx->CreateDataModel(name, nullptr, /*allow_missing=*/true); if(!ctor) return false; impl_->ctor.RegisterArray<std::vector<Rml::String>>(); impl_->handle = ctor.GetModelHandle(); created=true;`
- `bind_number(k,init)`: cria célula `auto& cell = nums[k] = make_unique<double>(init); ctor.Bind(k, cell.get());` (idem string/bool). `bind_list(k)`: `lists[k]=make_unique<vector<String>>(); ctor.Bind(k, lists[k].get());`
- `set_number(k,v)`: `auto it=nums.find(k); if(it==end) return; *it->second=v; handle.DirtyVariable(k);` (idem). `set_list(k,items,n)`: reconstrói o vector + `DirtyVariable(k)`.

- [ ] **Step 2: Engine encaminha (com guards de ordem) + fachadas**

`engine.hpp/.cpp`: membro `DataBinder data_; bool loaded_=false;`. `create_data_model(name)` = `if(loaded_||!ok_)return false; return data_.create(boot.context(), name);`. `bind_*` = `if(loaded_)return false; return data_.bind_*(...);`. `load()` existente passa a setar `loaded_=true`. `set_*` = encaminha a `data_.set_*` (no-op se chave desconhecida). `UiLayer`/`App`: adicionar os 9 métodos públicos (doc-comments bilíngues; ordem documentada: create → bind → load → set) encaminhando ao `Engine` com guard `ok()`.

- [ ] **Step 3: CMake + doc de teste + teste smoke (falha primeiro)**

`tests/ui/hud.rml`: `<body data-model="hud"> <h1 id="name">{{name}}</h1> <p id="hp">HP {{hp}}</p> </body>` (linka um rcss mínimo com fundo escuro).
`tests/data_model_smoke.cpp`: para cada modo (App; depois — processo/escopo separado — UiLayer com janela oculta + FBO, regra N3): `create_data_model("hud")`; `bind_string("name","Gus"); bind_number("hp",10);`; `load("tests/ui/hud.rml")`; `update(); render()` sem crash; imprime `data_model_smoke OK`.
CMake: add `src/data_binder.cpp` à lib; registrar o teste (run_xvfb).

- [ ] **Step 4: Build + rodar + gate de header + commit**
Run: `cd glintfx && cmake -S . -B build -DGLINTFX_BUILD_DEMOS=OFF && cmake --build build -j && ctest --test-dir build -R data_model_smoke --output-on-failure`
Expected: `data_model_smoke OK`; e os 13 testes seguem verdes (`ctest --test-dir build`).
Run: `! grep -rqE 'Rml|GLFW|GL_|gl3w' glintfx/include/glintfx/ && echo OK`
```bash
git add glintfx/ && git commit -m "feat(glintfx): API de data-model + ciclo de vida (create/bind/set) (v0.2.3 T1)"
```

---

### Task 2: Escalares vivos — `set_number`/`set_string` refletem no render

**Files:**
- Create: `glintfx/tests/data_model_scalar.cpp`
- Modify: `glintfx/tests/CMakeLists.txt` (+ rcss de teste se preciso)

- [ ] **Step 1: Teste (falha primeiro) — prova de propagação**

`data_model_scalar.cpp` (modo App, 320×160): `create_data_model("hud"); bind_string("label","");` `load` de um doc que mostra `{{label}}` como texto grande claro em fundo escuro; **snapshot A** (label vazio); `set_string("label","HELLO HP 99")`; `update(); snapshot B`. Asserção: B tem **mais pixels claros** que A (o texto apareceu) — estatística agregada determinística (estilo `render_sanity`, tolerante ao llvmpipe). Repetir com `bind_number("hp",0)` + `set_number("hp",99)` num `{{hp}}` para cobrir o caminho numérico. Imprime `data_model_scalar: PASS`.
> Prova que `set_*` + `update()` propaga a mudança ao DOM e ao render (não só "não crasha").

- [ ] **Step 2: Build + rodar + commit**
Run: `cd glintfx && cmake --build build -j && ctest --test-dir build -R data_model_scalar --output-on-failure`
Expected: `data_model_scalar: PASS`.
```bash
git add glintfx/ && git commit -m "test(glintfx): prova de propagação de escalares vivos no render (v0.2.3 T2)"
```

---

### Task 3: Lista viva — `bind_list`/`set_list` para o log (`data-for`)

**Files:**
- Create: `glintfx/tests/data_model_list.cpp`, `glintfx/tests/ui/log.rml`
- Modify: `glintfx/tests/CMakeLists.txt`

- [ ] **Step 1: Doc com `data-for` + teste (falha primeiro)**

`tests/ui/log.rml`: `<body data-model="hud"> <div data-for="line : log"><p>{{line}}</p></div> </body>` (fundo escuro, texto claro).
`data_model_list.cpp` (App, 400×300): `create_data_model("hud"); bind_list("log");` `load`; **snapshot A** (lista vazia); `const char* items[] = {"linha 1","linha 2","linha 3"}; set_list("log", items, 3);` `update(); snapshot B`. Asserção: B tem mais pixels claros que A (as 3 linhas renderizaram). Opcional: `set_list("log", items, 1)` reduz o brilho vs 3 (encolheu). Imprime `data_model_list: PASS`.
> Confirmar a sintaxe exata de `data-for` no fonte pinado (`Source/Core/DataView*` / docs) e ajustar se divergir; o `RegisterArray<std::vector<Rml::String>>()` do T1 habilita a lista.

- [ ] **Step 2: Build + rodar + commit**
Run: `cd glintfx && cmake --build build -j && ctest --test-dir build -R data_model_list --output-on-failure`
Expected: `data_model_list: PASS`.
```bash
git add glintfx/ && git commit -m "feat(glintfx): lista viva (bind_list/set_list) p/ o log via data-for (v0.2.3 T3)"
```

---

### Task 4: Docs (`embed-integration.md` + CHANGELOG) + gates finais

**Files:**
- Modify: `docs/embed-integration.md` (nova seção "Data model"), `CHANGELOG.md`, este plano (checkboxes)

- [ ] **Step 1: Seção "Data model" no `docs/embed-integration.md` (bilíngue en→pt, sem em-dash)**

Documentar: a ordem obrigatória (`create_data_model` → `bind_*` → `load` → `set_*`); que `bind_*` após `load` falha (e por quê: as views são compiladas no load); a superfície (9 métodos); o exemplo do cockpit (HP número, nome/verbo/alvo string, log lista); que vale nos dois modos; e o ponteiro-vive-no-glintfx (o consumidor não gerencia memória das células). Cross-link ao spec v2 (§ cockpit) e ao `ui_layer.hpp`.

- [ ] **Step 2: CHANGELOG `v0.2.3`**
Entrada: "Added — data-model binding (`create_data_model`/`bind_*`/`set_*`) em `UiLayer` e `App`; lista para logs via `data-for`; doc em `embed-integration.md`." Aditivo; sem breaking change.

- [ ] **Step 3: Build do zero + suíte completa + gates**
Run: `cd glintfx && rm -rf build && cmake -S . -B build -DGLINTFX_BUILD_DEMOS=ON && cmake --build build -j && ctest --test-dir build --output-on-failure`
Expected: **13** testes anteriores + **3** novos (`data_model_smoke`, `data_model_scalar`, `data_model_list`) verdes.
Run: `! grep -rqE 'Rml|GLFW|GL_|gl3w' glintfx/include/glintfx/ && echo "headers limpos"`
Run: SPDX em todo arquivo novo (`head -1 ... | grep SPDX`).

- [ ] **Step 4: Commit + fechar plano**
```bash
git add glintfx/ docs/ CHANGELOG.md && git commit -m "docs(glintfx): documenta data-model + CHANGELOG v0.2.3 (v0.2.3 T4)"
```
Marcar checkboxes. Próximo (fora deste plano): tag `v0.2.3` (decisão do líder) e adoção no GusWorld (cockpit liga HP/verbo/log vivos via `UiLayer`).

---

## Notas de execução

- **Ciclo de vida é o risco central:** `create_data_model` + `bind_*` ANTES de `load`; `set_*` DEPOIS. O `Engine` valida a ordem (retorna false fora de ordem). O constraint vem do RmlUi (views compiladas no load), não é escolha nossa.
- **Ponteiro estável:** as células do `DataBinder` (`unique_ptr` por chave) garantem endereço estável durante a vida do modelo; nunca usar realocação que invalide os ponteiros já passados a `Bind`. `std::map` (não `vector`) preserva endereço dos nós.
- **RmlUi global (N3):** `App` e `UiLayer` não vivos ao mesmo tempo no mesmo processo (relevante no `data_model_smoke`).
- **Zero impacto no default:** sem `create_data_model`, nada de data-model é criado; os 13 testes não tocam o caminho novo.
- **A confirmar no fonte pinado:** sintaxe de `{{var}}` em texto e `data-for="x : list"` (`Source/Core/DataView*`); `CreateDataModel(..., allow_missing_variables=true)` para tolerar `{{var}}` ainda não setada sem erro de log.
