{
  "version": 3,
  "cmakeMinimumRequired": { "major": 3, "minor": 16, "patch": 0 },
  "configurePresets": [
    {
      "name": "full",
      "displayName": "full -- window + app + ui/fx + text + image (ADR-0015 phase A0)",
      "description": "Default composition: every module glintfx has today, including the standalone GLFW-backed App facade. Equivalent to the plain 'cmake -S glintfx -B <dir>' invocation this repo's CI already uses (GLINTFX_BACKEND_GLFW defaults ON) -- this preset just names it. ADR-0015 (d)(5) 'named compositions'.",
      "binaryDir": "${sourceDir}/build-full",
      "cacheVariables": {
        "CMAKE_C_COMPILER": "clang",
        "CMAKE_CXX_COMPILER": "clang++",
        "GLINTFX_MODULE_WINDOW": "ON",
        "GLINTFX_MODULE_APP": "ON",
        "GLINTFX_BUILD_TESTS": "ON"
      }
    },
    {
      "name": "embed-only",
      "displayName": "embed-only -- no window/app module (ADR-0015 phase A0)",
      "description": "GLINTFX_MODULE_WINDOW=OFF: no GLFW dependency in the link line, no standalone App facade compiled. This is the composition GusWorld (the only real consumer today) actually ships -- Engine + UiLayer + RenderGl3 + Bootstrap + SystemClock (+ the fx decorators, still fused with ui per ADR-0015 (c)), embedded into a host that already owns the window/GL context. Equivalent to this repo's existing '-DGLINTFX_BACKEND_GLFW=OFF' CI leg -- this preset just names it under the new ADR-0015 flag. ADR-0015 (d)(5) 'named compositions'.",
      "binaryDir": "${sourceDir}/build-embed-only",
      "cacheVariables": {
        "CMAKE_C_COMPILER": "clang",
        "CMAKE_CXX_COMPILER": "clang++",
        "GLINTFX_MODULE_WINDOW": "OFF",
        "GLINTFX_BUILD_TESTS": "ON"
      }
    },
    {
      "name": "lean-ui",
      "displayName": "lean-ui -- ui without glintfx-authored fx (FX-CARVE-1, ADR-0015 (c)/(d)(5))",
      "description": "GLINTFX_MODULE_FX=OFF: the glintfx-tint/glintfx-ripple GLSL programs, their CompileShader/RenderShader/ReleaseShader dispatch, the backdrop-capture machinery, and the 3 decorator TUs (polygon/image-tint/ripple) are compiled out for real -- nm proof: zero 'Ripple'/'ImageTint'/'PolygonDecorator'/'Gl3FxHook' symbol matches in libglintfx.a. Honest boundary (declared, not disguised): RmlUi's OWN upstream filter/gradient shaders (glow/box-shadow/drop-shadow/blur/backdrop-blur/mask/gradients, Backends/RmlUi_Renderer_GL3.cpp) are NOT removed by this preset -- see GLINTFX_MODULE_FX's own doc string in CMakeLists.txt for why, and the low-ROI FX-CARVE-2 that would be needed to go further. This is the composition ADR-0015 (c)/(d)(5) exists to serve: the leanest ui-only build available today.",
      "binaryDir": "${sourceDir}/build-lean-ui",
      "cacheVariables": {
        "CMAKE_C_COMPILER": "clang",
        "CMAKE_CXX_COMPILER": "clang++",
        "GLINTFX_MODULE_FX": "OFF",
        "GLINTFX_BUILD_TESTS": "ON"
      }
    }
  ]
}
