# SPDX-License-Identifier: MPL-2.0
#
# EN: UndefinedBehaviorSanitizer suppression file for the TST-L1-MEM nightly
#     gate (GLINTFX_SANITIZE=ON). Consumed via UBSAN_OPTIONS=suppressions=<this
#     file>, injected for every test in glintfx/tests/CMakeLists.txt.
#
#     Scope rule (non-negotiable, same as lsan_suppressions.txt): every entry
#     here MUST be a documented, tracked, upstream finding — never a blanket
#     silence of a whole check category, and never anything that could also
#     match glintfx-authored code.
#
# PT: Arquivo de suppression do UndefinedBehaviorSanitizer para o gate noturno
#     TST-L1-MEM (GLINTFX_SANITIZE=ON). Consumido via UBSAN_OPTIONS=
#     suppressions=<este arquivo>, injetado para todo teste em
#     glintfx/tests/CMakeLists.txt.
#
#     Regra de escopo (não-negociável, igual a lsan_suppressions.txt): toda
#     entrada aqui DEVE ser um achado documentado e rastreado — nunca um
#     silenciamento genérico de categoria inteira de check, e nunca algo que
#     também possa casar com código autoral da glintfx.
#
# EN: 2026-07-10 — this file is currently EMPTY of entries (below this header). The 3
#     teardown-order findings previously listed here (`vptr:ElementDocument`,
#     `vptr:*WidgetScroll.cpp*`, `vptr:*RemoveEventListener*`) are now fixed at the root
#     cause by a tracked, explicit source patch applied to the fetched RmlUi via
#     PATCH_COMMAND — see glintfx/patches/rmlui-2cd28864-teardown-ub.patch and
#     glintfx/patches/README.md for the full writeup, and glintfx/CMakeLists.txt's
#     FetchContent_Declare(RmlUi ...) block for the patch application mechanism. They are
#     NOT suppressed anymore, they are corrected. The discipline this file exists to enforce
#     (every entry documented, tracked, scoped) is unchanged even at zero entries — any NEW
#     vptr/UBSan finding must still be gated through this same process (fix-at-root preferred
#     over suppression; if suppression is genuinely the only option, it must be as narrowly
#     scoped and as thoroughly documented as the 3 removed entries were, see git history/
#     git blame on this file for their full text).
# PT: 2026-07-10 — este arquivo está atualmente VAZIO de entradas (abaixo deste header). Os
#     3 achados de ordem-de-teardown antes listados aqui (`vptr:ElementDocument`,
#     `vptr:*WidgetScroll.cpp*`, `vptr:*RemoveEventListener*`) agora são corrigidos na causa
#     raiz por um patch de fonte explícito e rastreado aplicado ao RmlUi fetchado via
#     PATCH_COMMAND — ver glintfx/patches/rmlui-2cd28864-teardown-ub.patch e
#     glintfx/patches/README.md para o relato completo, e o bloco
#     FetchContent_Declare(RmlUi ...) de glintfx/CMakeLists.txt para o mecanismo de aplicação
#     do patch. Eles NÃO estão mais suprimidos, estão corrigidos. A disciplina que este
#     arquivo existe para impor (toda entrada documentada, rastreada, com escopo) continua
#     igual mesmo com zero entradas — todo achado NOVO de vptr/UBSan ainda deve passar pelo
#     mesmo processo (corrigir-na-raiz preferido sobre suprimir; se suprimir for genuinamente
#     a única opção, deve ter o mesmo escopo estreito e a mesma documentação completa que as
#     3 entradas removidas tiveram; ver o histórico git/git blame deste arquivo pro texto
#     completo delas).
